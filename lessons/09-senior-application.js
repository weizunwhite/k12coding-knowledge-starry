// 高中编程 · 应用·数据·智能 —— 详细小课（习题讲解版）
// 覆盖 7 个节点：inheritance-polymorphism / design-patterns / file-io /
//   database / web-basics / data-analysis-py / ai-ml-intro
// 数据结构对齐 node-detail.jsx 的渲染：
//   warmup{q,hint,answer} / intro[] / formulas[{name,latex,note}] /
//   methods[{name,when,steps[],example{problem,solution[]}}] / worked[{title,problem,steps[]}] /
//   pitfalls[] / exercises[{problem,hint,answer}]
// 说明：
//   · 行内公式用 $...$（renderRich 会交给 KaTeX）；formulas.latex 直接写 LaTeX（无需 $）
//   · JS 字符串里反斜杠写成 \\，所以 $y=wx+b$ 这类直接写，含 \text 时写 \\text{...}
//   · 含代码或引号的字段值一律用反斜杠模板字符串（反引号），代码换行用真实换行
//   · node-detail.jsx 不渲染 derivation，本文件不含该字段
//   · renderRich 会把 ** 当成加粗、$ 当成公式；正文不用行内反引号代码（避免提前结束模板串），
//     代码统一放进 worked 的多行模板串、或用 \`\`\`python 围栏块（反引号已转义）

Object.assign(window.CODE_LESSON, {

  // ───────────────────────────────────────────────────────────────
  // 1. 继承与多态
  // ───────────────────────────────────────────────────────────────
  'inheritance-polymorphism': {
    warmup: {
      q: '你要写一个动物园程序，里面有狗、猫、牛。每种动物都有名字、都会吃东西（吃法一样），但叫声不同：狗"汪"、猫"喵"、牛"哞"。如果给每种动物都单独写一个完整的类，"名字""吃东西"这些一模一样的代码就要复制三遍。有没有办法让它们"共享相同的部分、只各自写不同的部分"？',
      hint: '想一想：狗、猫、牛在本质上都"是一种"动物。能不能先写一个"动物"模板，让它们去"继承"这个模板里相同的部分，再各自改写叫声？',
      answer: '办法就是**继承**：先写一个父类 Animal，把公共的部分（名字、吃东西）写在里面；再让 Dog、Cat、Cow 这些子类去**继承** Animal，自动拥有这些公共功能，只需各自重写不同的 speak() 方法。这样公共代码只写一次。更妙的是**多态**——你可以把不同动物放进一个列表，统一调用 a.speak()，Python 会在运行时自动选中各自的实现，输出"汪 喵 哞"。一句话：**继承解决"代码复用"，多态解决"统一接口、各自表现"。**'
    },
    intro: [
      '这一节讲面向对象的两大支柱——**继承**和**多态**，它们是组织大型程序、避免重复代码的核心工具。前提是你已经理解"类与对象"：类是模板（如"狗"），对象是具体实例（如"我家的小白"）。',
      '**继承**让一个类（**子类**）自动获得另一个类（**父类**）的全部属性和方法，然后在此基础上**扩展**或**修改**。它表达的是"**是一种**（is-a）"的关系：狗"是一种"动物，所以 Dog 继承 Animal。语法是在类名后用括号写上父类，写作 class Dog(Animal):。子类天生就拥有父类的能力，只需写出"和父类不一样的部分"，公共部分一行都不用重复。',
      '子类经常要在父类的基础上"加一点自己的东西"。比如父类 Animal 的 __init__ 已经处理了 name，子类 Dog 还想多存一个 breed（品种）。这时要用 **super().__init__(...)** 先调用父类的初始化（把 name 交给父类处理），再补上自己的 breed。super() 的意思就是"我的父类"，它让你**复用父类已经写好的逻辑**，而不是把父类代码再抄一遍。',
      '**多态**（poly-morphism，意为"多种形态"）指的是：**同一个方法调用，作用在不同对象上时，自动表现出不同的行为**。当 Dog、Cat 都重写了父类的 speak()，你写一个 for 循环 for a in animals: a.speak()，循环里那行 a.speak() 一字没变，但每个对象会执行自己版本的 speak——狗叫"汪"、猫叫"喵"。调用方不必关心 a 到底是哪种动物，**用统一的接口就能驱动一群不同的对象**，这就是多态的威力。',
      '一句话记住：**继承是"子类复用并扩展父类"（用 super() 调父类逻辑），多态是"同一个调用对不同对象有不同表现"。** 但要注意一个常见误区——继承表达的是"是一种"，不要拿它去表达"有一个"（比如"汽车有一个引擎"应该用组合，让汽车对象持有一个引擎对象，而不是让汽车继承引擎）。'
    ],
    formulas: [
      { name: '定义子类（继承）', latex: '\\text{class Dog(Animal):}',
        note: '类名后的括号里写父类名，表示 Dog 继承 Animal。Dog 自动拥有 Animal 的所有属性和方法，可再扩展或重写。' },
      { name: '调用父类逻辑', latex: '\\text{super().\\_\\_init\\_\\_(name)}',
        note: 'super() 代表"父类"。在子类的 __init__ 里先调用它，复用父类已写好的初始化（如处理 name），再补充子类自己的属性。' },
      { name: '重写（覆盖）方法', latex: '\\text{override：子类同名方法覆盖父类}',
        note: '子类里定义一个和父类同名的方法（如 speak），调用该对象时就执行子类版本——这是实现多态的关键。' },
      { name: '多态的统一接口', latex: '\\text{for a in animals: a.speak()}',
        note: '同一行 a.speak() 作用在不同子类对象上，运行时各自执行自己的实现——这就是多态。调用方无需判断对象具体类型。' },
    ],
    methods: [
      {
        name: '用继承复用父类代码',
        when: '多个类有大量相同的属性和方法，只有少数地方不同，想避免重复代码时',
        steps: [
          '找出这些类的**公共部分**（共同的属性、共同的方法），把它们提取到一个**父类**里',
          '让各个**子类**用 class 子类(父类): 的写法继承父类，自动获得公共部分',
          '若子类初始化时还需父类没有的新属性，在子类 __init__ 里先写 super().__init__(...) 复用父类初始化，再补自己的属性',
          '在子类里只写"和父类不同"的方法（重写父类同名方法，或新增方法）',
        ],
        example: {
          problem: '有"学生"和"老师"两个类，都有 name、都有 introduce() 自我介绍（格式相同），但学生多一个 grade（年级）、老师多一个 subject（科目）。如何用继承减少重复？',
          solution: [
            '解：把公共的 name 和 introduce() 提取到父类 Person。',
            'class Person：__init__ 里存 name；introduce() 打印 "我叫 {name}"。',
            'class Student(Person)：__init__ 里先 super().__init__(name) 复用父类存 name，再 self.grade = grade。老师同理用 subject。',
            '答：这样 name 和 introduce() 只在父类写一次，Student 和 Teacher 都能直接用，各自只补自己多出来的属性，重复代码被消除。',
          ],
        },
      },
      {
        name: '用多态写出"对一群对象统一处理"的代码',
        when: '有一组不同类型的对象，希望用同一段代码统一驱动它们、各自表现不同行为时',
        steps: [
          '让这些不同的类都继承同一个父类，并各自**重写**同一个方法（如 speak、area、draw）',
          '把这些不同对象放进同一个列表（或集合）里',
          '用一个循环遍历列表，对每个对象调用那个同名方法——**不要写一堆 if 判断它是哪种类型**',
          '运行时 Python 会自动按对象的真实类型选中对应版本的方法，这就是多态',
        ],
        example: {
          problem: '有圆形 Circle 和矩形 Rectangle，都继承自 Shape，都有 area() 方法但算法不同。怎样写一段代码，求一个混合列表里所有图形的面积之和？',
          solution: [
            '解：让 Circle.area() 算 πr²，Rectangle.area() 算长×宽，都重写父类 Shape 的 area()。',
            '把不同图形放一个列表：shapes = [Circle(2), Rectangle(3, 4)]。',
            '写 total = 0；再 for s in shapes: total += s.area()。循环里这行 s.area() 不区分类型。',
            '答：圆和矩形会各自执行自己的 area()，自动算对面积并累加。新增一种图形（如三角形）也不用改这段求和代码，只要它也有 area()——这正是多态带来的"可扩展"。',
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 用继承与多态实现动物叫声',
        problem: '设计一个动物类体系：父类 Animal 保存名字、有公共方法 eat()；子类 Dog、Cat 各自重写 speak() 方法（狗叫"汪汪"、猫叫"喵喵"）。然后把一只狗和一只猫放进列表，用一个循环让它们依次自我介绍并叫一声，体会多态。',
        steps: [
          `第一步：写父类 Animal，放公共的属性和方法。
class Animal:
    def __init__(self, name):
        self.name = name          # 名字是所有动物的共性

    def eat(self):                # 吃东西的方式都一样，写在父类
        print(self.name, "正在吃东西")

    def speak(self):              # 父类给个默认叫声（子类会重写它）
        print(self.name, "发出了声音")`,
          `第二步：写子类 Dog、Cat，用 super() 复用父类初始化，再重写 speak()。
class Dog(Animal):               # Dog 是一种 Animal
    def speak(self):             # 重写：覆盖父类的 speak
        print(self.name, "汪汪")

class Cat(Animal):               # Cat 是一种 Animal
    def speak(self):
        print(self.name, "喵喵")`,
          '第三步：理解继承带来了什么。Dog 和 Cat 都没有再写 __init__ 和 eat()，但它们**自动拥有**了父类 Animal 的这两样——name 能正常存、eat() 能正常调。公共代码只在父类写了一次。',
          `第四步：创建对象，放进一个列表，用循环统一处理（多态登场）。
animals = [Dog("小白"), Cat("咪咪")]   # 列表里混着不同类型的对象
for a in animals:
    a.eat()        # 调的是从父类继承来的同一个 eat()
    a.speak()      # 同一行 speak()，但每个对象执行自己的版本`,
          '第五步：分析输出。循环第 1 轮 a 是 Dog："小白 正在吃东西" / "小白 汪汪"；第 2 轮 a 是 Cat："咪咪 正在吃东西" / "咪咪 喵喵"。',
          '第六步：作答与体会。a.speak() 这一行代码完全没变，却根据对象的真实类型分别输出了"汪汪""喵喵"——这就是**多态**。eat() 体现**继承复用**（子类没写却能用）。如果以后再加一种 Cow（重写 speak 返回"哞哞"），只要把它也放进列表，这段循环**一个字都不用改**。',
        ],
      },
      {
        title: '例题 · 用 super() 在子类里扩展父类的 __init__',
        problem: '父类 Person 初始化时保存 name。子类 Student 不仅要有 name，还要多保存一个 grade（年级）。请用 super() 正确地写出 Student 的 __init__，并解释为什么不直接写 self.name = name。',
        steps: [
          `第一步：写父类 Person。
class Person:
    def __init__(self, name):
        self.name = name
        print("Person 初始化：保存了 name")`,
          `第二步：写子类 Student，用 super() 复用父类初始化，再补自己的属性。
class Student(Person):
    def __init__(self, name, grade):
        super().__init__(name)    # 先让父类去处理 name（复用父类逻辑）
        self.grade = grade        # 再补充子类自己多出来的属性
        print("Student 初始化：又保存了 grade")`,
          '第三步：理解 super().__init__(name) 做了什么。它调用的是父类 Person 的 __init__，把 name 交给父类去 self.name = name。这样**父类初始化里的逻辑（哪怕以后变复杂）都被复用**，子类不必关心父类内部怎么存 name。',
          '第四步：为什么不直接在子类写 self.name = name？小例子里两种写法结果一样，但**直接抄等于把父类逻辑复制了一遍**——一旦父类的 __init__ 以后增加了新步骤（比如还要校验 name、记录创建时间），子类不调 super() 就会漏掉这些步骤，埋下 bug。用 super() 才能始终和父类保持一致。',
          `第五步：验证。
s = Student("小明", 8)
print(s.name, s.grade)   # 小明 8`,
          '第六步：作答。创建 Student("小明", 8) 会依次打印"Person 初始化：保存了 name""Student 初始化：又保存了 grade"，最后 s.name 是"小明"、s.grade 是 8。正确做法是**子类 __init__ 里先 super().__init__(...) 复用父类，再写自己新增的属性**。',
        ],
      },
    ],
    pitfalls: [
      '子类写了 __init__ 却忘了调 super().__init__(...)。这会导致父类的初始化被**完全跳过**——父类里设置的属性（如 self.name）根本没被赋值，之后一访问就报 AttributeError。子类自己写了 __init__ 就有责任调用父类的。',
      '用继承去表达"**有一个**（has-a）"的关系。继承表达的是"**是一种**（is-a）"：狗是一种动物，对。但"汽车有一个引擎"不该让 Car 继承 Engine——那应该用**组合**（让 Car 对象持有一个 Engine 对象）。滥用继承会让类体系混乱、难维护。',
      '以为子类重写方法后，父类的同名方法就消失了。父类版本仍然存在，子类对象调用时优先用子类版本（覆盖）；若在子类里仍想用父类那一版，可以用 super().方法名() 显式调用它。',
      '把"重写（override）"和"重载（overload）"搞混。Python 里讲的是**重写**——子类用同名方法覆盖父类。Python 没有像 Java 那样"同名不同参数"的重载机制，别指望靠参数个数区分同名方法。',
      '继承层次套得太深（A→B→C→D→…）。层级一多就很难看清某个方法到底来自哪一层、被谁覆盖了，调试困难。实际开发中继承不宜太深，"能用组合就别硬套继承"。',
    ],
    exercises: [
      {
        problem: '设计一个图形类体系：父类 Shape 有方法 area()（先返回 0 作占位）；子类 Circle（半径 r，面积 πr²）和 Rectangle（长 w、宽 h，面积 w×h）各自重写 area()。然后把一个圆和一个矩形放进列表，用一个循环求出所有图形的总面积。请写出完整代码并说明哪里体现了多态。',
        hint: 'Circle 和 Rectangle 都继承 Shape，各自重写 area()。求和时用一个 for 循环调 s.area()，不要写 if 判断类型。圆周率可用 3.14 或 import math 用 math.pi。',
        answer: `**详细讲解**：

**思路**：三步走——① 写父类 Shape 定义统一接口 area()；② 两个子类各自继承并重写 area() 写出自己的面积公式；③ 把不同图形放进同一列表，用一个循环统一调用 area() 累加。关键是求和代码**不区分图形类型**，全靠多态自动选中正确的 area()。

**参考代码**：
\`\`\`python
import math   # 用 math.pi 表示圆周率

# 父类：定义所有图形的统一接口
class Shape:
    def area(self):
        return 0          # 占位，具体由子类重写

# 子类：圆形
class Circle(Shape):
    def __init__(self, r):
        self.r = r        # 半径

    def area(self):       # 重写：圆面积 = πr²
        return math.pi * self.r * self.r

# 子类：矩形
class Rectangle(Shape):
    def __init__(self, w, h):
        self.w = w        # 长
        self.h = h        # 宽

    def area(self):       # 重写：矩形面积 = 长 × 宽
        return self.w * self.h

# 把不同图形放进同一个列表
shapes = [Circle(2), Rectangle(3, 4)]

# 多态：同一行 s.area()，每个对象执行自己的版本
total = 0
for s in shapes:
    total = total + s.area()

print("总面积：", total)
\`\`\`

**执行结果**：圆面积约 3.14×2×2 = 12.57，矩形面积 = 3×4 = 12，总面积约 24.57。

**哪里体现了多态**：核心在循环里的 s.area() 这一行。s 在第 1 轮是 Circle、第 2 轮是 Rectangle，但代码**一模一样**，Python 在运行时根据 s 的真实类型自动调用对应的 area()。

**为什么这样设计好**：如果以后再加一个三角形 Triangle（同样继承 Shape、重写 area()），只要把它放进 shapes 列表，**这段求和代码一个字都不用改**。这就是多态带来的"对扩展开放"——新增类型不影响已有逻辑。`
      },
      {
        problem: '小明写了下面的代码，运行时报错 AttributeError: \'Student\' object has no attribute \'name\'。请找出 bug 并改正：\nclass Person:\n    def __init__(self, name):\n        self.name = name\n\nclass Student(Person):\n    def __init__(self, grade):\n        self.grade = grade\n\ns = Student(8)\nprint(s.name)',
        hint: '子类 Student 写了自己的 __init__，但里面有没有调用父类的 __init__？父类负责设置的 self.name 被赋值了吗？',
        answer: `**详细讲解**：

**bug 在哪**：子类 Student 定义了自己的 __init__，但**没有调用父类的 __init__**，也没有接收 name 参数。父类 Person 的 __init__ 里那句 self.name = name 因此**从来没被执行过**，所以 Student 对象身上根本没有 name 这个属性，一访问 s.name 就报 AttributeError。

**为什么会这样**：当子类**自己写了** __init__，Python 创建对象时就只会执行子类的 __init__，**不会自动再去执行父类的** __init__。也就是说，父类初始化里设置的属性，子类必须主动用 super() 去触发，否则就被跳过了。

**正确做法**：子类 __init__ 要① 接收 name 参数，② 用 super().__init__(name) 把 name 交给父类去设置 self.name，③ 再补上自己的 grade。

**改正后代码**：
\`\`\`python
class Person:
    def __init__(self, name):
        self.name = name

class Student(Person):
    def __init__(self, name, grade):       # 也要接收 name
        super().__init__(name)             # 调用父类 __init__，设置 self.name
        self.grade = grade                 # 再补自己的属性

s = Student("小明", 8)
print(s.name)    # 小明
print(s.grade)   # 8
\`\`\`

**记住这条规律**：**子类一旦写了自己的 __init__，就有责任用 super().__init__(...) 调用父类的初始化**，否则父类负责的属性会全部丢失。这是初学继承时最常踩的坑。`
      },
      {
        problem: '有人想表达"汽车有一个引擎"，写成 class Car(Engine):（让汽车继承引擎）。这样设计合理吗？为什么？正确应该怎么做？请说明"是一种（is-a）"和"有一个（has-a）"的区别。',
        hint: '继承表达的是哪种关系？汽车"是一种"引擎，还是汽车"有一个"引擎？',
        answer: `**详细讲解**：

**结论：不合理**。让 Car 继承 Engine 是对继承的误用。

**为什么不合理**：继承表达的是"**是一种（is-a）**"的关系——子类必须真的"是一种"父类。比如"狗是一种动物"（Dog 继承 Animal）成立，因为狗确实属于动物。但"汽车**是一种**引擎"完全说不通——汽车不是引擎，汽车里**装着**一个引擎。两者是"**有一个（has-a）**"的关系，不是"是一种"。

**误用的后果**：如果硬让 Car 继承 Engine，Car 就会莫名其妙地拥有引擎的所有方法（如 start_combustion 燃烧），仿佛"汽车本身就是个引擎"，类的语义彻底乱套，后面越写越别扭。

**正确做法——用组合（让 Car 持有一个 Engine 对象）**：
\`\`\`python
class Engine:
    def start(self):
        print("引擎启动")

class Car:
    def __init__(self):
        self.engine = Engine()    # 汽车"有一个"引擎：把引擎对象作为属性

    def start(self):
        print("汽车准备启动……")
        self.engine.start()       # 通过持有的引擎对象去调用它的功能

c = Car()
c.start()
\`\`\`

**两种关系的判断口诀**：
- 能说"**A 是一种 B**"且通顺 → 用**继承**（如"学生是一种人" Student(Person)）。
- 只能说"**A 有一个 B**" → 用**组合**（把 B 当作 A 的属性，如"汽车有一个引擎"）。

**为什么这点重要**：滥用继承会让类之间产生不必要的强耦合、体系混乱。一个广为流传的设计建议是"**优先用组合而非继承**"——组合更灵活，需要哪个功能就持有哪个对象，关系清晰。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 2. 软件设计思想与设计模式
  // ───────────────────────────────────────────────────────────────
  'design-patterns': {
    warmup: {
      q: '你写一个小游戏，代码越堆越多：改一个小功能，结果三四个地方跟着崩；想加一种新道具，要翻遍整个文件到处插代码。明明每段代码单独看都"能跑"，为什么整个程序越来越难改、越来越容易出 bug？有没有一些被验证过的"套路"能让代码好改、好扩展？',
      hint: '想一想：代码"能跑"和代码"好维护"是一回事吗？前人遇到过同样的组织难题，会不会已经总结出可复用的解决模板？',
      answer: '当程序变大，"**怎么组织代码才好改、好扩展**"比"能不能跑"更重要。代码到处耦合（牵一发动全身）、加功能要四处改，正是组织得不好的表现。前人把"应对常见设计问题的可复用方案"总结成了**设计模式**（如单例、工厂、观察者、策略），还提炼出**设计原则**（高内聚低耦合、面向接口编程）。它们是被无数项目验证过的经验。但要记住：模式是为了解决真实问题，**别为了用模式而把简单代码搞复杂**——过度设计和不设计一样糟。'
    },
    intro: [
      '这一节讲的不是某种语法，而是**软件设计思想**——当程序规模变大时，"怎么把代码组织得好改、好扩展、好维护"。这是从"会写代码"迈向"会做工程"的关键一步。它建立在面向对象（封装、继承、多态）的基础上。',
      '先理解两条最重要的**设计原则**。① **高内聚、低耦合**："内聚"指一个模块内部的代码都在干同一件相关的事（职责单一）；"耦合"指模块之间的依赖程度。我们追求**高内聚**（每块只管好自己那摊事）和**低耦合**（模块之间联系尽量少、尽量松），这样改动一处不会牵连一大片。② **面向接口编程**：调用方只依赖"对方能做什么"（接口/方法），不依赖"对方具体怎么实现"。这样底层换了实现，上层代码不用动。',
      '**设计模式**就是前人总结的、针对常见设计问题的**可复用解决方案模板**。它们不是库、不是语法，而是"遇到这类问题时，这样组织代码比较好"的经验配方。常见的有：**单例模式**（保证某个类全程只有一个实例，如全局配置、日志器）；**工厂模式**（用一个统一的函数/类来创建对象，调用方不用关心创建哪个具体类）；**观察者模式**（一个对象状态变化时，自动通知所有"订阅"了它的对象）；**策略模式**（把可互换的算法各自封装起来，运行时灵活切换）。',
      '理解模式的最好方式是看它"解决了什么痛点"。比如**观察者模式**：界面上的数据变了，需要刷新好几个显示组件——如果数据对象直接去调用每个组件，就把数据和界面死死绑在一起（高耦合）。观察者模式让组件"订阅"数据对象，数据一变就自动挨个通知，数据对象**不需要知道**具体有哪些订阅者，新增一个显示组件也不用改数据这边的代码。这正是"低耦合 + 易扩展"的体现。',
      '一句话记住：**设计模式是"组织代码的经验配方"，目标是高内聚、低耦合、易扩展。** 但最后必须强调那条最重要的告诫——**模式是用来解决真实问题的，不要为了用模式而用**。简单的小程序硬套一堆模式，只会平添复杂度。判断标准永远是："用了它，代码是不是真的更好改了？"'
    ],
    formulas: [
      { name: '核心目标', latex: '\\text{高内聚 + 低耦合}',
        note: '内聚：一个模块只专注做一类相关的事；耦合：模块之间的依赖。好设计追求模块内部紧密、模块之间松散。' },
      { name: '面向接口编程', latex: '\\text{依赖"能做什么"，不依赖"怎么做"}',
        note: '调用方只依赖对方暴露的方法（接口），不依赖其内部实现。换实现时上层代码不必修改。' },
      { name: '单例模式', latex: '\\text{Singleton：全程只有一个实例}',
        note: '保证一个类在整个程序里只被创建一次、共享同一个对象。适合全局配置、日志器、数据库连接池等。' },
      { name: '观察者模式', latex: '\\text{Observer：状态变化自动通知订阅者}',
        note: '被观察对象状态改变时，自动逐个通知所有订阅了它的观察者。被观察者无需知道具体有谁订阅，新增订阅者不用改它的代码。' },
    ],
    methods: [
      {
        name: '识别并降低耦合（让代码好改）',
        when: '发现"改一处就要连带改好几处"、加功能要到处插代码时',
        steps: [
          '找出"改动总是一起发生"的地方——它们往往耦合过紧，是问题根源',
          '问自己：调用方是不是依赖了对方的**内部实现细节**？能不能改成只依赖一个稳定的方法（接口）',
          '把"会变化的部分"和"不变的部分"分开：让不变的代码依赖一个统一接口，变化的部分各自实现这个接口',
          '验证：新增一种情况时，是否只需"加一个新类/新实现"，而不必修改已有代码（这叫"对扩展开放、对修改关闭"）',
        ],
        example: {
          problem: '一个画图程序，到处写 if shape == "圆"画圆、elif shape == "方"画方。每加一种新图形，所有这种 if-elif 都要改一遍。怎样降低这种耦合？',
          solution: [
            '解：问题在于"判断类型 + 对应动作"这段逻辑被复制到了很多处，且和具体图形种类紧紧耦合。',
            '改用**面向接口 + 多态**：定义统一接口 Shape.draw()，让 Circle、Square 各自实现 draw()。',
            '所有原来的 if-elif 都换成统一的 shape.draw()——不再判断类型。',
            '答：这样新增一种图形只要写一个新类、实现 draw()，**所有调用处一行都不用改**。把"四处 if-elif"换成"一个统一接口"，耦合大幅降低，扩展变得容易。',
          ],
        },
      },
      {
        name: '用观察者模式实现"自动通知"',
        when: '一个对象的状态变化后，需要让另外若干对象随之做出反应，且希望两者解耦时',
        steps: [
          '确定**被观察者**（状态会变的那个，如"数据"）和**观察者**（要随之反应的那些，如"显示组件"）',
          '给被观察者一个"订阅者列表"，提供 subscribe() 让观察者登记进来',
          '给被观察者一个 notify() 方法：状态一变就遍历订阅者列表，挨个调用它们的 update()',
          '每个观察者实现自己的 update()——收到通知后各做各的反应（被观察者不必知道它们具体做了什么）',
        ],
        example: {
          problem: '一个"温度传感器"对象，温度变化时要同时更新"屏幕显示"和"报警器"。怎样用观察者模式让传感器不直接依赖这两个具体对象？',
          solution: [
            '解：传感器是被观察者，屏幕和报警器是观察者。',
            '传感器维护一个 observers 列表，提供 subscribe(o) 把观察者加进来；屏幕、报警器分别 subscribe 到传感器。',
            '温度更新时，传感器调 notify()：遍历 observers，对每个调 o.update(温度)。屏幕的 update 去刷新显示、报警器的 update 去判断是否超标。',
            '答：传感器**只管"通知"，不管"谁、怎么反应"**。以后再加一个"日志记录器"，只要让它 subscribe 进来即可，传感器代码一行不改——这就是观察者模式带来的解耦和可扩展。',
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 用单例模式实现全局配置',
        problem: '一个程序里有很多地方要读写"全局配置"（如音量、难度）。如果每处都各自创建一个配置对象，它们就互不相通、改了一个另一个不知道。请用单例模式保证全程只有一个配置对象，所有地方共享它。',
        steps: [
          '第一步：想清楚痛点。如果 A 处 Config()、B 处又 Config()，得到的是两个**互相独立**的对象——A 改了音量，B 拿到的还是旧值。我们需要"无论在哪创建，拿到的都是同一个对象"。',
          `第二步：用一个类变量缓存唯一实例，重写 __new__ 控制创建。
class Config:
    _instance = None          # 类变量：缓存全局唯一的那个实例

    def __new__(cls):
        # __new__ 负责"创建对象"。第一次创建时才真正新建，之后都返回同一个
        if cls._instance is None:
            cls._instance = super().__new__(cls)
            cls._instance.volume = 50   # 初始化默认配置（只会执行一次）
        return cls._instance            # 永远返回这同一个实例`,
          '第三步：理解原理。__new__ 是真正"造对象"的方法。这里用类变量 _instance 把第一次造好的对象记下来；以后每次 Config()，发现 _instance 已存在，就直接返回它，**不再造新的**。于是全程只有一个 Config 对象。',
          `第四步：验证"处处拿到同一个"。
a = Config()
b = Config()
print(a is b)        # True —— a 和 b 是同一个对象
a.volume = 80        # 通过 a 改音量
print(b.volume)      # 80 —— b 立刻看到了变化（因为本来就是同一个）`,
          '第五步：作答。a is b 为 True，证明两次 Config() 拿到的是**同一个实例**；通过 a 改了 volume，b 也变成 80。这样无论程序哪个角落访问 Config()，读写的都是同一份全局配置。',
          '第六步：提醒边界。单例确实方便共享全局状态，但它本质是"全局变量"的一种，用多了会让模块之间**隐式耦合**（大家都偷偷依赖这个全局对象），不利于测试和维护。所以单例要**克制使用**，只用在确实"全局唯一"的东西上（配置、日志器等）。',
        ],
      },
    ],
    pitfalls: [
      '**为了用模式而用模式（过度设计）**。给一个几十行的小脚本套上工厂、单例、观察者一堆模式，只会让代码无端变复杂、更难读。模式是解决真实痛点的，没痛点就别硬上——"简单直接"本身也是一种好设计。',
      '把"设计模式"当成"必须背下来的语法"。模式是**思路和经验**，不是 API。重点是理解"它解决什么问题、为什么这样组织更好"，而不是死记每个模式的代码模板。',
      '滥用单例当全局变量用。单例方便，但它让各模块**隐式共享状态**、互相耦合，难以单独测试、难以追踪谁改了它。只在"确实全局唯一"时才用，不要把什么都做成单例。',
      '混淆"高内聚低耦合"的方向。**内聚要高**（一个模块只干一类事）、**耦合要低**（模块间依赖要少）。有人记反了去追求"低内聚"或"高耦合"，正好南辕北辙。',
      '以为"代码能跑"就等于"设计好"。能跑只是最低要求。设计好坏看的是**好不好改、好不好扩展、好不好测试**——一段到处耦合、加功能要全文搜索的代码，哪怕能跑，也是糟糕的设计。',
    ],
    exercises: [
      {
        problem: '用自己的话解释"高内聚、低耦合"分别是什么意思，并各举一个生活或代码中的例子。再说明：为什么"低耦合"能让程序更好维护？',
        hint: '内聚说的是"一个模块内部"，耦合说的是"模块之间"。想想：如果两个模块绑得很死，改一个会怎样？',
        answer: `**详细讲解**：

**高内聚是什么**：指一个模块（函数、类）**内部的代码都在围绕同一件相关的事**，职责单一、不混入无关功能。

- 例子：一个 EmailSender 类只负责"发邮件"相关的事（连接服务器、组装邮件、发送），不去顺便管"读取数据库""画界面"。它内部很"内聚"。
- 反例：一个 Utils 类里塞了发邮件、算工资、画图表、连数据库……什么都管——这是低内聚，很糟。

**低耦合是什么**：指**模块之间的依赖尽量少、尽量松**，一个模块不依赖另一个模块的内部细节。

- 例子：模块 A 调用 B 时，只通过 B 公开的一个方法（接口）来用，不碰 B 内部怎么实现。B 内部改了实现，A 完全不受影响。
- 反例：A 直接读写 B 内部的变量、依赖 B 某段具体写法——B 一改，A 就跟着崩。这是高耦合（不好）。

**为什么低耦合让程序更好维护**：因为模块之间联系越松，**改动的影响范围就越小**。如果耦合很低，改 B 的内部时，只要它对外的方法（接口）不变，A、C、D 都不用动，改起来安全又省事。反之高耦合时，动一个模块会"牵一发而动全身"，到处连带出 bug，越改越怕、越改越乱。

**一句话总结**：**高内聚让每个模块"自己的事自己干好"，低耦合让模块之间"互不拖累"**。两者合起来，程序就像积木——可以单独替换、单独测试某一块而不影响其他，维护成本大大降低。`
      },
      {
        problem: '一个购物程序要支持多种支付方式（微信、支付宝、银行卡），下单时根据用户选择调用对应支付。小明写成一长串 if pay 等于"微信"就走微信、elif 等于"支付宝"就走支付宝……每加一种支付就要改这段。请说明这样写的问题，并用"面向接口/策略"的思想给出更好的设计（写出参考代码）。',
        hint: '把每种支付方式做成一个有统一方法（如 pay()）的类，让它们都遵守同一个接口；下单代码只调用统一的 pay()，不写 if-elif 判断。',
        answer: `**详细讲解**：

**这样写的问题**：用一长串 if-elif 判断支付类型，有两个毛病——① **耦合高**：下单逻辑和"具体有哪些支付方式"绑死了；② **难扩展**：每新增一种支付（如"数字人民币"），就得回去修改这段 if-elif，违反了"对扩展开放、对修改关闭"。支付方式一多，这段判断会越来越长、越来越容易改错。

**更好的设计（策略 / 面向接口）**：把"每一种支付方式"各自封装成一个类，都提供同名方法 pay()，遵守统一接口。下单时只调用 pay()，靠多态自动执行对应实现，**完全不需要 if-elif 判断类型**。

**参考代码**：
\`\`\`python
# 统一接口：所有支付方式都有 pay() 方法
class WeChatPay:
    def pay(self, amount):
        print("用微信支付了", amount, "元")

class AliPay:
    def pay(self, amount):
        print("用支付宝支付了", amount, "元")

class BankCardPay:
    def pay(self, amount):
        print("用银行卡支付了", amount, "元")

# 下单函数：只依赖"对方有一个 pay() 方法"，不关心它具体是哪种
def checkout(payment, amount):
    payment.pay(amount)      # 统一调用，多态自动选中对应实现

# 使用：传入哪种支付对象，就用哪种方式付
checkout(WeChatPay(), 100)     # 用微信支付了 100 元
checkout(AliPay(), 200)        # 用支付宝支付了 200 元
\`\`\`

**好在哪里**：
1. **下单代码 checkout 极其干净**——只有一行 payment.pay(amount)，再也没有 if-elif。
2. **新增支付方式零修改**：以后加"数字人民币"，只要写一个 DigitalRMBPay 类、实现 pay()，然后 checkout(DigitalRMBPay(), 300) 即可，**checkout 一个字都不用动**。

**这体现了什么思想**：① **面向接口编程**——checkout 只依赖"有 pay() 方法"这个约定，不依赖具体类；② **策略模式**——把可互换的"支付算法"各自封装，运行时灵活替换。结果就是低耦合、易扩展。`
      },
      {
        problem: '有人学了设计模式后很兴奋，给一个"打印 Hello World 的 5 行小脚本"也套上了工厂模式、单例模式和观察者模式，代码膨胀到上百行。这样做对吗？请评价，并说说应该如何判断"该不该用某个设计模式"。',
        hint: '设计模式是用来解决什么的？一个 5 行小脚本有那些"组织代码的痛点"吗？过度设计会带来什么后果？',
        answer: `**详细讲解**：

**结论：这样做不对，是典型的"过度设计"**。

**为什么不对**：设计模式的目的是**解决大型程序里"代码难组织、难维护、难扩展"的真实痛点**。而一个"打印 Hello World"的 5 行小脚本，根本没有这些痛点——它简单、清晰、一眼看懂。硬套工厂、单例、观察者三个模式，把 5 行撑成上百行，结果是：
- 代码**反而更难读**了（要绕过一堆类和抽象才找到那句 print）。
- 维护成本**凭空增加**（多出来的结构都是没必要的负担）。
- 别人接手时会一头雾水："这么简单的事，为什么搞这么复杂？"

**过度设计和"不设计"一样糟**：设计不足会让大程序乱成一团；但过度设计会让小程序无端复杂。两者都偏离了"用合适的复杂度解决问题"这个核心。**简单的问题就该用简单的写法**——简洁本身就是一种好设计。

**如何判断"该不该用某个设计模式"**，可以问自己几个问题：
1. **有没有它要解决的那个痛点？** 比如"到处 if-elif 判断类型、加功能要全文修改"——有这个痛点，才考虑用策略/工厂。没有就别用。
2. **用了之后，代码是不是真的更好改、更好扩展了？** 如果用了反而更绕、更难懂，那就是用错了。
3. **现在就需要，还是"以后可能用得上"？** 不要为了臆想中"将来也许要扩展"就提前堆砌模式（这叫"过早抽象"）。等真正需要时再重构也不迟。

**一句话总结**：**模式是手段，不是目的**。判断标准永远只有一个——"用了它，我的代码是不是真的变得更好维护了？"如果答案是否定的，哪怕这个模式再有名，也不该用。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 3. 文件与数据持久化
  // ───────────────────────────────────────────────────────────────
  'file-io': {
    warmup: {
      q: '你写了一个记账小程序，运行时能记录每笔开销。可是一关掉程序，再打开，之前记的账全没了，又得从头输入。为什么变量里的数据"一关就消失"？怎样才能让数据在程序关闭后还**保留下来**，下次打开还能读到？',
      hint: '想一想：变量是存在哪里的？那个地方在程序结束后还在吗？要长期保存，是不是得把数据写到一个"关机也不丢"的地方？',
      answer: '变量都存在**内存**里，而内存是"临时"的——程序一结束，它占用的内存就被回收，里面的数据自然全没了。要让数据**长期保留**，就得把它写进**文件**（存在硬盘上），这叫**数据持久化**。下次程序启动时，再从文件把数据读回来。Python 用 open() 打开文件、读写，并强烈建议用 **with 语句**自动管理（用完自动关闭）。常见格式有纯文本、CSV（表格）、JSON（结构化数据）。一句话：**内存里的数据会随程序消失，写进文件才能"关机也不丢"。**'
    },
    intro: [
      '这一节讲**文件读写与数据持久化**——如何让程序的数据在关闭后依然保留。这是从"玩具程序"走向"能处理真实数据的程序"的第一步。前提是你已经熟悉字符串。',
      '为什么需要文件？因为变量、列表、字典都存在**内存**里，内存是临时的——程序一退出，数据就全部消失。如果想保存用户的设置、记录的成绩、采集的实验数据，就必须把它们写到**硬盘上的文件**里，这样关机重启都还在。这个"把内存数据存到硬盘、需要时再读回来"的过程，就叫**数据持久化**。',
      '操作文件的基本套路是三步：**打开 → 读/写 → 关闭**。用 open(文件名, 模式, encoding) 打开。常用模式："r" 只读（文件须已存在）、"w" 写入（**会清空原内容**重新写）、"a" 追加（在末尾续写，不清空）。处理中文一定要加 encoding="utf-8"，否则可能乱码或报错。读用 f.read()（读全部）、f.readlines()（按行读成列表）或直接 for line in f（逐行）；写用 f.write(文本)。',
      '**强烈推荐用 with open(...) as f: 语句**而不是手动 open/close。with 会在代码块结束时**自动关闭文件**，哪怕中间出错也会关——这样既不会忘记关闭、也避免"数据没写全"或"资源泄漏"的问题。这是处理文件的标准写法，务必养成习惯。',
      '真实数据常用两种结构化格式存储：**CSV**（逗号分隔的表格，每行一条记录，像简化版 Excel，可用 csv 模块或 pandas 读）；**JSON**（能直接保存字典、列表等嵌套结构，用 json 模块：json.dump(数据, 文件) 存、json.load(文件) 读）。一句话记住：**内存数据要长期保留就写进文件，用 with 自动管理、中文记得 utf-8；字典/列表存 JSON、表格存 CSV。**'
    ],
    formulas: [
      { name: '安全地打开文件', latex: '\\text{with open(name, mode, encoding) as f:}',
        note: 'with 语句会在代码块结束时自动关闭文件（即使出错也关），是文件操作的标准写法，避免忘记关闭。' },
      { name: '常用打开模式', latex: '\\text{r 读 / w 覆盖写 / a 追加}',
        note: 'r 只读（文件须存在）；w 写入会先清空原内容；a 在末尾追加不清空。中文务必加 encoding="utf-8"。' },
      { name: '读与写', latex: '\\text{f.read() 读 / f.write(s) 写}',
        note: 'read() 读出全部内容；readlines() 按行读成列表；write(s) 把字符串写入。写入时换行要自己加换行符。' },
      { name: 'JSON 存取', latex: '\\text{json.dump(data, f) / json.load(f)}',
        note: 'dump 把字典/列表等写进文件；load 从文件读回成字典/列表。最适合保存有结构的数据。' },
    ],
    methods: [
      {
        name: '用 with 语句安全读写文本文件',
        when: '要把数据保存到文件、或从文件读取数据，且希望避免忘记关闭文件时',
        steps: [
          '想清楚是"读"还是"写"，选对模式：读用 "r"、覆盖写用 "w"、末尾追加用 "a"',
          '用 with open(文件名, 模式, encoding="utf-8") as f: 打开（中文必加 utf-8）',
          '在 with 代码块**缩进内**完成读或写：读用 f.read() 或逐行 for；写用 f.write()（换行自己加换行符）',
          '代码块结束时文件**自动关闭**——不用、也不要再手动 f.close()',
        ],
        example: {
          problem: '把一句话"今天学了文件读写"保存到 note.txt，然后再读出来打印。',
          solution: [
            '解：分两段——先写后读，都用 with。',
            '写：with open("note.txt", "w", encoding="utf-8") as f: f.write("今天学了文件读写")。',
            '读：with open("note.txt", "r", encoding="utf-8") as f: content = f.read()；再 print(content)。',
            '答：写入用 "w" 模式、读取用 "r" 模式，两段都用 with 自动关闭。最后会打印出"今天学了文件读写"。注意若再用 "w" 写一次别的内容，原来的会被清空覆盖。',
          ],
        },
      },
      {
        name: '用 JSON 保存和读取结构化数据',
        when: '要持久化的是字典、列表等有结构的数据（如一批学生记录、程序配置）时',
        steps: [
          'import json（标准库，无需安装）',
          '保存：用 with open(文件, "w", encoding="utf-8") as f:，再 json.dump(数据, f, ensure_ascii=False)（ensure_ascii=False 让中文正常显示而非乱码转义）',
          '读取：用 with open(文件, "r", encoding="utf-8") as f:，再 data = json.load(f)，得到的就是原来的字典/列表',
          '读回的数据类型和存进去时一致（字典还是字典、列表还是列表），可直接使用',
        ],
        example: {
          problem: '把一个学生信息字典 {"name": "小明", "scores": [90, 85]} 存进 student.json，再读回来取出名字。',
          solution: [
            '解：用 json.dump 存、json.load 读。',
            '存：import json；data = {"name": "小明", "scores": [90, 85]}；with open("student.json", "w", encoding="utf-8") as f: json.dump(data, f, ensure_ascii=False)。',
            '读：with open("student.json", "r", encoding="utf-8") as f: d = json.load(f)；再 print(d["name"])。',
            '答：json.dump 把整个字典（含里面的列表）一次写入文件；json.load 读回来还是一个字典，d["name"] 取出"小明"。JSON 的好处是嵌套结构（字典里套列表）也能原样保存。',
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 把成绩列表写入文件再统计读回',
        problem: '有一个成绩列表 scores = [88, 92, 79, 95, 83]。请把每个成绩写入文件 scores.txt（每行一个），然后从文件读回所有成绩，算出平均分。要求用 with 语句、处理好类型转换。',
        steps: [
          `第一步：写入。用 "w" 模式，每个成绩占一行（自己加换行符）。
scores = [88, 92, 79, 95, 83]
with open("scores.txt", "w", encoding="utf-8") as f:
    for s in scores:
        f.write(str(s) + "\\n")   # 数字要先转成字符串才能写；\\n 表示换行`,
          '第二步：注意两个细节。① write() 只能写**字符串**，所以数字 s 要用 str(s) 转换，否则报 TypeError。② 文件不会自动换行，必须自己在每行末尾加换行符，否则所有数字会挤成一行。',
          `第三步：读回。逐行读出，每行是带换行的字符串，要转回数字。
total = 0
count = 0
with open("scores.txt", "r", encoding="utf-8") as f:
    for line in f:                # 逐行读
        line = line.strip()       # 去掉行尾的换行和空白
        if line:                  # 跳过可能的空行
            total = total + int(line)   # 字符串转回整数再累加
            count = count + 1`,
          '第四步：注意读回的坑。从文件读出的每一行都是**字符串**（还带着行尾的换行符）。要先用 strip() 去掉换行，再用 int() 转回数字才能做运算——直接拿字符串相加会得到错误结果或报错。',
          `第五步：算平均分并输出。
average = total / count
print("平均分：", average)`,
          '第六步：作答。total = 88+92+79+95+83 = 437，count = 5，平均分 = 437 / 5 = 87.4。整个流程体现了文件读写的两大要点：**写时数字转字符串、自己加换行**；**读时去换行、字符串转回数字**。全程用 with 保证文件自动关闭。',
        ],
      },
    ],
    pitfalls: [
      '**用完文件不关闭**（或忘记关）。手动 open 后忘了 close，可能导致数据没真正写到硬盘、或占着资源不放。**最佳实践是用 with open(...) as f:**，它会在代码块结束时自动关闭文件，哪怕中途出错也会关。',
      '**误用 "w" 模式把原内容清空了**。"w" 一打开就**清空整个文件**重新写。想在原有内容后面续写，要用 **"a"（追加）**模式。很多"数据莫名丢失"都是错用了 "w"。',
      '中文不加 encoding="utf-8"。不指定编码时，不同系统默认编码不同，读写中文容易乱码或抛 UnicodeDecodeError。**凡涉及中文，open 一律加 encoding="utf-8"**。',
      '从文件读出的内容**忘了类型转换**。read/readlines 得到的永远是**字符串**，且每行带着行尾换行符。要参与计算就得先 strip() 去换行、再 int()/float() 转数字，否则会出错或结果不对。',
      'write() 不会自动换行。f.write("第一行") 后紧接着 f.write("第二行") 会连成"第一行第二行"。需要换行必须**自己在字符串末尾加换行符**。',
    ],
    exercises: [
      {
        problem: '写一个函数 save_diary(text)，把一条日记内容**追加**到文件 diary.txt 的末尾（每次调用都在文件最后加一行，不能覆盖之前的）。再写一个函数 read_diary() 读出全部日记内容打印。请说明为什么用 "a" 模式而不是 "w"。',
        hint: '"追加"要用 "a" 模式（在末尾续写）。"w" 会清空原内容。每条日记后记得加换行符。读取用 "r" 模式 + read()。',
        answer: `**详细讲解**：

**思路**：题目关键词是"**追加**"——每次都要在文件**末尾续写**、保留之前的内容。所以写入必须用 "a"（append 追加）模式，绝不能用 "w"。读取用 "r" 模式把全部内容读出。

**参考代码**：
\`\`\`python
def save_diary(text):
    # "a" 追加模式：在文件末尾续写，不清空已有内容
    with open("diary.txt", "a", encoding="utf-8") as f:
        f.write(text + "\\n")     # 每条日记占一行，末尾加换行

def read_diary():
    # "r" 只读模式：读出全部内容
    with open("diary.txt", "r", encoding="utf-8") as f:
        content = f.read()
    print(content)

# 使用：连续记三条，都会保留下来
save_diary("今天天气很好")
save_diary("学会了文件追加")
save_diary("明天继续加油")
read_diary()
\`\`\`

**执行结果**：diary.txt 里依次有三行——"今天天气很好""学会了文件追加""明天继续加油"，read_diary() 会把它们全部打印出来。

**为什么用 "a" 而不是 "w"**：
- **"w"（写入/覆盖）模式**：一打开就**清空整个文件**，再写入新内容。如果用 "w"，每调用一次 save_diary 都会把之前的日记全删掉、只剩最新这一条——完全达不到"日记累积"的目的。
- **"a"（追加）模式**：不清空原内容，每次都在文件**末尾续写**。这正是"日记一条条往后加"需要的行为。

**记住这条对照**：**想保留旧内容、往后续写 → 用 "a"；想推倒重来、清空重写 → 用 "w"**。日记、日志这类"只增不删"的场景，永远用 "a"。`
      },
      {
        problem: '小明想保存一个班级成绩字典 grades = {"小明": 90, "小红": 85, "小华": 78} 到文件，关机后还能读回来继续用。他用 f.write(grades) 结果报错 TypeError: write() argument must be str。请解释为什么报错，并用 JSON 给出正确做法（写出存和读的完整代码）。',
        hint: 'write() 只能写字符串，不能直接写字典。保存有结构的数据（字典/列表）应该用 json 模块的 dump/load。',
        answer: `**详细讲解**：

**为什么报错**：f.write() **只能写字符串**，而 grades 是一个**字典**，类型不符，所以报 TypeError。文件本质上存的是文本（字符），不能直接把一个字典对象"塞"进去——必须先把字典转成某种文本格式。

**正确做法——用 JSON**：JSON 模块就是专门干这个的：它能把字典、列表等结构**转成文本写入文件**（dump），需要时再**从文件读回成原来的字典/列表**（load）。这是保存结构化数据最方便的方式。

**完整代码**：
\`\`\`python
import json

grades = {"小明": 90, "小红": 85, "小华": 78}

# ===== 保存：把字典写进文件 =====
with open("grades.json", "w", encoding="utf-8") as f:
    # dump 把字典转成 JSON 文本写入；ensure_ascii=False 让中文正常显示
    json.dump(grades, f, ensure_ascii=False)

# ===== 读取：把文件读回成字典 =====
with open("grades.json", "r", encoding="utf-8") as f:
    data = json.load(f)        # load 读回来还是一个字典

print(data)              # {'小明': 90, '小红': 85, '小华': 78}
print(data["小明"])      # 90 —— 可以像普通字典一样用
\`\`\`

**关键点解释**：
1. **json.dump(grades, f)**：把整个字典转成 JSON 格式的文本，一次写入文件。不用自己一个个拼字符串。
2. **json.load(f)**：从文件读回来，**类型还原成字典**——读回的 data 是真正的字典，可以直接 data["小明"] 取值、可以继续增删改。
3. **ensure_ascii=False**：不加这个参数，中文会被转成 uXXXX 这样的转义码（虽然能读回但文件里看不懂）；加上它，文件里就是正常的中文。
4. 配合 **encoding="utf-8"** 一起用，中文才不会乱码。

**一句话总结**：**字典、列表这类结构化数据不能直接 write，要用 json.dump 存、json.load 读**。这比自己手动拼字符串、再手动解析省事得多，也不容易出错。`
      },
      {
        problem: '解释 with open(...) as f: 这种写法和"手动 f = open(...) ... f.close()"相比，好在哪里？特别说明：如果不用 with、又恰好在读写过程中程序出错了，会发生什么问题？',
        hint: 'with 的核心好处是"自动关闭"。想想：手动 close 时，如果 close 之前的代码崩了，close 还会执行吗？文件没关会有什么后果？',
        answer: `**详细讲解**：

**两种写法对比**：
- 手动写法：先 f = open("data.txt", "w")，然后做读写操作，最后 f.close()。需要**自己记得**最后调用 close。
- with 写法：用 with open("data.txt", "w") as f: 打开，在缩进块里读写。代码块结束时**自动**关闭，不用写 close。

**with 好在哪里**：

**好处一：永远不会忘记关闭。** 手动写法很容易漏写 f.close()，或者代码改来改去把它弄丢了。with 把"关闭"这件事交给 Python 自动完成，从根本上杜绝了"忘记关"的问题。

**好处二（最关键）：即使中途出错，文件也一定会被关闭。** 这是 with 真正不可替代的地方。

**如果不用 with、又恰好在读写中途出错会怎样**：
假设手动这样写：
\`\`\`python
f = open("data.txt", "w", encoding="utf-8")
f.write("第一部分")
x = 10 / 0            # 这里出错了！程序在此抛异常
f.close()            # 这一行根本执行不到
\`\`\`
当 10 / 0 抛出异常时，程序**直接跳过后面的代码**，那句 f.close() **永远不会被执行**。后果可能是：
1. **数据没真正写到硬盘**——很多系统为了效率会把写入暂存在缓冲区，要等 close 时才真正落盘。文件没关，"第一部分"可能根本没写进去，造成**数据丢失**。
2. **资源泄漏**——这个文件一直被程序占着没释放，长期累积可能耗尽系统资源。

**而用 with 就没这个问题**：
\`\`\`python
with open("data.txt", "w", encoding="utf-8") as f:
    f.write("第一部分")
    x = 10 / 0        # 即使这里出错……
# with 在退出代码块时仍会自动关闭 f，数据该落盘的落盘、资源该释放的释放
\`\`\`
哪怕代码块里抛了异常，with 也**保证**会先把文件正确关闭，再把异常往外抛。

**一句话总结**：**with 自动管理文件的关闭，既不会忘记关，又能在出错时仍然安全关闭**——所以它是 Python 里处理文件的标准、推荐写法，应当养成"读写文件就用 with"的习惯。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 4. 数据库与 SQL
  // ───────────────────────────────────────────────────────────────
  'database': {
    warmup: {
      q: '你的程序要管理全校 5000 名学生的信息，还要经常做这种查询："找出三年级所有数学 90 分以上的学生，按分数从高到低排列"。如果把数据都存在一个大文件里，每次查询都要把整个文件读出来、自己写代码一个个筛选、再排序……数据一多就又慢又麻烦。有没有专门为"海量数据的存储和查询"设计的工具？',
      hint: '想一想：文件适合存少量、简单的数据。但当数据成千上万、还要频繁地按各种条件查询、排序、关联时，是不是需要一个更专业的"数据仓库"？',
      answer: '当数据量大、关系复杂、查询频繁时，文件就不够用了，这时需要**数据库**。关系型数据库把数据存成一张张**表**（行是一条条记录，列是字段），并提供一种叫 **SQL** 的专门语言来高效地增、删、改、查。比如开头那个查询，用一句 SQL 就能搞定，形如 SELECT name, score FROM students WHERE ... ORDER BY score DESC。数据库会自动帮你筛选、排序，又快又简洁。一句话：**海量、关系复杂、要频繁查询的数据，用数据库 + SQL，而不是手写文件处理。**'
    },
    intro: [
      '这一节讲**数据库与 SQL**——当数据多到文件扛不住时，如何专业地存储和查询。前提是你已经理解列表和字典（数据库的"表"和它们有相通之处）。',
      '为什么要用数据库？文件适合存少量、简单的数据，但当数据量很大（成千上万条）、关系复杂（学生关联班级、班级关联老师）、又要**频繁地按各种条件查询和排序**时，文件就力不从心了——每次都要读全文件、自己写筛选排序代码，又慢又乱。**关系型数据库**专门解决这个问题。',
      '关系型数据库把数据组织成一张张**表（table）**，非常像 Excel 表格：每一**行（row）**是一条记录（如一个学生），每一**列（column / 字段）**是记录的一个属性（如姓名、成绩）。每张表通常有一个**主键（primary key）**——一个能**唯一标识每一行**的字段（如学号），保证不会有两条记录混淆。理解"表、行、列、主键"是用好数据库的基础。',
      '操作数据库用的语言是 **SQL（结构化查询语言）**。最核心、最常用的是**查询**语句，基本结构是 SELECT 要哪些列 FROM 哪张表 WHERE 满足什么条件 ORDER BY 按什么排序。例如 SELECT name, score FROM students WHERE score >= 90 ORDER BY score DESC; 意思是"从 students 表里，查出成绩不低于 90 的记录的 name 和 score 两列，并按 score 从高到低排列"。除了查（SELECT），还有增（INSERT）、改（UPDATE）、删（DELETE）。',
      '一句话记住：**数据库把数据存成"表"（行是记录、列是字段、主键唯一标识每行），用 SQL 高效增删改查；查询的骨架是 SELECT…FROM…WHERE…ORDER BY。** 两个要警惕的点：① 给经常用来查询的字段建**索引**能大幅提速，没建索引的大表查询会很慢；② **千万别把用户输入直接拼进 SQL 语句**（会有"SQL 注入"安全漏洞），要用参数化查询——这两点后面细讲。'
    ],
    formulas: [
      { name: '查询语句骨架', latex: '\\text{SELECT ... FROM ... WHERE ... ORDER BY ...}',
        note: 'SELECT 选列、FROM 选表、WHERE 设过滤条件、ORDER BY 排序（DESC 降序 / ASC 升序）。这是最常用的 SQL 结构。' },
      { name: '插入一条记录', latex: '\\text{INSERT INTO 表(列...) VALUES(值...)}',
        note: '向表里新增一行。列和值要一一对应。例：INSERT INTO students(name, score) VALUES("小明", 90)。' },
      { name: '修改记录', latex: '\\text{UPDATE 表 SET 列=值 WHERE 条件}',
        note: '改已有记录。WHERE 限定改哪些行——千万别漏写 WHERE，否则会把整张表的该列全改掉！' },
      { name: '删除记录', latex: '\\text{DELETE FROM 表 WHERE 条件}',
        note: '删除满足条件的行。同样必须带 WHERE，漏写会清空整张表的数据，极其危险。' },
    ],
    methods: [
      {
        name: '写一条查询 SQL（SELECT）',
        when: '要从表里按条件找出需要的数据，并可能需要排序时',
        steps: [
          '想清楚：要**哪几列**（写在 SELECT 后，要全部列用星号 *）、从**哪张表**（写在 FROM 后）',
          '加过滤条件：用 WHERE 写"满足什么的行才要"，多个条件用 AND（且）/ OR（或）连接',
          '要排序就加 ORDER BY 列名，后面跟 DESC（从大到小）或 ASC（从小到大，默认）',
          '语句末尾加分号表示结束',
        ],
        example: {
          problem: '有一张 students 表，含 name、grade（年级）、score 三列。写一条 SQL，查出三年级里成绩及格（不低于 60）的学生姓名和分数，按分数从高到低排列。',
          solution: [
            '解：逐部分拼出来。',
            '要的列：name, score → SELECT name, score；从哪张表：FROM students。',
            '条件：三年级 且 及格 → WHERE grade = 3 AND score >= 60。',
            '排序：分数从高到低 → ORDER BY score DESC。',
            '答：完整语句是 SELECT name, score FROM students WHERE grade = 3 AND score >= 60 ORDER BY score DESC; 数据库会自动完成筛选和排序，不用自己写循环。',
          ],
        },
      },
      {
        name: '安全地执行带用户输入的查询（防 SQL 注入）',
        when: '查询条件来自用户输入（如搜索框、登录表单），需要防止安全漏洞时',
        steps: [
          '**绝不**把用户输入用字符串拼接直接拼进 SQL 语句',
          '改用**参数化查询**：SQL 里要填用户值的位置写占位符（Python sqlite3 用问号 ?）',
          '把用户输入作为**单独的参数**传给 execute()，由数据库库负责安全地填入',
          '这样用户输入只会被当作"数据值"，绝不会被当成"SQL 命令"执行，注入攻击就失效了',
        ],
        example: {
          problem: '用户在搜索框输入一个名字 user_name，要查这个学生的成绩。为什么不能把 user_name 直接用加号拼进 SQL 字符串？正确怎么写？',
          solution: [
            '解：危险写法把用户输入直接拼进 SQL。若用户故意输入一段恒为真的条件（如 单引号 OR 1=1），拼出来的语句条件恒成立，会把**整张表**的数据都查出来——这就是 SQL 注入。',
            '正确做法用参数化：SQL 写成 "SELECT * FROM students WHERE name = ?"，把 user_name 作为参数单独传。',
            '代码：cursor.execute("SELECT * FROM students WHERE name = ?", (user_name,))。',
            '答：占位符 ? 配合单独传参，user_name 只会被当作一个"名字字符串"去匹配，无论用户输入什么都不会改变 SQL 的结构，从根上杜绝注入。',
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 用 Python 操作 SQLite 数据库（建表·插入·查询）',
        problem: '请用 Python 内置的 sqlite3 模块，完成一个完整的小流程：创建一张 students 表（含 id 主键、name、score），插入三条学生记录，然后查出成绩不低于 85 的学生并打印。',
        steps: [
          `第一步：连接数据库、建表。sqlite3 是 Python 自带的轻量数据库，无需安装。
import sqlite3

# 连接数据库（文件不存在会自动创建）
conn = sqlite3.connect("school.db")
cursor = conn.cursor()          # 游标：用来执行 SQL

# 建表：id 是主键（唯一标识每行），name 文本，score 整数
cursor.execute("""
    CREATE TABLE IF NOT EXISTS students (
        id INTEGER PRIMARY KEY,
        name TEXT,
        score INTEGER
    )
""")`,
          '第二步：理解几个概念。connect 打开（或创建）数据库文件；cursor（游标）是执行 SQL 的工具；CREATE TABLE 建表，IF NOT EXISTS 表示"表不存在才创建"（避免重复运行报错）；PRIMARY KEY 把 id 设为主键，保证每行 id 唯一。',
          `第三步：插入记录。用参数化（占位符 ?）安全地插入，避免拼字符串。
students = [(1, "小明", 90), (2, "小红", 82), (3, "小华", 88)]
for s in students:
    cursor.execute("INSERT INTO students (id, name, score) VALUES (?, ?, ?)", s)

conn.commit()      # 关键！提交事务，改动才真正写入数据库文件`,
          '第四步：注意两点。① 插入用占位符 ? + 传参，是防 SQL 注入的标准写法；② **增删改之后必须 conn.commit()**，否则改动只停留在内存、不会真正保存到数据库文件——这是初学者最常忘的一步。',
          `第五步：查询。用 SELECT 查出成绩不低于 85 的学生，fetchall 取出所有结果。
cursor.execute("SELECT name, score FROM students WHERE score >= 85 ORDER BY score DESC")
rows = cursor.fetchall()        # 取出所有匹配的行，是一个列表，每行是元组

for name, score in rows:
    print(name, score)

conn.close()       # 用完关闭连接`,
          '第六步：分析结果。三人中成绩不低于 85 的是小明(90)、小华(88)，按 score DESC 排序后输出："小明 90" / "小华 88"（小红 82 不满足条件被过滤掉）。整个流程展示了数据库操作的完整闭环：**连接 → 建表 → 插入(记得 commit) → 查询(fetchall) → 关闭**，且增删改查全程用参数化保证安全。',
        ],
      },
    ],
    pitfalls: [
      '**把用户输入直接拼进 SQL（SQL 注入漏洞）**。这是最严重的安全问题。把用户输入用加号拼进 WHERE 条件，用户输入恶意内容（如一段恒为真的条件）就能篡改语句、窃取或破坏数据。**永远用参数化查询（占位符 ? + 单独传参）**，绝不字符串拼接。',
      'UPDATE / DELETE **漏写 WHERE 条件**。DELETE FROM students 不带 WHERE 会**删光整张表**！UPDATE students SET score=0 不带 WHERE 会把**所有人**的成绩改成 0。改、删之前务必确认 WHERE 限定了正确的行。',
      '增删改之后**忘了 commit**。用 sqlite3 等执行 INSERT/UPDATE/DELETE 后，必须 conn.commit() 才会真正写入数据库文件；忘了 commit，程序一关改动就丢了，还以为"没保存成功"。',
      '**大表不建索引导致查询很慢**。WHERE、ORDER BY 经常用到的字段如果没建索引，数据库只能逐行扫描，数据一多就很慢。给常用查询字段建索引能大幅提速（代价是占用一点额外空间、写入略慢）。',
      '把 == 当成 SQL 的相等判断。SQL 里判断相等用**单个等号 =**（如 WHERE score = 90），不是编程语言里的 ==。另外字符串值要用**单引号**括起来（如 WHERE name = 小明 要给小明加单引号）。',
    ],
    exercises: [
      {
        problem: '有一张 books 表，字段有 title（书名）、author（作者）、price（价格）、year（出版年份）。请分别写出三条 SQL：\n(1) 查出所有 2020 年以后（含 2020）出版的书的书名和价格；\n(2) 查出价格低于 50 元的书，按价格从低到高排列；\n(3) 把作者"鲁迅"的所有书的价格都改成 35 元。',
        hint: '查询用 SELECT…FROM…WHERE，排序加 ORDER BY（ASC 升序）。修改用 UPDATE…SET…WHERE。字符串值用单引号，别忘了 WHERE。',
        answer: `**详细讲解**：

**(1) 查 2020 年以后出版的书的书名和价格**

思路：要 title、price 两列 → SELECT；条件是 year 不小于 2020 → WHERE year >= 2020。
\`\`\`sql
SELECT title, price FROM books WHERE year >= 2020;
\`\`\`
- "2020 年以后（含 2020）"是 year >= 2020（大于等于）。如果题目说"2020 年以后（不含）"才用 > 2020，看清楚是否含边界。

**(2) 查价格低于 50 的书，按价格从低到高排列**

思路：条件 price < 50 → WHERE；从低到高 → ORDER BY price ASC（ASC 是升序，也可省略，默认就是升序）。
\`\`\`sql
SELECT * FROM books WHERE price < 50 ORDER BY price ASC;
\`\`\`
- 用星号 * 表示查所有列。"从低到高"是升序 ASC（默认），"从高到低"才是 DESC。

**(3) 把作者"鲁迅"的所有书价格改成 35**

思路：这是修改 → UPDATE；改 price 这一列 → SET price = 35；只改作者是鲁迅的 → WHERE author = '鲁迅'。
\`\`\`sql
UPDATE books SET price = 35 WHERE author = '鲁迅';
\`\`\`

**最重要的提醒——千万别漏 WHERE**：如果第 (3) 题写成 UPDATE books SET price = 35;（没有 WHERE），就会把**表里所有书**的价格统统改成 35！UPDATE 和 DELETE 一旦漏写 WHERE，影响的就是整张表，是非常危险的错误。改、删之前一定要检查 WHERE 是否正确限定了目标行。

**两个语法细节**：
1. **字符串值要用单引号**：'鲁迅' 要加单引号，写成 author = 鲁迅 会报错。
2. **相等用单个 =**：SQL 里判断相等是一个等号 =，不是编程语言的 ==。`
      },
      {
        problem: '小明做了个登录功能，他把用户输入的 name 和 pwd 用加号直接拼进了 SQL 字符串：sql = "SELECT * FROM users WHERE username = ..." 这样拼出来。安全工程师说这有严重漏洞。请说明这是什么漏洞、举一个攻击例子，并给出正确的写法。',
        hint: '想想如果用户在密码框输入一段恒为真的条件（如 单引号 OR 1=1），拼出来的 SQL 会变成什么？条件会不会恒为真？正确做法是用参数化查询（占位符）。',
        answer: `**详细讲解**：

**这是什么漏洞**：这是 **SQL 注入（SQL Injection）**——把用户输入**直接用字符串拼接**进 SQL 语句，导致用户能通过精心构造的输入**篡改 SQL 的结构**，绕过验证、窃取甚至破坏数据。

**攻击例子**：假设攻击者在**密码框**输入一段构造好的内容，让它拼接后在 SQL 末尾形成 OR 1=1 这样的条件（用户名随便填个 admin）。拼接后的 SQL 大致变成：
\`\`\`sql
SELECT * FROM users WHERE username = 'admin' AND password = '' OR '1'='1'
\`\`\`
注意末尾的 OR '1'='1'——它**永远为真**，整个 WHERE 条件因此恒成立。结果数据库返回**所有用户**，攻击者**不知道密码也能登录成功**（甚至直接拿到第一个用户、往往是管理员）。更恶劣的注入还能删表、读取敏感数据。根本原因是：用户输入的内容被数据库**当成了 SQL 命令的一部分去执行**，而不是当成普通的"数据值"。

**正确写法——参数化查询**：SQL 里用占位符（sqlite3 用问号 ?），把用户输入作为**单独的参数**传给 execute，由数据库库负责安全地填入：
\`\`\`python
sql = "SELECT * FROM users WHERE username = ? AND password = ?"
cursor.execute(sql, (name, pwd))     # name、pwd 作为参数单独传，不拼进字符串
result = cursor.fetchone()
\`\`\`

**为什么参数化能防住注入**：用参数化时，无论用户输入什么（哪怕是一段构造的 OR 1=1），数据库都只会把它当作**一个普通的字符串值**去和 username/password 比较，**绝不会把它当成 SQL 命令解析**。也就是说，用户输入永远只能是"数据"、不可能变成"代码"，SQL 的结构被锁死了，注入自然就失效了。

**记住这条铁律**：**永远不要把用户输入用字符串拼接进 SQL，一律使用参数化查询（占位符 + 单独传参）**。这是数据库编程最重要的安全准则，没有例外。`
      },
      {
        problem: '一张 orders 表有 100 万条订单记录，程序经常执行 SELECT * FROM orders WHERE user_id = ? 来查某个用户的订单，但发现越来越慢。同事说"给 user_id 建个索引就快了"。请解释：为什么没索引会慢？索引为什么能加速？建索引有没有代价？',
        hint: '没索引时数据库怎么找符合条件的行？是不是要一行行看完 100 万条？索引类似什么（想想书的目录）？',
        answer: `**详细讲解**：

**为什么没索引会慢**：没有索引时，数据库执行 WHERE user_id = ? 只能做**全表扫描**——把 100 万条记录**从头到尾一行一行地检查**，看哪些行的 user_id 等于目标值。即使符合条件的只有几条，也得把 100 万行全看一遍才能确定。这就是 $O(n)$ 的线性查找，数据量越大越慢。

**索引为什么能加速**：**索引类似一本书的"目录"**。书没有目录时，要找某个内容只能一页页翻（全表扫描）；有了目录，先在目录里快速定位到页码，直接翻过去即可。数据库索引也是如此：它在 user_id 这个字段上建立一个**排好序的、便于快速查找的结构**（通常是 B 树），查询时不用逐行扫描，而是像查目录/二分查找一样**快速定位**到目标行，复杂度从 $O(n)$ 降到约 $O(\\log n)$。100 万条数据，全表扫描要看 100 万行，用索引大约只要 20 次左右——天壤之别。

**所以建索引后**：这条 WHERE user_id = ? 的查询会从"扫描 100 万行"变成"通过索引快速定位"，速度大幅提升。WHERE 和 ORDER BY 经常用到的字段，都值得考虑建索引。

**建索引有没有代价**：有，不是免费的午餐：
1. **占用额外存储空间**：索引本身是一份额外的数据结构，要占硬盘空间。
2. **拖慢写入**：每次 INSERT/UPDATE/DELETE 改动数据时，相关的索引也要**同步更新维护**，所以写操作会比没索引时稍慢。
3. 因此不该给每个字段都建索引——只给**经常用于查询/排序条件**的字段建。很少用来查的字段建索引，得不偿失。

**一句话总结**：**索引像书的目录，用"查目录"代替"逐页翻"，把大表查询从 $O(n)$ 提速到约 $O(\\log n)$；代价是占空间、让写入变慢，所以要给常查的字段建、而不是全建**。这正是 intro 里"没建索引的大表查询会很慢"那条提醒的原理。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 5. 网络与 Web 开发
  // ───────────────────────────────────────────────────────────────
  'web-basics': {
    warmup: {
      q: '你在浏览器地址栏输入一个网址、按下回车，几百毫秒后一个完整的网页就出现了。这中间到底发生了什么？网页的内容是"本来就在你电脑里"，还是从某个地方"取回来"的？你的浏览器和那个"地方"是怎么对话的？',
      hint: '想一想：同一个网址，你和地球另一端的人打开看到的内容一样，说明内容存在某个公共的地方。你的浏览器是不是先发了个"请求"过去、对方再"回应"？',
      answer: '上网的本质是**客户端（你的浏览器）向服务器发送"请求"、服务器返回"响应"**。网页内容存在远端的**服务器**上，你输入网址按回车，浏览器就向那台服务器发出一个 HTTP **请求**（"请把这个页面给我"），服务器处理后把网页内容作为**响应**发回来，浏览器再把它渲染成你看到的页面。这一来一回就是 **HTTP 协议**。理解"请求—响应""客户端—服务器""GET/POST 方法""API 接口"，你才能做网站、调用网络服务、写爬虫。一句话：**网页是浏览器向服务器"请求"后取回来的，二者靠 HTTP 一问一答。**'
    },
    intro: [
      '这一节讲**网络与 Web 开发的基本原理**——上网到底是怎么回事，以及怎么用程序去和网络服务对话。前提是你已经会用函数和导入库（模块与库）。',
      '上网的核心模型是**客户端—服务器（client-server）**：**客户端**是发起请求的一方（你的浏览器、手机 App、或你写的程序），**服务器**是提供数据和服务的一方（存着网页和数据的远程电脑）。它们之间靠 **HTTP 协议**通信，模式永远是"**一问一答**"——客户端发一个**请求（request）**，服务器回一个**响应（response）**。你打开网页、刷新朋友圈、查天气，背后都是这样一次次请求—响应。',
      'HTTP 请求里最重要的是**方法（method）**，最常见两种：**GET**——"我想**获取**数据"（如打开网页、查询信息），参数通常跟在网址后面；**POST**——"我想**提交**数据给服务器"（如登录、发帖、上传），数据放在请求体里。还有 PUT（更新）、DELETE（删除）等。每次响应会带一个**状态码**：200 表示成功、404 表示"找不到页面"、500 表示"服务器出错"——看状态码就能知道请求结果如何。',
      '现代网络应用普遍是**前后端分工**：**前端**（运行在浏览器里的页面，负责显示和交互）通过调用**API 接口**向**后端**（服务器程序，负责处理逻辑、读写数据库）要数据，后端查好数据库后把结果（通常是 **JSON 格式**）返回给前端，前端再展示出来。**API** 就是"程序之间约定好的调用接口"——你按它的规则发请求，它按约定返回数据。很多服务（天气、地图、翻译）都提供 API 供别人调用。',
      '用 Python 可以很方便地当"客户端"去调用网络服务，常用 **requests** 库：requests.get(网址) 发 GET 请求、.json() 把返回的 JSON 转成字典。一句话记住：**上网是客户端向服务器发请求、服务器回响应（HTTP 一问一答）；GET 取数据、POST 交数据；前端调 API、后端查库返回 JSON。** 一条必须牢记的安全准则——**永远不要相信用户输入的数据，要在服务器端校验**，否则会有安全漏洞。'
    ],
    formulas: [
      { name: '通信模型', latex: '\\text{客户端 请求 → 服务器 响应}',
        note: '上网的本质：客户端（浏览器/程序）发请求，服务器处理后返回响应，永远是"一问一答"。' },
      { name: '两种常用方法', latex: '\\text{GET 取数据 / POST 交数据}',
        note: 'GET 用来获取数据（参数常在网址上）；POST 用来提交数据（数据放请求体）。还有 PUT 改、DELETE 删。' },
      { name: '常见状态码', latex: '\\text{200 成功 / 404 找不到 / 500 服务器错}',
        note: '响应附带状态码表示结果：2xx 成功、4xx 客户端错误（如 404 页面不存在）、5xx 服务器错误。' },
      { name: 'Python 发请求', latex: '\\text{requests.get(url).json()}',
        note: '用 requests 库当客户端：get 发请求拿到响应，.json() 把返回的 JSON 文本转成 Python 字典。' },
    ],
    methods: [
      {
        name: '用 requests 调用一个 Web API 获取数据',
        when: '想用程序从网络服务（天气、汇率、翻译等 API）获取数据时',
        steps: [
          'import requests（第三方库，需先 pip install requests）',
          '看清 API 文档：要用哪个网址、是 GET 还是 POST、要带哪些参数',
          '发请求：GET 用 requests.get(url, params={...})，把查询参数放进 params 字典',
          '先检查 r.status_code == 200 确认成功，再用 r.json() 把返回的 JSON 转成字典，按字段取出需要的数据',
        ],
        example: {
          problem: '假设有一个天气 API：GET 请求某个网址，带参数 city（城市名），返回 JSON 形如 {"city": "北京", "temp": 25}。怎样用 Python 取出北京的温度？',
          solution: [
            '解：用 requests.get 发请求、params 传城市、json() 解析。',
            'import requests；r = requests.get(网址, params={"city": "北京"})。',
            '检查 if r.status_code == 200: data = r.json()（data 是字典 {"city":"北京","temp":25}）。',
            '取温度：print(data["temp"]) → 25。',
            '答：params 字典里的参数会被自动拼到网址上；返回的 JSON 经 .json() 变成字典后，像普通字典一样用 data["temp"] 取值即可。',
          ],
        },
      },
      {
        name: '在服务器端校验用户输入（保证安全）',
        when: '后端接收来自前端/用户的数据（表单、参数）时——也就是所有真实 Web 应用',
        steps: [
          '牢记前提：**前端的任何校验都不可信**——用户可以绕过浏览器直接发请求，所以校验必须在**服务器端**再做一遍',
          '检查数据**是否存在、类型/格式是否正确**（如年龄应是数字、邮箱要符合格式）',
          '检查数据**是否在合理范围**（如年龄 0~150、数量不能为负）',
          '涉及数据库时用**参数化查询**防 SQL 注入；不合法就拒绝并返回清晰的错误提示，绝不直接信任',
        ],
        example: {
          problem: '一个注册接口接收用户提交的 age（年龄）。前端已经做了校验，后端还需要再校验吗？为什么？应该校验什么？',
          solution: [
            '解：**必须再校验**。因为前端校验只是"方便用户"，用户完全可以绕过浏览器（用工具直接发请求）提交任意数据，前端那层挡不住恶意或错误输入。',
            '后端要校验：① age 是否真的传来了（存在性）；② 是否是合法数字（类型）；③ 是否在合理范围（如 0 < age < 150）。',
            '不合法就返回错误（如状态码 400 + 提示），不写入数据库。',
            '答：安全准则是"永远不要相信用户输入"。前端校验提升体验、后端校验保证安全，两者都要，且**后端校验绝不能省**。',
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 用 Python 调用公开 API 并解析返回的 JSON',
        problem: '假设有一个 IP 信息 API：向某个网址发 GET 请求，会返回 JSON 形如 {"ip": "1.2.3.4", "country": "中国", "city": "北京"}。请写一段 Python 程序，请求这个接口、检查是否成功、取出并打印 country 和 city，并对网络出错做基本处理。',
        steps: [
          `第一步：导入库、发出 GET 请求。
import requests

url = "https://api.example.com/ip"
# 发 GET 请求；加 timeout 防止网络卡住时一直等
response = requests.get(url, timeout=5)`,
          '第二步：理解这一步。requests.get(url) 就是以"客户端"身份向服务器发了一个 HTTP GET 请求（"请把 IP 信息给我"），返回的 response 对象里装着服务器的响应（状态码、内容等）。timeout=5 表示最多等 5 秒，避免网络异常时程序卡死。',
          `第三步：检查状态码，确认请求成功。
if response.status_code == 200:        # 200 表示成功
    data = response.json()             # 把返回的 JSON 文本转成字典
    print("国家：", data["country"])
    print("城市：", data["city"])
else:
    print("请求失败，状态码：", response.status_code)`,
          '第四步：理解 status_code 和 json()。每个响应都带状态码：**200 是成功**，404 是找不到、500 是服务器出错。确认是 200 后，用 response.json() 把返回的 JSON 字符串解析成 Python **字典**，之后就能像普通字典一样 data["country"] 取值。',
          `第五步：加上异常处理，应对网络问题。网络请求随时可能失败（断网、超时、服务器没响应），要用 try-except 兜住。
import requests

url = "https://api.example.com/ip"
try:
    response = requests.get(url, timeout=5)
    if response.status_code == 200:
        data = response.json()
        print("国家：", data["country"], "城市：", data["city"])
    else:
        print("请求失败，状态码：", response.status_code)
except requests.exceptions.RequestException as e:
    print("网络出错了：", e)      # 断网、超时等都会被这里接住`,
          '第六步：分析与作答。正常情况下程序会打印"国家： 中国 城市： 北京"。整段代码体现了调用 API 的完整、健壮流程：**发请求（带 timeout）→ 检查状态码（200 才算成功）→ json() 解析成字典取数据 → 用 try-except 兜住网络异常**。其中"检查状态码"和"异常处理"是初学者最容易省略、但真实项目里必不可少的两步。',
        ],
      },
    ],
    pitfalls: [
      '**相信用户/前端传来的数据**。这是头号安全错误。用户能绕过浏览器直接发请求，所以**前端校验不可信，服务器端必须再校验一遍**（存在性、类型、范围）。涉及数据库还要用参数化查询防注入。记住："永远不要相信用户输入"。',
      '搞混 GET 和 POST 的用途。**GET 用来"获取"数据**（参数在网址上、会被缓存和留在历史记录里，不要用 GET 传密码等敏感信息）；**POST 用来"提交"数据**（数据在请求体里，适合登录、上传）。用反了会有功能或安全问题。',
      '发完请求**不检查状态码就直接用数据**。请求可能失败（404、500），不检查 status_code 就 .json() 取值，会拿到错误内容或直接报错。应先确认 status_code == 200 再处理。',
      '网络请求**不做超时和异常处理**。网络随时可能断、慢、服务器没响应。不加 timeout 程序可能一直卡住；不用 try-except 兜住，一断网整个程序就崩。真实项目里这两样必不可少。',
      '以为前端（浏览器里看到的页面）就是全部。现代 Web 是**前后端分工**：前端只管显示和交互，真正的逻辑和数据在**后端 + 数据库**。把校验、密码处理等放在前端是不安全的（前端代码用户都能看到、改掉）。',
    ],
    exercises: [
      {
        problem: '用自己的话解释"客户端—服务器"模型和 HTTP 的"请求—响应"过程：当你在浏览器输入网址、按下回车，到看到网页，中间发生了哪些步骤？并说明 GET 和 POST 两种请求方法分别用在什么场景。',
        hint: '谁是客户端、谁是服务器？浏览器先做什么（发什么）、服务器再做什么（回什么）？GET 是"取"、POST 是"交"。',
        answer: `**详细讲解**：

**客户端—服务器模型**：上网涉及两个角色——**客户端**（你的浏览器，发起请求的一方）和**服务器**（远程电脑，存着网页和数据、提供服务的一方）。所有上网行为都是这两者之间的对话。

**输入网址到看到网页的步骤**（HTTP 请求—响应过程）：
1. **你输入网址、按回车** —— 告诉浏览器你想访问哪个页面。
2. **浏览器（客户端）发出 HTTP 请求** —— 浏览器根据网址找到对应的服务器，向它发送一个请求，内容相当于"请把这个页面的内容给我"。
3. **服务器接收并处理请求** —— 服务器收到请求，找到（或动态生成）对应的网页内容，可能还会去查数据库。
4. **服务器返回 HTTP 响应** —— 把网页内容（HTML、数据等）连同一个**状态码**（200 表示成功）作为响应发回给浏览器。
5. **浏览器渲染页面** —— 浏览器收到响应内容，把它解析、排版、渲染成你最终看到的图文页面。

整个过程就是一次"**一问一答**"：客户端问（请求）、服务器答（响应）。这就是 HTTP 协议的核心。

**GET 和 POST 的使用场景**：
- **GET——用来"获取/读取"数据。** 比如打开网页、查询天气、搜索内容。参数通常跟在网址后面（如 ?city=北京）。特点：会被缓存、会留在浏览器历史里——所以**不要用 GET 传密码等敏感信息**。
- **POST——用来"提交/发送"数据给服务器。** 比如登录（提交账号密码）、发帖、上传文件、注册。数据放在请求体里，不显示在网址上，更适合传敏感或大量的数据。

**一句话区分**：**想从服务器"拿东西"用 GET，想给服务器"交东西"用 POST**。这是最基本、最常用的两种 HTTP 方法。`
      },
      {
        problem: '假设有一个汇率 API：GET 请求某个网址，带参数 from 和 to（如 from=USD、to=CNY），返回 JSON 形如 {"from": "USD", "to": "CNY", "rate": 7.2}。请写一段 Python 程序，查询 1 美元(USD)兑人民币(CNY)的汇率，检查请求是否成功，取出 rate 并打印。',
        hint: '用 requests.get，参数用 params={"from": "USD", "to": "CNY"}。先判断 status_code == 200，再用 .json() 解析取 rate。',
        answer: `**详细讲解**：

**思路**：四步——① import requests；② 用 get 发请求、params 传 from/to 参数；③ 检查 status_code 是否 200；④ 用 json() 解析成字典、取出 rate。

**参考代码**：
\`\`\`python
import requests

url = "https://api.example.com/rate"
# 查询参数放进 params 字典，会被自动拼成 ?from=USD&to=CNY
params = {"from": "USD", "to": "CNY"}

try:
    response = requests.get(url, params=params, timeout=5)
    if response.status_code == 200:          # 200 表示请求成功
        data = response.json()               # JSON 转成字典
        rate = data["rate"]                  # 取出汇率字段
        print("1 美元 =", rate, "人民币")
    else:
        print("请求失败，状态码：", response.status_code)
except requests.exceptions.RequestException as e:
    print("网络出错：", e)
\`\`\`

**执行结果**：正常时打印"1 美元 = 7.2 人民币"。

**关键点解释**：
1. **params 字典**：把查询参数交给 params，requests 会自动把它们拼到网址后面变成 ?from=USD&to=CNY，比自己拼字符串更安全、更省事。
2. **检查 status_code == 200**：不能发完请求就直接取数据——万一接口返回 404（找不到）或 500（服务器错），response 里就没有正常数据。先确认是 200 才解析。
3. **response.json()**：服务器返回的是 JSON 格式的**文本**，要用 .json() 把它转成 Python **字典**，之后才能 data["rate"] 取值。
4. **try-except + timeout**：网络请求可能断网、超时，用 try-except 兜住异常、加 timeout 防止卡死，程序才健壮。

**一句话总结**：调用 API 的标准流程是 **get(带 params 和 timeout) → 检查 status_code → json() 解析取值 → try-except 兜住网络异常**。这几步缺一不可。`
      },
      {
        problem: '小明做了一个网站，登录时在浏览器里（前端 JavaScript）检查了密码长度必须不少于 6 位。他说"前端已经检查了，后端就不用再检查了"。这样安全吗？为什么"永远不要相信用户输入"？请举例说明只靠前端校验的危险。',
        hint: '前端代码运行在哪里？用户能不能看到、改掉前端代码，或者干脆绕过浏览器直接给服务器发请求？',
        answer: `**详细讲解**：

**结论：不安全**。只靠前端校验、后端不校验，是一个严重的安全隐患。

**为什么"永远不要相信用户输入"**：核心原因是——**前端代码运行在用户的浏览器里，用户对它有完全的控制权**。这意味着：
1. **用户能看到并修改前端代码**：前端的 JavaScript 校验逻辑，用户打开浏览器开发者工具就能看到，甚至能直接改掉或删掉那段校验。
2. **用户能完全绕过浏览器**：攻击者根本不需要用你的网页，可以用工具（如 curl、Postman、自己写的脚本）**直接向服务器发请求**，前端那层校验**压根不会被执行**。

所以前端校验拦得住"老老实实用网页的普通用户的手误"，但**拦不住任何有意绕过的人**。

**只靠前端校验的危险举例**：
- 小明的网站前端要求密码不少于 6 位。攻击者绕过浏览器，直接给后端发一个请求，提交一个空密码或 1 位密码。因为后端没校验，这种不合规的密码就被直接写进了数据库——埋下账户被轻易攻破的隐患。
- 更危险的是，如果后端对其他输入（如评论、搜索词）也不校验、不过滤，攻击者还能借机发起 **SQL 注入、XSS（跨站脚本）** 等攻击，窃取数据或破坏系统。

**正确做法——前后端都要校验，但职责不同**：
- **前端校验**：目的是**提升用户体验**——用户输错了立刻提示，不用等服务器来回，反应快、体验好。
- **后端校验**：目的是**保证安全**——这是真正的"防线"。无论数据从哪来，后端都要再检查一遍（存在性、长度、类型、范围、格式），不合法就拒绝。

**一句话总结**：**前端校验是"为了好用"，后端校验是"为了安全"，两者都要，而后端校验绝不能省**。安全的黄金准则就是这句话——**永远不要相信用户输入，所有来自客户端的数据都要在服务器端重新校验**。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 6. 数据分析与可视化
  // ───────────────────────────────────────────────────────────────
  'data-analysis-py': {
    warmup: {
      q: '你做了一个科创项目，采集了 500 行实验数据存在一个 CSV 表里。现在要回答："平均值是多少？""不同组之间有没有差异？""趋势是上升还是下降？"如果用纯 Python 一行行读、自己写循环求和、再手动画图，又慢又容易错。有没有"代码版的 Excel"，几行就能完成统计、还能一键画出图表？',
      hint: '想一想：处理表格数据（求平均、分组统计、筛选）有没有专门的工具？把数据变成图表，让结论一目了然，又该用什么？',
      answer: '有！Python 里 **pandas** 就是"代码版的 Excel"——它把表格读成一个叫 DataFrame 的结构，求平均、筛选、分组统计都只要一两行；**matplotlib** 则负责把数据画成柱状图、折线图等，让趋势和差异一眼看清。数据分析的标准流程是"**清洗 → 统计 → 可视化 → 得出结论**"：先把杂乱数据（缺失、重复、格式不一）整理干净，再统计，再画图，最后下结论。这是科创项目把"一堆原始数据"变成"有说服力的结论"的关键能力。一句话：**pandas 处理表格、matplotlib 画图，让数据说话。**'
    },
    intro: [
      '这一节讲**用 Python 做数据分析与可视化**——把一堆原始数据，变成清晰的统计结论和直观的图表。这在科创项目里尤其重要：实验采集了数据之后，正是靠它把数据变成有说服力的结论。前提是你熟悉列表和字典。',
      '真实世界的数据往往很**杂乱**：有缺失（某些格子是空的）、有重复（同一条记录录了两遍）、格式不统一（日期一会儿写 2024-1-1、一会儿写 2024/01/01）。所以数据分析的标准流程是四步：**清洗 → 统计 → 可视化 → 得出结论**。先把数据整理干净，再统计分析，再画成图，最后基于图表下结论。跳过清洗直接分析，很可能得出错误结论。',
      '主力工具是 **pandas**，它相当于"**代码版的 Excel**"。pandas 把表格数据读成一个叫 **DataFrame**（数据框）的结构——你可以把它想象成内存里的一张二维表，有行有列。常用操作：pd.read_csv("文件") 读入 CSV；df["列名"] 取某一列；df["score"].mean() 求平均（还有 sum 求和、max/min、count 计数、median 中位数等）；df[df["score"] >= 90] 筛选出满足条件的行；df.groupby("class").mean() 按某列**分组统计**（如按班级分别求平均）。这些用纯 Python 写要一堆循环，用 pandas 一行搞定。',
      '把数据变成图表用 **matplotlib**，让趋势和差异一目了然。常用图：**柱状图**（plt.bar，比较不同类别的数值，如各班平均分）、**折线图**（plt.plot，看随时间/顺序的变化趋势，如气温逐月变化）、**散点图**（plt.scatter，看两个变量的关系）、**饼图**（plt.pie，看占比）。画完用 plt.xlabel/ylabel 加坐标轴标签、plt.title 加标题、plt.show() 显示。**选对图表类型**很关键：比大小用柱状图、看趋势用折线图。',
      '一句话记住：**数据分析 = 清洗→统计→可视化→结论；pandas 当"代码版 Excel"处理表格（read_csv / mean / groupby / 筛选），matplotlib 画图（bar 柱状、plot 折线）。** 两条要警惕的准则：① **图表别误导人**——纵轴尽量从 0 开始（否则会夸大差异）、选对图表类型；② **相关不等于因果**——两个量一起变化，不代表一个导致了另一个，下结论要谨慎。'
    ],
    formulas: [
      { name: '读入表格数据', latex: '\\text{df = pd.read\\_csv("file.csv")}',
        note: '用 pandas 把 CSV 文件读成 DataFrame（数据框），即内存里的一张二维表，之后所有分析都基于它。' },
      { name: '常用统计', latex: '\\text{df["列"].mean() / sum() / count()}',
        note: 'mean 求平均、sum 求和、count 计数、max/min 极值、median 中位数。对某一列一行代码就出结果。' },
      { name: '分组统计', latex: '\\text{df.groupby("class").mean()}',
        note: '按某列的值分组，再对每组分别统计（如按班级分别求平均分）。这是数据分析里极常用的操作。' },
      { name: '画图', latex: '\\text{plt.bar(...) / plt.plot(...)}',
        note: 'matplotlib：bar 画柱状图（比大小）、plot 画折线图（看趋势）、scatter 散点、pie 饼图。最后 plt.show() 显示。' },
    ],
    methods: [
      {
        name: '用 pandas 读取并统计表格数据',
        when: '有一份表格数据（CSV/Excel），要快速求平均、筛选、分组统计时',
        steps: [
          'import pandas as pd（习惯简称 pd）',
          '用 df = pd.read_csv("文件.csv") 读入数据成 DataFrame；可先 df.head() 看前几行、df.info() 了解结构',
          '**清洗**：用 df.dropna() 删掉有缺失的行（或 fillna 填补）、df.drop_duplicates() 去重',
          '**统计**：取列 df["列名"] 后调 mean/sum/count；按组统计用 df.groupby("分组列")["数值列"].mean()；筛选用 df[df["列"] > 阈值]',
        ],
        example: {
          problem: '有一个成绩表 scores.csv，含 name、class（班级）、score 三列。怎样求全体平均分，以及各班级的平均分？',
          solution: [
            '解：读入后分别用 mean 和 groupby。',
            'import pandas as pd；df = pd.read_csv("scores.csv")。',
            '全体平均分：df["score"].mean()。',
            '各班平均分：df.groupby("class")["score"].mean()——按 class 分组，对每组的 score 求平均。',
            '答：mean() 一行求出全体平均；groupby("class") 把数据按班级分组后，每个班单独得到一个平均分。这比自己写循环按班级累加再除，简洁太多。',
          ],
        },
      },
      {
        name: '用 matplotlib 把统计结果画成图表',
        when: '想把统计数据直观展示出来（比较大小、看趋势、看占比）时',
        steps: [
          'import matplotlib.pyplot as plt',
          '**选对图表类型**：比较不同类别的数值用 plt.bar(类别, 数值)（柱状图）；看随时间/顺序的趋势用 plt.plot(x, y)（折线图）',
          '准备好 x（类别/横轴）和 y（对应数值）两组数据，传给对应的画图函数',
          '加标注让图能读懂：plt.xlabel/ylabel 标坐标轴、plt.title 加标题，最后 plt.show() 显示（注意纵轴尽量从 0 开始，避免夸大差异）',
        ],
        example: {
          problem: '已知三个班的平均分：一班 85、二班 78、三班 90。怎样画一个柱状图直观比较？',
          solution: [
            '解：比较不同类别（班级）的数值，用柱状图最合适。',
            'import matplotlib.pyplot as plt。',
            'classes = ["一班", "二班", "三班"]；avgs = [85, 78, 90]。',
            'plt.bar(classes, avgs)；plt.ylabel("平均分")；plt.title("各班平均分")；plt.show()。',
            '答：plt.bar 把每个班画成一根柱子，高度对应平均分，三个班的高低一眼就能比出来。比较"不同类别的数值大小"是柱状图最擅长的场景。',
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 从读数据到清洗、统计、画图的完整流程',
        problem: '有一份学生成绩 CSV 文件 scores.csv，列为 name、class、score，但里面有几行 score 是空的（缺失值），还有重复的记录。请完成完整流程：读入数据 → 清洗（去缺失、去重）→ 算出各班平均分 → 用柱状图展示。',
        steps: [
          `第一步：导入库、读入数据。
import pandas as pd
import matplotlib.pyplot as plt

df = pd.read_csv("scores.csv")    # 读成 DataFrame（二维表）
print(df.head())                  # 看看前 5 行长什么样`,
          '第二步：理解 DataFrame。读进来的 df 就像内存里的一张表，有 name、class、score 三列。df.head() 看前几行、df.info() 能看到每列类型和有没有缺失，是分析前"先认识数据"的好习惯。',
          `第三步：清洗数据——去掉缺失行和重复行。这是真实数据分析最关键、最不能跳过的一步。
df = df.dropna(subset=["score"])      # 删掉 score 为空（缺失）的行
df = df.drop_duplicates()             # 删掉完全重复的行`,
          '第四步：理解清洗的必要性。如果不去掉缺失值，求平均时空值会出问题；不去重，重复记录会让某些数据被重复计算，统计结果失真。**先清洗、再统计**，结论才可靠——这正是"清洗→统计→可视化→结论"流程把清洗放第一步的原因。',
          `第五步：分组统计——算各班平均分。
class_avg = df.groupby("class")["score"].mean()
print(class_avg)
# 例如输出：
# class
# 一班    85.0
# 二班    78.5
# 三班    90.0`,
          '第六步：理解 groupby。groupby("class") 把所有行按班级分成几组，["score"].mean() 对每组的 score 分别求平均。一行代码就得到了每个班的平均分，等价于"按班级把成绩分堆、每堆求平均"，但不用写任何循环。',
          `第七步：画柱状图展示，加上标注。
class_avg.plot(kind="bar")        # pandas 可直接画图，等价于 plt.bar
plt.ylabel("平均分")
plt.title("各班平均分对比")
plt.tight_layout()                # 自动调整布局，防止标签被遮
plt.show()`,
          '第八步：分析与作答。最终得到一张柱状图，每个班一根柱子、高度是平均分，各班高低一目了然。整个流程完整体现了数据分析四步：**读入 → 清洗（dropna 去缺失、drop_duplicates 去重）→ 统计（groupby 分组求平均）→ 可视化（柱状图）**。其中"清洗"是保证结论可靠的前提，"选柱状图"是因为要比较不同班级的数值大小。最后别忘了纵轴是从 0 开始的"平均分"，这样比较才不会夸大差异。',
        ],
      },
    ],
    pitfalls: [
      '**跳过清洗直接分析**。真实数据常有缺失、重复、格式错误。不先用 dropna/fillna 处理缺失、drop_duplicates 去重就直接统计，结论很可能是错的（如重复记录让平均值失真）。**清洗是数据分析的第一步，不能省**。',
      '**图表纵轴不从 0 开始，夸大了差异**。比如三个班平均分 85、87、90，如果纵轴从 84 开始画柱状图，会显得差距巨大、误导读者。比较数值大小的柱状图，**纵轴应尽量从 0 开始**，如实反映真实差异。',
      '**选错图表类型**。比较不同类别的数值用**柱状图**、看随时间/顺序的趋势用**折线图**、看占比用**饼图**。用反了（如用折线图比较几个无序类别）会让图表难懂甚至误导。',
      '**把"相关"当成"因果"**。两个量一起变化（相关）不等于一个导致另一个（因果）。比如"冰淇淋销量和溺水人数都在夏天上升"，是因为天热（共同原因），不是冰淇淋导致溺水。从图表下因果结论要极其谨慎。',
      '直接修改原始数据文件。分析时应在读入的 DataFrame 上操作、或另存结果，**保留原始数据不动**。一旦把清洗、计算的结果覆盖回原文件，出了错就无法追溯、数据也找不回来了。',
    ],
    exercises: [
      {
        problem: '有一份气温记录 CSV 文件 temp.csv，含 month（月份 1~12）和 temp（当月平均气温）两列。请写一段 Python 程序：读入数据，求出全年平均气温，并画一张折线图展示气温随月份的变化趋势。说明为什么这里用折线图而不是柱状图。',
        hint: '用 pandas 读入、df["temp"].mean() 求平均。画趋势用 plt.plot（折线图），x 是 month、y 是 temp。记得加坐标轴标签和标题。',
        answer: `**详细讲解**：

**思路**：① 用 pandas 读入 CSV；② df["temp"].mean() 求全年平均气温；③ 用 matplotlib 的 plt.plot 画折线图，横轴是月份、纵轴是气温，展示趋势。

**参考代码**：
\`\`\`python
import pandas as pd
import matplotlib.pyplot as plt

# 读入数据
df = pd.read_csv("temp.csv")

# 求全年平均气温
avg = df["temp"].mean()
print("全年平均气温：", avg)

# 画折线图展示气温随月份变化的趋势
plt.plot(df["month"], df["temp"], marker="o")   # marker="o" 在每个点上标记
plt.xlabel("月份")
plt.ylabel("平均气温(℃)")
plt.title("各月平均气温变化趋势")
plt.show()
\`\`\`

**执行结果**：先打印全年平均气温，再弹出一张折线图——横轴 1~12 月，纵轴气温，一条折线把各月气温连起来，能清楚看到"先升后降"（夏高冬低）的趋势。

**为什么用折线图而不是柱状图**：
- **折线图擅长展示"随顺序/时间的连续变化趋势"**。月份是有先后顺序的（1 月→2 月→…→12 月），气温是随时间连续变化的。折线把相邻月份连起来，能直观看出"上升""下降""峰值在哪"这种**趋势和走向**。
- **柱状图擅长比较"不同类别之间的数值大小"**，类别之间通常是并列、无顺序的（如比较几个不同班级、不同产品）。如果用柱状图画气温，每个月一根孤立的柱子，**看不出连续的变化趋势**，不如折线直观。

**判断口诀**：**看趋势（随时间/顺序变化）用折线图，比大小（不同类别并列）用柱状图**。气温随月份变化是典型的"趋势"场景，所以折线图最合适。

**小提醒**：标题和坐标轴标签一定要加，否则别人看不懂这张图在表达什么。`
      },
      {
        problem: '小明分析班级数据时，直接对原始 CSV 求了平均分，但忘了里面有 3 行 score 是空的、还有 2 行是重复录入的。请说明这两类"脏数据"会怎样影响他的统计结果，正确的处理顺序应该是什么？写出清洗的关键代码。',
        hint: '缺失值参与计算会怎样？重复记录会让某些数据被算几次？数据分析四步里，清洗排在第几步？',
        answer: `**详细讲解**：

**两类脏数据的影响**：

**1. 缺失值（score 为空的 3 行）**：
- 缺失值参与统计会出问题。比如求平均，空值要么导致结果异常，要么被某些函数自动忽略但你**没意识到实际只算了部分数据**，得出的"平均分"和你以为的不一样。
- 更糟的是，如果缺失值被当成 0 处理，会把平均分**严重拉低**，得出错误结论。

**2. 重复记录（重复录入的 2 行）**：
- 重复的记录会让那几个学生的成绩**被计算多次**，等于给他们"加了权重"。
- 这会让平均值、计数等统计结果**失真**——比如某高分学生被录了两遍，会把全班平均分不正常地抬高。

**正确的处理顺序**：数据分析的标准流程是"**清洗 → 统计 → 可视化 → 结论**"，**清洗排在第一步**。必须**先把脏数据清理干净，再做任何统计**。小明的错误就是跳过清洗、直接对原始数据求平均，所以结果不可靠。

**清洗的关键代码**：
\`\`\`python
import pandas as pd

df = pd.read_csv("scores.csv")

# 第一步：清洗（一定要在统计之前做）
df = df.dropna(subset=["score"])      # 删掉 score 为空（缺失）的行
df = df.drop_duplicates()             # 删掉完全重复的行

# 第二步：清洗之后再统计，结果才可靠
print("正确的平均分：", df["score"].mean())
\`\`\`

**两个清洗函数说明**：
- **dropna(subset=["score"])**：删掉 score 列为空的行（subset 指定只看这一列的缺失）。如果不想删、想填补，可用 fillna 填入某个值（如该列平均值），但要谨慎。
- **drop_duplicates()**：删掉内容完全重复的行，保证每条记录只算一次。

**一句话总结**：**脏数据（缺失、重复）会让统计结果失真，所以必须遵循"先清洗、再统计"的顺序**。dropna 处理缺失、drop_duplicates 去重是最常用的两步清洗。跳过清洗直接分析，是数据分析的大忌。`
      },
      {
        problem: '小红发现"每年游泳溺水的人数"和"冰淇淋的销量"高度正相关（都在夏天升高）。她下结论说："所以应该禁止卖冰淇淋来减少溺水。"她的推理错在哪里？请解释"相关不等于因果"，并说明分析数据下结论时该注意什么。',
        hint: '两个量一起变化，一定是一个导致了另一个吗？有没有可能是第三个共同原因（比如季节、天气）同时影响了它们？',
        answer: `**详细讲解**：

**她的推理错在哪**：小红犯了"**把相关当成因果**"的典型错误。冰淇淋销量和溺水人数确实**一起升高**（这叫"相关"），但这**不代表**"卖冰淇淋导致了溺水"（这才是"因果"）。

**真正的原因——存在第三个共同因素**：让这两个数字同时上升的，其实是**夏天/天气炎热**这个"幕后共同原因"：
- 天热 → 大家更想吃冰淇淋 → 冰淇淋销量上升。
- 天热 → 更多人去游泳玩水 → 溺水事故也随之增多。

所以冰淇淋和溺水都是"天热"这个共同原因的**结果**，它们之间**没有直接的因果关系**。禁止卖冰淇淋，丝毫不会减少溺水——因为根本原因是天气和游泳人数，跟冰淇淋无关。

**什么是"相关不等于因果"**：
- **相关**：两个量在数据上**一起变化**（一个涨另一个也涨，或一个涨另一个跌）。
- **因果**：一个量的变化**真正导致**了另一个量的变化。
- **关键**：两个量相关，可能是 A 导致 B、可能是 B 导致 A、也可能是第三个因素 C 同时影响了 A 和 B（像这个例子），甚至可能纯属巧合。**光看"一起变化"无法判断到底是哪种**。

**分析数据下结论时该注意什么**：
1. **看到相关，先别急着下因果结论**——多问一句"会不会有共同的第三个原因？""会不会是反过来？"
2. **想验证因果，需要更严谨的方法**（如控制变量的对照实验），而不是仅凭两条曲线一起涨就断定。
3. **警惕图表的误导**：两个一起上升的曲线放在一起，很容易给人"一个导致另一个"的错觉，要保持批判性思维。

**一句话总结**：**"相关不等于因果"——两个量一起变化，不代表一个导致了另一个，背后可能有共同原因或其他解释**。这是数据分析中最重要的思维准则之一，下结论务必谨慎，避免像"禁冰淇淋防溺水"这样的荒唐推论。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 7. 人工智能与机器学习初步
  // ───────────────────────────────────────────────────────────────
  'ai-ml-intro': {
    warmup: {
      q: '要写一个程序判断"一封邮件是不是垃圾邮件"。传统办法是人来定规则：含"中奖"就标记、含"免费"就标记……但垃圾邮件花样不断翻新，规则永远写不完、防不胜防。有没有另一种思路：**不靠人写死规则，而是给程序看大量"已经标好是不是垃圾"的邮件，让它自己总结出规律**？',
      hint: '想一想：与其人绞尽脑汁列规则，不如给机器大量"输入+正确答案"的例子，让它自己找其中的规律。这种"从数据里学"的方式叫什么？',
      answer: '这种"**不靠人写死规则，而是从大量数据里自己学规律**"的思路，就是**机器学习**。传统程序是"人写规则、机器执行"；机器学习是"人给大量带答案的样本（如成千上万封标好是/否垃圾的邮件），机器自己找出规律"，然后用学到的规律去判断新邮件。基本流程是：**准备数据 → 训练模型 → 评估 → 预测**。它能解决很多"规则写不尽"的问题，是人工智能的核心方法之一。一句话：**传统编程是人写规则，机器学习是让机器从数据里学出规则。**'
    },
    intro: [
      '这一节是**人工智能与机器学习的入门**——让计算机从数据里"学"出规律、自动做预测或分类，而不是靠人把规则一条条写死。这是整个知识体系里最高阶的内容，建立在前面的数据分析和算法基础之上。',
      '先理解机器学习和传统编程的**根本区别**。**传统程序**：人来写规则（如"如果含中奖就是垃圾邮件"），机器照着规则执行。**机器学习**：人不写规则，而是给机器大量"**输入 + 正确答案**"的样本（如成千上万封标好"是/否垃圾"的邮件），让机器**自己从数据里找出规律**，再用这个规律去判断没见过的新数据。一句话：传统编程是"人写规则"，机器学习是"机器从数据学规则"。',
      '机器学习的基本流程是四步：**准备数据 → 训练模型 → 评估 → 预测**。① **准备数据**：收集并清洗带标签的样本；② **训练**：把样本喂给模型，让它调整内部参数去拟合"输入到答案"的规律；③ **评估**：用模型**没见过的数据**检验它学得好不好；④ **预测**：用训练好的模型对新数据给出结果。最简单的模型之一是**线性回归**，它假设输入和输出大致是直线关系 $y=wx+b$，训练就是去找最合适的 $w$（斜率）和 $b$（截距）。',
      '按"样本有没有标准答案"，机器学习分两大类。**监督学习**：训练样本**带标签**（有正确答案）。又分两种任务——**回归**（预测一个连续数值，如根据面积预测房价）和**分类**（预测属于哪一类，如判断邮件是否垃圾、图片是猫是狗）。**无监督学习**：样本**没有标签**，让模型自己发现数据里的结构（如把顾客自动聚成几类，叫"聚类"）。初学先重点理解监督学习。',
      '有一个**至关重要**的概念必须记牢：**训练集 / 测试集的划分**。我们要把数据分成两部分——大部分（如 80%）作**训练集**让模型学习，留一部分（如 20%）作**测试集**来检验。**绝不能用训练时见过的数据来评估模型**（那等于"考试考原题"，是作弊，看不出真实水平）。与之相关的大坑是**过拟合**：模型把训练集"背"得太死、连噪声都记住了，结果在训练集上表现极好、一遇到新数据就拉垮。一句话记住：**机器学习是从数据学规律（监督=有标签，分回归和分类）；流程是 准备→训练→评估→预测；必须用没见过的测试集评估，警惕过拟合。**'
    ],
    formulas: [
      { name: '与传统编程的区别', latex: '\\text{传统：人写规则；ML：从数据学规则}',
        note: '传统程序由人定规则、机器执行；机器学习给机器大量"输入+答案"样本，让它自己找出规律。' },
      { name: '基本流程', latex: '\\text{准备数据 → 训练 → 评估 → 预测}',
        note: '先备好带标签的数据，训练模型拟合规律，用没见过的数据评估效果，最后对新数据做预测。' },
      { name: '线性回归模型', latex: 'y = wx + b',
        note: '最简单的模型之一：假设输入 x 和输出 y 近似直线关系。训练就是找最合适的权重 w 和偏置 b。' },
      { name: '训练集/测试集划分', latex: '\\text{train 80\\% 学习 / test 20\\% 检验}',
        note: '数据分两部分：训练集用来学、测试集用来评估。绝不能用训练数据评估模型——那等于考试考原题。' },
    ],
    methods: [
      {
        name: '理清一个机器学习任务的四步流程',
        when: '拿到一个"想让机器自动判断/预测"的问题，想知道怎么用机器学习解决时',
        steps: [
          '**准备数据**：收集大量"输入 + 正确答案"的样本，清洗干净（去缺失、去重）；明确是回归（预测数值）还是分类（预测类别）',
          '**划分**：把数据随机分成训练集（如 80%）和测试集（如 20%）',
          '**训练**：用训练集喂给模型（如线性回归、决策树），让它学出"输入到答案"的规律',
          '**评估 + 预测**：用**测试集**（模型没见过的）检验准确率是否过关；满意后再用它对全新数据做预测',
        ],
        example: {
          problem: '想做一个"根据房屋面积预测房价"的程序。这属于哪类任务？用机器学习的四步该怎么做？',
          solution: [
            '解：预测的是房价（一个连续数值），所以是**监督学习里的回归**任务。',
            '准备数据：收集很多"面积 + 真实成交价"的样本，清洗干净。',
            '划分：80% 作训练集、20% 作测试集。',
            '训练 + 评估：用训练集训练一个回归模型（如线性回归 y=wx+b，x 是面积、y 是房价）；用测试集看预测价和真实价差多少。满意后就能输入一个新房子的面积、预测它的价格。',
            '答：这是回归任务，按"准备数据→划分→训练→用测试集评估→预测"四步走，关键是用没见过的测试集检验效果。',
          ],
        },
      },
      {
        name: '判断监督学习的任务类型（回归还是分类）',
        when: '面对一个监督学习问题，想确定它是回归还是分类时',
        steps: [
          '先确认是**监督学习**：训练样本是否带"正确答案"（标签）？带标签才是监督学习',
          '看要预测的**答案是什么类型**',
          '若答案是**一个连续的数值**（如价格、温度、身高）→ 是**回归**',
          '若答案是**有限的几个类别**（如是/否、猫/狗/兔、好/中/差）→ 是**分类**',
        ],
        example: {
          problem: '判断下面两个任务分别是回归还是分类：(甲)根据学习时长预测考试分数；(乙)根据一张图片判断里面是猫还是狗。',
          solution: [
            '解：看"要预测的答案"是连续数值还是几个类别。',
            '(甲)预测的是考试分数——一个连续的数值（可以是 0~100 之间任意值）→ **回归**。',
            '(乙)预测的是"猫"还是"狗"——有限的几个类别 → **分类**。',
            '答：甲是回归（预测数值）、乙是分类（预测类别）。区分口诀：**答案是数值→回归，答案是类别→分类**。',
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 用 sklearn 跑一个完整的机器学习流程',
        problem: '用 Python 的机器学习库 scikit-learn（sklearn），完成一个最简单的回归任务：根据"学习时长"预测"考试分数"。给出从准备数据、划分训练/测试集、训练模型到评估的完整代码，并解释每一步在做什么、特别说明为什么要划分测试集。',
        steps: [
          `第一步：准备数据。这里用一小批示例数据（真实项目数据会多得多）。
import numpy as np
from sklearn.model_selection import train_test_split
from sklearn.linear_model import LinearRegression

# 输入 X：学习时长（小时）；答案 y：对应的考试分数
# sklearn 要求 X 是二维（每行一个样本），所以用 reshape 变成一列
X = np.array([1, 2, 3, 4, 5, 6, 7, 8]).reshape(-1, 1)
y = np.array([52, 58, 65, 70, 78, 82, 88, 95])`,
          '第二步：理解数据格式。X 是输入（学习时长），y 是对应的正确答案（分数）——这是带标签的样本，属于**监督学习**；预测的分数是连续数值，所以是**回归**任务。X 要 reshape 成二维是 sklearn 的格式要求（每行代表一个样本）。',
          `第三步：划分训练集和测试集。这是机器学习的关键一步。
X_train, X_test, y_train, y_test = train_test_split(
    X, y, test_size=0.25, random_state=0
)
# test_size=0.25 表示留 25% 作测试集，其余 75% 作训练集`,
          '第四步：**为什么必须划分测试集**。如果用训练时见过的数据来评估模型，就像"考试考原题"——模型把答案背下来就能得高分，但这**看不出它对新数据的真实能力**，是一种自欺欺人（作弊）。所以要留一部分模型**从没见过**的数据（测试集），专门用来检验它学到的规律到底通不通用。train_test_split 帮我们随机分好两份。',
          `第五步：训练模型。用训练集让模型学出规律。
model = LinearRegression()        # 创建一个线性回归模型 y = wx + b
model.fit(X_train, y_train)       # 用训练集训练：自动找最合适的 w 和 b
print("学到的斜率 w：", model.coef_)
print("学到的截距 b：", model.intercept_)`,
          '第六步：理解 fit 做了什么。fit（拟合）就是"训练"——模型用训练集自动调整内部的 w（斜率）和 b（截距），找到一条最能贴合"时长到分数"这些点的直线 y=wx+b。学完后 model.coef_ 是学到的 w、model.intercept_ 是 b。',
          `第七步：评估 + 预测。用测试集检验，再预测新数据。
score = model.score(X_test, y_test)   # 用没见过的测试集评估（越接近 1 越好）
print("在测试集上的得分：", score)

# 用训练好的模型预测：学习 9 小时大约能考多少分
pred = model.predict(np.array([[9]]))
print("预测学习 9 小时的分数：", pred)`,
          '第八步：分析与作答。model.score 用**测试集**算出模型表现（回归里这个分数越接近 1，说明拟合越好）；predict 对新输入（9 小时）给出预测分数。整段代码完整走了一遍机器学习流程：**准备数据 → 划分训练/测试集 → fit 训练 → 用测试集 score 评估 → predict 预测**。其中"划分测试集、用没见过的数据评估"是最不能省、也最容易被初学者忽视的一步——它保证我们看到的是模型的**真实**能力，而不是"背原题"的假象。',
        ],
      },
    ],
    pitfalls: [
      '**用训练数据来评估模型（等于作弊）**。拿模型训练时见过的数据测它，就像"考试考原题"，分数虚高、看不出真实水平。**必须用单独留出的、模型没见过的测试集来评估**，这是机器学习的铁律。',
      '**过拟合**——模型把训练集"背"得太死。模型连训练数据里的噪声、偶然细节都记住了，导致在训练集上近乎完美、一遇到新数据就表现很差。过拟合的模型"记性好但不会举一反三"，要靠更多数据、更简单的模型、正则化等手段缓解。',
      '**以为机器学习是万能的、不需要好数据**。机器学习的效果**严重依赖数据的数量和质量**——"垃圾进、垃圾出"。数据太少、有偏、标签错误，再好的算法也学不出靠谱的规律。准备和清洗数据往往是整个流程里最耗时、最重要的部分。',
      '混淆回归和分类。**回归预测连续数值**（房价、温度），**分类预测离散类别**（是/否、猫/狗）。选错任务类型、用错评估指标（回归看误差、分类看准确率）会让结果无意义。',
      '把"训练准确率高"当成"模型好"。训练集上表现好可能只是过拟合。判断模型好不好，要看它在**测试集（新数据）**上的表现——训练好、测试差，正是过拟合的典型信号。',
    ],
    exercises: [
      {
        problem: '用自己的话说清楚"机器学习"和"传统编程"的根本区别，并各举一个例子。再说明：为什么有些问题（如识别垃圾邮件、人脸识别）更适合用机器学习而不是人写规则？',
        hint: '传统编程是谁来定规则？机器学习是谁来找规律？想想垃圾邮件、人脸这类问题，规则能不能被人穷尽地写出来？',
        answer: `**详细讲解**：

**根本区别**：
- **传统编程**：**人来写规则**，机器照着规则执行。程序员把"遇到什么情况该怎么做"用 if-else 等明确写出来。
  - 例子：判断闰年——规则很清楚（能被 4 整除且不能被 100 整除，或能被 400 整除），人直接写成代码即可。
- **机器学习**：**机器从数据里自己学规律**。人不写规则，而是给机器大量"输入 + 正确答案"的样本，让它自己总结出"输入到答案"的规律，再用这个规律处理新数据。
  - 例子：判断邮件是否垃圾——人不去列规则，而是给机器成千上万封**已经标好"是/否垃圾"**的邮件，让它自己学出垃圾邮件的特征。

**为什么垃圾邮件、人脸识别更适合机器学习**：
关键原因是——**这类问题的"规则"太复杂、太多变，人根本写不尽、写不准**：
1. **规则写不完**：垃圾邮件花样不断翻新，今天封了"中奖"，明天就改成谐音、图片；人靠手写规则永远追不上、漏洞百出。
2. **规律说不清**：人脸识别更极端——你能用语言精确描述"怎样从像素判断这是某个人的脸"吗？光照、角度、表情千变万化，这种规律**人脑能识别却说不清、写不成明确规则**。
3. **而机器学习恰好擅长这个**：只要给足够多带答案的样本（大量标好的邮件、大量标好身份的人脸照片），机器就能自己从数据里**统计出**那些人难以言表的复杂规律，而且数据越多、学得越准、还能随新数据更新。

**一句话总结**：**传统编程是"人写规则"，适合规则清晰明确的问题（如闰年、计算器）；机器学习是"机器从数据学规则"，适合那些规则太复杂、太多变、人难以穷尽描述的问题（如垃圾邮件、人脸、语音识别）**。选哪种，取决于"规则能不能被人清楚地写出来"。`
      },
      {
        problem: '小明训练了一个模型，用全部 1000 条数据训练，然后又用这**同样的 1000 条数据**测试，得到准确率 99%，很高兴地说"我的模型太厉害了！"。请指出他的做法错在哪里，解释什么是"过拟合"，并说明正确应该怎么评估模型。',
        hint: '用训练时见过的数据测试，相当于考试考什么？这个 99% 能代表模型对新数据的真实能力吗？正确做法是把数据怎么分？',
        answer: `**详细讲解**：

**他错在哪里**：小明用**训练时见过的同一批数据**来测试模型，这相当于"**考试考原题**"——模型在训练时已经"见过"这 1000 条数据的答案，测试时当然能答得很好。这个 99% 的准确率是**虚高的、靠不住的**，它**根本反映不出模型对从没见过的新数据的真实能力**。这在机器学习里是一种典型的错误（等于作弊）。

**什么是过拟合**：**过拟合**指模型把训练数据"**背**"得太死——不仅学到了真正的规律，连训练数据里的**噪声、偶然的细节**都一并记住了。后果是：
- 在**训练集上表现极好**（因为都背下来了，甚至能到 99%、100%）。
- 但一遇到**新数据就表现很差**（因为它记的是这批数据的"死答案"，而不是能举一反三的通用规律）。

小明的 99% 很可能正是过拟合的表现——模型也许只是把这 1000 条背熟了，换一批新数据就原形毕露。

**正确应该怎么评估模型**：
1. **划分训练集和测试集**：把 1000 条数据**随机分成两部分**，比如 800 条作**训练集**、200 条作**测试集**。
2. **只用训练集训练**：模型学习时只能看到这 800 条。
3. **用测试集（模型没见过的 200 条）评估**：这 200 条模型训练时从没见过，用它们测出的准确率，才**真实反映模型对新数据的能力**。
\`\`\`python
from sklearn.model_selection import train_test_split
X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.2)
model.fit(X_train, y_train)            # 只用训练集学习
print(model.score(X_test, y_test))    # 用没见过的测试集评估——这才可信
\`\`\`

**怎么识别过拟合**：如果模型在**训练集上准确率很高、但测试集上明显低**，就是过拟合的信号。缓解办法包括：用更多训练数据、用更简单的模型、正则化等。

**一句话总结**：**绝不能用训练数据评估模型（那是考原题、会得到虚高的假成绩）；必须留出模型没见过的测试集来评估，这样才能看出真实水平、并及时发现过拟合**。小明该做的是先划分数据、用测试集重新评估，那个"真实的准确率"很可能远没有 99%。`
      },
      {
        problem: '把下面 4 个任务分别归类为"回归"还是"分类"，并说明判断依据：\n(1) 根据历年数据预测明天的最高气温；\n(2) 根据一段录音判断说话人是男是女；\n(3) 根据学生的多门成绩预测他高考总分；\n(4) 根据肿瘤的各项指标判断是良性还是恶性。',
        hint: '看每个任务"要预测的答案"是什么：是一个连续的数值，还是有限的几个类别？数值→回归，类别→分类。',
        answer: `**详细讲解**：

**判断方法**：监督学习里区分回归和分类，只看一件事——**要预测的"答案"是什么类型**：
- 答案是**一个连续的数值**（可以取某个范围内的任意值）→ **回归**。
- 答案是**有限的几个类别**（离散的、可数的几种可能）→ **分类**。

**逐个分析**：

**(1) 预测明天的最高气温 → 回归**
- 要预测的是气温，是一个**连续的数值**（如 23.5℃、28℃，可以是任意实数）。
- 答案是数值 → **回归**。

**(2) 判断说话人是男是女 → 分类**
- 要预测的答案只有两种类别："男"或"女"。
- 答案是有限的类别（这里是两类）→ **分类**（具体叫"二分类"）。

**(3) 预测高考总分 → 回归**
- 要预测的是总分，是一个**连续的数值**（如 587 分、632 分，在一定范围内取值）。
- 答案是数值 → **回归**。

**(4) 判断肿瘤是良性还是恶性 → 分类**
- 要预测的答案是"良性"或"恶性"两种类别。
- 答案是有限的类别 → **分类**（二分类）。

**汇总**：
- (1) 预测气温 —— 回归（连续数值：温度）
- (2) 判断男女 —— 分类（两个类别：男/女）
- (3) 预测总分 —— 回归（连续数值：分数）
- (4) 良性/恶性 —— 分类（两个类别：良/恶）

**一句话口诀**：**预测"一个数"是回归，预测"哪一类"是分类**。看答案是连续数值还是离散类别，就能立刻判断。区分清楚很重要——因为回归和分类用的模型、评估指标（回归看误差大小、分类看准确率）都不一样。`
      },
    ],
  },

});
