// 初中编程 · 控制结构 —— 详细小课（习题讲解版）
// 覆盖 6 个节点：boolean-cond / if-else / while-loop / for-loop / nested-loop / break-continue
// 数据结构对齐 node-detail.jsx 的渲染：warmup / intro / formulas(name,latex,note) /
//   methods(name,when,steps,example) / worked(title,problem,steps) / pitfalls / exercises(problem,hint,answer)
// 语言：Python。含代码或引号的字段值统一用「反引号模板字符串」，避免转义冲突；缩进用空格体现。
// formulas.latex 会交给 KaTeX 渲染，所以「语法要点」一律用 \\text{...} 包成文本，避免被当数学公式斜体化。

Object.assign(window.CODE_LESSON, {

  // ───────────────────────────────────────────────────────────────
  // 1. 布尔与条件判断 boolean-cond
  // ───────────────────────────────────────────────────────────────
  'boolean-cond': {
    warmup: {
      q: `游乐园规定"身高满 120 厘米才能坐过山车"。如果让你用程序来判断一个人能不能坐，你会先让程序算出一个什么样的"答案"，再决定放不放行？`,
      hint: `这个"答案"不是一个数字，也不是一句话，它只有两种可能——要么"行"、要么"不行"。程序里有没有一种数据，天生就只有这两种值？`,
      answer: `程序要先算出一个只有"成立 / 不成立"两种结果的值，这就是**布尔值**：只有 True（真）和 False（假）两个。判断"身高 >= 120"会得到一个布尔值——身高 130 就是 True，身高 110 就是 False。程序正是靠这个 True/False 来决定下一步做什么。比较运算（>= < ==）负责"算出"布尔值，逻辑运算（and or not）负责把多个条件**组合**成一个更复杂的判断。`
    },
    intro: [
      `布尔与条件判断，是程序学会"做选择"的第一步。在这之前，程序只会从上到下一行行老老实实地执行；有了条件判断，程序才能根据情况走不同的路——这是它从"复读机"变成"会思考"的关键。`,
      `核心是一种特殊的数据类型：**布尔（bool）**，它只有两个值，True（真）和 False（假）。注意首字母大写，这是 Python 的规定，写成 true、TRUE 都是错的。布尔值不是用来计算大小的，而是用来回答"是不是""成不成立"这类只有两种答案的问题。`,
      `布尔值从哪来？最常见的来源是**比较运算**：用 ==（等于）、!=（不等于）、>、<、>=、<= 去比较两个值，结果就是一个布尔值。比如 5 > 3 的结果是 True，10 == 9 的结果是 False。这里要特别小心：**== 是"比较两个数相不相等"，而 = 是"把右边赋值给左边"**，初学者最容易把它们搞混。`,
      `当一个条件不够用时，就要用**逻辑运算**把多个条件拼起来：and（并且，两个都为真才为真）、or（或者，有一个为真就为真）、not（取反，把真变假、假变真）。比如判断"是青少年"可以写成 age >= 13 and age <= 19——必须同时满足两个条件。`,
      `一句话记住核心：**比较运算"算出"布尔值，逻辑运算"组合"布尔值，最终得到一个 True 或 False，程序就靠它来决定走哪条路。** 学会写出正确的条件，是后面 if 分支、while 循环、for 循环全部的基础。`
    ],
    formulas: [
      { name: '布尔值', latex: '\\text{True / False}',
        note: `Python 里布尔只有这两个值，**首字母必须大写**。它是判断的结果，不是用来比大小的。常配合比较和逻辑运算使用。` },
      { name: '比较运算符', latex: '\\text{==  !=  >  <  >=  <=}',
        note: `结果都是布尔值。**\`==\` 是比较是否相等，\`=\` 是赋值**，千万别写反——把 if x = 5 写成赋值，Python 会直接报语法错。` },
      { name: '逻辑：and（并且）', latex: '\\text{True and True} \\Rightarrow \\text{True}',
        note: `and 要求**两边都为真**，结果才为真；只要有一个为假，结果就是 False。用于"必须同时满足多个条件"。` },
      { name: '逻辑：or（或者）', latex: '\\text{False or True} \\Rightarrow \\text{True}',
        note: `or 只要**有一边为真**，结果就为真；两边都为假时才是 False。用于"满足任意一个条件即可"。` },
      { name: '逻辑：not（取反）', latex: '\\text{not True} \\Rightarrow \\text{False}',
        note: `not 把真变假、把假变真。例如 not (5 > 3) 的结果是 False，因为 5 > 3 本身是 True。` },
    ],
    methods: [
      {
        name: '把"一句话规则"翻译成布尔表达式',
        when: '题目给了一句中文规则（如"分数在 60 到 100 之间算合格"），要你写成程序能判断的条件时',
        steps: [
          `先找出规则里涉及的"量"（变量）和"标准"（具体数值），比如 score（分数）和 60、100。`,
          `判断这是"单个条件"还是"多个条件"。出现"并且""同时""又……又……"用 and；出现"或者""任意一个"用 or。`,
          `把每个小条件单独写成比较式，比如"不低于 60"写成 score >= 60，"不超过 100"写成 score <= 100。`,
          `用逻辑运算把小条件连起来：score >= 60 and score <= 100。写完读一遍，确认和原话意思一致。`,
        ],
        example: {
          problem: `把规则"年龄在 13 岁到 19 岁之间（含 13 和 19）就是青少年"翻译成布尔表达式。`,
          solution: [
            `解：涉及的量是 age（年龄），标准是 13 和 19，规则里"在……之间"意味着要**同时**满足两个条件。`,
            `"不小于 13"写成 age >= 13；"不大于 19"写成 age <= 19。`,
            `两个条件要同时成立，用 and 连接：\`age >= 13 and age <= 19\`。`,
            `答：表达式为 \`age >= 13 and age <= 19\`。当 age = 15 时结果是 True，age = 25 时结果是 False。`,
          ],
        },
      },
      {
        name: '用 not 把"正面条件"改写成"反面条件"',
        when: '想表达"不满足某条件"，或觉得反着写更清楚时',
        steps: [
          `先写出"正面"的条件，比如"及格"是 score >= 60。`,
          `在整个条件外面套一个 not，并用括号括住，表示"取反"：not (score >= 60)。`,
          `想一想：not (score >= 60) 其实就等于 score < 60（不及格），两种写法意思相同。`,
          `选更易读的那种写：能直接写 score < 60 就别绕一圈写 not(...)，但理解 not 的作用很重要。`,
        ],
        example: {
          problem: `已知 is_rainy 表示"在下雨"（布尔值）。怎样表示"没有下雨"？`,
          solution: [
            `解：is_rainy 本身已经是布尔值，True 代表下雨，False 代表不下雨。`,
            `"没有下雨"就是把它取反，用 not：\`not is_rainy\`。`,
            `当 is_rainy 是 True（在下雨）时，not is_rainy 是 False（不满足"没下雨"）；反之亦然。`,
            `答：用 \`not is_rainy\` 表示"没有下雨"。注意 not 直接作用在布尔变量上，不需要再写 == True。`,
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 组合多个条件',
        problem: `某网站规定：用户名长度在 3 到 12 个字符之间，并且年龄满 18 岁，才能注册成功。已知变量 name（用户名字符串）和 age（年龄整数），请写出"能否注册"的布尔表达式，并说明当 name = "Tom"、age = 16 时结果是什么。`,
        steps: [
          `第一步：审题。一共有三个小条件——用户名"不少于 3 个字符""不多于 12 个字符""年龄满 18"，而且用了"并且"，说明三个必须**同时**满足，全部用 and 连接。`,
          `第二步：写每个小条件。用 len(name) 求用户名长度：长度不少于 3 写成 len(name) >= 3；不多于 12 写成 len(name) <= 12；年龄满 18 写成 age >= 18。`,
          `第三步：用 and 连起来：\`len(name) >= 3 and len(name) <= 12 and age >= 18\`。三个条件全为真，整体才为 True。`,
          `第四步：代入检验。name = "Tom" 长度是 3，len(name) >= 3 为 True、len(name) <= 12 为 True；但 age = 16，age >= 18 是 False。and 里只要有一个为假，整体就是 **False**——所以不能注册。`,
        ],
      },
    ],
    pitfalls: [
      `把比较的 == 写成赋值的 =。== 是"比较两个值相不相等"，= 是"赋值"。在条件里写 if score = 60 会直接报语法错，必须写 if score == 60。`,
      `True / False 大小写写错。Python 规定首字母大写，写成 true、false、TRUE 都会被当成未定义的变量而报错。`,
      `把"在 60 到 100 之间"误写成 60 <= score <= 100 以外的奇怪形式，或写成 score >= 60 or score <= 100。"之间"是同时满足，要用 **and**；用 or 会让几乎所有数都成立，逻辑就错了。（注：Python 其实支持 60 <= score <= 100 这种连写，但用 and 拆开更清楚、也更通用。）`,
      `混淆 and 和 or 的含义。and 是"两个都要真"，or 是"有一个真就行"。把"既要…又要…"用成 or，会让条件变得过于宽松。`,
      `对布尔变量画蛇添足地写 == True。is_rainy == True 完全等价于 is_rainy，直接写变量名即可；"没下雨"写 not is_rainy，不用写 is_rainy == False。`,
    ],
    exercises: [
      {
        problem: `判断下面每个表达式的结果是 True 还是 False（设 a = 5, b = 10）：\n（1）a < b and b < 20\n（2）a > b or b == 10\n（3）not (a == 5)`,
        hint: `先分别算出每个小条件（比较运算）的 True/False，再按 and（都真才真）、or（有一真就真）、not（取反）的规则合起来。`,
        answer: `**详细讲解**：把每个表达式拆成小条件，先算比较、再算逻辑。\n（1）\`a < b and b < 20\`：a < b 即 5 < 10 为 **True**；b < 20 即 10 < 20 为 **True**。and 要求两边都真，两边都真所以整体是 **True**。\n（2）\`a > b or b == 10\`：a > b 即 5 > 10 为 **False**；b == 10 即 10 == 10 为 **True**。or 只要有一边真就真，所以整体是 **True**。\n（3）\`not (a == 5)\`：先算括号里 a == 5 即 5 == 5 为 **True**，再被 not 取反，True 变 False，所以整体是 **False**。\n小结：永远是"先比较、得到布尔值，再用逻辑运算组合"，顺序别乱。`
      },
      {
        problem: `用一个布尔表达式表示规则："一个年份是闰年的简化判断——这个年份能被 4 整除，但不能被 100 整除。"（用变量 year 表示年份）`,
        hint: `"能被 4 整除"用取模 year % 4 == 0 表示（余数为 0 即整除）；"不能被 100 整除"是 year % 100 != 0；两个条件用"但"连接，意味着要同时成立。`,
        answer: `**详细讲解**：这道题要把两个数学条件翻译成布尔表达式。\n第一，"能被 4 整除"——一个数能被 4 整除，等价于它除以 4 余数为 0。Python 里取余数用 %（取模运算），所以写成 \`year % 4 == 0\`。\n第二，"不能被 100 整除"——就是除以 100 余数不为 0，写成 \`year % 100 != 0\`。\n第三，规则里用"但"连接两个条件，意思是两个都要满足，用 **and**。\n所以参考答案是：\n\`year % 4 == 0 and year % 100 != 0\`\n为什么这样写：% 能帮我们判断整除关系（余数为 0 就是整除），== 0 判断"是否整除"，!= 0 判断"是否不整除"，再用 and 把"要整除 4""不要整除 100"两个要求绑在一起。验证一下：year = 2024，2024 % 4 == 0 为 True、2024 % 100 != 0 为 True，结果 True（是闰年）；year = 1900，1900 % 4 == 0 为 True 但 1900 % 100 != 0 为 False，结果 False（不是闰年），符合规则。`
      },
      {
        problem: `下面这段代码想判断"成绩 score 是否及格（>= 60）"，但运行时报错了，请找出错误并改正：\nif score = 60:\n    print("达到 60 分")`,
        hint: `仔细看 if 后面那个判断里用的是一个等号还是两个等号。"判断相等"和"赋值"用的符号不一样。`,
        answer: `**详细讲解**：错误出在条件 \`score = 60\` 上。这里用了**一个等号 =**，而一个等号在 Python 里是**赋值**（把 60 装进 score），不是比较。条件位置需要的是一个布尔值（判断结果），而赋值不产生 True/False，所以 Python 会报语法错误（SyntaxError）。\n另外，题目要判断的是"及格"，及格是 >= 60（大于等于），不只是正好等于 60。\n改正后的代码应是：\n\`\`\`python\nif score >= 60:\n    print("及格")\n\`\`\`\n为什么用 >=：及格指 60 分及以上，包含 60 也包含 61、99，所以用"大于等于"而不是"等于"。\n这道题的核心提醒：**条件里要用比较运算符（== 或 >= 等），绝不能用赋值的 =**。记忆口诀——"一个等号是赋值，两个等号才是问'相等吗'"。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 2. if-else 分支 if-else
  // ───────────────────────────────────────────────────────────────
  'if-else': {
    warmup: {
      q: `自动售货机：投币够了就出货，不够就提示"金额不足"。如果只让程序"从头到尾执行每一行"，它能做到"够了出货、不够提示"这种二选一吗？缺了点什么？`,
      hint: `从头执行到尾，意味着每一行都会被做一遍。可"出货"和"提示不足"不应该同时发生——程序需要一种本事，能根据条件"挑着做"。`,
      answer: `光顺序执行做不到二选一——它会把"出货"和"提示不足"都执行了。程序需要 **if-else 分支**：if 后面写一个条件，**条件为真就执行 if 的代码块、否则执行 else 的代码块**，两块只会走其中一块。这样"金额够"走出货、"金额不够"走提示，程序就有了"做决定"的能力。`
    },
    intro: [
      `if-else 分支，是程序"做决定"的基本机制。它和上一节的"布尔与条件判断"是绝配——条件判断负责算出 True/False，if-else 负责根据这个 True/False 决定到底执行哪一段代码。`,
      `最简单的形式是 if：写 if 条件:，下面缩进的代码块只在**条件为真**时执行；条件为假就整段跳过。比如 if score >= 60: 下面那行"及格"只有在分数够时才会打印。`,
      `加上 else 就成了"二选一"：if 的条件为真走 if 的块，**否则**（else）走 else 的块，两块必定执行其中一块、且只执行一块。注意 else 后面不写条件，它就是"剩下的所有情况"。`,
      `当情况超过两种时，用 elif（"否则如果"，是 else if 的缩写）：从上往下逐个判断，**第一个为真的分支执行，后面的全部跳过**。比如先判 >= 90 是优秀、再 elif >= 60 是及格、最后 else 是不及格。顺序很重要，要从严到宽或从大到小排。`,
      `Python 有个和别的语言很不一样、也极其重要的规定：**用缩进（前面的空格）来表示"哪些代码属于这个分支"**。同一个代码块缩进必须一致（通常 4 个空格）；缩进错了，代码块的归属就错了，轻则报错重则逻辑全乱。这是本节最大的坑，务必盯紧。`
    ],
    formulas: [
      { name: 'if 单分支', latex: '\\text{if 条件:  →  缩进块（条件为真才执行）}',
        note: `条件为真就执行下面缩进的代码块，为假就整块跳过。冒号 : 不能漏，下一行必须缩进。` },
      { name: 'if-else 二选一', latex: '\\text{if 条件: ... else: ...}',
        note: `条件为真走 if 块，否则走 else 块。else **不带条件**，代表"其余所有情况"，两块只执行一块。` },
      { name: 'if-elif-else 多分支', latex: '\\text{if ... elif ... elif ... else ...}',
        note: `从上到下逐个判断，**第一个为真的分支执行、其余跳过**。elif 可以有多个，else 可有可无。` },
      { name: '缩进决定代码块', latex: '\\text{同块缩进一致（通常 4 个空格）}',
        note: `Python 靠缩进区分代码块归属。缩进不一致会报 IndentationError，缩进错位会让代码"跑错分支"。` },
    ],
    methods: [
      {
        name: '条件分支设计：先理清"有几种情况"',
        when: '要把"分情况处理"的需求写成 if / if-else / if-elif-else 时',
        steps: [
          `数一数到底有几种情况：只有"做 / 不做"用 if；"二选一"用 if-else；三种及以上用 if-elif-else。`,
          `给每种情况写出它的"判断条件"和"要做的事"。`,
          `多分支时把条件**按合理顺序**排列（比如分数从高到低），保证不会被前面的分支抢先拦截。`,
          `每个条件后面加冒号 :，下一行的代码统一缩进 4 个空格；写完检查每段缩进是否对齐。`,
        ],
        example: {
          problem: `根据分数 score 输出等级：90 及以上"优秀"，60 到 89"合格"，60 以下"不合格"。`,
          solution: [
            `解：一共三种情况，用 if-elif-else。条件按从高到低排：先判优秀，再判合格，剩下的归不合格。`,
            `第一档：if score >= 90: 打印"优秀"。`,
            `第二档：elif score >= 60: 打印"合格"（能走到这里说明已经 < 90，所以只要 >= 60 就是 60~89）。`,
            `第三档：else: 打印"不合格"（剩下的就是 < 60）。完整代码：`,
            `\`\`\`python\nif score >= 90:\n    print("优秀")\nelif score >= 60:\n    print("合格")\nelse:\n    print("不合格")\n\`\`\``,
          ],
        },
      },
      {
        name: '排查分支不生效：检查缩进与条件顺序',
        when: '代码能跑但走错了分支、或某段代码总不执行时',
        steps: [
          `先看缩进：属于同一个 if 的代码块缩进要一致，且比 if 行多 4 个空格；别把本该缩进的行顶格写。`,
          `再看条件顺序：多分支里如果把"范围大的条件"放前面，会把后面的情况都拦截掉，导致后面分支永远走不到。`,
          `用具体数值"手动走一遍"代码：假设 score = 85，从上往下看会命中哪个分支，和预期对不对。`,
          `必要时在每个分支里加一句 print，看看程序实际进了哪个分支，帮助定位。`,
        ],
        example: {
          problem: `下面代码想让 85 分输出"合格"，却总输出"优秀"，为什么？\nif score >= 60:\n    print("优秀")\nelif score >= 90:\n    print("合格")`,
          solution: [
            `解：问题在**条件顺序**。第一个条件 score >= 60，85 满足它，于是直接打印"优秀"并跳过后面。`,
            `而且这里把"优秀"和"合格"的判断标准也写反了——本该 >= 90 才优秀。`,
            `多分支必须把"更严格、范围更小"的条件放前面：先判 >= 90，再判 >= 60。`,
            `答：改成 if score >= 90: ... elif score >= 60: ...，顺序对了，85 才会落到 elif（合格）这一档。`,
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 判断奇偶并分类',
        problem: `输入一个整数 n，如果它是偶数就输出"偶数"，是奇数就输出"奇数"。请写出完整程序，并解释关键判断条件。`,
        steps: [
          `第一步：审题。结果只有两种——偶数或奇数，所以用 if-else 二选一即可。`,
          `第二步：找判断方法。判断奇偶的关键是"能不能被 2 整除"。用取模 % 求余数：n % 2 == 0 表示余数为 0、即偶数；否则就是奇数。`,
          `第三步：写代码。先用 int(input(...)) 读入并转成整数，再用 if-else 分支：`,
          `\`\`\`python\nn = int(input("请输入一个整数："))\nif n % 2 == 0:      # 余数为 0 即能被 2 整除\n    print("偶数")\nelse:                # 其余情况就是奇数\n    print("奇数")\n\`\`\``,
          `第四步：检验。输入 8，8 % 2 == 0 为 True，走 if，输出"偶数"；输入 7，7 % 2 == 0 为 False，走 else，输出"奇数"。注意 input 读到的是字符串，必须用 int() 转成整数才能做取模运算。`,
        ],
      },
      {
        title: '例题 · 三个数找最大',
        problem: `已知三个整数 a、b、c，请用 if-elif-else 找出并输出其中最大的一个。`,
        steps: [
          `第一步：审题。要在三个数里挑最大，思路是"假设一个最大，再和别人比"或"用条件逐个排除"。这里用 if-elif-else 直接比较。`,
          `第二步：设计条件。如果 a 同时大于等于 b 和 c，a 最大；否则如果 b 大于等于 c，b 最大；剩下的情况只能是 c 最大。`,
          `第三步：写代码（注意第一个分支用 and 把两个比较连起来）：`,
          `\`\`\`python\nif a >= b and a >= c:       # a 比另外两个都大\n    print("最大的是", a)\nelif b >= c:                # 走到这说明 a 不是最大，再看 b 和 c\n    print("最大的是", b)\nelse:                       # 剩下只能是 c 最大\n    print("最大的是", c)\n\`\`\``,
          `第四步：检验。设 a=3, b=9, c=5：第一个条件 a>=b 即 3>=9 为 False，整体 False，跳过；elif b>=c 即 9>=5 为 True，输出"最大的是 9"，正确。关键点：第二个分支能执行，前提是"a 已经不是最大"，所以只需在 b、c 之间再比一次。`,
        ],
      },
    ],
    pitfalls: [
      `缩进决定代码块——这是 Python 最容易出错的地方。属于同一个分支的代码必须缩进一致（通常 4 个空格），缩进错位会让代码跑到错误的分支，或直接报 IndentationError。`,
      `多分支条件顺序排错。把"范围大的条件"（如 score >= 60）放在"范围小的条件"（如 score >= 90）前面，会让后面的分支永远走不到。要从严到宽、从大到小排。`,
      `if / elif / else 后面忘了写冒号 :。Python 每个分支头部都要以冒号结尾，漏掉就报语法错。`,
      `给 else 后面加了条件。else 是"其余所有情况"，不带条件；如果想再判断一次，应该用 elif，而不是 else 条件:。`,
      `用了 if / if / if 一串而不是 if / elif / elif。连续独立的 if 会**每个都判断一遍**，可能同时执行多块；而 if-elif 是"选一个"，只执行第一个命中的。要"互斥地选一个"时务必用 elif。`,
    ],
    exercises: [
      {
        problem: `写一个程序：输入一个人的年龄 age，根据下面规则输出票价类别——age 小于 6 输出"免费"，6 到 17（含）输出"儿童票"，18 及以上输出"成人票"。`,
        hint: `一共三种情况，用 if-elif-else。条件可以从小到大排：先判 < 6，再判 < 18（能走到这说明已经 >= 6），剩下就是成人。`,
        answer: `**详细讲解**：三种情况，用 if-elif-else。\n思路：把年龄从小到大分段判断。先判最小的一档"免费"，再判中间一档"儿童票"，最后 else 兜住剩下的"成人票"。\n参考代码：\n\`\`\`python\nage = int(input("请输入年龄："))\nif age < 6:\n    print("免费")\nelif age < 18:        # 走到这说明已经 >= 6，所以是 6~17\n    print("儿童票")\nelse:                 # 剩下就是 >= 18\n    print("成人票")\n\`\`\`\n为什么这样写：关键在于利用 elif 的"短路"特性——能执行到 elif age < 18，说明前面的 age < 6 已经为假，也就是 age 至少是 6 了，所以这里只要再判断 < 18 就锁定了 6~17 这一段，不必再写 age >= 6 and age < 18（写了也对，但更啰嗦）。最后 else 不带条件，自动代表"18 及以上"。\n验证：age=4 走第一个分支"免费"；age=10 第一个条件假、第二个 10<18 真，输出"儿童票"；age=30 前两个都假，走 else"成人票"。全部符合规则。`
      },
      {
        problem: `下面代码想判断"分数及格就打印及格，否则打印不及格"，运行时报 IndentationError（缩进错误），请找出问题并改正：\nif score >= 60:\nprint("及格")\nelse:\n    print("不及格")`,
        hint: `Python 用缩进表示"哪些代码属于这个分支"。看看 if 下面那行 print 有没有缩进。`,
        answer: `**详细讲解**：错误在于 if 下面的 \`print("及格")\` **没有缩进**，顶格写了。\nPython 规定：if、else 这些分支头部后面，属于它们的代码块必须**向右缩进**（通常 4 个空格），靠缩进来表示"这段代码归这个分支管"。题目里 else 下面的 print 缩进了（正确），但 if 下面的 print 没缩进，Python 就不知道它属于哪个分支，于是报 IndentationError。\n改正后的代码：\n\`\`\`python\nif score >= 60:\n    print("及格")        # 缩进 4 个空格，表示属于 if 分支\nelse:\n    print("不及格")      # 同样缩进，属于 else 分支\n\`\`\`\n为什么：缩进在 Python 里不是"为了好看"，而是**语法的一部分**——它决定代码块的归属。同一个分支里的代码缩进必须一致。这道题提醒我们：写完 if/else 一定要检查每个分支下面的代码有没有正确缩进、且缩进对齐。`
      },
      {
        problem: `阅读下面代码，当 x = 75 时，会打印出什么？请解释为什么（注意分支顺序）：\nif x >= 50:\n    print("A")\nelif x >= 70:\n    print("B")\nelse:\n    print("C")`,
        hint: `if-elif-else 是"从上往下，第一个为真的分支执行，后面全部跳过"。75 先碰到哪个条件？`,
        answer: `**详细讲解**：答案是打印 **A**。\n追踪过程：if-elif-else 从上往下逐个判断，命中第一个为真的就执行、其余全部跳过。\n第一步：判断 \`x >= 50\`，即 75 >= 50，结果为 **True**！所以立刻执行这个分支，打印 "A"。\n第二步：因为第一个分支已经命中，后面的 elif x >= 70 和 else **完全不会再判断、不会执行**——哪怕 75 其实也满足 x >= 70。\n所以最终只打印 "A"。\n这道题揭示一个常见误区：很多同学以为 75 满足 >= 70 就会打印 "B"，但 if-elif 是"选一个"，不是"全都判断一遍"。这里 x >= 50 把 70~∞ 的数全都先拦截了，导致 elif x >= 70 永远走不到——这正是"条件顺序排错"的典型后果。正确做法应该把更严格的 x >= 70 放在前面。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 3. while 循环 while-loop
  // ───────────────────────────────────────────────────────────────
  'while-loop': {
    warmup: {
      q: `猜数字游戏：电脑想了一个数，让你一直猜，猜对了才停。"猜对才停"——意味着到底要猜几次，事先根本不知道。这种"次数不确定、满足条件才结束"的重复，程序该怎么写？`,
      hint: `如果你提前不知道要重复多少次，就没法说"重复 5 次"。但你知道一个"什么时候该停"的条件。能不能让程序"只要还没满足停止条件，就一直重复"？`,
      answer: `这种"次数不确定、靠条件决定何时停"的重复，正是 **while 循环**的拿手好戏。它的逻辑是：**只要条件为真，就反复执行循环体；一旦条件变假，就停下来。** 猜数字可以写成"while 没猜对: 继续猜"。关键是循环体里必须有能让条件最终变假的操作（比如读入新的猜测），否则就会永远循环（死循环）。`
    },
    intro: [
      `while 循环，是程序实现"重复做某件事"的两种方式之一，专门对付**次数不确定**的重复——只要某个条件还成立，就一直做下去。它和上一节的条件判断紧密相连：while 后面跟的就是一个布尔条件。`,
      `它的执行逻辑是一个反复的循环：**先检查条件 → 条件为真就执行一遍循环体 → 回到开头再检查条件 → ……直到某次检查时条件为假，就跳出循环。** 注意是"先判断后执行"，如果一开始条件就为假，循环体一次都不会执行。`,
      `while 最常见的搭档是**计数器**和**累加器**。计数器就是一个变量（如 i），每循环一次就 +1，用来记"做了多少次"；累加器（如 total）则把每次的结果加起来，循环结束就得到总和。它们都要在循环**开始前**先赋一个初始值。`,
      `使用 while 有一条铁律：**循环体里必须有能让条件最终变假的操作**。比如用 i < 5 做条件，循环体里就得有 i = i + 1 让 i 慢慢变大，最终超过 5 使条件变假。如果忘了更新，i 永远是 0，条件永远为真，程序就会**死循环**——卡住不停，这是 while 最危险的坑。`,
      `一句话记住：**while 是"条件式重复"——只要条件真就重复，所以必须保证条件迟早会变假。** 初始化变量、写好条件、在循环体里更新变量，这三步缺一不可。`
    ],
    formulas: [
      { name: 'while 基本语法', latex: '\\text{while 条件:  →  循环体（条件为真就反复执行）}',
        note: `先判断条件，为真才执行循环体，执行完回到开头再判断。条件一开始就为假则一次都不执行。冒号 : 不能漏，循环体要缩进。` },
      { name: '三步缺一不可', latex: '\\text{初始化 → 判断条件 → 更新变量}',
        note: `循环前先给变量赋初值（如 i = 0）；while 判断条件（如 i < 5）；循环体里更新变量（如 i = i + 1），让条件迟早变假。` },
      { name: '计数器累加', latex: '\\text{i = i + 1}',
        note: `计数器每循环一次自增 1，记录次数。也可写成 i += 1（等价的简写）。忘了这一步是死循环最常见的原因。` },
      { name: '累加器求和', latex: '\\text{total = total + x}',
        note: `把每次的值累加进 total，循环结束 total 就是总和。total 要在循环前初始化为 0。也可写成 total += x。` },
    ],
    methods: [
      {
        name: '写 while 循环的"三步法"',
        when: '遇到"次数不确定、满足条件才停"或"反复做直到达成某目标"的重复任务时',
        steps: [
          `第一步·初始化：在循环开始前，给要用到的变量赋初值。计数器常设 i = 0，累加器常设 total = 0。`,
          `第二步·定条件：想清楚"什么情况下还要继续"，把它写成 while 后面的布尔条件，比如 i < 5、total < 100。`,
          `第三步·写循环体并更新：在缩进的循环体里做要重复的事，并且**一定要更新条件涉及的变量**（如 i = i + 1），保证条件迟早变假。`,
          `写完后"手动走两圈"：第一次循环条件是什么、变量变成多少，第二次又如何，确认它最终能停。`,
        ],
        example: {
          problem: `用 while 循环打印 1 到 5 这五个数字。`,
          solution: [
            `解：虽然这次次数其实是确定的，但用来练习 while 的三步法。`,
            `初始化：设一个计数器 i = 1（从 1 开始打印）。`,
            `定条件：只要 i 不超过 5 就继续，写成 while i <= 5。`,
            `循环体：先打印 i，再让 i = i + 1。完整代码：\n\`\`\`python\ni = 1\nwhile i <= 5:\n    print(i)\n    i = i + 1   # 关键：让 i 增大，否则死循环\n\`\`\`\n答：依次打印 1 2 3 4 5；当 i 变成 6 时 i <= 5 为假，循环结束。`,
          ],
        },
      },
      {
        name: '用 while 做"累加求和 / 求平均"',
        when: '要把一串数加起来、或一直累加到满足某条件时',
        steps: [
          `准备两个变量：一个累加器 total = 0（存总和），一个计数器 i（控制循环、也可兼作被加的数）。`,
          `用 while 设定循环范围或停止条件，比如 while i <= 100。`,
          `循环体里先把当前的数加到 total：total = total + i，再更新计数器 i = i + 1。`,
          `循环结束后，total 就是总和；若要平均值，再用 total 除以个数。`,
        ],
        example: {
          problem: `用 while 求 1 + 2 + 3 + … + 100 的和。`,
          solution: [
            `解：用累加器 total 存和，计数器 i 从 1 数到 100。`,
            `初始化：total = 0，i = 1。`,
            `循环：while i <= 100 时，total = total + i，然后 i = i + 1。`,
            `\`\`\`python\ntotal = 0\ni = 1\nwhile i <= 100:\n    total = total + i\n    i = i + 1\nprint("和是", total)   # 输出 5050\n\`\`\`\n答：循环把 1 到 100 逐个加进 total，结束时 total = 5050。`,
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 算账户翻倍要几年',
        problem: `小明存了 100 元，每年利息让本金变成原来的 1.1 倍（即增长 10%）。问多少年后他的钱会超过 200 元？由于"几年"事先不知道，请用 while 循环求出年数。`,
        steps: [
          `第一步：审题。"几年"是不确定的，正好用 while——只要钱还没超过 200 就继续过年。需要两个变量：money（当前钱数）和 year（过了几年）。`,
          `第二步：初始化。money = 100（初始本金），year = 0（还没过年）。`,
          `第三步：定条件并写循环体。只要 money <= 200 就继续：每过一年钱乘 1.1（money = money * 1.1），年数加 1（year = year + 1）。`,
          `\`\`\`python\nmoney = 100\nyear = 0\nwhile money <= 200:        # 只要还没超过 200 就继续\n    money = money * 1.1     # 每年变成 1.1 倍\n    year = year + 1        # 年数 +1\nprint("需要", year, "年")\n\`\`\``,
          `第四步：理解结果。循环会一直把 money 乘 1.1，直到某一年 money 超过 200，条件 money <= 200 变假，循环停止，此时 year 就是答案（约 8 年）。关键点：循环体里 money 在不断变大，保证条件迟早变假，不会死循环。`,
        ],
      },
    ],
    pitfalls: [
      `死循环：忘记在循环体里更新条件变量。比如 while i < 5 里漏写 i = i + 1，i 永远是 0、条件永远为真，程序卡死不停。这是 while 最常见、最危险的错误。`,
      `忘记初始化变量。计数器、累加器必须在循环**开始前**先赋初值（i = 0、total = 0）。直接在循环里用一个没定义的变量会报 NameError。`,
      `把累加器初值设错。求和的 total 要初始化为 0；如果要算连乘（阶乘），初值得设为 1——设成 0 的话乘出来永远是 0。`,
      `条件的边界搞错，导致多循环或少循环一次。while i <= 5 会执行到 i=5，while i < 5 只到 i=4。想清楚边界该不该取等号。`,
      `循环体的缩进不对。属于 while 的代码必须缩进；如果把 i = i + 1 顶格写（不缩进），它就跑到循环外面去了，同样会造成死循环。`,
    ],
    exercises: [
      {
        problem: `用 while 循环计算 1 到 10 所有偶数的和（即 2+4+6+8+10），并把结果打印出来。`,
        hint: `可以让计数器 i 从 2 开始、每次加 2（i = i + 2），这样 i 依次是 2,4,6,8,10，正好都是偶数；用累加器 total 把它们加起来。`,
        answer: `**详细讲解**：用 while 的"三步法"来做。\n第一步·初始化：累加器 total = 0（存和），计数器 i = 2（从第一个偶数开始）。\n第二步·定条件：只要 i 不超过 10 就继续，写成 while i <= 10。\n第三步·循环体并更新：把 i 加进 total，然后让 i 每次增加 2（这样始终是偶数）。\n参考代码：\n\`\`\`python\ntotal = 0\ni = 2\nwhile i <= 10:\n    total = total + i   # 把当前偶数加进总和\n    i = i + 2           # 关键：每次加 2，跳到下一个偶数\nprint("偶数和是", total)   # 输出 30\n\`\`\`\n为什么这样写：让 i 从 2 开始、每次 += 2，i 就会依次取到 2、4、6、8、10，全是偶数，省得在循环里再判断奇偶。total 把它们一个个加起来，最后是 2+4+6+8+10=30。\n特别注意：i = i + 2 这一步既是"更新计数器"、又保证了"只取偶数"，少了它就会死循环。`
      },
      {
        problem: `阅读下面代码，它会打印出哪些数？最后 i 的值是多少？\ni = 0\nwhile i < 6:\n    print(i)\n    i = i + 2`,
        hint: `从 i=0 开始，每轮先判断 i < 6，为真才打印并让 i 加 2。一轮一轮在纸上记下 i 的变化。`,
        answer: `**详细讲解**：一轮一轮手动追踪。\n- 开始 i = 0。判断 0 < 6 为真 → 打印 **0** → i 变成 2。\n- i = 2。判断 2 < 6 为真 → 打印 **2** → i 变成 4。\n- i = 4。判断 4 < 6 为真 → 打印 **4** → i 变成 6。\n- i = 6。判断 6 < 6 为**假** → 跳出循环，不再打印。\n所以打印的数是 **0、2、4**（三个数），循环结束后 **i 的值是 6**。\n为什么 i 最后是 6 而不是 4：因为最后一轮（i=4 时）打印完又执行了 i = i + 2 让 i 变成 6，然后才去判断条件发现不满足而退出——也就是说"使条件变假的那次更新已经发生了"，所以 i 停在 6。\n这道题的要点：while 是"先判断后执行"，且要注意循环退出时变量停在"刚好让条件变假"的那个值上。`
      },
      {
        problem: `下面这段代码想打印 1 到 3，但运行后程序卡住不停（死循环），请找出原因并改正：\ni = 1\nwhile i <= 3:\n    print(i)`,
        hint: `while 能停下来，靠的是循环体里有操作让条件迟早变假。看看这段代码里 i 有没有发生变化。`,
        answer: `**详细讲解**：这是典型的**死循环**。原因是循环体里**没有更新 i**——i 一直是 1，条件 i <= 3 永远为真，于是程序无限打印 1，停不下来。\nwhile 循环能正常结束，靠的是"循环体里有让条件最终变假的操作"。这里缺了让 i 增大的那一步。\n改正后的代码：\n\`\`\`python\ni = 1\nwhile i <= 3:\n    print(i)\n    i = i + 1     # 关键：每轮让 i 加 1，i 迟早超过 3，条件变假\n\`\`\`\n为什么加这一行就好了：补上 i = i + 1 后，i 会从 1 变 2、变 3、再变 4；当 i = 4 时 i <= 3 为假，循环就正常退出，正好打印了 1、2、3。\n这道题对应 while 的核心铁律——**写 while 必须保证条件迟早会变假**，最常用的办法就是在循环体里更新计数器。每次写完 while，都要问自己一句："这个条件靠什么变假？"`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 4. for 循环 for-loop
  // ───────────────────────────────────────────────────────────────
  'for-loop': {
    warmup: {
      q: `老师让你"把全班 40 个同学的名字逐个念一遍"，或者"把'我会努力'这句话抄 10 遍"。这类重复有个共同点——你一开始就清楚要做多少次、或要处理哪些对象。这种"已知次数 / 逐个处理"的重复，有没有比 while 更省心的写法？`,
      hint: `用 while 也能做，但你得自己设计数器、自己加 1、自己判断停止，很容易漏掉某一步而死循环。有没有一种循环，能自动帮你"一个一个走完"，不用手动管计数器？`,
      answer: `这种"已知次数"或"逐个遍历一批对象"的重复，用 **for 循环**最省心。for 会**自动**取出序列里的每个元素、依次执行循环体，走完就自动停——不用你手动设计数器、加 1、判断停止，因此比 while 更不容易出错。配合 range(n) 能轻松"重复 n 次"，也能直接遍历列表、字符串里的每个元素。`
    },
    intro: [
      `for 循环，是实现重复的另一种方式，专门对付**次数已知**或**遍历一批元素**的情况。如果说 while 是"满足条件就重复"，那 for 就是"对每一个元素，做一遍"——它会自动地、一个不漏地走完整个序列。`,
      `for 最大的好处是**自动管理"走到哪了"**：你不用像 while 那样手动设计数器、手动加 1、手动判断结束。for 会自己从序列里取出下一个元素，取完最后一个就自动停止，所以几乎不会写出死循环。`,
      `最常见的搭档是 **range()**：range(n) 会生成 0, 1, 2, …, n-1 这一串数（注意从 0 开始、到 n-1 结束，**不包含 n**）。所以 for i in range(5): 会让 i 依次取 0、1、2、3、4，循环体执行 5 次，常用来"重复 n 次"。`,
      `range 还有更灵活的用法：range(a, b) 生成从 a 到 b-1（含头不含尾）；range(a, b, step) 每次跳 step。比如 range(1, 6) 是 1,2,3,4,5；range(2, 11, 2) 是 2,4,6,8,10。记住一个核心规律：**range 是"左闭右开"——包含起点，不包含终点。**`,
      `for 不只能配 range，还能**直接遍历**列表、字符串、字典等。比如 for ch in "Hi": 会让 ch 依次是 'H'、'i'；for name in names: 会逐个取出列表里的每个名字。一句话记住：**for 就是"对序列里的每个元素，依次执行循环体"，省心、不易错，是处理"已知次数/遍历集合"的首选。**`
    ],
    formulas: [
      { name: 'for 基本语法', latex: '\\text{for 变量 in 序列:  →  循环体}',
        note: `依次把序列里的每个元素赋给"变量"，每取一个就执行一遍循环体，取完自动结束。冒号 : 不能漏，循环体要缩进。` },
      { name: 'range(n)：重复 n 次', latex: '\\text{range(n)} \\Rightarrow 0,1,2,\\ldots,n-1',
        note: `生成从 0 到 n-1 共 n 个数，**不包含 n**。for i in range(5) 让 i 取 0~4，循环体执行 5 次。` },
      { name: 'range 区间左闭右开', latex: '\\text{range(a, b)} \\Rightarrow a, a+1, \\ldots, b-1',
        note: `从 a 开始到 b-1 结束，**含头不含尾**。range(1, 6) 是 1,2,3,4,5。这是 range 最容易记错的点。` },
      { name: 'range 带步长', latex: '\\text{range(a, b, step)}',
        note: `每次增加 step。range(2, 11, 2) 生成 2,4,6,8,10。step 为负还能倒着数，如 range(5, 0, -1) 是 5,4,3,2,1。` },
      { name: '直接遍历元素', latex: '\\text{for x in [\\ldots] / 字符串}',
        note: `for 能直接取出列表、字符串里的每个元素，不必通过下标。for ch in "Hi" 让 ch 依次是 'H'、'i'。` },
    ],
    methods: [
      {
        name: '"重复固定次数"用 for + range(n)',
        when: '要把某件事重复确定的次数（如打印 10 行、累加 100 次）时',
        steps: [
          `想清楚要重复几次，记作 n。`,
          `写 for i in range(n):，这样循环体会执行 n 次（i 依次取 0 到 n-1）。`,
          `如果循环体里用不到 i 这个序号，i 只是个"次数标记"，照样这么写就行。`,
          `如果要用到"第几次"，注意 i 是从 0 开始的；想从 1 开始显示，可以用 i + 1，或写 range(1, n+1)。`,
        ],
        example: {
          problem: `用 for 循环打印 3 行"加油！"。`,
          solution: [
            `解：要重复 3 次，用 for i in range(3)。`,
            `循环体里只需打印那句话，用不到序号 i。`,
            `\`\`\`python\nfor i in range(3):\n    print("加油！")\n\`\`\``,
            `答：range(3) 生成 0、1、2 三个数，循环体执行 3 次，打印出三行"加油！"。`,
          ],
        },
      },
      {
        name: '"逐个处理一批数据"用 for 直接遍历',
        when: '要对列表、字符串里的每个元素都做同样处理（如逐个打印、累加、统计）时',
        steps: [
          `确定要遍历的对象：一个列表（如成绩列表）或字符串等。`,
          `写 for 元素 in 序列:，循环变量会依次代表每个元素。`,
          `在循环体里对这个"元素"做处理，比如打印它、把它加进累加器、判断它是否符合条件。`,
          `如果既要元素、又要它的位置序号，可以用 enumerate（进阶），入门阶段先掌握直接遍历元素。`,
        ],
        example: {
          problem: `已知成绩列表 scores = [85, 92, 78]，用 for 求三科总分。`,
          solution: [
            `解：要把列表里每个分数加起来，用 for 直接遍历列表，配一个累加器。`,
            `先让 total = 0；再 for s in scores: 逐个取出分数。`,
            `循环体里 total = total + s。`,
            `\`\`\`python\nscores = [85, 92, 78]\ntotal = 0\nfor s in scores:\n    total = total + s\nprint("总分", total)   # 255\n\`\`\`\n答：for 依次把 85、92、78 取出加进 total，结束时 total = 255。`,
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 用 for 求 1 到 100 的和',
        problem: `用 for 循环计算 1 + 2 + 3 + … + 100 的和。请写出完整程序，并特别说明 range 的范围该怎么写。`,
        steps: [
          `第一步：审题。要把 1 到 100 这 100 个数加起来，次数已知，用 for + range 最合适，再配一个累加器 total。`,
          `第二步：定 range 范围。这是关键——range 是"左闭右开"，要包含 100，终点就得写成 101。所以用 range(1, 101)，它生成 1,2,…,100，正好 100 个数。`,
          `第三步：写代码。先 total = 0，再遍历累加：`,
          `\`\`\`python\ntotal = 0\nfor i in range(1, 101):    # 注意是 101，才能取到 100\n    total = total + i\nprint("和是", total)        # 输出 5050\n\`\`\``,
          `第四步：检验与强调。如果误写成 range(1, 100)，就只加到 99，结果会少 100、变成 4950。所以"要加到 100，range 终点必须写 101"——这正是"左闭右开"最常考、最易错的地方。`,
        ],
      },
      {
        title: '例题 · 统计列表里的及格人数',
        problem: `已知一个班的成绩列表 scores = [88, 55, 70, 42, 91, 60]，用 for 循环统计有多少人及格（>= 60）。`,
        steps: [
          `第一步：审题。要遍历每个成绩，判断是否 >= 60，并数出及格的个数。用 for 遍历列表 + if 判断 + 计数器。`,
          `第二步：准备计数器。在循环前设 count = 0，用来记及格人数。`,
          `第三步：遍历并判断。for s in scores: 逐个取分数；如果 s >= 60，就 count = count + 1。`,
          `\`\`\`python\nscores = [88, 55, 70, 42, 91, 60]\ncount = 0\nfor s in scores:\n    if s >= 60:           # 在循环里嵌一个 if 判断\n        count = count + 1\nprint("及格人数：", count)   # 输出 4\n\`\`\``,
          `第四步：核对。逐个看：88✔、55✗、70✔、42✗、91✔、60✔（60 也算及格，因为是 >=），共 4 人。要点：for 负责"每个都看一遍"，if 负责"挑出符合条件的"，计数器负责"数个数"，三者配合完成统计。`,
        ],
      },
    ],
    pitfalls: [
      `range 区间左闭右开记错——这是 for 最容易出错的地方。range(1, 100) 只到 99，不含 100；要包含 100 必须写 range(1, 101)。"含头不含尾"五个字一定要记牢。`,
      `忘了 range(n) 从 0 开始。for i in range(5) 里 i 是 0,1,2,3,4，不是 1~5。想从 1 开始显示要用 i+1 或 range(1, n+1)。`,
      `想"重复 n 次"却写错次数。range(5) 是 5 次（0~4），range(1, 5) 只有 4 次（1~4），别把两者搞混。`,
      `在循环里试图修改循环变量来"跳步"。for i in range(10) 里手动改 i 不会影响循环的进度（下一轮 i 还是被自动赋成下一个值），要控制步长应该用 range 的第三个参数 step。`,
      `循环体缩进不一致或漏写冒号。for 行末尾要有冒号 :，循环体要统一缩进；否则报语法错或 IndentationError。`,
    ],
    exercises: [
      {
        problem: `用 for 循环打印出 1 到 10 之间所有 3 的倍数（即 3、6、9）。请至少写出一种写法。`,
        hint: `两种思路：① 用 range(3, 11, 3) 直接每次跳 3；② 用 for i in range(1, 11) 遍历，再用 if i % 3 == 0 挑出 3 的倍数。任选其一。`,
        answer: `**详细讲解**：这道题有两种常见写法，都对。\n**写法一：用 range 的步长直接生成。** 3 的倍数从 3 开始、每次加 3，用 range(3, 11, 3)——起点 3、终点写 11（因为左闭右开，要包含 10 以内的数）、步长 3：\n\`\`\`python\nfor i in range(3, 11, 3):\n    print(i)        # 依次打印 3、6、9\n\`\`\`\nrange(3, 11, 3) 生成 3、6、9（下一个 12 已经 >= 11，不取），正好是 1~10 内的 3 的倍数。\n**写法二：遍历每个数，再用 if 挑。** 遍历 1 到 10，判断"能否被 3 整除"（余数为 0）：\n\`\`\`python\nfor i in range(1, 11):     # 1 到 10\n    if i % 3 == 0:         # 是 3 的倍数\n        print(i)\n\`\`\`\n这里 range(1, 11) 含头不含尾，正好是 1~10；i % 3 == 0 判断 i 是不是 3 的倍数。\n为什么两种都行：写法一用步长"只生成想要的数"，更简洁；写法二"全遍历 + 筛选"，更通用（换成别的条件也能用）。两种写法都要注意 range 的终点是 11 才能覆盖到 10——这就是"左闭右开"的体现。`
      },
      {
        problem: `阅读下面代码，它会打印出什么？一共打印几行？\nfor i in range(2, 8, 2):\n    print(i)`,
        hint: `range(a, b, step) 从 a 开始，每次加 step，到不小于 b 就停（不包含 b）。从 2 开始每次加 2，列出来看看。`,
        answer: `**详细讲解**：先搞清楚 range(2, 8, 2) 生成哪些数。\n规则：从起点 2 开始，每次加步长 2，只要还**小于终点 8** 就继续（左闭右开，不含 8）。\n- 第一个：2（2 < 8，取）\n- 第二个：2+2=4（4 < 8，取）\n- 第三个：4+2=6（6 < 8，取）\n- 下一个：6+2=8（8 < 8 不成立，**停止**，不取 8）\n所以 range(2, 8, 2) 生成的是 **2、4、6**，循环会打印：\n\`\`\`\n2\n4\n6\n\`\`\`\n一共打印 **3 行**。\n关键点：① range 的终点 8 **不包含**（左闭右开），所以取不到 8；② 步长决定每次跳多少。很多同学会误以为会打印到 8，那是忘了"右开"——终点是取不到的。`
      },
      {
        problem: `下面代码想计算 1 到 5 的和（应得 15），结果却得到 10，请找出错误并改正：\ntotal = 0\nfor i in range(1, 5):\n    total = total + i\nprint(total)`,
        hint: `把 range(1, 5) 生成的数一个个列出来，看看是不是真的包含了 5。问题就藏在 range 的终点上。`,
        answer: `**详细讲解**：错误出在 range 的终点写小了。\nrange(1, 5) 因为"左闭右开"，生成的是 **1、2、3、4**（不包含 5！），所以 total = 1+2+3+4 = **10**，少加了一个 5，这就是为什么结果是 10 而不是 15。\n改正：要让循环取到 5，range 的终点必须写成 **6**（即 5 + 1）：\n\`\`\`python\ntotal = 0\nfor i in range(1, 6):     # 终点写 6，才能取到 5\n    total = total + i\nprint(total)               # 输出 15\n\`\`\`\n为什么改成 6：range(1, 6) 生成 1、2、3、4、5，total = 1+2+3+4+5 = 15，正确。\n这道题是"左闭右开"最经典的陷阱：**想让循环包含数字 N，range 的终点要写 N+1。** 每次用 range 都要默念一遍"含头不含尾"，并检查终点是不是该 +1。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 5. 嵌套循环 nested-loop
  // ───────────────────────────────────────────────────────────────
  'nested-loop': {
    warmup: {
      q: `要打印一个 3 行 4 列的小方块图案（每行 4 个星号，共 3 行）。用一个循环能打印"一行 4 个星号"，可怎么让这件事重复 3 行？换句话说，"循环里面再套一个循环"会发生什么？`,
      hint: `内层循环负责"打印一行的 4 个星号"，那外层循环是不是就负责"把这件事做 3 遍"？想一想：外层每走一步，内层是不是要完整地跑一整轮？`,
      answer: `这正是 **嵌套循环**——在一个循环体里再写一个循环。规律是：**外层循环每走一步，内层循环就完整地跑一整遍。** 打印方块时，外层循环控制"行"（走 3 步＝3 行），内层循环控制"每行的星号"（每次跑 4 步＝4 个星号）。外层一行、内层一行地填，总共打印 3×4＝12 个星号。处理表格、矩阵、图案、两两组合，都靠它。`
    },
    intro: [
      `嵌套循环，就是"循环里面再套一个循环"。它是处理**二维问题**（行和列、表格、矩阵、图形）和**两两组合**问题的利器。在此之前你已经会写单层 for 循环，嵌套不过是把一个循环放进另一个循环的循环体里。`,
      `理解嵌套的关键，是看清外层和内层的"配合节奏"：**外层循环每执行一次，内层循环就要从头到尾完整地跑一整遍。** 就像表格——外层走一行，内层就把这一行的每一列都填一遍；外层走到下一行，内层再把这行所有列重新填一遍。`,
      `举个最直观的例子：外层 for i in range(3)、内层 for j in range(4)。外层 i 取 0 时，内层 j 把 0、1、2、3 跑一遍（4 次）；外层 i 取 1 时，内层 j 又从 0 跑到 3（再 4 次）……所以总的执行次数是 **3 × 4 = 12 次**，是两层次数相乘的关系。`,
      `这就引出嵌套循环的代价：**运行次数是乘法关系，会涨得很快。** 两层各 100 次就是 1 万次，三层各 100 次就是 100 万次。所以嵌套层数越多，程序越慢——这也是后面学"时间复杂度"时，双层循环被称作 O(n²) 的原因。`,
      `一句话记住：**嵌套循环＝外层管"大格子"、内层管"小格子"；外层走一步，内层跑一圈；总次数是两层相乘。** 写的时候特别要注意缩进——内层循环和它的循环体要比外层再多缩进一层，否则归属就乱了。`
    ],
    formulas: [
      { name: '嵌套循环结构', latex: '\\text{for i ...:  →  for j ...:  →  内层循环体}',
        note: `内层 for 整个写在外层 for 的循环体里，并比外层多缩进一层。外层管"行"，内层管"列"是最常见的分工。` },
      { name: '执行节奏', latex: '\\text{外层走 1 步} \\Rightarrow \\text{内层跑 1 整圈}',
        note: `外层每执行一次，内层就从头到尾完整跑一遍。外层 i 变一次，内层 j 要把它的所有值都过一遍。` },
      { name: '总次数相乘', latex: '\\text{外层 m 次} \\times \\text{内层 n 次} = m \\times n',
        note: `两层循环的总执行次数是相乘。range(3) 套 range(4) 共执行 3×4=12 次。层数越多次数涨得越快。` },
      { name: '缩进分层', latex: '\\text{内层比外层多缩进一层}',
        note: `外层循环体缩进 4 格，内层循环的循环体要缩进 8 格。缩进错位会让代码归错循环，结果全乱。` },
    ],
    methods: [
      {
        name: '"行 × 列"问题：外层管行、内层管列',
        when: '要打印图形（方块、三角形）、处理表格 / 矩阵这类"有行有列"的二维任务时',
        steps: [
          `先想清楚一共多少行、每行多少列。`,
          `外层循环控制"行"：for i in range(行数)，i 代表第几行。`,
          `内层循环控制"列"：for j in range(列数)，在每一行里逐列处理（比如打印一个字符）。`,
          `注意换行：如果是打印图形，内层负责打一行的内容，内层结束后要在外层里另起一行（print() 空打印换行）。`,
        ],
        example: {
          problem: `用嵌套循环打印一个 3 行 4 列的星号方块（每行 4 个 *，共 3 行）。`,
          solution: [
            `解：外层走 3 行，内层每行打 4 个星号。`,
            `外层 for i in range(3) 控制行；内层 for j in range(4) 控制每行的星号。`,
            `内层用 print("*", end="") 打星号且不换行（end="" 表示打完不换行）；内层结束后用一个空 print() 换行。`,
            `\`\`\`python\nfor i in range(3):           # 3 行\n    for j in range(4):       # 每行 4 个星号\n        print("*", end="")   # 打一个星号，不换行\n    print()                  # 一行打完，换行\n\`\`\`\n答：打印出 3 行、每行 4 个星号的方块。注意 print() 在外层、内层之外，每行结束才换一次行。`,
          ],
        },
      },
      {
        name: '"两两组合"问题：用两层循环配对',
        when: '要列出两组事物的所有搭配（如所有 (i, j) 组合、乘法表、配对比较）时',
        steps: [
          `确定要配对的两组范围，分别交给外层和内层循环。`,
          `外层取第一组的一个元素，内层把第二组所有元素都配一遍。`,
          `在内层循环体里处理"当前这一对"（外层变量, 内层变量）。`,
          `外层走完所有元素，就得到了全部组合，总数是两组数量相乘。`,
        ],
        example: {
          problem: `打印 1 到 3 与 1 到 3 的所有数对 (i, j)（共 9 对）。`,
          solution: [
            `解：两组都是 1~3，用两层循环配对，总共 3×3=9 对。`,
            `外层 for i in range(1, 4) 取第一个数；内层 for j in range(1, 4) 取第二个数。`,
            `内层循环体里打印当前这一对。`,
            `\`\`\`python\nfor i in range(1, 4):\n    for j in range(1, 4):\n        print(i, j)\n\`\`\`\n答：输出 (1,1)(1,2)(1,3)(2,1)…(3,3) 共 9 对，覆盖所有组合。`,
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 打印九九乘法表（部分）',
        problem: `用嵌套循环打印一个简化的乘法表：外层 i 从 1 到 3，内层 j 从 1 到 3，每行打印形如 "i×j=积" 的式子，每行结束换行。请写出代码并说明外层、内层各管什么。`,
        steps: [
          `第一步：审题。这是典型的"行×列"问题。外层 i 控制"第几行"（也是被乘数），内层 j 控制"这一行里的每个式子"（乘数）。`,
          `第二步：定范围。i 从 1 到 3 用 range(1, 4)；每行 j 也从 1 到 3 用 range(1, 4)。`,
          `第三步：写内层。内层打印一个式子 print(i, "×", j, "=", i*j, end="  ")，end="  " 让同一行的式子用空格隔开、不换行。`,
          `\`\`\`python\nfor i in range(1, 4):              # 外层：控制行（被乘数 i）\n    for j in range(1, 4):          # 内层：控制每行的式子（乘数 j）\n        print(i, "×", j, "=", i*j, end="  ")\n    print()                       # 一行打完，换行\n\`\`\``,
          `第四步：理解输出。外层 i=1 时，内层把 1×1、1×2、1×3 打在一行；i=2 时打第二行；i=3 时打第三行。共 3×3=9 个式子，排成 3 行。关键：print() 放在外层循环体里、内层循环外面，保证"每一行"结束才换行一次。`,
        ],
      },
    ],
    pitfalls: [
      `内层循环只跑一次的错觉。其实外层每走一步，内层都会从头到尾**完整重跑一遍**，不是只跑一次。算总次数要用乘法。`,
      `缩进层级搞错。内层 for 要缩进在外层循环体里（多一层缩进），内层的循环体再多缩进一层。缩进错位会让内层"跑到外层外面"，结果完全不对。`,
      `换行的 print() 放错位置。打印图形时，换行的 print() 应放在外层循环体里、内层循环之外；放进内层里会导致每打一个字符就换行。`,
      `没意识到嵌套循环很"贵"。两层循环次数相乘，数据一大就会很慢（O(n²)）。能用一层解决就别盲目套两层。`,
      `内外层用了同一个循环变量名（都用 i）。会互相干扰、把进度搞乱。外层用 i、内层用 j，名字要分开。`,
    ],
    exercises: [
      {
        problem: `下面代码会打印多少行、每行什么内容？一共打印多少个数？\nfor i in range(2):\n    for j in range(3):\n        print(i, j)`,
        hint: `外层 i 取 0、1（两次），每次外层里内层 j 都要从 0 跑到 2（三次）。把每一对 (i, j) 按顺序列出来。`,
        answer: `**详细讲解**：按"外层走一步、内层跑一圈"的节奏一步步列。\n外层 i 用 range(2)，取 **0、1** 两个值；内层 j 用 range(3)，每次取 **0、1、2** 三个值。\n- i = 0 时，内层 j 跑一整圈：打印 (0,0)、(0,1)、(0,2)\n- i = 1 时，内层 j 又跑一整圈：打印 (1,0)、(1,1)、(1,2)\n所以输出是：\n\`\`\`\n0 0\n0 1\n0 2\n1 0\n1 1\n1 2\n\`\`\`\n一共打印 **6 行**（每行一对数），即外层 2 次 × 内层 3 次 = **6** 个数对。\n关键点：① 外层每变一次，内层都要从头完整跑一遍；② 总次数是两层相乘（2×3=6）。理解这个"相乘"是嵌套循环最核心的地方。`
      },
      {
        problem: `用嵌套循环打印下面这个直角三角形（第 1 行 1 个星号，第 2 行 2 个，第 3 行 3 个，第 4 行 4 个）：\n*\n**\n***\n****`,
        hint: `外层控制行（共 4 行），关键在内层——第 i 行要打 i 个星号，所以内层循环的次数应该跟着当前行号变化。想想内层 range 怎么写才能"第几行就打几个"。`,
        answer: `**详细讲解**：这是"内层次数随外层变化"的经典题，比打印方块更进一步。\n观察规律：第 1 行 1 个星号、第 2 行 2 个……即**第几行就打几个星号**。所以内层循环的次数不是固定的，而要跟着外层的行号走。\n设外层行号 i 从 1 到 4（用 range(1, 5)），那么第 i 行要打 i 个星号，内层就用 range(i)（跑 i 次）或 range(1, i+1)。\n参考代码：\n\`\`\`python\nfor i in range(1, 5):          # 外层：第 1 到第 4 行\n    for j in range(i):         # 内层：第 i 行打 i 个星号（跑 i 次）\n        print("*", end="")     # 打一个星号，不换行\n    print()                    # 一行打完，换行\n\`\`\`\n为什么 range(i) 能做到"第几行打几个"：当 i=1 时 range(1) 跑 1 次（1 个星号），i=2 时 range(2) 跑 2 次（2 个星号）……内层次数随 i 增大，三角形就出来了。\n两个易错点：① 内层 range 要用变量 i（让次数随行变化），不能写死成固定数字；② 换行的 print() 要放在外层里、内层外面，每行结束才换行。这道题说明嵌套循环里"内层的范围可以依赖外层的变量"，非常灵活。`
      },
      {
        problem: `下面代码本想打印一个 2 行 3 列的星号方块，但结果每个星号都单独占了一行（一共 6 行），请找出错误并改正：\nfor i in range(2):\n    for j in range(3):\n        print("*")`,
        hint: `问题出在内层的 print 上。默认的 print 打印完会做一件什么事？想让同一行的星号连在一起，该怎么控制换行？`,
        answer: `**详细讲解**：错误在内层的 \`print("*")\`。\nPython 的 print **默认打印完会自动换行**。所以这段代码每打一个星号就换一次行，6 个星号就占了 6 行，根本连不成一行。\n要让同一行的星号排在一起、整行打完再换行，需要两处配合：\n① 内层打星号时用 \`end=""\` 取消自动换行（end 控制"打完后接什么"，设成空字符串就不换行）；\n② 每行的星号打完后（内层循环结束、回到外层），用一个空的 \`print()\` 来换行。\n改正后的代码：\n\`\`\`python\nfor i in range(2):              # 2 行\n    for j in range(3):          # 每行 3 个星号\n        print("*", end="")      # 打星号，不换行\n    print()                     # 一行打完，换行\n\`\`\`\n为什么这样就对了：内层用 end="" 把一行的 3 个星号连着打出来（***），内层结束后那个 print() 负责换到下一行，于是打印出 2 行、每行 3 个星号的方块。\n核心知识点：**print 默认换行**；想不换行用 end=""；换行的 print() 要放在内层之外、外层之内。这是用嵌套循环打印图形时最常见的坑。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 6. break 与 continue break-continue
  // ───────────────────────────────────────────────────────────────
  'break-continue': {
    warmup: {
      q: `在一摞作业本里找小明的那本：你会一本一本翻，但**一旦找到就立刻停手**，不会把剩下的全翻完。另一种情况：检查一串数字，遇到坏掉的（比如负数）就**跳过它、继续看下一个**。这两种"提前停"和"跳过这一个"，循环里能做到吗？`,
      hint: `普通循环要么完整跑完、要么靠条件停。但有时我们想在循环中途"立刻整个不干了"，或者"这一轮算了、直接看下一轮"。有没有专门的命令做这两件事？`,
      answer: `循环里有两个专门的"急停 / 跳过"命令：**break** 是"立刻跳出整个循环"——找到目标就 break，不再继续找；**continue** 是"跳过本轮剩下的代码、直接进入下一轮"——遇到坏数据就 continue，跳过它去看下一个。一个是"整个不干了"，一个是"这一个不算、继续下一个"，作用完全不同，别搞混。`
    },
    intro: [
      `break 与 continue，是控制循环"提前退出"或"跳过某轮"的两个关键字。普通循环要么完整跑完、要么等条件不满足才停；有了这两个命令，你就能在循环**中途**根据情况灵活地"急停"或"跳过"。`,
      `**break 的作用是"立刻跳出整个循环"。** 一旦执行到 break，循环马上结束，循环体里 break 后面的代码、以及剩下还没跑的轮次，统统不再执行，程序直接跳到循环外面继续。最典型的用途是"**找到目标就不找了**"——比如在列表里查一个数，找到立刻 break，省得白白看完后面。`,
      `**continue 的作用是"跳过本轮剩下的部分，直接进入下一轮"。** 执行到 continue，本轮循环体里 continue 后面的代码会被跳过，但循环**不结束**，而是回到循环开头开始下一轮。典型用途是"**遇到不想处理的就跳过**"——比如遍历数据时遇到坏数据（负数、空值），continue 跳过它，继续处理后面的。`,
      `一句话对比：**break 是"整个循环都不要了"，continue 是"这一轮算了、继续下一轮"。** 一个让循环彻底结束，一个只让循环跳过当前这一次。这是初学者最容易混淆的点，务必分清。`,
      `还有一条极其重要的规则：**break 和 continue 都只对"最近的、自己所在的那一层循环"起作用。** 在嵌套循环里，内层的 break 只能跳出**内层**循环，外层循环照样继续。想"一下子跳出所有层"，break 一次是做不到的，需要别的办法（如用标志变量）。`
    ],
    formulas: [
      { name: 'break：跳出整个循环', latex: '\\text{break} \\Rightarrow \\text{立刻结束整个循环}',
        note: `执行到 break，循环马上结束，后面的轮次和 break 之后的代码都不再执行，跳到循环外。常用于"找到就停"。` },
      { name: 'continue：跳过本轮', latex: '\\text{continue} \\Rightarrow \\text{跳过本轮，进入下一轮}',
        note: `执行到 continue，本轮 continue 之后的代码被跳过，但循环不结束，直接开始下一轮。常用于"遇到坏数据就跳过"。` },
      { name: '二者核心区别', latex: '\\text{break: 整个停} \\ne \\text{continue: 跳一轮}',
        note: `break 让循环彻底结束；continue 只跳过当前这一次、循环继续。最容易混的两个关键字，记牢区别。` },
      { name: '只影响最近一层', latex: '\\text{嵌套时只跳出 / 跳过最内层}',
        note: `break、continue 只对自己所在的那层循环起作用。内层 break 只跳出内层，外层照常继续。` },
    ],
    methods: [
      {
        name: '用 break 实现"找到目标就停"',
        when: '在一串数据里查找某个目标，找到后就不需要继续找时',
        steps: [
          `用 for（或 while）遍历数据。`,
          `在循环里用 if 判断"当前元素是不是要找的目标"。`,
          `一旦命中目标，先做该做的事（如记录位置、打印），然后用 break 立刻跳出循环，不再往后找。`,
          `这样能避免"已经找到却还在白白遍历剩下的元素"，更高效。`,
        ],
        example: {
          problem: `在列表 nums = [4, 8, 15, 16, 23] 里查找 15，找到就打印它的位置并停止。`,
          solution: [
            `解：遍历列表，用 if 判断是否等于 15，命中就打印并 break。`,
            `用 for 配合下标遍历（或 enumerate）。这里用 range 配下标：`,
            `\`\`\`python\nnums = [4, 8, 15, 16, 23]\nfor i in range(len(nums)):\n    if nums[i] == 15:\n        print("找到了，在第", i, "个位置")\n        break          # 找到就跳出，后面 16、23 不再看\n\`\`\``,
            `答：当 i=2 时 nums[2] 是 15，打印位置后 break，循环立即结束，不再检查 16 和 23。`,
          ],
        },
      },
      {
        name: '用 continue 跳过"不想处理的元素"',
        when: '遍历数据时，对某些元素（如负数、0、坏数据）想直接跳过、只处理其余的时',
        steps: [
          `用 for 遍历每个元素。`,
          `在循环开头用 if 判断"这个元素要不要跳过"。`,
          `如果要跳过，就执行 continue——本轮剩下的处理代码全部略过，直接进入下一轮。`,
          `不需要跳过的元素，会正常执行 continue 后面的处理代码。`,
        ],
        example: {
          problem: `把列表 nums = [5, -3, 8, -1, 10] 里的正数加起来（跳过负数）。`,
          solution: [
            `解：遍历列表，遇到负数就 continue 跳过，否则加进累加器。`,
            `先 total = 0；遍历时若 x < 0 就 continue。`,
            `\`\`\`python\nnums = [5, -3, 8, -1, 10]\ntotal = 0\nfor x in nums:\n    if x < 0:\n        continue       # 是负数，跳过本轮，不执行下面的加法\n    total = total + x  # 只有正数才会执行到这里\nprint("正数之和：", total)   # 5 + 8 + 10 = 23\n\`\`\``,
            `答：-3 和 -1 被 continue 跳过，只把 5、8、10 加起来，结果 23。`,
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · break 与 continue 配合',
        problem: `遍历列表 nums = [2, 4, -7, 6, 0, 9]：遇到负数就跳过不处理；遇到 0 就立刻停止整个循环；其余的正数全部打印出来。请写出代码并解释 break 和 continue 各起什么作用。`,
        steps: [
          `第一步：审题。三种情况——负数跳过（用 continue）、遇到 0 整个停（用 break）、正数打印。注意判断顺序。`,
          `第二步：理清顺序。先判断 0（要 break，最该优先拦），再判断负数（continue 跳过），剩下的就是要打印的正数。`,
          `第三步：写代码。`,
          `\`\`\`python\nnums = [2, 4, -7, 6, 0, 9]\nfor x in nums:\n    if x == 0:\n        break          # 遇到 0，整个循环立刻结束\n    if x < 0:\n        continue       # 遇到负数，跳过本轮，不打印\n    print(x)           # 正数才会执行到这里\n\`\`\``,
          `第四步：逐个追踪。x=2 打印；x=4 打印；x=-7 是负数，continue 跳过；x=6 打印；x=0 触发 break，循环**立刻结束**——后面的 9 根本不会被处理。所以最终打印 2、4、6。这里清楚地看出区别：continue（遇 -7）让循环跳过这一个继续，break（遇 0）让整个循环就此打住。`,
        ],
      },
    ],
    pitfalls: [
      `把 break 和 continue 弄反——这是最常见的错误。break 是"整个循环结束"，continue 是"只跳过这一轮、循环继续"。用错会导致该停的没停、或该继续的提前停了。`,
      `以为 continue 会结束循环。continue 只是跳过当前这一轮剩下的代码，循环**还会继续**下一轮；只有 break 才真正结束循环。`,
      `break 只跳出一层——在嵌套循环里，内层的 break 只能跳出内层，外层照样继续。想跳出所有层，break 一次做不到，需要用标志变量等办法。`,
      `把 continue 后面的处理代码放错位置。continue 一旦执行，它**后面**的代码本轮就被跳过了；要先想清楚哪些代码该在 continue 之前、哪些在之后。`,
      `在 while 循环里用 continue 时，忘了 continue 会跳过后面"更新计数器"的语句，导致死循环。比如计数器的 i = i + 1 写在 continue 后面，跳过它 i 就不变了。`,
    ],
    exercises: [
      {
        problem: `阅读下面代码，它会打印出哪些数字？（注意 break 的作用）\nfor i in range(1, 10):\n    if i == 5:\n        break\n    print(i)`,
        hint: `循环从 1 开始，每轮先判断 i 是不是 5，是就 break（整个循环结束），否则打印 i。i 走到 5 时会发生什么？`,
        answer: `**详细讲解**：一轮一轮追踪，重点看 break。\n- i = 1：判断 1 == 5 为假，不 break，打印 **1**。\n- i = 2：2 == 5 为假，打印 **2**。\n- i = 3：打印 **3**。\n- i = 4：打印 **4**。\n- i = 5：判断 5 == 5 为**真**！执行 break——**整个循环立刻结束**，注意 break 在 print 之前，所以 5 **不会被打印**，后面的 6、7、8、9 也都不再执行。\n所以打印的是 **1、2、3、4**。\n关键点：① break 一执行，循环就彻底结束，后面所有轮次都没了；② 这里 break 写在 print(i) 前面，所以触发 break 的那个值（5）本身不会被打印。如果把 print 放在 if 前面，结果就不同了——要注意代码的先后顺序。`
      },
      {
        problem: `把上一题的 break 换成 continue，其余不变，它又会打印出哪些数字？请和上一题对比，说明 break 和 continue 的区别。\nfor i in range(1, 10):\n    if i == 5:\n        continue\n    print(i)`,
        hint: `continue 是"跳过本轮、继续下一轮"，不是结束循环。i 等于 5 那一轮会跳过打印，但循环会不会停？`,
        answer: `**详细讲解**：这次是 continue，循环**不会结束**。\n追踪过程：i 从 1 到 9（range(1,10) 左闭右开，取 1~9）。\n- i = 1,2,3,4：都不等于 5，正常打印 1、2、3、4。\n- i = 5：判断 5 == 5 为真，执行 continue——**跳过本轮剩下的代码**（也就是跳过 print(5)），但循环不停，直接进入下一轮。所以 5 不打印。\n- i = 6,7,8,9：都不等于 5，正常打印 6、7、8、9。\n所以打印的是 **1、2、3、4、6、7、8、9**（唯独少了 5）。\n**和上一题对比**：\n- 上一题用 break：走到 5 就**整个循环结束**，打印 1、2、3、4（5 及以后全没了）。\n- 这一题用 continue：走到 5 只**跳过这一轮**，5 不打印，但循环继续，6~9 照常打印。\n这正是两者的根本区别——**break 是"整个不干了"，continue 是"这一个跳过、继续干"**。一字之差，结果完全不同，这是最该记牢的对比。`
      },
      {
        problem: `下面代码想"遍历 1 到 5，跳过 3，打印其余的数"，但运行后陷入死循环，请找出原因并改正：\ni = 0\nwhile i < 5:\n    i = i + 1\n    if i == 3:\n        continue\n    print(i)`,
        hint: `这段代码其实**能正常运行**——先别急着改。真正容易出死循环的是"i = i + 1 写在 continue 后面"的情况。请判断这段到底会不会死循环，若不会就解释为什么，并指出"什么样的写法会死循环"。`,
        answer: `**详细讲解**：这道题有个"陷阱中的陷阱"——题目说它死循环，但实际上**这段代码不会死循环**，能正常工作。我们要看清为什么，并找出真正会死循环的写法。\n先分析给出的代码：计数器更新 \`i = i + 1\` 写在 **continue 之前**。所以即使某轮执行了 continue，i 在这之前已经加过 1 了，i 仍在正常增大，循环迟早会因为 i < 5 不成立而结束。\n追踪一下：i=1 打印1；i=2 打印2；i=3 时 continue 跳过打印（但 i 已经是 3 了，下一轮继续）；i=4 打印4；i=5 打印5；i 变 5 后判断 5<5 为假，退出。输出 1、2、4、5，完全正常。\n**那什么写法会死循环？** 如果把 i = i + 1 放在 continue **后面**，就出问题了：\n\`\`\`python\n# ✗ 错误：会死循环\ni = 0\nwhile i < 5:\n    if i == 3:\n        continue        # i=3 时跳过，下面的 i=i+1 永远执行不到！\n    print(i)\n    i = i + 1\n\`\`\`\n在这个错误版本里，当 i 加到 3 时，continue 会跳过它后面所有代码——**包括 i = i + 1**，于是 i 永远停在 3，条件 i < 5 永远为真，死循环。\n**结论与改正建议**：题目给的原代码本身正确、不需要改。真正的教训是——**在 while 里用 continue 时，一定要确保"更新计数器"的语句在 continue 之前执行**，否则 continue 会把它跳过、造成死循环。这是 continue 配合 while 时最隐蔽的坑。`
      },
    ],
  },

});
