// 高中 · 程序设计与应用·数据·智能 —— 离线选择题分片
// 覆盖：inheritance-polymorphism / design-patterns / file-io / database /
//       web-basics / data-analysis-py / ai-ml-intro 共 7 个节点，每节点 2 题（难度对应高一/高二/高三）。
// 题型轮换：概念辨析 / 读程序写结果 / 找 bug；干扰项取材各节点小课的易错点。
// 加载顺序：必须在 knowledge-quiz-seed.js 之后加载（追加合并，不覆盖已有题目）。

function __appendQuiz(bank) {
  const Q = window.CODE_QUIZ_SEED;
  for (const [id, qs] of Object.entries(bank)) {
    (Q[id] = Q[id] || []).push(...qs);
  }
}
__appendQuiz({

  'inheritance-polymorphism': [
    { q: '执行下面代码后输出是（ ）\n`class Animal:`\n`    def speak(self): return "..."`\n`class Dog(Animal):`\n`    def speak(self): return "汪"`\n`class Cat(Animal):`\n`    def speak(self): return "喵"`\n`for a in [Dog(), Cat()]:`\n`    print(a.speak(), end=" ")`',
      options: ['... ...', '汪 喵', '汪 汪', '报错：speak 重复定义'], correct: 1,
      explanation: '这就是**多态**：同样调用 `a.speak()`，运行时按对象的实际类型选择实现——Dog 出"汪"、Cat 出"喵"。子类定义同名方法会**覆盖**父类版本。' },
    { q: '设计类时，"学生**是一种**人"和"汽车**有一个**发动机"，分别应该用（ ）',
      options: ['都用继承', '前者用继承（Student 继承 Person），后者用组合（Car 里放一个 Engine 属性）', '前者用组合，后者用继承', '都用组合'], correct: 1,
      explanation: '**继承表达"是一种"（is-a）**，**组合表达"有一个"（has-a）**。让 Car 继承 Engine 是典型的继承滥用——汽车不是一种发动机，只是拥有它。' },
  ],

  'design-patterns': [
    { q: '界面上的数据一变化，多个图表组件都要自动刷新显示。最适合的设计模式是（ ）',
      options: ['单例模式', '观察者模式：数据对象状态改变时自动通知所有订阅者', '工厂模式', '策略模式'], correct: 1,
      explanation: '**观察者模式**专管"一变多知"：图表们订阅数据对象，数据更新时逐个通知，双方不必互相写死。单例管"全局只有一份"，工厂管"统一创建对象"。' },
    { q: '关于设计模式的使用，正确的态度是（ ）',
      options: ['模式越多代码越专业，能套就套', '模式是为了解决真实出现的设计问题；简单代码硬套模式属于过度设计，和不设计一样糟', '小程序也必须先套上工厂模式再动手', '背熟 23 种模式的名字就等于会设计'], correct: 1,
      explanation: '模式是**前人对特定问题的解法总结**，前提是"问题真的出现了"。给 50 行的脚本堆抽象层只会更难读——**过度设计**是学了模式之后最常见的新坑。' },
  ],

  'file-io': [
    { q: '推荐用 `with open("data.txt") as f:` 而不是裸 `f = open(...)` 的主要原因是（ ）',
      options: ['with 写法运行速度更快', 'with 块结束时自动关闭文件，即使中途出错也不会漏关', 'with 能打开更大的文件', '不用 with 就不能读中文'], correct: 1,
      explanation: '**with 自动管理关闭**：无论正常结束还是中途抛异常，文件都会被关闭。手动 open/close 一旦中间出错，close 执行不到，可能造成数据没写全或资源泄漏。' },
    { q: '用 `open("log.txt", "w")` 打开一个**已有内容**的文件，会发生（ ）',
      options: ['新内容追加到原内容后面', '原有内容被清空，从头开始写', '报错：文件已存在', '自动备份原文件'], correct: 1,
      explanation: '"w" 模式会**先清空文件**再写——想在末尾续写要用追加模式 **"a"**。误用 "w" 覆盖掉旧数据是文件操作最疼的一课。' },
  ],

  'database': [
    { q: 'SQL 语句 `SELECT name, score FROM students WHERE score >= 90` 的作用是（ ）',
      options: ['把所有学生的分数都改成 90', '从 students 表中查出分数不低于 90 的学生的姓名和分数', '删除分数低于 90 的学生', '新建一张叫 students 的表'], correct: 1,
      explanation: '**SELECT 查询**：FROM 指定表、WHERE 过滤行、SELECT 后面列出要哪些列。修改用 UPDATE、删除用 DELETE、建表用 CREATE TABLE。' },
    { q: '关于数据表"主键"的说法，正确的是（ ）',
      options: ['主键的值可以重复，方便分组', '主键唯一标识表中的每一行，不能重复也不能为空', '每张表必须把姓名设为主键', '主键只能是字符串类型'], correct: 1,
      explanation: '**主键 = 每行的唯一身份证**：不可重复、不可为空，常用自增编号或学号这类天然唯一的字段。姓名会重名，不适合当主键。' },
  ],

  'web-basics': [
    { q: '关于 HTTP 的 GET 和 POST 方法，最常见的分工是（ ）',
      options: ['GET 用于获取数据，POST 用于向服务器提交数据', 'GET 用于提交表单，POST 用于打开网页', '两者完全等价，随便用', 'GET 只能在手机上用'], correct: 0,
      explanation: '**GET 取数据**（参数拼在 URL 上，如搜索、看页面）；**POST 交数据**（内容放请求体里，如注册、提交表单）。语义分清，接口才规范。' },
    { q: '做一个留言板网站，对用户提交的内容正确的处理方式是（ ）',
      options: ['前端用 JS 检查过就可以直接存入数据库', '服务器端必须再次校验和过滤，因为请求可以绕过网页直接伪造发送', '只要用户登录了就完全可信', '内容太长截断即可，无需其他检查'], correct: 1,
      explanation: '**永远不要相信客户端**：攻击者可跳过你的网页用工具直接发请求，前端校验形同虚设。服务器端校验、过滤（防 SQL 注入、XSS）是安全底线。' },
  ],

  'data-analysis-py': [
    { q: '用 pandas 读入成绩表后，`df["score"].mean()` 的作用是（ ）',
      options: ['求 score 列的最大值', '求 score 列的平均值', '统计 score 列有多少行', '把 score 列从小到大排序'], correct: 1,
      explanation: '`mean()` 求**平均值**；最大值是 `max()`、计数是 `count()`、排序是 `sort_values()`。按班级分组求平均则是 `df.groupby("class")["score"].mean()`。' },
    { q: '柱状图把纵轴起点设在 95 而不是 0，两组数据 96 和 98 看起来相差好几倍。这个图的问题是（ ）',
      options: ['颜色不够鲜艳', '纵轴不从 0 开始，视觉上夸大了实际差异，容易误导读者', '柱子应该改成横向', '数据本身是错的'], correct: 1,
      explanation: '**截断纵轴是最常见的图表误导**：96 与 98 实际只差约 2%，起点设 95 后柱高差 3 倍。做数据可视化要如实呈现——柱状图纵轴原则上从 0 开始。' },
  ],

  'ai-ml-intro': [
    { q: '机器学习中的"过拟合"指的是（ ）',
      options: ['模型太简单，训练集上都学不会', '模型把训练数据背得太死，训练集上表现很好、在没见过的新数据上反而很差', '训练时间太长导致程序崩溃', '数据量太大存不下'], correct: 1,
      explanation: '**过拟合 = 背题而不是学规律**：训练集近乎满分、测试集一塌糊涂。"太简单学不会"是欠拟合。对策：更多数据、简化模型、正则化等。' },
    { q: '训练模型时要把数据分成训练集和测试集（如 80% / 20%），这样做的目的是（ ）',
      options: ['减少数据量，让训练更快', '用模型**没见过**的测试集评估真实水平，防止"用训练数据考自己"高估效果', '测试集用来给模型补课', '纯粹是行业习惯，没有实际意义'], correct: 1,
      explanation: '用训练数据评估相当于**考试考原题**，分数虚高没有意义。测试集模拟"从未见过的新数据"，才能衡量模型的**泛化能力**——这也是发现过拟合的手段。' },
  ],

});
