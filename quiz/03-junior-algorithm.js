// 初中 · 算法 —— 离线选择题分片
// 覆盖：linear-search / bubble-sort / selection-insertion-sort / quick-merge-sort / greedy / dp-intro 共 6 个节点，每节点 2 题。
// 题型轮换：概念辨析 / 读程序写结果 / 找 bug；干扰项取材各节点小课的易错点。
// 加载顺序：必须在 knowledge-quiz-seed.js 之后加载（追加合并，不覆盖已有题目）。

function __appendQuiz(bank) {
  const Q = window.CODE_QUIZ_SEED;
  for (const [id, qs] of Object.entries(bank)) {
    (Q[id] = Q[id] || []).push(...qs);
  }
}
__appendQuiz({

  'linear-search': [
    { q: '在列表 `[4, 7, 1, 9]` 中线性查找 9（从头逐个比较，找到就停），共需要比较多少次（ ）',
      options: ['1 次', '3 次', '4 次', '9 次'], correct: 2,
      explanation: '依次比较 4、7、1、9，第 **4** 次遇到目标。线性查找最坏要把 n 个元素全看一遍，所以是 O(n)。' },
    { q: '关于线性查找和二分查找的选择，正确的说法是（ ）',
      options: ['线性查找要求数据必须先排序', '数据无序时只能线性查找；二分查找必须建立在有序数据上', '二分查找任何时候都比线性查找快，线性查找没有存在价值', '线性查找的复杂度是 O(log n)'], correct: 1,
      explanation: '线性查找**对数据没有要求**、无序也能用（O(n)）；二分的 O(log n) 快是靠"**有序**"换来的。数据只查一次时，先排序再二分反而更亏。' },
  ],

  'bubble-sort': [
    { q: '对 `[5, 3, 8, 1]` 执行**一轮**冒泡排序（从左到右两两比较、逆序就交换）后，列表变为（ ）',
      options: ['[1, 3, 5, 8]', '[3, 5, 1, 8]', '[3, 5, 8, 1]', '[1, 5, 3, 8]'], correct: 1,
      explanation: '(5,3) 逆序交换 → [3,5,8,1]；(5,8) 不换；(8,1) 交换 → **[3, 5, 1, 8]**。一轮只保证把**最大值沉到末尾**，不是一轮就排好。' },
    { q: '关于冒泡排序的说法，正确的是（ ）',
      options: ['每一轮把最小的元素放到最前面', 'n 个元素最多需要 n−1 轮，每轮结束时最大的未排定元素就位到末尾', '冒泡排序的时间复杂度是 O(n log n)', '冒泡一轮之后整个列表就有序了'], correct: 1,
      explanation: '冒泡每轮把**当前最大值"冒"到未排序部分的末尾**，最多 n−1 轮完成，比较总次数约 n²/2，复杂度 **O(n²)**。"每轮选最小放前面"说的是选择排序。' },
  ],

  'selection-insertion-sort': [
    { q: '对 `[5, 3, 8, 1]` 执行**一轮**选择排序（找出最小值与第一个位置交换）后，列表变为（ ）',
      options: ['[3, 5, 8, 1]', '[1, 3, 8, 5]', '[1, 5, 8, 3]', '[1, 3, 5, 8]'], correct: 1,
      explanation: '第一轮扫描全表找到最小值 1（在末尾），与开头的 5 **交换位置**：得 **[1, 3, 8, 5]**。注意是"交换"而不是"插入挪位"。' },
    { q: '一份数据已经"基本有序"（只有个别元素错位），此时哪种排序表现最好、接近 O(n)（ ）',
      options: ['选择排序', '插入排序', '冒泡排序（无提前退出优化）', '三种都一样是 O(n²)'], correct: 1,
      explanation: '**插入排序**对基本有序的数据几乎不用挪动，每个元素找位置很快，接近 O(n)；选择排序不管数据多有序都要完整扫描找最小值，固定 O(n²)。' },
  ],

  'quick-merge-sort': [
    { q: '快速排序在哪种情况下会退化到最坏复杂度 O(n²)（ ）',
      options: ['数据完全随机时', '数据已经有序、且每次固定取第一个元素当基准时', '数据量特别大时', '任何情况下都是 O(n log n)，不会退化'], correct: 1,
      explanation: '已有序 + 固定取端点做基准 → 每次划分**一边空、一边 n−1 个**，递归 n 层、每层 O(n)，总计 **O(n²)**。随机选基准可以有效避免。' },
    { q: '关于归并排序的说法，正确的是（ ）',
      options: ['归并排序最坏也是 O(n log n)，但合并时需要 O(n) 的额外空间', '归并排序不需要任何额外空间', '归并排序最坏会退化到 O(n²)', '归并排序只能排整数'], correct: 0,
      explanation: '归并排序**任何情况都是 O(n log n)**、且稳定，代价是合并阶段要一个 O(n) 的辅助数组。"最坏退化 O(n²)"是快排的特点，两者正好互补。' },
  ],

  'greedy': [
    { q: '贪心算法的基本特点是（ ）',
      options: ['把所有可能方案都枚举一遍再挑最好的', '每一步都做当前看起来最优的选择，且不回头修改', '把大问题拆成子问题并把答案存表复用', '随机尝试直到碰到正确答案'], correct: 1,
      explanation: '贪心 = **每步取局部最优 + 从不反悔**。它快，但只有问题满足"局部最优能拼出全局最优"时才正确——所以用贪心前要先想清楚甚至证明。' },
    { q: '用面值 `[10, 5, 1]` 的硬币找零 25 分，贪心策略（每次拿不超过余额的最大面值）共用掉几枚硬币（ ）',
      options: ['2 枚', '3 枚', '4 枚', '5 枚'], correct: 1,
      explanation: '25 → 拿 10（剩 15）→ 拿 10（剩 5）→ 拿 5（剩 0），共 **3 枚**。这套面值下贪心恰好最优；但换成面值 [9,6,1] 找 12，贪心给 9+1+1+1 四枚，而最优是 6+6 两枚——贪心不是万能的。' },
  ],

  'dp-intro': [
    { q: '爬楼梯：每次可以上 1 级或 2 级，设走到第 n 级的方法数 f(n) = f(n−1) + f(n−2)，且 f(1)=1、f(2)=2。那么 f(4) 等于（ ）',
      options: ['3', '4', '5', '8'], correct: 2,
      explanation: 'f(3) = f(2)+f(1) = 3，f(4) = f(3)+f(2) = 3+2 = **5**。从小到大递推、每个答案只算一次，这正是动态规划的做法。' },
    { q: '纯递归计算斐波那契是 O(2ⁿ)，加了"记忆化"（把算过的答案存进表）后降到 O(n)，根本原因是（ ）',
      options: ['查表比加法运算更快', '每个子问题只在第一次计算，之后直接查表，避免了大量重复计算', '记忆化让递归深度变浅了', '存表后就不需要递归终止条件了'], correct: 1,
      explanation: '纯递归会把 fib(3)、fib(2) 等子问题**重复算指数多遍**；记忆化保证**每个子问题只算一次**，n 个子问题各 O(1) 合并，总共 O(n)。"避免重复子问题"就是 DP 的核心。' },
  ],

});
