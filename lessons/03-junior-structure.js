// 初中编程 · 数据结构 —— 详细小课（习题讲解版）
// 覆盖 7 个节点：list / dict / set-type / stack-queue / linked-list / tree-intro / graph-intro
// 数据结构对齐 node-detail.jsx 的渲染：warmup / intro / formulas(name,latex,note) /
//   methods(name,when,steps,example{problem,solution}) / worked(title,problem,steps) /
//   pitfalls / exercises(problem,hint,answer)
// 注意：node-detail.jsx 不渲染 derivation，故 formulas 只放 name/latex/note。
// 文本支持 **加粗** 与 $LaTeX$（行内）；复杂度统一用 KaTeX，如 $O(1)$ / $O(n)$。
// 含代码或带引号的字段值一律用反引号模板字符串；代码内换行写 \n。

Object.assign(window.CODE_LESSON, {

  // ═══════════════════════════════════════════════════════════════
  //   列表 list
  // ═══════════════════════════════════════════════════════════════
  'list': {
    warmup: {
      q: `老师要登记全班 40 个同学的语文成绩。如果给每个人都起一个变量名——score1、score2、score3 … 一直到 score40，会怎么样？要是想求平均分、想把不及格的挑出来，你打算怎么写？`,
      hint: `想一想：如果改成 50 个人、100 个人呢？变量名是不是要一个个手写？"对每个分数做同一件事"，有没有办法用一个循环就搞定？`,
      answer: `给 40 个分数起 40 个变量名几乎没法用——求平均要把 40 个名字全加一遍，换成 50 人又得重写。真正的办法是把这一串分数装进**一个列表**：\`scores = [85, 92, 78, ...]\`。这样它们有了统一的"门牌号"（下标 0、1、2…），可以用一个 \`for\` 循环挨个处理，求和、筛选、排序都只要几行。**列表就是"一串有顺序、可修改的数据"的容器**，是几乎所有程序都离不开的基础数据结构。`
    },
    intro: [
      `**列表（list）**是 Python 里最常用的数据结构，用来把"一串有先后顺序的数据"装进**一个**变量里。写法是把元素放进方括号、用逗号隔开：\`scores = [85, 92, 78]\`。它解决的核心问题是——当你有很多个同类数据时，不必起很多个变量名，用一个列表统一管理。`,
      `列表里的每个元素都有一个**位置编号，叫下标（索引），从 0 开始数**。\`scores[0]\` 是第 1 个元素 85，\`scores[1]\` 是第 2 个 92。注意"第几个"和"下标"差 1：第 3 个元素的下标是 2。Python 还支持**负下标**，\`scores[-1]\` 表示**最后一个**，这在不知道列表多长时特别好用。`,
      `列表是**可变（mutable）**的，意思是建好之后还能随时改：用 \`scores[1] = 95\` 改掉某一个、用 \`scores.append(100)\` 在末尾添一个、用 \`scores.pop()\` 删掉末尾一个。这一点和"不可变"的字符串不同——字符串任何修改都会生成新串，列表是在原地变。`,
      `列表的另一个利器是**切片**：\`scores[1:3]\` 取出下标 1、2 两个元素（**含头不含尾**，到 3 之前停）。切片会得到一个**新的列表**，不影响原来的。配合 \`len(scores)\` 拿到长度、用 \`for x in scores\` 遍历每个元素，列表的"增删改查 + 遍历"就齐全了。`,
      `一句话记住：**列表 = 有顺序 + 可修改 + 下标从 0 开始的一串数据**。按下标直接取某个元素非常快（$O(1)$，一步到位）；但要在开头插入或删除会慢（$O(n)$，后面的元素都得挪位）。理解了列表，后面的字典、集合、栈、队列才好学——它们很多都用列表来搭。`
    ],
    formulas: [
      { name: '列表索引（下标从 0 开始）', latex: 'lst[0],\\ lst[1],\\ \\dots,\\ lst[n-1]',
        note: `长度为 $n$ 的列表，合法下标是 $0$ 到 $n-1$。\`lst[0]\` 是第 1 个、\`lst[-1]\` 是最后一个。下标越界（如对长 3 的列表取 \`lst[3]\`）会报 IndexError。` },
      { name: '切片（含头不含尾）', latex: 'lst[a:b]\\ \\to\\ \\text{下标 } a,\\,a{+}1,\\dots,b{-}1',
        note: `\`lst[1:3]\` 取下标 1、2，**到 3 之前停**。省略写法：\`lst[:2]\` 从头到下标 1，\`lst[2:]\` 从下标 2 到末尾，\`lst[:]\` 整个复制一份。` },
      { name: '常用操作的时间复杂度', latex: '\\text{索引/末尾增删}=O(1),\\quad \\text{查找/头部增删}=O(n)',
        note: `按下标取值、\`append\`、\`pop()\`（删末尾）都是 $O(1)$，很快；\`x in lst\`（查找）、\`insert(0, x)\`（头部插入）、\`pop(0)\`（删头部）是 $O(n)$，因为要逐个比较或整体挪位。` },
      { name: '长度与遍历', latex: '\\texttt{len(lst)},\\quad \\texttt{for x in lst}',
        note: `\`len(lst)\` 返回元素个数（$O(1)$）。\`for x in lst:\` 依次取出每个元素，是处理整张列表的标准写法。` },
    ],
    methods: [
      {
        name: '列表常用操作（增删改查）',
        when: `需要对一串数据做添加、删除、修改、查找、遍历时——这是用列表做任何事的基本功`,
        steps: [
          `**查**：按下标取值 \`lst[i]\`；判断在不在用 \`x in lst\`；找位置用 \`lst.index(x)\``,
          `**改**：直接给某个位置赋新值 \`lst[i] = 新值\``,
          `**增**：末尾添加用 \`lst.append(x)\`（最常用、最快）；指定位置插入用 \`lst.insert(i, x)\``,
          `**删**：删末尾 \`lst.pop()\`；删指定位置 \`lst.pop(i)\`；按值删第一个 \`lst.remove(x)\``,
          `**遍历**：\`for x in lst:\` 取每个元素；若同时要下标，用 \`for i, x in enumerate(lst):\``,
        ],
        example: {
          problem: `有 \`scores = [85, 92, 78]\`。请：在末尾加一个 100；把第 2 个改成 95；删掉第 1 个；最后输出列表和它的长度。`,
          solution: [
            `解：逐步操作。先 \`scores.append(100)\` → \`[85, 92, 78, 100]\`。`,
            `把"第 2 个"（下标 1）改成 95：\`scores[1] = 95\` → \`[85, 95, 78, 100]\`。`,
            `删"第 1 个"（下标 0）：\`scores.pop(0)\` → \`[95, 78, 100]\`。`,
            `答：\`print(scores)\` 输出 \`[95, 78, 100]\`；\`print(len(scores))\` 输出 \`3\`。`,
          ],
        },
      },
      {
        name: '用循环遍历列表做统计',
        when: `要对列表里的所有元素算总和、平均、最大值，或按条件筛选出一部分时`,
        steps: [
          `准备一个"累加器"或一个"新列表"，初始值设好（求和设 0，筛选设空列表 \`[]\`）`,
          `用 \`for x in lst:\` 把每个元素取出来`,
          `在循环体里对 \`x\` 做处理：求和就 \`total += x\`；筛选就 \`if 条件: 新列表.append(x)\``,
          `循环结束后，累加器里就是结果（平均分还要再除以 \`len(lst)\`）`,
        ],
        example: {
          problem: `\`scores = [85, 92, 78, 60, 95]\`。求平均分，并把所有 90 分及以上的成绩单独挑进一个新列表。`,
          solution: [
            `解：先求和再求平均。代码：`,
            `\`\`\`total = 0\nfor x in scores:\n    total += x          # 把每个分数累加\naverage = total / len(scores)   # 总和 ÷ 个数\`\`\``,
            `\`total = 410\`，\`average = 410 / 5 = 82.0\`。`,
            `再筛选高分：\`\`\`high = []\nfor x in scores:\n    if x >= 90:\n        high.append(x)     # 满足条件才放进新列表\`\`\``,
            `答：平均分 \`82.0\`，高分列表 \`high = [92, 95]\`。`,
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 找出列表中的最大值和它的位置',
        problem: `给定一个成绩列表 \`scores = [78, 92, 85, 92, 60]\`，请用循环找出最高分是多少、它**第一次**出现在第几个位置（下标）。不能直接用内置的 max()。`,
        steps: [
          `第一步：审题。要同时记住两样东西——目前见过的"最大值"和它的"下标"。需要两个变量来保存。`,
          `第二步：定起点。先假设第 0 个就是最大：\`max_val = scores[0]\`（值 78），\`max_idx = 0\`。`,
          `第三步：从头扫到尾，逐个比较。用 \`enumerate\` 同时拿到下标和值：\`\`\`for i, x in enumerate(scores):\n    if x > max_val:        # 发现更大的\n        max_val = x        # 更新最大值\n        max_idx = i        # 同时更新它的位置\`\`\``,
          `第四步：走一遍过程。i=0 时 78 不大于 78，不变；i=1 时 92>78，更新 max_val=92、max_idx=1；i=2 时 85 不大于 92；i=3 时 92 不**大于**92（注意是严格大于，所以第二个 92 不会覆盖），不变；i=4 时 60 不大于 92。`,
          `第五步：作答。\`max_val = 92\`，\`max_idx = 1\`。最高分 92 分，第一次出现在下标 1（第 2 个）。关键点：用 \`>\` 而不是 \`>=\`，才能保证拿到的是**第一次**出现的位置。`,
        ],
      },
    ],
    pitfalls: [
      `**下标从 0 开始，"第 n 个"的下标是 n−1。** 想取第 3 个元素要写 \`lst[2]\`，不是 \`lst[3]\`。长为 5 的列表最大合法下标是 4，写 \`lst[5]\` 会报 IndexError。`,
      `**列表是可变对象，赋值是"引用"不是"复制"。** 写 \`b = a\` 后，\`b\` 和 \`a\` 指向**同一个**列表，改 \`b\` 会连 \`a\` 一起变！要真正复制一份，用 \`b = a[:]\` 或 \`b = a.copy()\`。这是初学者最容易踩的坑。`,
      `**切片"含头不含尾"。** \`lst[1:3]\` 只取下标 1 和 2，**不含** 3。想取到最后一个，写 \`lst[1:]\`（省略尾），别写 \`lst[1:len(lst)-1]\`（那会漏掉最后一个）。`,
      `**在循环里一边遍历一边删元素，会漏删或出错。** 删掉一个元素后，后面的元素下标都变了，循环指针会"跳过"。正确做法是新建一个列表只放要保留的元素，或者倒着遍历。`,
      `**\`append\` 和 \`+\` 不一样。** \`lst.append([1,2])\` 是把 \`[1,2]\` 当成**一个元素**塞进去（列表里套列表）；\`lst = lst + [1,2]\` 才是把 1、2 两个元素接到后面。想逐个追加多个元素用 \`extend\`。`,
    ],
    exercises: [
      {
        problem: `有列表 \`a = [1, 2, 3]\`。小明写了 \`b = a\`，然后执行 \`b.append(4)\`。这时 \`print(a)\` 会输出什么？\`print(b)\` 又是什么？如果他真正想要的是"\`b\` 是 \`a\` 的一份独立副本，改 \`b\` 不影响 \`a\`"，应该怎么改这行代码？`,
        hint: `关键在于 \`b = a\` 到底复制了"列表本身"，还是只复制了"指向列表的引用"？想想 CLAUDE.md 里提到的"变量是引用而不是值本身"。`,
        answer: `**详细讲解**：\n\n**思路**：在 Python 里，列表是**可变对象**，\`b = a\` 这行**并没有复制列表**，只是让 \`b\` 和 \`a\` 这两个名字指向**内存里同一个列表**。就像给同一个房间挂了两块门牌。所以通过 \`b\` 去 \`append\`，改的就是那个唯一的列表，\`a\` 看到的当然也变了。\n\n**结果**：\`print(a)\` 输出 \`[1, 2, 3, 4]\`，\`print(b)\` 也输出 \`[1, 2, 3, 4]\`——两个一模一样，因为它们本来就是同一个。\n\n**参考代码（正确复制）**：\n\`\`\`b = a[:]        # 用切片复制一份新列表\n# 或 b = a.copy()  # 用 copy() 方法，效果一样\nb.append(4)\nprint(a)   # [1, 2, 3]  —— 不受影响\nprint(b)   # [1, 2, 3, 4]\`\`\`\n\`a[:]\` 是"从头切到尾"，会生成一个**全新的列表**，\`b\` 指向这个新列表，从此和 \`a\` 各走各的。\n\n**复杂度**：复制一份列表要把每个元素都拷过去，是 $O(n)$；而原来的 \`b = a\`（只复制引用）是 $O(1)$。这也是为什么 Python 默认不自动深拷贝——避免不必要的开销。`
      },
      {
        problem: `给定 \`nums = [3, 8, 1, 8, 5, 8]\`。请写一段代码，统计数字 8 一共出现了多少次（不能直接用 \`nums.count(8)\`，要自己用循环数）。`,
        hint: `准备一个计数器从 0 开始，遍历列表，每遇到一个 8 就让计数器加 1。`,
        answer: `**详细讲解**：\n\n**思路**：这是典型的"计数"问题。准备一个计数器变量 \`count\`，初始为 0；用 \`for\` 循环把每个元素取出来和 8 比较，相等就 \`count += 1\`。循环走完，\`count\` 就是 8 的个数。\n\n**参考代码**：\n\`\`\`nums = [3, 8, 1, 8, 5, 8]\ncount = 0                 # 计数器清零\nfor x in nums:            # 逐个取出元素\n    if x == 8:            # 是 8 就计一次\n        count += 1\nprint(count)              # 输出 3\`\`\`\n\n**走一遍**：x 依次是 3（不是 8）、8（count=1）、1、8（count=2）、5、8（count=3）。最后 \`count = 3\`。\n\n**复杂度**：要把列表从头看到尾一遍，是 $O(n)$（$n$ 是列表长度）。这种"遍历一遍 + 满足条件就计数/累加"的模式，是列表统计类问题的通用套路，求和、求平均、找最大都是它的变形。`
      },
      {
        problem: `\`scores = [55, 88, 42, 90, 67, 30]\`。请把所有**及格（≥60）**的成绩挑出来放进一个新列表 \`passed\`，并算出**及格人数**。要求不改动原列表 \`scores\`。`,
        hint: `新建一个空列表，遍历时用 \`if x >= 60\` 判断，满足就 \`append\` 进去。及格人数就是新列表的长度。`,
        answer: `**详细讲解**：\n\n**思路**：这是"筛选 + 计数"的组合。先建一个空列表 \`passed = []\` 用来装及格分；遍历 \`scores\`，凡是 \`>= 60\` 的就 \`append\` 进 \`passed\`。因为只是把元素**读出来**放进新列表、没有动 \`scores\`，所以原列表保持不变。最后及格人数就等于 \`len(passed)\`，不用再单独数。\n\n**参考代码**：\n\`\`\`scores = [55, 88, 42, 90, 67, 30]\npassed = []                  # 装及格成绩的新列表\nfor x in scores:\n    if x >= 60:              # 及格线\n        passed.append(x)\nprint(passed)                # [88, 90, 67]\nprint("及格人数:", len(passed))  # 及格人数: 3\`\`\`\n\n**走一遍**：55 不及格跳过；88 及格放入；42 跳过；90 放入；67 放入；30 跳过。\`passed = [88, 90, 67]\`，长度 3。\n\n**复杂度**：遍历一遍 $O(n)$，\`len()\` 是 $O(1)$，总体 $O(n)$。\n\n**易错提醒**：及格线是"≥60"用 \`>=\`，不能写成 \`>\`，否则刚好 60 分的会被漏掉。另外全程没碰 \`scores\`，所以它仍是原来的六个数。`
      },
      {
        problem: `把列表 \`a = [1, 2, 3, 4, 5]\` **反转**，得到 \`[5, 4, 3, 2, 1]\`。请用循环自己实现（不能用 \`a.reverse()\` 或 \`a[::-1]\`），并说明你的方法的复杂度。`,
        hint: `一种思路：新建空列表，从原列表**最后一个**往前取，依次 \`append\`。负下标 \`a[-1]\` 是最后一个，或者用 \`range\` 倒着生成下标。`,
        answer: `**详细讲解**：\n\n**思路**：最直观的办法是"倒着读、正着写"——新建一个空列表 \`result\`，从原列表的最后一个元素开始往前取，每取一个就 \`append\` 到 \`result\`。这样最先放进去的是原来的末尾元素，自然就反过来了。\n\n**参考代码（写法一：倒序下标遍历）**：\n\`\`\`a = [1, 2, 3, 4, 5]\nresult = []\n# range(起点, 终点, 步长)；从最后一个下标 len(a)-1 走到 0\nfor i in range(len(a) - 1, -1, -1):\n    result.append(a[i])\nprint(result)   # [5, 4, 3, 2, 1]\`\`\`\n这里 \`range(4, -1, -1)\` 生成 4、3、2、1、0，对应从末尾到开头的下标。注意终点写 \`-1\` 才能取到下标 0（含头不含尾，到 −1 之前停，正好停在 0）。\n\n**参考代码（写法二：原地双指针交换，不开新列表）**：\n\`\`\`a = [1, 2, 3, 4, 5]\nleft = 0\nright = len(a) - 1\nwhile left < right:\n    a[left], a[right] = a[right], a[left]  # 头尾对调\n    left += 1\n    right -= 1\nprint(a)   # [5, 4, 3, 2, 1]\`\`\`\n一头一尾两个指针向中间靠拢，每次交换一对，相遇就停。\n\n**复杂度**：写法一遍历一遍并逐个 append，是时间 $O(n)$、额外空间 $O(n)$（开了新列表）；写法二只在原列表上对调，时间 $O(n)$、额外空间 $O(1)$（不开新列表，更省内存）。两种都对，双指针法是更"聪明"的解。`
      },
    ],
  },

  // ═══════════════════════════════════════════════════════════════
  //   字典 dict
  // ═══════════════════════════════════════════════════════════════
  'dict': {
    warmup: {
      q: `你想存一个同学的多项信息：名字"小明"、年龄 14、成绩 92。如果用列表 \`["小明", 14, 92]\`，过几天再看，你还记得"下标 1 是年龄、下标 2 是成绩"吗？要是再加个"身高"，下标又乱了。有没有更清楚的存法？`,
      hint: `想想现实里的"通讯录"或"字典书"——你是按"位置"找电话的，还是按"名字"找？如果能直接用"年龄"这个词去取出 14，会不会清楚得多？`,
      answer: `用列表存这种"一个东西的多个属性"，全靠记下标，又乱又容易错。更好的办法是用**字典**：\`student = {"name": "小明", "age": 14, "score": 92}\`。它存的是一对对"**键-值**"——用有意义的**键**（如 \`"age"\`）直接取出对应的**值**（14），写成 \`student["age"]\`。这就像查字典书：给一个词（键），翻到它的解释（值）。而且字典查找几乎是瞬间的（平均 $O(1)$），添加新属性也只要 \`student["height"] = 165\`，再也不用数下标。`
    },
    intro: [
      `**字典（dict）**存的是一对对"**键值对**"：每个**键（key）**对应一个**值（value）**。写法是用花括号、键和值之间加冒号、各对之间加逗号：\`student = {"name": "小明", "age": 14}\`。它和列表最大的不同是——列表靠**位置（下标）**找元素，字典靠**键（名字）**找元素。`,
      `取值、改值、加值都用方括号加键名。**取**：\`student["age"]\` 得到 14。**改**：\`student["age"] = 15\` 把年龄改成 15。**加**：\`student["score"] = 92\`——如果键不存在就**新增**这一对，如果键已存在就**覆盖**原值。所以"改"和"加"用的是同一种写法，由"键在不在"决定行为。`,
      `字典最大的优势是**快**。不管字典里有 10 对还是 100 万对，用键去查、改、加，平均都只要 $O(1)$（一步到位），远快于在列表里一个个找（$O(n)$）。这是因为字典底层用了"哈希表"——把键算成一个存储位置直接定位，不用逐个比较。所以**需要"用名字快速查东西"的场景，几乎都该用字典**。`,
      `字典有两条硬规则。**第一，键必须唯一**：同一个键只能对应一个值，重复赋值会覆盖。**第二，键必须是"不可变"的类型**：字符串、数字、元组可以当键，但**列表不能当键**（因为列表可变）。值则没有限制，什么都行。键唯一这一点决定了字典天然适合做"一对一的对应表"。`,
      `常用操作还有：\`key in d\` 判断某个键在不在（平均 $O(1)$）；\`for k in d:\` 遍历所有键；\`for k, v in d.items():\` 同时拿键和值；\`d.get("age", 0)\` 安全地取值——键不存在时返回默认值 0 而不报错。一句话：**字典 = 键值对 + 键唯一不可变 + 按键极快查找**，统计、分组、查表都靠它。`
    ],
    formulas: [
      { name: '字典的取值与赋值', latex: 'd[\\text{key}]\\ \\to\\ \\text{value}',
        note: `\`d["age"]\` 按键取值；\`d["age"] = 15\` 按键赋值（键不存在则新增，存在则覆盖）。键不存在时**直接取**会报 KeyError。` },
      { name: '判断键是否存在', latex: '\\texttt{key in d}\\ \\to\\ \\text{True/False}',
        note: `用 \`in\` 检查键在不在，平均 $O(1)$。注意 \`in\` 查的是**键**不是值。安全取值用 \`d.get(key, 默认值)\`，键不存在时返回默认值而不报错。` },
      { name: '核心操作的时间复杂度', latex: '\\text{查/改/增/删（按键）}=O(1)\\ \\text{（平均）}',
        note: `按键查找、修改、添加、删除平均都是 $O(1)$，这是字典最大的卖点。代价是字典在 Python 3.7 前不保证顺序（3.7+ 按插入顺序，但仍不能用下标取）。` },
      { name: '遍历字典', latex: '\\texttt{for k, v in d.items()}',
        note: `\`for k in d:\` 只遍历键；\`for v in d.values():\` 只遍历值；\`for k, v in d.items():\` 同时拿到键和值，最常用。` },
    ],
    methods: [
      {
        name: '字典常用操作（建、查、改、增、删、遍历）',
        when: `需要按"名字/编号"去存取数据，或维护一张"对应关系表"时`,
        steps: [
          `**建**：\`d = {"name": "小明", "age": 14}\`，或先 \`d = {}\` 建空字典再逐个加`,
          `**查**：\`d["age"]\` 取值；不确定键在不在时用 \`d.get("age", 0)\` 防报错`,
          `**改 / 增**：\`d["age"] = 15\`——键在就改、键不在就新增`,
          `**删**：\`del d["age"]\` 删掉这一对；或 \`d.pop("age")\` 删并返回它的值`,
          `**遍历**：\`for k, v in d.items():\` 同时取键和值做处理`,
        ],
        example: {
          problem: `建一个学生字典存名字"小红"、年龄 13。然后把年龄改成 14，新增一项成绩 88，最后遍历输出每一项"键：值"。`,
          solution: [
            `解：先建字典：\`stu = {"name": "小红", "age": 13}\`。`,
            `改年龄：\`stu["age"] = 14\`（键 age 已存在，覆盖）。`,
            `加成绩：\`stu["score"] = 88\`（键 score 不存在，新增）。现在 \`stu = {"name": "小红", "age": 14, "score": 88}\`。`,
            `遍历输出：\`\`\`for k, v in stu.items():\n    print(k, ":", v)\`\`\``,
            `答：依次输出 \`name : 小红\`、\`age : 14\`、\`score : 88\`。`,
          ],
        },
      },
      {
        name: '用字典做"计数统计"（出现次数）',
        when: `要统计每个东西出现了多少次时——比如词频、投票、字符计数，这是字典最经典的用途`,
        steps: [
          `建一个空字典 \`count = {}\`，用来记"每个东西 → 它的次数"`,
          `遍历数据，对每个元素 \`x\` 检查它在不在字典里`,
          `如果 \`x\` 已经是键，就把它的值加 1；如果还不是键，就设为 1（第一次出现）`,
          `用 \`count.get(x, 0) + 1\` 可以把上面两种情况合并成一行，更简洁`,
        ],
        example: {
          problem: `统计列表 \`votes = ["A", "B", "A", "C", "A", "B"]\` 里每个选项的票数。`,
          solution: [
            `解：用字典记票。代码：`,
            `\`\`\`count = {}\nfor x in votes:\n    count[x] = count.get(x, 0) + 1\n    # get(x, 0)：x 还没出现时当作 0，再加 1\`\`\``,
            `走一遍：A→{A:1}，B→{A:1,B:1}，A→{A:2,...}，C→{...,C:1}，A→{A:3,...}，B→{B:2,...}。`,
            `答：\`count = {"A": 3, "B": 2, "C": 1}\`，即 A 得 3 票、B 得 2 票、C 得 1 票。`,
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 用字典统计字符串里每个字母出现的次数',
        problem: `给定字符串 \`s = "banana"\`，请用字典统计每个字母出现了多少次，输出形如 \`{'b': 1, 'a': 3, 'n': 2}\` 的结果。`,
        steps: [
          `第一步：审题。要的是"字母 → 次数"的对应关系，这正是字典的拿手好戏。键是字母，值是出现次数。`,
          `第二步：建空字典。\`count = {}\`，一开始什么都没统计。`,
          `第三步：遍历字符串。字符串可以直接用 \`for\` 遍历，每次取出一个字符 \`ch\`：\`\`\`for ch in s:\n    if ch in count:        # 这个字母之前出现过\n        count[ch] = count[ch] + 1   # 次数加 1\n    else:                  # 第一次见到这个字母\n        count[ch] = 1      # 记为 1\`\`\``,
          `第四步：走一遍过程。b→{b:1}；a→{b:1,a:1}；n→{b:1,a:1,n:1}；a→a 变 2；n→n 变 2；a→a 变 3。`,
          `第五步：作答。\`count = {'b': 1, 'a': 3, 'n': 2}\`。补充：第三步那段 if-else 可以用一行 \`count[ch] = count.get(ch, 0) + 1\` 代替，效果完全一样——\`get\` 在键不存在时返回 0，省掉了判断。`,
        ],
      },
    ],
    pitfalls: [
      `**键唯一：重复的键会被覆盖，不会报错。** 写 \`{"a": 1, "a": 2}\` 最终只剩 \`{"a": 2}\`，前一个悄悄被盖掉。所以字典里不可能有两个相同的键。`,
      `**键必须是不可变类型，列表不能当键。** \`d[[1,2]] = 3\` 会报 TypeError（unhashable type: 'list'）。能当键的有字符串、数字、元组；想用"一组值"当键，把列表换成元组 \`d[(1,2)] = 3\`。`,
      `**取不存在的键会报 KeyError。** 直接写 \`d["不存在的键"]\` 程序会崩。不确定键在不在时，先用 \`if key in d:\` 判断，或用 \`d.get(key, 默认值)\` 安全取值。`,
      `**\`in\` 查的是键，不是值。** \`"age" in d\` 检查的是有没有"age"这个**键**，不是有没有某个值。想查值要用 \`值 in d.values()\`。`,
      `**字典不能用下标取值。** 字典靠键取值，没有"第 0 个"的概念，写 \`d[0]\` 是在找键为 \`0\` 的项，不是"第一项"。需要顺序就遍历 \`d.items()\`。`,
    ],
    exercises: [
      {
        problem: `有字典 \`prices = {"苹果": 5, "香蕉": 3}\`。小明执行了 \`prices["苹果"] = 6\`，又执行了 \`prices["橙子"] = 4\`。请说明这两行各自做了什么、为什么不同，并写出最终的字典。`,
        hint: `两行写法一模一样，差别只在于"这个键原来在不在字典里"。在的会怎样？不在的会怎样？`,
        answer: `**详细讲解**：\n\n**思路**：字典里 \`d[键] = 值\` 这种写法有"两副面孔"，到底是"修改"还是"新增"，**取决于这个键原来在不在字典里**：键已存在 → 覆盖旧值（修改）；键不存在 → 添加新的一对（新增）。\n\n**逐行分析**：\n- \`prices["苹果"] = 6\`：键 \`"苹果"\` 原来就在字典里（值是 5），所以这是**修改**，把 5 覆盖成 6。\n- \`prices["橙子"] = 4\`：键 \`"橙子"\` 原来不在字典里，所以这是**新增**，添上一对 \`"橙子": 4\`。\n\n**最终字典**：\`{"苹果": 6, "香蕉": 3, "橙子": 4}\`。\n\n**复杂度**：按键修改和按键新增平均都是 $O(1)$——字典通过键直接定位到存储位置，不用像列表那样从头找。这正是字典"快"的体现。\n\n**小结**：记住"同一种写法，键在就改、键不在就加"，是用好字典的关键。如果你想"只在键不存在时才添加、已存在就别动"，可以先用 \`if 键 not in d:\` 判断，或用 \`d.setdefault(键, 值)\`。`
      },
      {
        problem: `给定一句话拆好的单词列表 \`words = ["cat", "dog", "cat", "bird", "dog", "cat"]\`，请用字典统计每个单词出现的次数，输出这个统计字典。`,
        hint: `经典的字典计数题：空字典起步，遍历列表，用 \`get(单词, 0) + 1\` 累加。`,
        answer: `**详细讲解**：\n\n**思路**：这是字典最经典的应用——**计数统计**。用字典记"单词 → 出现次数"。遍历列表时，对每个单词：如果它已经是字典的键，就让对应次数加 1；如果还不是键，就设为 1。用 \`d.get(单词, 0) + 1\` 可以把这两种情况合并：键不存在时 \`get\` 返回默认值 0，加 1 后正好是第一次出现的 1。\n\n**参考代码**：\n\`\`\`words = ["cat", "dog", "cat", "bird", "dog", "cat"]\ncount = {}                      # 空字典起步\nfor w in words:\n    count[w] = count.get(w, 0) + 1   # 没出现过当 0，出现过取原值，统一加 1\nprint(count)\`\`\`\n\n**走一遍**：cat→{cat:1}；dog→{cat:1,dog:1}；cat→cat 变 2；bird→{...,bird:1}；dog→dog 变 2；cat→cat 变 3。\n\n**输出**：\`{"cat": 3, "dog": 2, "bird": 1}\`。\n\n**复杂度**：遍历列表一遍是 $O(n)$，每次字典的查找和赋值都是 $O(1)$，所以总体 $O(n)$。如果改用列表来数（每个单词都从头扫一遍看出现过没有），会变成 $O(n^2)$，慢得多。这就是为什么"统计次数"几乎总是用字典。`
      },
      {
        problem: `有学生成绩字典 \`scores = {"小明": 85, "小红": 92, "小刚": 78}\`。请写代码完成两件事：(1) 安全地查询"小华"的成绩——如果没有这个人，输出"查无此人"而不要让程序崩溃；(2) 找出成绩最高的同学的名字。`,
        hint: `第 (1) 问：直接 \`scores["小华"]\` 会报 KeyError，改用 \`get\` 或先用 \`in\` 判断。第 (2) 问：遍历 \`items()\`，边比边记当前最高分和对应名字。`,
        answer: `**详细讲解**：\n\n**第 (1) 问 —— 安全查询**：\n直接写 \`scores["小华"]\` 在键不存在时会抛 KeyError 让程序崩溃。两种安全写法：\n\`\`\`# 写法一：用 in 判断\nif "小华" in scores:\n    print(scores["小华"])\nelse:\n    print("查无此人")\n\n# 写法二：用 get 给默认值（更简洁）\nname = "小华"\nif scores.get(name) is None:\n    print("查无此人")\nelse:\n    print(scores.get(name))\`\`\`\n核心是 \`get\` 在键不存在时返回 \`None\`（或你指定的默认值）而**不报错**。\n\n**第 (2) 问 —— 找最高分的人**：\n遍历字典的"键值对"，用两个变量记住目前的"最高分"和"对应名字"，发现更高的就更新。\n\`\`\`best_name = None\nbest_score = -1            # 设成一个比所有分都小的起点\nfor name, score in scores.items():\n    if score > best_score:\n        best_score = score # 更新最高分\n        best_name = name   # 同时记住是谁\nprint(best_name)           # 小红\`\`\`\n走一遍：小明 85>−1，记小明；小红 92>85，记小红；小刚 78 不大于 92，不变。最后 \`best_name = "小红"\`。\n\n**复杂度**：第 (1) 问的 \`in\` / \`get\` 都是 $O(1)$；第 (2) 问遍历所有键值对是 $O(n)$。\n\n**小结**：这题把字典的两大基本功都用上了——"按键安全取值"（防 KeyError）和"遍历 items() 做统计"。记住找极值的套路：先设一个不可能的起点，再边遍历边更新。`
      },
    ],
  },

  // ═══════════════════════════════════════════════════════════════
  //   集合 set-type
  // ═══════════════════════════════════════════════════════════════
  'set-type': {
    warmup: {
      q: `一次活动有人重复报了名，名单是 \`["小明", "小红", "小明", "小刚", "小红"]\`。你想知道"到底有多少个**不同**的人"。用列表一个个比对去重，写起来很啰嗦。有没有一种"自动不存重复"的容器？`,
      hint: `想想数学课上的"集合"——同一个元素在集合里只算一个，没有"重复"这回事。如果 Python 里有这种东西，把名单往里一放，是不是重复的就自动没了？`,
      answer: `用列表手动去重要写双层循环，又慢又麻烦。Python 正好有对应数学"集合"的类型——**set（集合）**：它**自动去重**，同一个元素只保留一个。把名单丢进集合 \`set(names)\`，重复的"小明""小红"立刻合并，再用 \`len()\` 一数就知道有几个不同的人。集合还能极快地判断"某人在不在名单里"（平均 $O(1)$），并且能像数学那样求两个名单的**交集、并集、差集**。代价是——集合**没有顺序、不能用下标**。`
    },
    intro: [
      `**集合（set）**是一种"**无序、不重复**"的元素容器，对应数学里的集合概念。写法是用花括号列元素：\`s = {1, 2, 3}\`。它有两个鲜明特点：**自动去重**（重复的元素只保留一个）和**无序**（元素没有固定顺序，也就没有下标）。`,
      `集合最常见的用途是**去重**。把一个有重复的列表丢进 \`set(...)\`，重复元素立刻被合并：\`set([1, 2, 2, 3, 3, 3])\` 得到 \`{1, 2, 3}\`。要拿回列表，再 \`list(...)\` 转回去即可。一行 \`list(set(lst))\` 就完成"列表去重"，是非常实用的小技巧（但去重后顺序可能变）。`,
      `集合的第二大优势是**判断"在不在"极快**。\`x in s\` 平均只要 $O(1)$，而在列表里 \`x in lst\` 要从头找、最坏 $O(n)$。所以当你需要反复检查"这个东西出现过没有""它属不属于某一批"时，把那批东西放进集合，查起来飞快。`,
      `集合天生适合做**数学集合运算**，三个最常用：**交集** \`a & b\`（两边都有的）、**并集** \`a | b\`（两边合起来、自动去重）、**差集** \`a - b\`（在 a 里但不在 b 里的）。比如求"两个班共同选了的科目""A 班选了而 B 班没选的"，用集合一个符号就解决。`,
      `集合的限制要记牢：**无序所以不能用下标**（写 \`s[0]\` 会报错），也**不能切片**；元素必须是**不可变**的（数字、字符串、元组能进集合，列表不能）。增删用 \`s.add(x)\` 和 \`s.remove(x)\`。一句话：**集合 = 自动去重 + 极快判断在不在 + 数学集合运算，但没有顺序**。`
    ],
    formulas: [
      { name: '创建集合 / 列表去重', latex: '\\texttt{set([1,2,2,3])}\\ \\to\\ \\{1,2,3\\}',
        note: `\`{1, 2, 3}\` 直接写；\`set(列表)\` 把列表去重转成集合。**注意：空集合要写 \`set()\`，不能写 \`{}\`**——\`{}\` 是空字典！\`list(set(lst))\` 实现列表去重。` },
      { name: '判断元素是否在集合中', latex: '\\texttt{x in s}\\ \\to\\ O(1)\\ \\text{（平均）}',
        note: `集合判断"在不在"平均 $O(1)$，远快于列表的 $O(n)$。这是把数据放进集合的主要理由之一。` },
      { name: '集合运算（交、并、差）', latex: 'a\\&b,\\quad a\\,|\\,b,\\quad a-b',
        note: `\`a & b\` 交集（都有的）；\`a | b\` 并集（合起来去重）；\`a - b\` 差集（在 a 不在 b）。对应数学的 $\\cap$、$\\cup$、$\\setminus$。` },
      { name: '增删与限制', latex: '\\texttt{s.add(x)},\\ \\texttt{s.remove(x)};\\ \\text{无下标}',
        note: `\`add\` 加一个、\`remove\` 删一个。集合**无序**：不能 \`s[0]\` 取值、不能切片、不能排序（要先 \`sorted(s)\` 转成列表）。元素须不可变。` },
    ],
    methods: [
      {
        name: '用集合去重与判重',
        when: `要给一串数据去掉重复，或反复检查"某个东西出现过没有"时`,
        steps: [
          `**去重**：把列表丢进 \`set()\`，重复元素自动合并；需要列表就再 \`list()\` 转回`,
          `**判重 / 查在不在**：把要反复查的那批数据先放进集合，再用 \`x in s\` 极快判断`,
          `**统计不同元素的个数**：\`len(set(lst))\` 一步得到"有多少个不同的值"`,
          `注意：去重后顺序可能改变；若必须保持原顺序，要另想办法（如边遍历边用集合记录已见过的）`,
        ],
        example: {
          problem: `名单 \`names = ["小明", "小红", "小明", "小刚", "小红"]\`。求有多少个不同的人，并得到一份去重后的名单。`,
          solution: [
            `解：用集合自动去重。\`unique = set(names)\` → \`{"小明", "小红", "小刚"}\`（重复的合并了）。`,
            `数不同的人：\`len(unique)\` → \`3\`。`,
            `要列表形式的去重名单：\`name_list = list(unique)\`，得到 3 个名字的列表（顺序可能和原来不同）。`,
            `答：共 \`3\` 个不同的人；去重名单 \`["小明", "小红", "小刚"]\`（顺序不定）。`,
          ],
        },
      },
      {
        name: '用集合运算求交集 / 并集 / 差集',
        when: `要比较两批数据——找"共同有的""合起来的""一边独有的"时`,
        steps: [
          `先把两批数据各自变成集合：\`a = set(列表1)\`，\`b = set(列表2)\``,
          `**共同有的**（交集）：\`a & b\``,
          `**合起来去重**（并集）：\`a | b\``,
          `**a 有 b 没有的**（差集）：\`a - b\`；反过来 b 独有的是 \`b - a\``,
        ],
        example: {
          problem: `甲班选修 \`{"音乐", "美术", "编程"}\`，乙班选修 \`{"编程", "体育", "音乐"}\`。求两班都选的、合起来一共选的、甲班选了而乙班没选的。`,
          solution: [
            `解：直接用集合运算。设 \`a = {"音乐", "美术", "编程"}\`，\`b = {"编程", "体育", "音乐"}\`。`,
            `两班都选（交集）：\`a & b\` → \`{"音乐", "编程"}\`。`,
            `合起来一共（并集）：\`a | b\` → \`{"音乐", "美术", "编程", "体育"}\`（共 4 门，重复的只算一次）。`,
            `答：甲选乙没选（差集）\`a - b\` → \`{"美术"}\`。`,
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 找出两个列表的共同元素并去重',
        problem: `列表 \`a = [1, 2, 2, 3, 4]\`，\`b = [2, 3, 3, 5]\`。请找出同时出现在两个列表里的数字（不重复），结果输出为一个列表。`,
        steps: [
          `第一步：审题。要的是"两边都有的、且不重复"——这正是**交集**。先想到把列表转成集合。`,
          `第二步：转集合（顺便去重）。\`set_a = set(a)\` → \`{1, 2, 3, 4}\`；\`set_b = set(b)\` → \`{2, 3, 5}\`。转的过程已经把各自的重复去掉了。`,
          `第三步：求交集。\`common = set_a & set_b\`——\`&\` 取两边都有的元素。比一比：1 只在 a，4 只在 a，5 只在 b，而 2 和 3 两边都有。所以 \`common = {2, 3}\`。`,
          `第四步：转回列表。题目要列表，所以 \`result = list(common)\` → \`[2, 3]\`（顺序可能不同）。`,
          `第五步：作答。共同元素是 \`[2, 3]\`。一行写法：\`list(set(a) & set(b))\`。对比：如果用双层循环硬找共同元素再去重，要写十几行且复杂度 $O(n \\times m)$；用集合一行搞定，且交集运算很快。`,
        ],
      },
    ],
    pitfalls: [
      `**空集合要写 \`set()\`，不能写 \`{}\`！** \`{}\` 创建的是空**字典**不是空集合。这是最隐蔽的坑：\`s = {}\` 后 \`s.add(1)\` 会报错，因为字典没有 add 方法。`,
      `**集合无序，不能用下标、不能切片。** 写 \`s[0]\`、\`s[1:3]\` 都会报错。集合里的元素没有"第几个"的概念。要按顺序处理，先 \`sorted(s)\` 或 \`list(s)\` 转成列表。`,
      `**集合会自动去重，重复元素"消失"了。** \`{1, 1, 2}\` 实际只是 \`{1, 2}\`。如果你需要保留重复（比如统计每个出现几次），该用列表或字典，别用集合。`,
      `**集合的元素必须不可变，列表不能放进集合。** \`{[1, 2], 3}\` 会报 TypeError。想放"一组值"就用元组：\`{(1, 2), 3}\` 可以。`,
      `**去重后顺序可能变。** \`list(set([3, 1, 2]))\` 不保证还是 3、1、2 的顺序。如果必须保持原顺序去重，要边遍历边用一个集合记录"已见过的"，而不能直接 \`list(set(...))\`。`,
    ],
    exercises: [
      {
        problem: `给定 \`nums = [4, 7, 4, 1, 7, 9, 1, 4]\`。请用集合求出：(1) 一共有多少个**不同**的数字；(2) 去重后的数字列表。再说说为什么这里用集合比用列表手动去重好。`,
        hint: `\`set(nums)\` 自动去重，\`len()\` 数个数，\`list()\` 转回列表。想想手动去重要写几层循环、复杂度是多少。`,
        answer: `**详细讲解**：\n\n**思路**：集合的看家本领就是"自动去重"。把列表丢进 \`set()\`，重复的数字会自动合并成一个，于是：不同数字的个数 = 这个集合的长度；去重列表 = 把集合再转回列表。\n\n**参考代码**：\n\`\`\`nums = [4, 7, 4, 1, 7, 9, 1, 4]\nunique = set(nums)        # 自动去重 -> {1, 4, 7, 9}\nprint(len(unique))        # (1) 不同数字个数: 4\nprint(list(unique))       # (2) 去重列表: [1, 4, 7, 9]（顺序可能不同）\`\`\`\n\`set(nums)\` 把 4、7、1、9 这些不同值各留一个，三个重复的 4、两个重复的 7、两个重复的 1 都合并了。\n\n**结果**：(1) 共 4 个不同数字；(2) \`[1, 4, 7, 9]\`。\n\n**为什么集合比手动去重好**：\n- **代码简洁**：集合去重一行搞定，手动去重要么开新列表+遍历判断（"没见过才加"），要么双层循环两两比对。\n- **速度更快**：集合判断"在不在"平均 $O(1)$，整体去重约 $O(n)$；而用列表手动去重，每加一个都要 \`x in 新列表\` 检查（$O(n)$），整体退化成 $O(n^2)$。数据一多，差距非常明显。\n\n**提醒**：集合去重后顺序可能变，如果你需要保持原来的先后顺序，就得边遍历边用集合记录已见过的元素（见易错点）。`
      },
      {
        problem: `甲、乙两位同学各自喜欢的运动：\`jia = {"篮球", "游泳", "跑步"}\`，\`yi = {"跑步", "羽毛球", "篮球"}\`。请用集合运算求出：(1) 两人都喜欢的；(2) 两人喜欢的所有运动（合起来）；(3) 只有甲喜欢、乙不喜欢的。`,
        hint: `三个符号：交集 \`&\`、并集 \`|\`、差集 \`-\`。差集有方向，"甲有乙没有"是 \`jia - yi\`。`,
        answer: `**详细讲解**：\n\n**思路**：这是集合运算的直接应用，三个问题对应三个运算符：\n- "都喜欢的" → **交集** \`&\`（两边都有的元素）\n- "合起来所有的" → **并集** \`|\`（两边元素合并，自动去重）\n- "只有甲、乙没有的" → **差集** \`-\`（在甲里、但不在乙里）\n\n**参考代码**：\n\`\`\`jia = {"篮球", "游泳", "跑步"}\nyi = {"跑步", "羽毛球", "篮球"}\n\nprint(jia & yi)   # (1) 都喜欢: {"篮球", "跑步"}\nprint(jia | yi)   # (2) 所有: {"篮球", "游泳", "跑步", "羽毛球"}\nprint(jia - yi)   # (3) 只有甲: {"游泳"}\`\`\`\n\n**逐个分析**：\n- (1) \`jia & yi\`：篮球两边都有、跑步两边都有，游泳只在甲、羽毛球只在乙 → \`{"篮球", "跑步"}\`。\n- (2) \`jia | yi\`：把所有运动并起来，篮球和跑步虽两边都有但只算一次 → 4 项。\n- (3) \`jia - yi\`：从甲里去掉"乙也有的"（篮球、跑步），剩下 → \`{"游泳"}\`。\n\n**复杂度**：交、并、差运算的复杂度大致是 $O(n)$（与集合元素个数成正比），都很高效。\n\n**易错提醒**：差集**有方向**！\`jia - yi\`（甲独有）和 \`yi - jia\`（乙独有=\`{"羽毛球"}\`）结果不同，别搞反。而交集、并集没有方向，\`jia & yi\` 和 \`yi & jia\` 一样。`
      },
      {
        problem: `小明想做一个"判断输入的字符是不是元音字母（a、e、i、o、u）"的功能。他先写了 \`vowels = ["a", "e", "i", "o", "u"]\`，用 \`if ch in vowels\` 判断。同学说"用集合更好"。同学说得对吗？为什么？请写出用集合的版本。`,
        hint: `重点不在功能对不对（列表也能跑），而在"反复判断在不在"时，集合和列表的查找速度差别。`,
        answer: `**详细讲解**：\n\n**同学说得对**，用集合更好——不是因为列表写法错（它能正常工作），而是因为**集合判断"在不在"更快、语义也更贴切**。\n\n**为什么集合更好**：\n- **速度**：\`ch in 列表\` 是从头一个个比对，最坏要看完整个列表，复杂度 $O(n)$；\`ch in 集合\` 用哈希直接定位，平均 $O(1)$。元音只有 5 个时差别不大，但如果是判断"在不在一个上万词的词表里"，反复查时集合会快得多。这种"建一次、反复查"的场景，正是集合的主场。\n- **语义**：元音字母本来就是"一组不重复、不讲顺序"的字符，用集合表达比用列表更准确。\n\n**参考代码**：\n\`\`\`vowels = {"a", "e", "i", "o", "u"}   # 用集合\nch = input("输入一个字母：").lower()  # 转小写，兼容大写输入\nif ch in vowels:\n    print(ch, "是元音")\nelse:\n    print(ch, "不是元音")\`\`\`\n注意加了 \`.lower()\` 把输入转成小写，这样输入大写 "A" 也能正确判断。\n\n**复杂度对比**：判断一次，列表 $O(n)$、集合 $O(1)$（平均）。\n\n**小结**：凡是"建好一批数据后，要反复检查某个东西在不在其中"，优先用集合。这是集合除了去重之外，最值得记住的用途。`
      },
    ],
  },

  // ═══════════════════════════════════════════════════════════════
  //   栈与队列 stack-queue
  // ═══════════════════════════════════════════════════════════════
  'stack-queue': {
    warmup: {
      q: `两个生活场景：① 食堂叠在一起的一摞盘子，你拿盘子和放盘子都只能从**最上面**来；② 排队买奶茶，**先来的先买**、后到的排后面。这两种"取东西的规矩"明显不一样。你能用自己的话说出它们各自的规则吗？`,
      hint: `盘子：最后放上去的那个，反而最先被拿走。排队：最先来的那个，最先被服务。一个是"后进先出"，一个是"先进先出"。`,
      answer: `① 一摞盘子：**只能从顶端拿和放**，最后放上去的最先被取走——这叫**后进先出（LIFO，Last In First Out）**，对应数据结构里的**栈（stack）**。② 排队买奶茶：**先来的先服务**，最早排的最先离开——这叫**先进先出（FIFO，First In First Out）**，对应**队列（queue）**。栈和队列不是新的容器，而是两种"取数据的规矩"，用列表就能模拟。栈常用于"撤销""括号匹配""函数调用"；队列常用于"排队任务""广度优先搜索"。`
    },
    intro: [
      `**栈（stack）和队列（queue）**是两种最基础的"受限的"数据结构——它们本身可以用列表存数据，但规定了**只能从特定的一端存取**，从而形成两种不同的"取数据顺序"。理解它们，关键是抓住各自的"出入规则"。`,
      `**栈：后进先出（LIFO）。** 想象一摞盘子，只能从**顶端**放（叫"入栈 push"）和取（叫"出栈 pop"）。最后放进去的，最先被取出来。用列表模拟：入栈 \`stack.append(x)\`（加到末尾），出栈 \`stack.pop()\`（取走末尾那个）。**两个操作都在列表末尾进行，都是 $O(1)$，很快。**`,
      `**队列：先进先出（FIFO）。** 想象排队买票，从**队尾**加入（"入队"），从**队头**离开（"出队"），先来的先走。用列表模拟：入队 \`queue.append(x)\`（加到末尾=队尾），出队 \`queue.pop(0)\`（取走第一个=队头）。注意 \`pop(0)\` 要把后面所有元素往前挪，是 $O(n)$；真正项目里会用 \`collections.deque\` 让两端都 $O(1)$，但初学先用列表理解原理就够了。`,
      `**为什么需要它们？** 因为很多问题天然就是这两种顺序。**栈**适合"最近的最先处理"：浏览器/编辑器的**撤销**（最后做的操作最先撤销）、检查**括号是否匹配**、计算器解析表达式、以及函数调用（先调用的后返回）。**队列**适合"按到达顺序公平处理"：打印任务排队、消息处理、以及图的**广度优先搜索（BFS）**。`,
      `一句话对照记忆：**栈像一摞盘子（顶端进出，后进先出）；队列像排队买票（一头进另一头出，先进先出）**。它们最大的价值是用"出入规则"把复杂问题简化——很多看起来很难的问题，一旦想到"该用栈还是队列"，思路立刻就清楚了。`
    ],
    formulas: [
      { name: '栈：后进先出 LIFO', latex: '\\texttt{push}=\\texttt{append(x)},\\quad \\texttt{pop}=\\texttt{pop()}',
        note: `栈用列表末尾当"顶端"：入栈 \`stack.append(x)\`、出栈 \`stack.pop()\`（不带参数，取最后一个）。两者都是 $O(1)$。后进的先出。` },
      { name: '队列：先进先出 FIFO', latex: '\\texttt{enqueue}=\\texttt{append(x)},\\quad \\texttt{dequeue}=\\texttt{pop(0)}',
        note: `队列用列表末尾当队尾、开头当队头：入队 \`queue.append(x)\`、出队 \`queue.pop(0)\`（取第一个）。先进的先出。` },
      { name: '操作复杂度对比', latex: '\\text{栈入出}=O(1),\\quad \\text{队出 pop(0)}=O(n)',
        note: `栈两个操作都在末尾，都是 $O(1)$。队列的 \`pop(0)\` 要把后面元素整体前移，是 $O(n)$；用 \`collections.deque\` 的 \`popleft()\` 可优化到 $O(1)$。` },
      { name: '查看栈顶 / 判空', latex: '\\texttt{stack[-1]},\\quad \\texttt{len(stack)==0}',
        note: `\`stack[-1]\` 看栈顶元素但不取走。\`pop\` 前必须先判空——对空栈/空队列 \`pop\` 会报 IndexError。判空用 \`len(s) == 0\` 或直接 \`if not s:\`。` },
    ],
    methods: [
      {
        name: '用列表实现栈（后进先出）',
        when: `需要"最后放进去的最先拿出来"——撤销、括号匹配、表达式求值、反转顺序时`,
        steps: [
          `用一个普通列表当栈：\`stack = []\``,
          `**入栈**：\`stack.append(x)\`——加到列表末尾（栈顶）`,
          `**出栈**：先判空 \`if stack:\`，再 \`top = stack.pop()\`——取走并返回末尾元素`,
          `**看栈顶**（不取走）：\`stack[-1]\`；**判空**：\`if not stack:\``,
        ],
        example: {
          problem: `依次把 1、2、3 入栈，然后连续出栈两次，分别出来的是什么？栈里最后剩什么？`,
          solution: [
            `解：入栈用 append。\`stack.append(1)\` → \`[1]\`；\`append(2)\` → \`[1,2]\`；\`append(3)\` → \`[1,2,3]\`。此时栈顶是 3。`,
            `第一次 \`stack.pop()\`：取走末尾 3，返回 **3**，栈变 \`[1,2]\`。`,
            `第二次 \`stack.pop()\`：取走末尾 2，返回 **2**，栈变 \`[1]\`。`,
            `答：两次出栈依次得到 **3、2**（后进的先出）；栈里最后剩 \`[1]\`。`,
          ],
        },
      },
      {
        name: '用列表实现队列（先进先出）',
        when: `需要"先来的先处理"——任务排队、消息处理、广度优先搜索时`,
        steps: [
          `用一个普通列表当队列：\`queue = []\``,
          `**入队**：\`queue.append(x)\`——加到末尾（队尾）`,
          `**出队**：先判空 \`if queue:\`，再 \`first = queue.pop(0)\`——取走并返回开头元素（队头）`,
          `**看队头**（不取走）：\`queue[0]\`；性能敏感时改用 \`from collections import deque\`，出队用 \`popleft()\``,
        ],
        example: {
          problem: `依次把"小明""小红""小刚"入队（排队），然后出队两次，谁先离开？队里还剩谁？`,
          solution: [
            `解：入队用 append。队列依次变成 \`["小明"]\` → \`["小明","小红"]\` → \`["小明","小红","小刚"]\`。队头是先来的"小明"。`,
            `第一次 \`queue.pop(0)\`：取走开头"小明"，返回 **"小明"**，队列变 \`["小红","小刚"]\`。`,
            `第二次 \`queue.pop(0)\`：取走开头"小红"，返回 **"小红"**，队列变 \`["小刚"]\`。`,
            `答：先离开的是 **"小明"、"小红"**（先进的先出）；队里还剩"小刚"。`,
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 用栈检查括号是否匹配',
        problem: `给一串只含括号的字符串，如 \`"(())"\` 或 \`"(()"\`，判断括号是否正确匹配（每个左括号都有对应的右括号、且顺序正确）。请用栈实现，并说明思路。`,
        steps: [
          `第一步：审题与选型。括号匹配的关键是"**最近的左括号要最先被右括号配掉**"——这正是后进先出，所以用**栈**。`,
          `第二步：定规则。遍历字符串：遇到**左括号** \`(\` 就**入栈**；遇到**右括号** \`)\` 就看栈顶——如果栈非空，说明有左括号能配对，**出栈**一个；如果栈是空的，说明这个右括号没有左括号配，直接判定**不匹配**。`,
          `第三步：写代码。\`\`\`def is_matched(s):\n    stack = []\n    for ch in s:\n        if ch == '(':\n            stack.append(ch)      # 左括号入栈\n        else:                     # 右括号\n            if not stack:         # 栈空却来了右括号\n                return False      # 无人配对，失败\n            stack.pop()           # 配掉一个左括号\n    return len(stack) == 0        # 最后栈空才算全配对\`\`\``,
          `第四步：走一遍 \`"(())"\`。( 入栈→[(]；( 入栈→[(,(]；) 出栈→[(]；) 出栈→[]。结束时栈空 → 返回 True，**匹配**。`,
          `第五步：走一遍 \`"(()"\`。( 入栈→[(]；( 入栈→[(,(]；) 出栈→[(]。结束时栈里还剩一个 ( → \`len(stack)\` 不为 0 → 返回 False，**不匹配**（有个左括号没配上）。关键点：最后一定要检查栈是否为空，否则会漏掉"左括号多余"的情况。复杂度 $O(n)$。`,
        ],
      },
    ],
    pitfalls: [
      `**分不清"后进先出"和"先进先出"。** 栈（stack）是 **LIFO 后进先出**（像盘子，顶端进出）；队列（queue）是 **FIFO 先进先出**（像排队，一头进一头出）。这是这一节最核心的区别，记反了整个逻辑就错了。`,
      `**栈出栈用 \`pop()\`，队列出队用 \`pop(0)\`，别混。** \`pop()\` 取**末尾**（栈顶），\`pop(0)\` 取**开头**（队头）。栈两端都在末尾操作，队列是一头加（末尾）另一头取（开头）。`,
      `**对空栈/空队列 \`pop\` 会报 IndexError。** 出栈、出队前必须先判空：\`if stack:\` 再 \`pop()\`。直接对空列表 \`pop\` 程序会崩。`,
      `**队列用列表 \`pop(0)\` 效率低（$O(n)$）。** 因为删开头要把后面所有元素前移。数据量大或频繁出队时，应改用 \`from collections import deque\`，\`popleft()\` 是 $O(1)$。初学理解原理可先用列表，但要知道这个性能问题。`,
      `**\`append\` 加在末尾，是栈和队列共用的"入"操作，但"出"完全不同。** 别因为入队入栈都用 \`append\` 就以为它们一样——决定是栈还是队列的是**出**的那一端。`,
    ],
    exercises: [
      {
        problem: `用一个列表 \`s = []\` 当**栈**。依次执行：入栈 'a'、入栈 'b'、出栈、入栈 'c'、出栈。请写出每一步后栈的内容，以及两次出栈分别取出的是什么。`,
        hint: `栈是后进先出：入栈 \`append\` 加末尾，出栈 \`pop()\` 取末尾。出栈取走的总是当前"最后进来的"。`,
        answer: `**详细讲解**：\n\n**思路**：栈遵循**后进先出（LIFO）**。入栈用 \`s.append(x)\` 把元素加到末尾（栈顶），出栈用 \`s.pop()\` 取走末尾那个。每次出栈，取出的都是"目前最后进来的"。\n\n**逐步跟踪**：\n| 操作 | 代码 | 栈内容（左→右，右端是栈顶） | 取出 |\n|---|---|---|---|\n| 入栈 'a' | \`s.append('a')\` | \`['a']\` | — |\n| 入栈 'b' | \`s.append('b')\` | \`['a', 'b']\` | — |\n| 出栈 | \`s.pop()\` | \`['a']\` | **'b'** |\n| 入栈 'c' | \`s.append('c')\` | \`['a', 'c']\` | — |\n| 出栈 | \`s.pop()\` | \`['a']\` | **'c'** |\n\n**参考代码**：\n\`\`\`s = []\ns.append('a')\ns.append('b')\nprint(s.pop())   # b\ns.append('c')\nprint(s.pop())   # c\nprint(s)         # ['a']\`\`\`\n\n**结果**：两次出栈依次取出 **'b'** 和 **'c'**；最后栈里剩 \`['a']\`。\n\n**关键**：第一次出栈时，最后进来的是 'b'（不是 'a'），所以出 'b'；第二次出栈前刚入了 'c'，所以出 'c'。这就是"后进先出"——和往一摞盘子上放、再从顶上拿，完全一样。每个操作都是 $O(1)$。`
      },
      {
        problem: `把上一题完全相同的操作序列改成用**队列**（先进先出）来做：入队 'a'、入队 'b'、出队、入队 'c'、出队。请写出两次出队分别取出什么，并和上一题的栈结果对比。`,
        hint: `队列是先进先出：入队 \`append\` 加末尾（队尾），出队 \`pop(0)\` 取开头（队头）。出队取走的总是"最早进来的"。`,
        answer: `**详细讲解**：\n\n**思路**：队列遵循**先进先出（FIFO）**。入队用 \`q.append(x)\` 加到末尾（队尾），出队用 \`q.pop(0)\` 取走开头（队头）。每次出队，取出的都是"目前最早进来的"。\n\n**逐步跟踪**：\n| 操作 | 代码 | 队列内容（左端是队头） | 取出 |\n|---|---|---|---|\n| 入队 'a' | \`q.append('a')\` | \`['a']\` | — |\n| 入队 'b' | \`q.append('b')\` | \`['a', 'b']\` | — |\n| 出队 | \`q.pop(0)\` | \`['b']\` | **'a'** |\n| 入队 'c' | \`q.append('c')\` | \`['b', 'c']\` | — |\n| 出队 | \`q.pop(0)\` | \`['c']\` | **'b'** |\n\n**参考代码**：\n\`\`\`q = []\nq.append('a')\nq.append('b')\nprint(q.pop(0))  # a\nq.append('c')\nprint(q.pop(0))  # b\nprint(q)         # ['c']\`\`\`\n\n**结果**：两次出队依次取出 **'a'** 和 **'b'**；最后队里剩 \`['c']\`。\n\n**和栈对比**：完全相同的操作，栈取出的是 'b'、'c'，队列取出的是 'a'、'b'——**顺序正好不同**！栈先吐出后进的，队列先吐出先进的。这道题最能体现 LIFO 和 FIFO 的本质区别。\n\n**复杂度提醒**：队列出队 \`pop(0)\` 是 $O(n)$（要前移后面的元素），数据多时应改用 \`collections.deque\` 的 \`popleft()\`（$O(1)$）。`
      },
      {
        problem: `判断一个单词是不是"回文"（正着读和倒着读一样，如 "level"、"noon"）。请利用**栈**的"后进先出"特性来实现这个判断，并说明为什么栈能帮上忙。`,
        hint: `把每个字符依次入栈，再一个个出栈——出来的顺序正好是原字符串的**倒序**。把倒序和原串比较即可。`,
        answer: `**详细讲解**：\n\n**为什么用栈**：栈是**后进先出**，如果把字符串的字符**按顺序全部入栈**，再**一个个出栈**，出来的顺序恰好是原字符串的**倒序**。把"出栈得到的倒序串"和"原串"比较：相等就是回文，不等就不是。栈天生能"反转顺序"，正好用在这里。\n\n**参考代码**：\n\`\`\`def is_palindrome(word):\n    stack = []\n    # 第一步：所有字符入栈\n    for ch in word:\n        stack.append(ch)\n    # 第二步：依次出栈，拼成倒序字符串\n    reversed_word = ""\n    while stack:                 # 栈非空就继续出\n        reversed_word += stack.pop()\n    # 第三步：倒序和原串比较\n    return word == reversed_word\n\nprint(is_palindrome("level"))  # True\nprint(is_palindrome("hello"))  # False\`\`\`\n\n**走一遍 "level"**：入栈后栈是 \`['l','e','v','e','l']\`；依次 pop 出 l、e、v、e、l，拼成 \`"level"\`，和原串相等 → 是回文。\n走一遍 "hello"：pop 出 o、l、l、e、h，拼成 \`"olleh"\`，和 "hello" 不等 → 不是回文。\n\n**复杂度**：入栈 $O(n)$、出栈 $O(n)$、比较 $O(n)$，总体 $O(n)$；额外用了一个栈，空间 $O(n)$。\n\n**小结**：这题展示了栈最典型的本领之一——**反转顺序**。凡是需要"把一串东西倒过来处理"的场景，都可以想想栈。（当然这题也能用 \`word == word[::-1]\` 一行解决，但用栈能帮你真正理解 LIFO。）`
      },
    ],
  },

  // ═══════════════════════════════════════════════════════════════
  //   链表 linked-list
  // ═══════════════════════════════════════════════════════════════
  'linked-list': {
    warmup: {
      q: `课间排队，老师说"小明，你站第 1 个；小红，你拉着小明的衣角站他后面；小刚，你拉着小红的衣角……"。这样排队，每个人只知道"我后面是谁"。现在想在小明和小红中间插一个新同学，需要全队重新报数吗？如果是把同学们坐成固定排号的座位，插一个人又会怎样？`,
      hint: `拉衣角排队：插人只要改一下"谁拉谁的衣角"，旁边两个人调整一下就行，别人不受影响。固定座位：插一个人，后面所有人都得往后挪一个座。`,
      answer: `"拉衣角排队"里，每个人只记得"我的下一个是谁"，要在中间插人，只需让前一个人改拉新同学、新同学拉原来的下一个，**旁边两人改一下连接就行，其他人完全不动**。这就是**链表（linked list）**的思想：数据散落各处，靠"指针"一个串一个。而"固定座位"像数组/列表——插一个人，后面全体挪位（$O(n)$）。链表的代价是：想找"第 3 个人"必须从队头一个个数过去（$O(n)$），不能像座位那样直接报号入座。`
    },
    intro: [
      `**链表（linked list）**是一种"用指针把数据串起来"的数据结构。它由一个个**节点（node）**组成，每个节点装两样东西：**自己的数据** + **指向下一个节点的"指针"**。节点们在内存里**并不挨着**，全靠指针一个接一个连成一条链。最后一个节点的指针指向 \`None\`，表示"到头了"。`,
      `画出来是这样：\`(数据=5 | 下一个→) → (数据=3 | 下一个→) → (数据=8 | 下一个→None)\`。要访问链表，先抓住**头节点（head）**，然后顺着每个节点的"下一个"指针往后走，直到 \`None\`。这种"只能从头顺着走"的方式，和列表"任意下标直接取"很不一样。`,
      `链表和列表（数组）是一对"各有所长"的结构。**链表的强项是中间插入/删除快**：在两个节点间插一个，只要改两根指针（$O(1)$，前提是已经站在那个位置），不用像列表那样把后面元素整体挪位。**链表的弱项是随机访问慢**：想找"第 $k$ 个"必须从头一个个数过去（$O(n)$），没有"下标直达"。`,
      `反过来看**列表（数组）**：随机访问 \`lst[k]\` 是 $O(1)$（直接算出位置），但在开头/中间插入删除是 $O(n)$（要挪动后面所有元素）。所以选哪个看需求——**频繁随机读取用列表，频繁在中间增删用链表**。这正是"没有最好的数据结构，只有最合适的"。`,
      `在 Python 里，列表已经很强大，平时不一定要手写链表；但**理解链表的"节点 + 指针"思想至关重要**——它是后面**树**和**图**的基石（树就是每个节点指向多个子节点，图就是节点间任意连指针）。一句话：**链表 = 节点（数据 + 指向下一个的指针）串成的链，中间增删快、随机访问慢**。`
    ],
    formulas: [
      { name: '节点的结构', latex: '\\text{Node} = (\\text{data},\\ \\text{next})',
        note: `每个节点存"数据 data"和"指向下一个节点的指针 next"。在 Python 里常用一个类表示：\`self.data\` 存值，\`self.next\` 存下一个节点（最后一个的 next 是 \`None\`）。` },
      { name: '随机访问的复杂度', latex: '\\text{链表查第 } k \\text{ 个}=O(n),\\quad \\text{列表 } lst[k]=O(1)',
        note: `链表要从头节点顺着指针走 $k$ 步才能到第 $k$ 个（$O(n)$）；列表按下标直接定位（$O(1)$）。这是链表最大的劣势。` },
      { name: '中间插入/删除的复杂度', latex: '\\text{链表（已在位置）}=O(1),\\quad \\text{列表}=O(n)',
        note: `链表在已知节点后插入/删除只需改指针（$O(1)$）；列表在中间增删要挪动后面所有元素（$O(n)$）。这是链表最大的优势。` },
      { name: '遍历链表', latex: '\\texttt{cur = head;\\ while cur:\\ cur = cur.next}',
        note: `从头节点 \`head\` 开始，用一个"游标" \`cur\` 顺着 \`next\` 一直走，到 \`None\` 停。遍历整条链是 $O(n)$。` },
    ],
    methods: [
      {
        name: '用类定义节点并手搭一条链表',
        when: `想真正理解"节点 + 指针"如何工作，或题目要求手动实现链表时`,
        steps: [
          `定义节点类：每个节点有数据 \`data\` 和指向下一个的 \`next\`（初始为 \`None\`）`,
          `\`\`\`class Node:\n    def __init__(self, data):\n        self.data = data\n        self.next = None\`\`\``,
          `创建几个节点：\`a = Node(5)\`，\`b = Node(3)\`，\`c = Node(8)\``,
          `用 \`next\` 把它们串起来：\`a.next = b\`，\`b.next = c\`（\`c.next\` 默认 None，表示结尾）。此时 \`a\` 就是头节点`,
        ],
        example: {
          problem: `手动建一条链表 5 → 3 → 8，然后从头遍历，依次打印每个节点的数据。`,
          solution: [
            `解：先定义 Node 类，再建三个节点并串起来：`,
            `\`\`\`a = Node(5); b = Node(3); c = Node(8)\na.next = b   # 5 指向 3\nb.next = c   # 3 指向 8\`\`\``,
            `从头节点 \`a\` 开始遍历，游标 \`cur\` 顺着 next 走：`,
            `\`\`\`cur = a\nwhile cur:            # cur 不是 None 就继续\n    print(cur.data)   # 打印当前数据\n    cur = cur.next    # 移到下一个节点\`\`\``,
            `答：依次打印 \`5\`、\`3\`、\`8\`。当 cur 走到 8 的 next（None）时循环停止。`,
          ],
        },
      },
      {
        name: '在链表中间插入一个新节点',
        when: `要在某个已知节点的后面插入新数据，体会链表"改指针即可"的优势时`,
        steps: [
          `先创建要插入的新节点：\`new = Node(新数据)\``,
          `找到"要插在它后面"的那个节点，叫它 \`p\``,
          `**先接后**：让新节点指向 p 原来的下一个——\`new.next = p.next\`（顺序关键！不能先断）`,
          `**再接前**：让 p 指向新节点——\`p.next = new\`。两步只改了两根指针，$O(1)$`,
        ],
        example: {
          problem: `在链表 5 → 8 中，把 3 插到 5 的后面，变成 5 → 3 → 8。设 \`p\` 是数据为 5 的节点。`,
          solution: [
            `解：现在 \`p.next\` 是数据 8 的节点。新建 \`new = Node(3)\`。`,
            `第一步（先接后）：\`new.next = p.next\`——让 3 指向原来 5 的下一个（也就是 8）。现在 3 → 8。`,
            `第二步（再接前）：\`p.next = new\`——让 5 指向 3。现在 5 → 3，而 3 已经 → 8。`,
            `答：链表变成 5 → 3 → 8，**只改了两根指针**，没动其他节点。注意两步顺序不能反——若先 \`p.next = new\`，就会丢失通往 8 的路。`,
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 遍历链表求节点个数与总和',
        problem: `已知一条链表的头节点 \`head\`，链表是 4 → 7 → 2 → 9。请写代码统计链表一共有多少个节点，以及所有节点数据的总和。`,
        steps: [
          `第一步：审题。链表不能像列表那样直接 \`len()\`，必须**从头顺着走、边走边数**。需要一个计数器和一个累加器。`,
          `第二步：准备变量。\`count = 0\`（数节点个数），\`total = 0\`（累加数据），\`cur = head\`（游标，从头开始）。`,
          `第三步：遍历。只要 \`cur\` 不是 None，就处理当前节点、再移到下一个：\`\`\`cur = head\ncount = 0\ntotal = 0\nwhile cur:                 # 没走到结尾\n    count += 1             # 数一个节点\n    total += cur.data      # 累加它的数据\n    cur = cur.next         # 移到下一个\`\`\``,
          `第四步：走一遍。cur=4：count=1,total=4；cur=7：count=2,total=11；cur=2：count=3,total=13；cur=9：count=4,total=22；cur=None：停。`,
          `第五步：作答。节点个数 \`count = 4\`，总和 \`total = 22\`。关键点：\`while cur:\` 这个条件——当游标走到最后一个节点的 next（None）时，循环自然结束，不会越界。复杂度 $O(n)$，因为每个节点恰好访问一次。`,
        ],
      },
    ],
    pitfalls: [
      `**链表不能用下标随机访问。** 想取"第 3 个节点"不能写 \`head[2]\`，必须从 \`head\` 顺着 \`next\` 走 2 步。这是链表和列表最根本的区别：链表只能"顺藤摸瓜"。`,
      `**遍历时忘了 \`cur = cur.next\`，会死循环。** 循环体里必须让游标往后移，否则 \`cur\` 一直停在原地、\`while cur\` 永远为真。这和 while 循环忘了更新计数器是同一类错误。`,
      `**插入/删除时指针的修改顺序很重要。** 在中间插入要"**先让新节点接上后面，再让前面接新节点**"。若顺序反了（先改前面的指针），就会丢失指向后半段的唯一线索，后面的节点全"失联"。`,
      `**对 None 取 \`.data\` 或 \`.next\` 会报错。** 走到链表末尾后 \`cur\` 是 None，再访问 \`cur.data\` 会报 AttributeError。所以循环条件要先判 \`while cur:\`，确保 cur 非空才访问。`,
      `**链表 vs 数组（列表）的取舍要分清。** 频繁"按位置随机读取"用列表（$O(1)$）；频繁"在中间插入删除"用链表（改指针 $O(1)$）。用错场景会让程序变慢——别因为链表"高级"就到处用它。`,
    ],
    exercises: [
      {
        problem: `用下面的 Node 类手动建一条链表 10 → 20 → 30，然后写遍历代码把每个节点的数据打印出来。\n\`\`\`class Node:\n    def __init__(self, data):\n        self.data = data\n        self.next = None\n\`\`\``,
        hint: `先建三个 Node、用 \`next\` 串起来；遍历时用游标 \`cur = head\`，循环里 \`print(cur.data)\` 后别忘 \`cur = cur.next\`。`,
        answer: `**详细讲解**：\n\n**思路**：分两步。第一步"搭链"：创建三个节点，用每个节点的 \`next\` 指向下一个，把它们串成 10 → 20 → 30，第一个节点就是头节点。第二步"遍历"：从头节点出发，用一个游标 \`cur\` 顺着 \`next\` 一路走，每到一个节点就打印它的数据，直到 \`cur\` 变成 \`None\`。\n\n**参考代码**：\n\`\`\`class Node:\n    def __init__(self, data):\n        self.data = data\n        self.next = None\n\n# 第一步：建节点并串起来\nhead = Node(10)\nsecond = Node(20)\nthird = Node(30)\nhead.next = second   # 10 -> 20\nsecond.next = third  # 20 -> 30\n# third.next 默认是 None，表示链表结尾\n\n# 第二步：遍历打印\ncur = head\nwhile cur:               # cur 不是 None 就继续\n    print(cur.data)      # 打印当前节点的数据\n    cur = cur.next       # 移到下一个节点\`\`\`\n\n**走一遍**：cur=head(10) 打印 10，移到 20；cur=20 打印 20，移到 30；cur=30 打印 30，移到 third.next=None；\`while None\` 为假，循环结束。\n\n**输出**：\`10\`、\`20\`、\`30\`（各一行）。\n\n**复杂度**：遍历每个节点一次，$O(n)$。\n\n**关键提醒**：循环里 \`cur = cur.next\` 绝不能漏——它是让游标前进的唯一动作，漏了就会卡在第一个节点死循环。这和列表用下标遍历不同，链表必须靠"指针接力"才能往后走。`
      },
      {
        problem: `小明说："链表和列表都能存一串数据，那我干脆全用列表好了，链表没用。"这个说法对吗？请从"随机访问"和"中间插入删除"两个角度，比较链表和列表的快慢，说明各自适合什么场景。`,
        hint: `列表按下标取值 $O(1)$、中间增删 $O(n)$；链表随机访问 $O(n)$、已知位置增删 $O(1)$。两者强弱正好相反。`,
        answer: `**详细讲解**：\n\n**这个说法不全对**——列表确实更常用，但链表在特定场景有不可替代的优势。关键看两种操作：\n\n**① 随机访问（按位置取第 k 个）**：\n- **列表**：\`lst[k]\` 直接根据下标算出内存位置，**一步到位，$O(1)$**。\n- **链表**：没有下标，必须从头节点顺着 \`next\` 走 k 步才能到，**$O(n)$**。\n- → 这方面**列表完胜**。\n\n**② 在中间插入 / 删除**：\n- **列表**：在中间插一个或删一个，后面所有元素都要整体挪位，**$O(n)$**。\n- **链表**：只要站在那个位置，改两根指针就行，**$O(1)$**（不用挪动其他节点）。\n- → 这方面**链表完胜**。\n\n**结论（各自适合的场景）**：\n- **频繁按位置随机读取、增删少** → 用**列表**（比如存一批成绩、反复按下标取值）。\n- **频繁在中间插入/删除、很少随机读取** → 用**链表**（比如需要不断在序列中间增删元素的场景）。\n\n**一句话**：列表和链表的强弱**正好相反**，没有谁绝对更好，只有"谁更适合当前需求"。所以"全用列表、链表没用"是错的——理解链表的"节点+指针"思想还特别重要，因为后面的**树和图**都是它的延伸。`
      },
      {
        problem: `已知一条链表的头节点 \`head\`（如 3 → 5 → 7 → 9），请写一个函数 \`get(head, k)\`，返回**第 k 个**节点的数据（k 从 1 数起）。如果 k 超过链表长度，返回 \`None\`。并说明它的复杂度，对比列表取第 k 个有什么不同。`,
        hint: `链表没有下标，得从头"走 k−1 步"。用游标 \`cur = head\`，循环往后走，同时数步数；中途如果 \`cur\` 变 None 说明 k 太大。`,
        answer: `**详细讲解**：\n\n**思路**：链表不能像列表那样 \`lst[k-1]\` 直接取，必须**从头节点出发，顺着 next 往后走**。要取第 k 个（k 从 1 起），就从头走 k−1 步。走的过程中如果游标提前变成 \`None\`，说明链表没那么长，返回 \`None\`。\n\n**参考代码**：\n\`\`\`def get(head, k):\n    cur = head\n    count = 1                # 头节点算第 1 个\n    while cur is not None:\n        if count == k:       # 走到了第 k 个\n            return cur.data\n        cur = cur.next       # 往后走一步\n        count += 1\n    return None              # 走完都没到第 k 个，说明 k 太大\n\n# 假设 head 是 3 -> 5 -> 7 -> 9\nprint(get(head, 3))   # 7\nprint(get(head, 10))  # None\`\`\`\n\n**走一遍 \`get(head, 3)\`**：cur=3,count=1（不等3）→走到5,count=2→走到7,count=3（等于3）→返回 7。\n走一遍 \`get(head, 10)\`：一路走到 9,count=4，再 \`cur = 9.next = None\`,count=5，\`while None\` 为假退出 → 返回 None。\n\n**复杂度**：最坏要走到第 k 个或走完整条链，是 **$O(n)$**（n 为链表长度）。\n\n**对比列表**：列表取第 k 个 \`lst[k-1]\` 是 **$O(1)$**——直接按下标算出位置。这正是链表的"软肋"：**随机访问慢**。如果一个程序需要频繁"取第几个元素"，用列表远比链表合适。这道题让你切身体会到：链表的"按位置取值"是要"一步步走"的，没有捷径。`
      },
    ],
  },

  // ═══════════════════════════════════════════════════════════════
  //   树 tree-intro
  // ═══════════════════════════════════════════════════════════════
  'tree-intro': {
    warmup: {
      q: `你的电脑里，"文档"文件夹下有"作业"和"照片"两个子文件夹，"作业"下又有"语文""数学"……这种"一层套一层、不断分叉"的结构，和我们之前学的"一条线串下去"的链表很不一样。它有什么特点？为什么文件系统、家谱都长这样？`,
      hint: `链表是一根直线，每个节点只有一个"下一个"。文件夹不一样——一个文件夹下可以有**好几个**子文件夹。这种"一变多"的分叉结构像什么？倒过来看是不是像一棵树？`,
      answer: `文件夹这种结构叫**树（tree）**：从一个**根**（如"文档"）出发，每个节点可以有**多个子节点**（"文档"下有"作业""照片"），子节点下面还能再分叉，层层展开。它和链表的区别是——链表每个节点只连"一个下一个"（一条线），**树每个节点能连多个子节点（会分叉）**。文件系统、家谱、公司组织架构都是树，因为它们本质都是"分层 + 一对多"的关系。树还有个特点：**有唯一的根、没有环**（顺着子节点走下去不会绕回来）。`
    },
    intro: [
      `**树（tree）**是一种"分层、会分叉"的数据结构，长得像一棵**倒过来的树**——**根（root）在最上面**，向下不断分出**子节点（children）**。它由节点和"父子连接"组成：每个节点可以有 0 个或多个子节点，最上面那个没有父亲的叫**根节点**，最下面没有子节点的叫**叶子节点（leaf）**。`,
      `树是链表的"升级版"。链表每个节点只指向**一个**"下一个"，是一条直线；而树的每个节点可以指向**多个**子节点，于是就分叉成了层层展开的结构。正因如此，树能表达"一对多"的层级关系，这是直线型的链表做不到的。`,
      `树有两条重要性质：**第一，有唯一的根**——整棵树从一个根长出来。**第二，没有环**——从根顺着子节点往下走，永远不会绕回到走过的节点（不像图可以有环）。还有个常识：n 个节点的树恰好有 n−1 条边（每个非根节点都靠一条边连着它的父亲）。`,
      `最常见的一种树是**二叉树**：规定**每个节点最多只有 2 个子节点**，分别叫"左子节点"和"右子节点"。二叉树结构整齐、便于处理，是很多高级结构（如二叉搜索树、堆）的基础。当然，普通的树（如文件夹）每个节点的子节点个数不限。`,
      `树的"访问每个节点"叫**遍历**，常见有四种顺序：**前序**（先根，再左子树，再右子树）、**中序**（先左、再根、再右）、**后序**（先左、再右、再根）、**层序**（一层一层、从上到下从左到右）。前三种通常用**递归**实现（"处理一个节点 = 处理它 + 递归处理子树"），层序用**队列**。一句话：**树 = 有唯一根、会分叉、无环的分层结构，靠遍历来访问每个节点**，文件系统、家谱、表达式解析都靠它。`
    ],
    formulas: [
      { name: '树的基本构成', latex: '\\text{根 root} + \\text{子节点 children} + \\text{叶子 leaf}',
        note: `最上面无父节点的是**根**；最下面无子节点的是**叶子**；其余是中间节点。每个节点（除根外）恰有一个父节点。` },
      { name: '节点数与边数', latex: 'n \\text{ 个节点的树有 } n-1 \\text{ 条边}',
        note: `因为除了根，每个节点都靠且仅靠一条边连向它的父节点，所以边数总比节点数少 1。这也说明树是"恰好连通且无环"的。` },
      { name: '二叉树', latex: '\\text{每个节点最多 } 2 \\text{ 个子节点}',
        note: `二叉树规定每个节点最多有"左子节点"和"右子节点"。它结构规整，是二叉搜索树、堆等结构的基础。普通树子节点个数不限。` },
      { name: '遍历的复杂度', latex: '\\text{遍历所有节点}=O(n)',
        note: `不管前序/中序/后序/层序，都要访问每个节点恰好一次，所以遍历整棵树是 $O(n)$（n 为节点总数）。前三种常用递归，层序用队列。` },
    ],
    methods: [
      {
        name: '用类定义二叉树节点并建一棵小树',
        when: `想理解树在代码里怎么表示，或题目要求手动构建二叉树时`,
        steps: [
          `定义节点类：每个节点有数据，以及指向左、右子节点的引用（初始都为 None）`,
          `\`\`\`class TreeNode:\n    def __init__(self, data):\n        self.data = data\n        self.left = None\n        self.right = None\`\`\``,
          `创建节点：\`root = TreeNode(1)\`，\`root.left = TreeNode(2)\`，\`root.right = TreeNode(3)\``,
          `继续往下接：\`root.left.left = TreeNode(4)\`，就给节点 2 加了个左孩子 4。这样一棵树就搭好了`,
        ],
        example: {
          problem: `建一棵二叉树：根是 1，1 的左孩子是 2、右孩子是 3，2 的左孩子是 4。画出它的形状。`,
          solution: [
            `解：按父子关系逐个连接：`,
            `\`\`\`root = TreeNode(1)\nroot.left = TreeNode(2)\nroot.right = TreeNode(3)\nroot.left.left = TreeNode(4)\`\`\``,
            `形状（根在上）：第一层是 1；第二层是 1 的两个孩子 2（左）和 3（右）；第三层是 2 的左孩子 4。`,
            `答：根 1 下面分出 2 和 3，2 下面再挂一个 4。其中 3 和 4 没有子节点，是叶子节点。`,
          ],
        },
      },
      {
        name: '用递归遍历树（前序为例）',
        when: `要访问树里的每一个节点——打印、求和、查找时，递归是最自然的方式`,
        steps: [
          `想清楚一次递归对"一个节点"做什么：处理它自己 + 递归处理它的子树`,
          `**前序遍历**的顺序是"**根 → 左子树 → 右子树**"：先处理当前节点，再递归左、再递归右`,
          `**写终止条件**：节点是 None（空）就直接返回，什么都不做——这是递归不可少的"刹车"`,
          `\`\`\`def preorder(node):\n    if node is None:        # 终止条件：空节点\n        return\n    print(node.data)        # 处理根（当前节点）\n    preorder(node.left)     # 递归左子树\n    preorder(node.right)    # 递归右子树\`\`\``,
        ],
        example: {
          problem: `对上面那棵树（根 1，左 2、右 3，2 的左孩子 4）做前序遍历，输出顺序是什么？`,
          solution: [
            `解：前序 = 根 → 左 → 右。从根 1 开始。`,
            `先打印根 \`1\`；进入左子树（节点 2）：打印 \`2\`，再进 2 的左子树（节点 4）：打印 \`4\`（4 无子节点，左右都返回）；2 的右子树为空，返回。`,
            `回到根，进入右子树（节点 3）：打印 \`3\`（3 无子节点）。`,
            `答：前序遍历输出 \`1 → 2 → 4 → 3\`。可以看到规律：总是"先彻底走完左边，再走右边"。`,
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 递归求二叉树所有节点的总和',
        problem: `已知一棵二叉树的根节点 \`root\`（每个节点有 \`data\`、\`left\`、\`right\`）。请用递归求出整棵树所有节点数据的总和。`,
        steps: [
          `第一步：审题与选型。树是分叉的，没法用一个简单的 \`while\` 走完，但它有"自相似"结构——整棵树的和 = 根的值 + 左子树的和 + 右子树的和。这种"大问题拆成同类小问题"正适合**递归**。`,
          `第二步：定义递归含义。设 \`tree_sum(node)\` 返回"以 node 为根的子树的所有节点之和"。`,
          `第三步：写终止条件（基础情形）。空树（node 是 None）的和是 0——这是递归的"出口"，没有它会无限递归：\`if node is None: return 0\`。`,
          `第四步：写递归式。非空时：当前节点的值 + 左子树的和 + 右子树的和：\`\`\`def tree_sum(node):\n    if node is None:        # 空子树\n        return 0\n    left_sum = tree_sum(node.left)    # 左子树的和\n    right_sum = tree_sum(node.right)  # 右子树的和\n    return node.data + left_sum + right_sum\`\`\``,
          `第五步：走一遍（树：根 1，左 2、右 3，2 的左孩子 4）。tree_sum(4)=4+0+0=4；tree_sum(2)=2+4(左)+0(右)=6；tree_sum(3)=3+0+0=3；tree_sum(1)=1+6(左)+3(右)=10。答：总和为 \`10\`。关键点：递归把"求整棵树"自动拆成"求每个子树"，终止条件 \`None→0\` 保证能停下来。复杂度 $O(n)$，每个节点访问一次。`,
        ],
      },
    ],
    pitfalls: [
      `**树和链表搞混。** 链表每个节点只有**一个**"下一个"（直线）；树每个节点可以有**多个**子节点（分叉）。树是链表的推广，别把它们当成一回事。`,
      `**递归遍历忘了写终止条件。** 处理到空节点（None）时必须 \`return\`，否则会对 None 取 \`.left\`/\`.right\` 报错，或无限递归爆栈。**终止条件是递归的"刹车"，绝不能漏**。`,
      `**前序/中序/后序的区别在于"根在什么时候处理"。** 前序是"根左右"（先处理根）、中序是"左根右"（中间处理根）、后序是"左右根"（最后处理根）。三者只是处理根的时机不同，别记混。`,
      `**以为树一定是二叉树。** 二叉树只是"每个节点最多 2 个子节点"的特例；普通的树（如文件夹）一个节点可以有任意多个子节点。看到"树"别默认它是二叉的。`,
      `**对空节点取属性会报错。** 走到 None 还访问 \`node.data\` 会报 AttributeError。所以递归一开始就要判 \`if node is None: return\`，先挡住空节点再处理。`,
    ],
    exercises: [
      {
        problem: `用下面的 TreeNode 类建一棵二叉树：根是 'A'，A 的左孩子是 'B'、右孩子是 'C'，B 的右孩子是 'D'。然后说出 A、B、C、D 中哪些是叶子节点。\n\`\`\`class TreeNode:\n    def __init__(self, data):\n        self.data = data\n        self.left = None\n        self.right = None\n\`\`\``,
        hint: `按父子关系用 \`left\`/\`right\` 连接。叶子节点 = 没有任何子节点的节点（left 和 right 都是 None）。`,
        answer: `**详细讲解**：\n\n**思路**：先按描述把节点连接好，再判断叶子。叶子节点的定义是"**没有任何子节点**"，即它的 \`left\` 和 \`right\` 都是 None。\n\n**参考代码**：\n\`\`\`class TreeNode:\n    def __init__(self, data):\n        self.data = data\n        self.left = None\n        self.right = None\n\nroot = TreeNode('A')\nroot.left = TreeNode('B')      # A 的左孩子 B\nroot.right = TreeNode('C')     # A 的右孩子 C\nroot.left.right = TreeNode('D')  # B 的右孩子 D\`\`\`\n\n**树的形状**（根在上）：\n\`\`\`        A\n       / \\\n      B   C\n       \\\n        D\n\`\`\`\n\n**判断叶子**：\n- **A**：有左孩子 B、右孩子 C → 不是叶子。\n- **B**：有右孩子 D（左孩子为 None，但只要有一个孩子就不是叶子）→ 不是叶子。\n- **C**：left 和 right 都是 None → **是叶子**。\n- **D**：left 和 right 都是 None → **是叶子**。\n\n**结论**：叶子节点是 **C 和 D**。\n\n**小结**：根（A）是最上面无父节点的；叶子（C、D）是最下面无子节点的；B 是中间节点。注意 B 虽然只有一个孩子，但仍不是叶子——叶子要求"一个孩子都没有"。`
      },
      {
        problem: `对下面这棵二叉树做**前序遍历**（根→左→右），写出访问节点的顺序。\n\`\`\`        1\n       / \\\n      2   3\n     / \\   \\\n    4   5   6\n\`\`\``,
        hint: `前序口诀"根左右"：先访问当前节点，再**完整地**遍历左子树，再完整地遍历右子树。对每个子树都按同样规则递归。`,
        answer: `**详细讲解**：\n\n**思路**：前序遍历的顺序是"**根 → 左子树 → 右子树**"。对每个节点都一样：先处理它自己，再**整个**遍历它的左子树（左子树内部也按前序），最后整个遍历右子树。关键是"左子树要彻底走完，才轮到右子树"。\n\n**一步步推**：\n1. 访问根 **1**。\n2. 进入 1 的**左子树**（根为 2）：\n   - 访问 **2**；\n   - 进入 2 的左子树（只有 4）：访问 **4**；\n   - 进入 2 的右子树（只有 5）：访问 **5**；\n   - 2 的子树走完。\n3. 进入 1 的**右子树**（根为 3）：\n   - 访问 **3**；\n   - 3 没有左子树；\n   - 进入 3 的右子树（只有 6）：访问 **6**。\n\n**访问顺序**：**1 → 2 → 4 → 5 → 3 → 6**。\n\n**参考代码**：\n\`\`\`def preorder(node):\n    if node is None:\n        return\n    print(node.data)      # 根\n    preorder(node.left)   # 左子树\n    preorder(node.right)  # 右子树\`\`\`\n\n**复杂度**：每个节点访问一次，$O(n)$。\n\n**对比记忆**：若改成**中序**（左根右）结果是 4→2→5→1→3→6；**后序**（左右根）是 4→5→2→6→3→1。三者的差别只在于"根在左、右子树之间的哪个时刻被访问"，前序是最先访问根。`
      },
      {
        problem: `小明说："链表我会遍历——用 while 顺着 next 走就行。可是树也想遍历，为什么大家都用**递归**而不用 while？"请解释为什么遍历树通常用递归，并用"求树的节点总数"为例写出递归代码。`,
        hint: `链表是一条线（一个方向），while 能顺下去；树会**分叉**（一个节点有左右两个方向），一个 while 没法同时走两边。想想"整棵树的事 = 根 + 左子树同样的事 + 右子树同样的事"。`,
        answer: `**详细讲解**：\n\n**为什么树用递归而不用 while**：\n- **链表是一条直线**，每个节点只有一个"下一个"，所以用一个游标 + while 顺着 \`next\` 一直走就能访问完——只有一个前进方向。\n- **树会分叉**，一个节点有**左、右两个**子节点（甚至更多），一个简单的 while 循环没法同时沿两个方向走下去。如果硬用循环，得自己拿一个栈/队列记住"还没走的分支"，很麻烦。\n- 而树有天然的**自相似（递归）结构**："处理整棵树"可以拆成"处理根 + 用同样的方法处理左子树 + 用同样的方法处理右子树"。每个子树又是一棵更小的树，规则完全一样。这种"大问题拆成同类小问题"正是**递归**最擅长的，写出来又短又直观。\n\n**用递归求节点总数**：\n\`\`\`def count_nodes(node):\n    if node is None:        # 终止条件：空子树有 0 个节点\n        return 0\n    # 节点总数 = 1（自己）+ 左子树节点数 + 右子树节点数\n    return 1 + count_nodes(node.left) + count_nodes(node.right)\`\`\`\n\n**走一遍**（根 1，左 2、右 3，2 的左孩子 4）：count(4)=1+0+0=1；count(2)=1+1(左)+0=2；count(3)=1；count(1)=1+2(左)+1(右)=4。共 4 个节点。\n\n**复杂度**：每个节点恰好被访问一次，$O(n)$。\n\n**小结**：链表"一条道走到黑"用 while；树"会分岔"用递归。递归的两个要点别忘——**终止条件**（空节点返回）和**递归式**（当前 + 各子树）。理解了树的递归遍历，后面的图遍历、分治算法也会顺很多。`
      },
    ],
  },

  // ═══════════════════════════════════════════════════════════════
  //   图 graph-intro
  // ═══════════════════════════════════════════════════════════════
  'graph-intro': {
    warmup: {
      q: `班里的好友关系：小明和小红是朋友、小明和小华是朋友、小红和小华也是朋友……朋友关系是**互相**的，而且可能"绕成一圈"（小明→小红→小华→小明）。这种关系，用之前学的"有唯一根、不能绕回来"的树能装下吗？`,
      hint: `树有唯一的根、没有环（不能绕回来）。可朋友关系没有"谁是根"，还能绕圈（A 的朋友的朋友又是 A）。需要一种更自由、允许成环的结构。`,
      answer: `朋友关系装不进树——树有唯一的根、不能成环，而朋友关系**没有"根"、还能绕成圈**。这需要更一般的结构：**图（graph）**。图由**节点**（每个人）和**边**（朋友关系）组成，边可以随便连、可以成环，没有"根"的概念。树其实是图的一种**特例**（无环、连通、有根的图）。图能描述朋友圈、地图、互联网这些"任意互联"的关系。在 Python 里常用**字典**存图：键是一个节点，值是它的邻居列表，比如 \`{"小明": ["小红", "小华"], ...}\`。`
    },
    intro: [
      `**图（graph）**是最一般、最灵活的数据结构，由**节点（顶点 vertex）**和连接节点的**边（edge）**组成。和树相比，图有两个"放开了的限制"：**可以有环**（顺着边走能绕回起点）、**没有唯一的根**（任何节点都平等）。所以树其实是图的一个特例——"无环、连通、有根"的图。`,
      `图能描述现实中大量"互相连接"的关系：**朋友圈**（人是节点，朋友关系是边）、**地图**（地点是节点，道路是边）、**互联网**（网页是节点，链接是边）、**地铁线路**（站是节点，相邻站间的线是边）。凡是"一堆东西，两两之间可能有关系"的场景，都能用图建模。`,
      `图分两种。**无向图**：边没有方向，关系是相互的（你是我朋友 = 我是你朋友），如好友关系。**有向图**：边有方向（A→B 不代表 B→A），如"关注"（你关注他，他不一定关注你）、单行道。画图时，无向图的边是普通线，有向图的边是箭头。`,
      `在代码里，最常用的存图方式是**邻接表**，而 Python 里**用字典实现邻接表最自然**：**键是每个节点，值是它直接相连的邻居列表**。例如好友关系 \`graph = {"小明": ["小红", "小华"], "小红": ["小明"], "小华": ["小明"]}\`——一看就知道小明和小红、小华相连。这种存法省空间、查"某节点的邻居"很方便。`,
      `图最重要的操作是**遍历（搜索）**：从一个节点出发，系统地访问所有能到达的节点。两种基本方法是**广度优先搜索（BFS）**（用队列，一层层向外扩，适合求最少步数）和**深度优先搜索（DFS）**（用递归/栈，沿一条路走到底再回退，适合找连通、找路径）。这一节先打好"图是什么、怎么用字典存"的基础，遍历算法是后面的内容。一句话：**图 = 节点 + 边、可成环、无根，是树的推广，常用字典存邻接表**。`
    ],
    formulas: [
      { name: '图的构成', latex: '\\text{图} = \\text{节点（顶点）} + \\text{边}',
        note: `**节点**是图里的"点"（人、地点、网页…），**边**是连接两个节点的关系。和树不同，图可以**有环**、**没有根**。` },
      { name: '用字典存邻接表', latex: '\\texttt{\\{节点: [邻居1, 邻居2, ...]\\}}',
        note: `Python 里最常用的存图法：键是节点，值是它直接相连的邻居列表。如 \`{"小明": ["小红", "小华"]}\` 表示小明和小红、小华相连。查邻居方便。` },
      { name: '无向图 vs 有向图', latex: 'A\\!-\\!B\\ \\text{（无向）}\\quad vs \\quad A\\!\\to\\! B\\ \\text{（有向）}',
        note: `**无向图**边没方向、关系互相（朋友）：A 的邻居有 B，B 的邻居也得有 A。**有向图**边有方向（关注、单行道）：A→B 不代表 B→A。` },
      { name: '遍历的复杂度', latex: '\\text{BFS/DFS}=O(V+E)',
        note: `遍历图要访问每个节点（V 个）和每条边（E 条），所以是 $O(V+E)$。BFS 用队列、DFS 用递归或栈。遍历时必须用 visited 标记防止因环而无限循环。` },
    ],
    methods: [
      {
        name: '用字典建一张图（邻接表）',
        when: `要在程序里表示"一堆东西之间的关系"——朋友圈、地图、网络时`,
        steps: [
          `想清楚：谁是节点（点），谁是边（关系）`,
          `用字典，键放每个节点，值放它的邻居列表：\`graph = {}\``,
          `**无向图**：加一条 A—B 的边，要在两边都登记——\`graph["A"].append("B")\` **且** \`graph["B"].append("A")\``,
          `**有向图**：加 A→B 的边只登记一边——只 \`graph["A"].append("B")\``,
        ],
        example: {
          problem: `用字典表示这样的好友关系（无向）：小明—小红、小明—小华、小红—小华。然后查小明有哪些好友。`,
          solution: [
            `解：好友关系是无向的，每条边两边都要登记。直接写出邻接表：`,
            `\`\`\`graph = {\n    "小明": ["小红", "小华"],\n    "小红": ["小明", "小华"],\n    "小华": ["小明", "小红"]\n}\`\`\``,
            `查小明的好友：\`graph["小明"]\` → \`["小红", "小华"]\`。`,
            `答：小明的好友是小红和小华。注意每条边出现两次（如"小明—小红"在小明和小红的列表里各登记一次），这正是无向图的特点。`,
          ],
        },
      },
      {
        name: '查询图的信息（邻居、是否相连）',
        when: `图建好后，要回答"某节点有哪些邻居""两个节点直接相连吗"这类问题时`,
        steps: [
          `**查某节点的邻居**：直接取字典的值 \`graph[节点]\`（先确认节点是键，否则 KeyError）`,
          `**判断 A、B 是否直接相连**：看 B 在不在 A 的邻居列表里——\`"B" in graph["A"]\``,
          `**数某节点的"度"**（连了几条边）：\`len(graph[节点])\``,
          `**遍历所有节点**：\`for node in graph:\`；遍历某节点的邻居：\`for nb in graph[node]:\``,
        ],
        example: {
          problem: `用上面的好友图，回答：小红和小华直接是朋友吗？小明的好友有几个？`,
          solution: [
            `解：判断小红、小华是否相连——看小华在不在小红的邻居里：\`"小华" in graph["小红"]\`。\`graph["小红"]\` 是 \`["小明", "小华"]\`，小华在里面 → \`True\`。`,
            `数小明的好友个数：\`len(graph["小明"])\`，\`graph["小明"]\` 是 \`["小红", "小华"]\`，长度 2。`,
            `答：小红和小华**直接是朋友**；小明有 \`2\` 个好友。`,
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 用字典建图并统计每个人的好友数',
        problem: `已知三对好友关系（无向）：A—B、A—C、B—C。请用字典建出这张好友图，然后输出每个人各有几个好友，并判断 A 和 C 是否直接是朋友。`,
        steps: [
          `第一步：审题。节点是 A、B、C 三个人，边是三对好友关系。无向图，每条边两边都要登记。`,
          `第二步：建邻接表字典。把每个人和他的好友列表对应起来：\`\`\`graph = {\n    "A": ["B", "C"],   # A 和 B、C 是朋友\n    "B": ["A", "C"],   # B 和 A、C 是朋友\n    "C": ["A", "B"]    # C 和 A、B 是朋友\n}\`\`\` 注意 A—B 这条边在 A 的列表里有 B、在 B 的列表里也有 A，出现两次。`,
          `第三步：统计每个人的好友数。某人的好友数 = 他邻居列表的长度。遍历字典：\`\`\`for person in graph:\n    print(person, "有", len(graph[person]), "个好友")\`\`\``,
          `第四步：走一遍。A：len(["B","C"])=2；B：len(["A","C"])=2；C：len(["A","B"])=2。每人 2 个好友。`,
          `第五步：判断 A 和 C 是否直接是朋友。看 C 在不在 A 的邻居里：\`"C" in graph["A"]\`——\`graph["A"]\` 是 \`["B","C"]\`，C 在里面 → \`True\`。答：每人各有 2 个好友；A 和 C 直接是朋友。关键点：无向图加边要"两边登记"，否则关系会不对称（变成有向图）。`,
        ],
      },
    ],
    pitfalls: [
      `**图和树搞混。** 树是图的特例——树**有唯一根、无环**；图**没有根、可以有环**（顺着边能绕回起点）。看到"互相连接、可能成环、没有明显层级"的关系，那是图不是树。`,
      `**无向图加边忘了"两边都登记"。** 无向的 A—B 关系，必须 A 的邻居里有 B、**且** B 的邻居里也有 A。只登记一边，就变成了有向边 A→B，关系不对称。这是建图最常见的错误。`,
      `**用字典存图时取不存在的节点会 KeyError。** 写 \`graph["不存在的人"]\` 会报错。访问前先确认这个节点是字典的键，或用 \`graph.get(节点, [])\`（不存在时返回空列表）。`,
      `**遍历图时不标记"已访问"会无限循环。** 因为图可能有环（A→B→C→A），不做标记会一直绕圈。遍历（BFS/DFS）必须用一个 \`visited\` 集合记下走过的节点，去过的不再去。`,
      `**有向图和无向图别用错。** 朋友、同桌这种"互相"的关系用无向图；关注、单行道、谁是谁上级这种"单向"的关系用有向图。建图前先想清楚关系有没有方向。`,
    ],
    exercises: [
      {
        problem: `用字典表示下面这张无向好友图，并查出"小李"有哪些好友：关系是 小李—小王、小李—小张、小王—小赵。`,
        hint: `无向图每条边两边都要登记。先把每个人作为键、好友列表作为值写出字典，再 \`graph["小李"]\` 取小李的好友。`,
        answer: `**详细讲解**：\n\n**思路**：用字典存邻接表——键是每个人，值是他的好友列表。因为是**无向图**，每条好友关系要在**两个人**的列表里都登记。逐条边处理：\n- 小李—小王：小李的列表加"小王"，小王的列表加"小李"\n- 小李—小张：小李的列表加"小张"，小张的列表加"小李"\n- 小王—小赵：小王的列表加"小赵"，小赵的列表加"小王"\n\n**参考代码**：\n\`\`\`graph = {\n    "小李": ["小王", "小张"],   # 小李和小王、小张是朋友\n    "小王": ["小李", "小赵"],   # 小王和小李、小赵是朋友\n    "小张": ["小李"],           # 小张只和小李是朋友\n    "小赵": ["小王"]            # 小赵只和小王是朋友\n}\n\nprint(graph["小李"])   # ['小王', '小张']\`\`\`\n\n**结果**：\`graph["小李"]\` → \`["小王", "小张"]\`，即小李的好友是**小王和小张**。\n\n**复杂度**：按键取邻居列表是 $O(1)$。\n\n**易错提醒**：注意每条边都登记了两次——比如"小李—小王"在小李的列表里有"小王"、在小王的列表里有"小李"。如果只登记一边（比如忘了在小王列表里加小李），就会变成有向关系，逻辑就错了。这是无向图建图最容易出错的地方。`
      },
      {
        problem: `小明说："树和图我分不清，它们看起来都是'节点连节点'。"请你帮他用三条关键区别说清楚树和图的不同，并举一个"只能用图、不能用树"的现实例子。`,
        hint: `从"有没有根""能不能成环""谁是谁的特例"三个角度对比。朋友关系能绕圈、没有根，正好说明问题。`,
        answer: `**详细讲解**：\n\n树和图确实都是"节点 + 连接"，但有三条关键区别：\n\n**① 有没有根**：\n- **树**有**唯一的根**，整棵树从根往下分层（如文件夹从根目录展开）。\n- **图**没有根，所有节点是平等的，没有"从哪开始"的规定。\n\n**② 能不能成环**：\n- **树无环**——从根顺着往下走，永远不会绕回走过的节点。\n- **图可以有环**——顺着边能绕回起点（如 A→B→C→A）。\n\n**③ 谁是谁的特例**：\n- **树是图的特例**：一棵树就是"无环、连通、有根"的特殊图。反过来图不一定是树。\n\n**"只能用图、不能用树"的例子——朋友关系**：\n小明和小红是朋友、小红和小华是朋友、小华又和小明是朋友。这里：\n- 没有"根"——谁都不是中心；\n- 有环——小明→小红→小华→小明 绕回来了；\n所以朋友关系**装不进树**（树不能成环、要有根），只能用**图**来表示。地图（道路能绕圈）、地铁线路（环线）也是同理。\n\n**小结**：一句话记忆——**树是"有根、不绕圈"的图**；只要关系里出现"没有明显的根"或"能绕回来成环"，就该用图。`
      },
      {
        problem: `已知一张用字典存的好友图 \`graph = {"A": ["B", "C"], "B": ["A", "D"], "C": ["A"], "D": ["B"]}\`。请写代码完成：(1) 判断 A 和 D 是否**直接**是朋友；(2) 找出 A 的"朋友的朋友"（A 的好友们各自的好友，去掉 A 自己）。`,
        hint: `(1) 看 D 在不在 \`graph["A"]\` 里。(2) 先取 A 的好友列表，再对每个好友取他的好友，收集起来，记得排除 A 本人（也可去重）。`,
        answer: `**详细讲解**：\n\n**第 (1) 问 —— A 和 D 是否直接是朋友**：\n"直接是朋友"就是"D 在 A 的邻居列表里"。用 \`in\` 判断即可：\n\`\`\`print("D" in graph["A"])   # graph["A"] 是 ["B", "C"]，D 不在 -> False\`\`\`\nA 的好友是 B 和 C，没有 D，所以 A 和 D **不是直接朋友**（输出 \`False\`）。\n\n**第 (2) 问 —— 找 A 的"朋友的朋友"**：\n思路分两层：先拿到 A 的好友列表（B、C），再对**每个好友**取他自己的好友，全部收集起来，最后**去掉 A 本人**（因为 A 的好友的好友里会包含 A 自己）。用集合自动去重更干净。\n\`\`\`fof = set()                      # friends of friends，用集合去重\nfor friend in graph["A"]:        # A 的每个好友：B、C\n    for ff in graph[friend]:     # 这个好友的好友\n        if ff != "A":            # 排除 A 自己\n            fof.add(ff)\nprint(fof)\`\`\`\n\n**走一遍**：\n- friend = "B"：\`graph["B"]\` = ["A", "D"] → A 排除，D 加入 → fof = {"D"}\n- friend = "C"：\`graph["C"]\` = ["A"] → 只有 A，排除 → fof 不变\n最终 \`fof = {"D"}\`，即 A 的"朋友的朋友"是 **D**（A 通过 B 间接认识 D）。\n\n**复杂度**：(1) 在邻居列表里用 \`in\` 查找是 $O(度数)$；(2) 两层遍历是 $O(V+E)$ 量级（访问 A 的好友及他们的边）。\n\n**小结**：这题是图遍历的雏形——"一层层往外找"。第 (2) 问其实就是**广度优先搜索（BFS）走两步**的思想：第一步是直接好友，第二步是朋友的朋友。社交软件的"二度人脉""可能认识的人"就是这么算出来的。用集合去重、排除自己是两个关键细节。`
      },
    ],
  },

});
