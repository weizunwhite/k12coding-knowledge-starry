// 初中编程 · 算法 —— 详细小课（习题讲解版）
// 覆盖 8 个节点：complexity / linear-search / binary-search / bubble-sort /
//   selection-insertion-sort / quick-merge-sort / greedy / dp-intro
// 数据结构对齐 node-detail.jsx 的渲染：warmup / intro / formulas(name,latex,note) /
//   methods(name,when,steps,example) / worked(title,problem,steps) / pitfalls / exercises(problem,hint,answer)
// 说明：
//   · 行内公式用 $...$（renderRich 会交给 KaTeX）；formulas.latex 直接写 LaTeX（无需 $）
//   · JS 字符串里反斜杠写成 \\，所以 O(n log n) 写作 $O(n\\log n)$
//   · 含代码或引号的字段值一律用反斜杠模板字符串（反引号），代码换行用真实换行
//   · node-detail.jsx 不渲染 derivation，本文件不含该字段
//   · renderRich 会把 ** 当成加粗、$ 当成公式，故代码里避免出现 ** 和裸 $（Python 幂运算改用 pow 或文字说明）

Object.assign(window.CODE_LESSON, {

  // ───────────────────────────────────────────────────────────────
  // 1. 时间复杂度（大 O）
  // ───────────────────────────────────────────────────────────────
  'complexity': {
    warmup: {
      q: '同一道题，小明写的程序处理 100 万个数据要 1 秒，小红写的要 1 个小时。两人用的是同一台电脑。问题出在哪？我们能不能在「不运行」的情况下，就预先判断一个程序快不快？',
      hint: '电脑一样快，差距只能来自「算法」。想一想：数据从 100 个变成 100 万个，两个程序「多算的次数」是不是按不同的速度在涨？',
      answer: '差距来自**算法的增长趋势**，不是电脑。我们用**时间复杂度（大 O 记号）**来描述「当数据规模 $n$ 变大时，运算次数大约按什么速度增长」。比如一层循环是 $O(n)$（数据翻倍、时间翻倍），双层嵌套循环是 $O(n^2)$（数据翻倍、时间变 4 倍）。$O(n^2)$ 的程序在 100 万数据时要算上万亿次，自然就慢了。大 O 让我们**不用真的运行**，光看代码结构就能预判快慢。'
    },
    intro: [
      '时间复杂度，是用来**描述算法运行时间随数据规模增长趋势**的一把尺子。它不告诉你「这段程序跑几秒」（那和电脑、语言都有关），而是告诉你「当数据从少变多时，运算次数大约按什么速度往上涨」。',
      '为什么不直接用「秒」来衡量？因为秒数受太多因素影响：换台快电脑会变快、换种语言也会变。但「数据翻一倍，运算次数翻几倍」这个**增长趋势**是算法自身的性质，换电脑也不会变。所以我们用大 O 记号 $O(\\cdots)$ 来抓住这个趋势。',
      '怎么数？核心是**看循环**：没有循环、只做固定几步，是 $O(1)$（常数）；走一遍数据（一层循环 $n$ 次），是 $O(n)$；循环里再套一层循环（$n\\times n$ 次），是 $O(n^2)$；每次把范围砍一半（如二分查找），是 $O(\\log n)$；先砍半再合并（如归并排序），是 $O(n\\log n)$。',
      '大 O 有两个「偷懒规则」让它好算：① **只保留最高次项**——比如 $n^2+n$ 在 $n$ 很大时由 $n^2$ 主导，记 $O(n^2)$；② **去掉常数系数**——$3n$ 和 $100n$ 都记 $O(n)$，因为我们关心的是「增长趋势」而不是精确倍数。',
      '一句话记住：**大 O 比的是「数据变大时谁涨得快」**。常见快慢排序（从快到慢）是 $O(1) < O(\\log n) < O(n) < O(n\\log n) < O(n^2) < O(2^n)$。学会估计复杂度，你才能在写之前就判断「这个方法数据一多会不会卡死」。'
    ],
    formulas: [
      { name: '常数时间', latex: 'O(1)',
        note: '运算次数与数据规模 $n$ 无关，永远是固定的几步。例如取列表第一个元素 nums[0]、判断一个数的奇偶。' },
      { name: '对数时间', latex: 'O(\\log n)',
        note: '每一步都把问题规模**砍掉一半**。$n$ 翻倍，只多算一步。二分查找是典型代表，非常快。' },
      { name: '线性时间', latex: 'O(n)',
        note: '把数据从头到尾走一遍。一层循环 $n$ 次就是它。$n$ 翻倍，时间也翻倍。' },
      { name: '线性对数时间', latex: 'O(n\\log n)',
        note: '比 $O(n)$ 略慢、远快于 $O(n^2)$。快速排序、归并排序的平均复杂度，是大规模排序的主流速度。' },
      { name: '平方时间', latex: 'O(n^2)',
        note: '双层嵌套循环，里外各 $n$ 次。$n$ 翻倍，时间变 **4 倍**。冒泡、选择、插入排序都属于它。' },
    ],
    methods: [
      {
        name: '数循环层数估复杂度',
        when: '拿到一段代码，想快速判断它大概是 $O(1)$、$O(n)$ 还是 $O(n^2)$ 时',
        steps: [
          '找出代码里和数据规模 $n$ 有关的循环——一层 for/while 走 $n$ 次，记一个 $n$',
          '看循环是否嵌套：外层 $n$ 次、内层每次又 $n$ 次，相乘得 $n\\times n=n^2$',
          '看循环每轮范围是否「对半缩小」（如每次 i = i // 2）——若是，则这层是 $\\log n$ 而非 $n$',
          '把各部分相加，套用偷懒规则：只留最高次项、去掉常数系数，写出最终大 O',
        ],
        example: {
          problem: '判断下面两段代码的时间复杂度。\n（甲）一层循环：for i in range(n): print(i)\n（乙）双层循环：for i in range(n): / 　for j in range(n): / 　　print(i, j)',
          solution: [
            '解：（甲）只有一层循环，循环体执行 $n$ 次，所以是 $O(n)$。',
            '（乙）外层 $n$ 次，外层每走一步内层都完整跑 $n$ 次，总次数 $n\\times n=n^2$，所以是 $O(n^2)$。',
            '答：甲是 $O(n)$，乙是 $O(n^2)$。这正是「一层循环对应 $n$、嵌套循环对应相乘」的基本规律。',
          ],
        },
      },
      {
        name: '用「数据翻倍法」直观感受',
        when: '想验证某个复杂度到底意味着多快/多慢时',
        steps: [
          '设原来数据规模是 $n$，运算次数大约是 $T$',
          '把数据规模变成 $2n$（翻一倍），重新算运算次数',
          '$O(n)$：次数变 $2T$（翻倍）；$O(n^2)$：次数变 $4T$（4 倍）；$O(\\log n)$：只多 1 步；$O(1)$：不变',
          '由「翻倍后涨多少」反推它是哪种复杂度',
        ],
        example: {
          problem: '某算法在 1000 个数据时约算 100 万次（即 $1000^2$）。如果数据增加到 2000 个，大约要算多少次？它是什么复杂度？',
          solution: [
            '解：$1000^2=10^6$（100 万），说明运算次数约等于 $n^2$。',
            '数据翻倍到 2000，次数变为 $2000^2=4\\times 10^6$（400 万），正好是原来的 **4 倍**。',
            '答：数据翻倍、时间变 4 倍，符合 $O(n^2)$。这说明 $O(n^2)$ 的算法在数据变大时增长很快，要警惕。',
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 给代码标复杂度并比较快慢',
        problem: '下面三个函数都用来处理一个长度为 n 的列表 nums。请分别写出它们的时间复杂度，并按从快到慢排序。',
        steps: [
          `第一步：看清三段代码。
def f1(nums):          # 只取第一个元素
    return nums[0]

def f2(nums):          # 走一遍求和
    total = 0
    for x in nums:
        total = total + x
    return total

def f3(nums):          # 两两配对都打印一次
    for i in nums:
        for j in nums:
            print(i, j)`,
          '第二步：分析 f1。它只做一次取值 nums[0]，和 n 大小无关，是常数步——复杂度 $O(1)$。',
          '第三步：分析 f2。一层 for 循环走遍 n 个元素，每个做一次加法，共 n 步——复杂度 $O(n)$。',
          '第四步：分析 f3。外层 n 次、内层每次又 n 次，总共 $n\\times n$ 次 print——复杂度 $O(n^2)$。',
          '第五步：排序作答。按从快到慢：$O(1) < O(n) < O(n^2)$，即 f1 最快、f2 居中、f3 最慢。当 n 很大时，f3 会比 f1、f2 慢得多。',
        ],
      },
    ],
    pitfalls: [
      '把「运行秒数」当成复杂度。复杂度描述的是**增长趋势**，不是具体几秒。换台快电脑秒数会变，但 $O(n^2)$ 还是 $O(n^2)$。',
      '忘了去掉常数和低次项。$2n^2+5n+10$ 不写成那一长串，按规则**只留最高次项、去常数**，记作 $O(n^2)$。',
      '把嵌套循环错当成相加。两层独立的循环（先一个 for 再一个 for）是 $O(n)+O(n)=O(n)$；**嵌套**（一个 for 套在另一个里）才是 $O(n^2)$。',
      '没注意「对半缩小」的循环。每轮把范围除以 2 的循环只跑约 $\\log n$ 次，不是 $n$ 次，别把二分这类误判成 $O(n)$。',
      '以为 $O(\\log n)$ 里的对数底数重要。大 O 里底数（2、10）只差一个常数倍，统一写 $O(\\log n)$ 即可，不必纠结底数。',
    ],
    exercises: [
      {
        problem: '判断下面这段代码的时间复杂度，并说明理由：\nfor i in range(n):\n    for j in range(n):\n        for k in range(n):\n            print(i, j, k)',
        hint: '数一数有几层嵌套循环，每层各跑多少次，是相乘还是相加？',
        answer: `**详细讲解**：

**思路**：这是三层**嵌套**循环。判断嵌套循环复杂度的关键是「相乘」——外层每走一步，里面那层要完整跑一遍。

**逐层分析**：
- 最外层 for i：跑 n 次
- 中间层 for j：外层每跑一次，它就完整跑 n 次
- 最里层 for k：上面每跑一次，它又完整跑 n 次

所以 print 总共执行的次数是 n × n × n = n 的三次方。

**复杂度**：$O(n^3)$（读作「大 O n 的三次方」）。

**复杂度分析与提醒**：嵌套层数越多，增长越恐怖。如果 n = 1000，$O(n^3)$ 就是 10 亿次运算，普通电脑要算好几秒甚至更久。所以写代码时看到「三层及以上嵌套循环」要警觉，想想能不能用更聪明的方法降下来。`
      },
      {
        problem: '小明说："我的算法用了 5 个独立的 for 循环（一个接一个，不是嵌套），每个都走 n 遍，所以复杂度是 $O(5n)$，比 $O(n)$ 慢。"他说得对吗？请改正。',
        hint: '大 O 有两条偷懒规则，其中一条专门处理「常数系数」。5 个独立循环是相加还是相乘？',
        answer: `**详细讲解**：

**他说得不全对**。要分两点纠正。

**第一点：独立循环是相加，不是相乘。** 5 个一个接一个的 for 循环（不嵌套），总次数是 n + n + n + n + n = 5n，确实是 5n 步，这一步他没错。

**第二点：大 O 要去掉常数系数。** 大 O 关心的是「数据变大时的增长趋势」，常数倍不影响趋势——n 翻倍，5n 也跟着翻倍，和 O(n) 的涨法完全一样。按照「去掉常数系数」的规则，$O(5n)$ 要写成 $O(n)$。

**正确结论**：这个算法的复杂度就是 $O(n)$，**不比** $O(n)$ 慢——它们本来就是同一个。

**复杂度分析**：$O(5n)$ 和 $O(n)$ 在大 O 意义下是相等的。真正会改变复杂度等级的是「嵌套」（相乘）或「对半缩小」，而不是循环的个数。记住：大 O 比的是「等级」（是 n 还是 n 的平方），不是「精确倍数」。`
      },
      {
        problem: '把下面 4 个复杂度按「从快到慢」排序，并各举一个对应的算法例子：\n$O(n^2)$、$O(1)$、$O(n\\log n)$、$O(\\log n)$',
        hint: '记住那条快慢链：常数最快，平方较慢；带 log 的比纯 n 系列在对应位置更快。',
        answer: `**详细讲解**：

**排序方法**：记住这条从快到慢的链条：$O(1) < O(\\log n) < O(n) < O(n\\log n) < O(n^2) < O(2^n)$。把题目里的 4 个对号入座即可。

**从快到慢排序**：
1. $O(1)$ —— 最快，运算次数和数据多少无关。例：取列表第一个元素 nums[0]、判断一个整数是奇是偶。
2. $O(\\log n)$ —— 每步砍掉一半，很快。例：**二分查找**（在有序列表里找一个数）。
3. $O(n\\log n)$ —— 中等偏快，大规模排序的主流速度。例：**快速排序、归并排序**。
4. $O(n^2)$ —— 这 4 个里最慢。例：**冒泡排序、选择排序、插入排序**（双层嵌套）。

**复杂度分析**：可以这样直观感受——当 n = 100 万时，$O(\\log n)$ 大约只要 20 步，$O(n)$ 要 100 万步，$O(n\\log n)$ 约 2000 万步，而 $O(n^2)$ 要一万亿步。差距是天文数字，这就是「选对算法」远比「换台快电脑」重要的原因。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 2. 线性查找
  // ───────────────────────────────────────────────────────────────
  'linear-search': {
    warmup: {
      q: '老师把全班 40 本作业本随便摞成一摞（没有按学号排），让你从里面找出「学号 23」那本。你会怎么找？最多要翻几本才能确定找到（或确定没有）？',
      hint: '本子是乱的，没有任何顺序线索。你能跳着找吗？还是只能一本一本看？',
      answer: '因为本子**没有顺序**，你没法跳着找，只能从最上面一本一本往下翻、逐本核对学号——这就是**线性查找**。运气好第一本就是；运气最差要翻到最后一本（第 40 本）才找到，或翻完全部 40 本才能确定「没有」。所以线性查找最坏要看 $n$ 个元素，复杂度是 $O(n)$。它的优点是简单、对**任何顺序**的数据都能用。'
    },
    intro: [
      '线性查找，又叫**顺序查找**，是最直白的一种查找方法：从数据的第一个元素开始，**一个一个往后比对**，直到找到目标（返回它的位置），或者走到末尾都没找到（返回「找不到」）。',
      '它的核心思想就一句话：**挨个看，不跳过**。正因为不依赖任何顺序，所以对**乱序数据**它是唯一可行的简单办法——这也是它最大的价值。后面要学的二分查找虽然快得多，但有个硬条件：数据必须先排好序。数据是乱的时候，就轮到线性查找上场。',
      '速度上，线性查找在最坏情况下要把 $n$ 个元素全看一遍，时间复杂度是 $O(n)$。最好的情况是目标恰好在第一个，只比 1 次（$O(1)$）；平均下来大约要比较一半，即 $\\frac{n}{2}$ 次，仍属于 $O(n)$ 这一档。',
      '实现上有个常用技巧：用 enumerate 同时拿到「位置（下标）」和「值」，找到目标就**立刻 return 它的下标**（提前结束，不必看完剩下的）；如果循环正常走完都没 return，说明没找到，最后 return -1（或 None）表示「不存在」。',
      '一句话记住：**没有顺序就只能一个一个找，这就是线性查找，最坏 $O(n)$。** 它不快，但胜在通用、好写、不挑数据。'
    ],
    formulas: [
      { name: '最坏时间复杂度', latex: 'O(n)',
        note: '目标在最后一个、或根本不存在时，要把 $n$ 个元素全比一遍，这是线性查找的最坏情形。' },
      { name: '最好时间复杂度', latex: 'O(1)',
        note: '目标恰好是第一个元素，只比较 $1$ 次就找到。但不能指望运气，衡量算法看的是最坏情况。' },
      { name: '平均比较次数', latex: '\\dfrac{n}{2}',
        note: '目标等概率出现在各位置时，平均约比较一半。去掉常数 $\\frac12$ 后仍是 $O(n)$ 这一档。' },
    ],
    methods: [
      {
        name: '线性查找标准步骤',
        when: '在一个**未排序**（或不确定是否有序）的列表里查找某个目标值时',
        steps: [
          '从下标 0 开始，用循环依次取出每个元素',
          '把当前元素和目标值比较：相等就说明找到了',
          '一旦找到，立刻返回当前下标（提前结束循环，省掉后面的比较）',
          '若循环完整走完都没相等，返回 -1（或 None），表示列表里没有这个目标',
        ],
        example: {
          problem: '在列表 nums = [8, 3, 5, 9, 2] 里查找 9，用线性查找说明每一步在比什么。',
          solution: [
            '解：从头开始一个一个比。',
            '下标 0：nums[0]=8，8≠9，继续；下标 1：nums[1]=3，3≠9，继续；下标 2：nums[2]=5，5≠9，继续。',
            '下标 3：nums[3]=9，9=9，找到！立刻返回下标 3，不再看后面的 2。',
            '答：目标 9 在下标 3，共比较了 4 次。如果找的是不存在的 7，就要把 5 个元素全比一遍才能确定没有。',
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 写出线性查找并追踪执行',
        problem: '请写一个函数 linear_search(nums, target)，在列表 nums 中查找 target，找到就返回它第一次出现的下标，找不到返回 -1。然后追踪在 nums = [4, 7, 1, 7, 9] 中查找 7 的执行过程。',
        steps: [
          `第一步：写出代码。用 enumerate 同时拿到下标 i 和元素 x。
def linear_search(nums, target):
    # 从头到尾依次检查每个元素
    for i, x in enumerate(nums):
        if x == target:   # 找到目标
            return i      # 立刻返回下标，提前结束
    return -1             # 走完都没找到，返回 -1`,
          '第二步：理解逻辑。enumerate 让循环每轮同时得到「位置 i」和「该位置的值 x」；只要 x 等于 target 就 return i；循环正常结束（没 return 过）说明没找到，最后 return -1。',
          '第三步：追踪 nums = [4, 7, 1, 7, 9]、target = 7。i=0, x=4：4≠7，继续。i=1, x=7：7=7，匹配！return 1，函数结束。',
          '第四步：作答。返回的是 1（第一次出现 7 的下标）。注意：列表里下标 3 处也有个 7，但因为我们「一找到就返回」，所以只会得到**第一个** 7 的位置，后面的不再看。',
          '第五步：补充最坏情况。若改成查找 100（不存在），循环会把 4、7、1、7、9 全比一遍都不相等，最后 return -1，共比较 5 次——这就是 $O(n)$ 的来历。',
        ],
      },
    ],
    pitfalls: [
      '在**无序**数据上误用二分查找。二分要求数据有序；数据是乱的时候，老老实实用线性查找，否则二分会得到错误结果。',
      '循环里「没找到」时也提前返回。-1 必须等**整个循环走完**都没匹配时才返回；如果写在循环体内，第一个不相等就 return -1 了，等于只看了一个元素。',
      '把「找到第一个」误以为「找到全部」。线性查找一 return 就结束，只给出第一次出现的位置；要找所有出现位置，得去掉提前 return、把下标都收集起来。',
      '下标和值搞混。enumerate 给的是 (下标 i, 值 x)，要返回的是位置 i，比较的是值 x，别返回成 x 或拿 i 去比较。',
      '以为线性查找一定慢、应该避免。数据量很小、或数据无序无法排序时，线性查找简单可靠，反而是合适选择。',
    ],
    exercises: [
      {
        problem: '用线性查找的思想，写一个函数 count_target(nums, target)，统计 target 在列表 nums 中出现了**多少次**（不是位置）。并说明它的时间复杂度。',
        hint: '和「找到就返回」不同，统计次数不能提前停——要一直走到末尾。用一个计数器累加。',
        answer: `**详细讲解**：

**思路**：普通线性查找「一找到就返回」，但统计**出现次数**必须把整个列表看完（因为后面可能还有），所以**不能提前 return**。做法是设一个计数器 count，从头走到尾，每遇到一个等于 target 的元素就 count 加 1。

**参考代码**：
\`\`\`python
def count_target(nums, target):
    count = 0                 # 计数器，记录出现次数
    for x in nums:            # 从头到尾每个都看
        if x == target:       # 遇到一个匹配的
            count = count + 1 # 计数加一（不要 return，要继续看）
    return count              # 看完整个列表后返回总次数
\`\`\`

**执行示例**：nums = [4, 7, 1, 7, 9]、target = 7。遍历中 x=4 不计，x=7 计 1，x=1 不计，x=7 计 2，x=9 不计，最后返回 2。

**复杂度分析**：因为要**必须遍历完整个列表**（不能像查找那样提前停），所以无论 target 在哪、出现几次，都要走 n 步，时间复杂度是 $O(n)$。这里和普通查找的区别是：查找有「最好 $O(1)$」（第一个就中），但计数没有提前结束的机会，每次都是 n 步。`
      },
      {
        problem: '小红写的线性查找如下，她说"测试时老是返回 -1，找不到"。请找出 bug 并改正：\ndef find(nums, target):\n    for i, x in enumerate(nums):\n        if x == target:\n            return i\n        else:\n            return -1',
        hint: '盯住那个 else: return -1。循环第一轮如果没匹配，会发生什么？',
        answer: `**详细讲解**：

**bug 在哪**：问题出在 \`else: return -1\` 写在了**循环体内部**。

**为什么会错**：循环第一轮检查第 0 个元素，如果它不等于 target，就立刻走 else 分支 return -1 —— 函数当场结束了，**根本没机会看第 1 个、第 2 个元素**。也就是说：只要第一个元素不是目标，它就直接报「找不到」。除非目标恰好是第一个，否则永远返回 -1。

**正确做法**：「没找到」不能在循环里下结论，必须等**整个循环都走完**、确认每个元素都不匹配，才能返回 -1。所以 return -1 要放到循环**外面**（后面）。

**参考代码**：
\`\`\`python
def find(nums, target):
    for i, x in enumerate(nums):
        if x == target:
            return i        # 找到就返回下标
    return -1               # 循环全部走完都没找到，才返回 -1
\`\`\`

**复杂度分析**：改对后，最坏情况（目标在末尾或不存在）要比较 n 次，复杂度 $O(n)$，和标准线性查找一致。这个 bug 提醒我们：**「找到」可以提前下结论，「没找到」必须看完全部才能下结论**。`
      },
      {
        problem: '有 1000 个学生的成绩存在一个**乱序**列表里。小明想查"有没有人考了 95 分"。同桌说"用二分查找快"。小明该用线性查找还是二分查找？为什么？',
        hint: '二分查找有一个前提条件。这个列表满足吗？',
        answer: `**详细讲解**：

**结论**：这种情况应该用**线性查找**，不能直接用二分查找。

**原因**：二分查找有一个**硬性前提——数据必须是有序的**。它每次看中间元素，靠「目标比中间大就往右、比中间小就往左」来砍掉一半范围；如果数据是乱的，「中间元素左边一定更小、右边一定更大」这个假设就不成立，二分会跳错方向，得到错误答案。题目明确说成绩是**乱序**的，所以二分用不了。

**两种可行方案对比**：
1. **直接线性查找**：从头到尾扫一遍，最坏比较 1000 次，复杂度 $O(n)$。代码简单，立刻能用。
2. **先排序再二分**：先把 1000 个成绩排好序（$O(n\\log n)$），再二分查找（$O(\\log n)$）。

**怎么选**：因为只查这**一次**，方案 2 光排序就要 $O(n\\log n)$，比方案 1 的 $O(n)$ 还慢，不划算。所以只查一次、数据又是乱的，**线性查找最合适**。

**补充**：什么时候才值得「先排序再二分」？当数据**要被反复查很多次**时——排序只做一次，之后每次查询都只要 $O(\\log n)$，多次查询摊下来就赚了。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 3. 二分查找
  // ───────────────────────────────────────────────────────────────
  'binary-search': {
    warmup: {
      q: '你要在一本厚厚的、按拼音排好序的字典里找「数学」这个词。你会从第一页开始一页一页翻吗？大多数人会怎么翻？这种翻法为什么快？',
      hint: '字典是**有序**的。先翻到中间看看在「数学」前面还是后面，是不是就能一下子排除掉一半？',
      answer: '没人会一页页翻字典——大家会先翻到**大概中间**，看当前页在「数学」之前还是之后：如果当前页排在「数学」前面，就只在后半本找；反之只在前半本找。每翻一次就**砍掉一半**，几下就定位了。这就是**二分查找**的思想：在**有序**数据里，每次看中间元素、淘汰一半范围，速度是 $O(\\log n)$——100 万个数据最多只要约 20 次比较。前提是数据必须**先排好序**。'
    },
    intro: [
      '二分查找，是一种在**有序数据**里飞快定位目标的方法。核心思想是**每次都把搜索范围对半砍**：看中间那个元素，根据它和目标的大小关系，决定下一步只在左半边找还是只在右半边找，从而一次淘汰一半的数据。',
      '它为什么快？因为每比较一次，剩下要找的范围就缩小到原来的一半。$n$ 个数据，最多砍 $\\log_2 n$ 次就剩 1 个——所以复杂度是 $O(\\log n)$。直观感受一下：1024 个数据，最多 10 次（因为 $2^{10}=1024$）；100 万个，最多约 20 次。和线性查找的 $O(n)$ 相比，差距随数据量爆炸式拉大。',
      '它有一个**不可违背的前提：数据必须是有序的**（比如从小到大排好）。因为「目标比中间大就往右、比中间小就往左」这个判断，依赖「中间元素左边都更小、右边都更大」这个有序性质。数据是乱的，二分就会跳错方向、给出错误答案——这时只能用线性查找。',
      '实现上用两个「指针」框出当前的搜索范围：left 指向范围最左、right 指向范围最右。每轮算出中点 mid = (left + right) // 2，比较 nums[mid] 和 target：相等就找到了；nums[mid] 比目标小，说明目标在右半边，把 left 移到 mid+1；nums[mid] 比目标大，目标在左半边，把 right 移到 mid-1。重复到找到、或 left 超过 right（范围空了）为止。',
      '一句话记住：**有序数据找东西，先看中间、每次砍一半，这就是二分查找，$O(\\log n)$。** 写的时候要特别小心两个细节：循环条件是 left <= right（带等号），以及更新边界用 mid+1 / mid-1（不是 mid），否则容易死循环或漏掉元素。'
    ],
    formulas: [
      { name: '时间复杂度', latex: 'O(\\log n)',
        note: '每次比较把范围减半，最多 $\\log_2 n$ 次。$n$ 翻倍只多比较 $1$ 次，所以对大数据极快。' },
      { name: '前提条件', latex: '\\text{数据必须有序（如从小到大）}',
        note: '这是二分能用的**铁律**。无序数据上二分会跳错方向、得到错误结果，只能改用线性查找。' },
      { name: '取中点', latex: '\\text{mid} = (\\text{left} + \\text{right}) // 2',
        note: '用整除 // 取中间下标。left、right 是当前搜索范围的左右边界；mid 是这次要比较的中间位置。' },
    ],
    methods: [
      {
        name: '二分查找步骤（左右指针法）',
        when: '在一个**已排好序**的列表里查找某个目标值，且希望比线性查找更快时',
        steps: [
          '设 left = 0（最左下标）、right = 列表长度 - 1（最右下标），框出整个范围',
          '当 left <= right（范围非空）时循环：算中点 mid = (left + right) // 2',
          '比较 nums[mid] 和 target：相等→找到，返回 mid',
          'nums[mid] < target（中间偏小）→目标在右半边，令 left = mid + 1，舍弃左半',
          'nums[mid] > target（中间偏大）→目标在左半边，令 right = mid - 1，舍弃右半',
          '若循环结束（left > right）仍没找到，返回 -1，表示目标不在列表中',
        ],
        example: {
          problem: '在有序列表 nums = [1, 3, 5, 7, 9, 11] 里用二分查找 7，写出每一轮的 left、right、mid。',
          solution: [
            '解：初始 left=0、right=5（共 6 个元素）。',
            '第 1 轮：mid=(0+5)//2=2，nums[2]=5，5<7（偏小），目标在右半→left=mid+1=3。',
            '第 2 轮：left=3、right=5，mid=(3+5)//2=4，nums[4]=9，9>7（偏大），目标在左半→right=mid-1=3。',
            '第 3 轮：left=3、right=3，mid=3，nums[3]=7，7=7，找到！返回下标 3。',
            '答：只比较了 3 次就找到（线性查找要比 4 次）。数据越多，二分的优势越明显。',
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 写出二分查找并追踪执行',
        problem: '请写一个函数 binary_search(nums, target)，在**已升序排列**的列表 nums 中用二分查找 target，找到返回下标，找不到返回 -1。然后追踪在 nums = [2, 4, 6, 8, 10, 12, 14] 中查找 10 的过程。',
        steps: [
          `第一步：写出代码。用 left、right 框范围，循环条件带等号。
def binary_search(nums, target):
    left = 0                       # 搜索范围最左下标
    right = len(nums) - 1          # 搜索范围最右下标
    while left <= right:           # 范围非空时持续二分
        mid = (left + right) // 2  # 取中间下标
        if nums[mid] == target:    # 正好找到
            return mid
        elif nums[mid] < target:   # 中间偏小，目标在右半
            left = mid + 1
        else:                      # 中间偏大，目标在左半
            right = mid - 1
    return -1                      # 范围空了还没找到`,
          '第二步：理解三种情况。比较中间元素后只有三种走向——相等（找到，返回 mid）、偏小（target 在右边，left 进到 mid+1）、偏大（target 在左边，right 退到 mid-1）。每轮都把范围缩一半。',
          '第三步：追踪 nums = [2,4,6,8,10,12,14]、target=10。初始 left=0、right=6。',
          '第四步：第 1 轮 mid=(0+6)//2=3，nums[3]=8，8<10（偏小）→ left=mid+1=4，范围缩到右半 [10,12,14]。',
          '第五步：第 2 轮 left=4、right=6，mid=(4+6)//2=5，nums[5]=12，12>10（偏大）→ right=mid-1=4，范围缩到 [10]。',
          '第六步：第 3 轮 left=4、right=4，mid=4，nums[4]=10，10=10，找到！返回下标 4。共比较 3 次，而线性查找要比 5 次。',
        ],
      },
    ],
    pitfalls: [
      '**数据没排序就用二分**。这是头号错误。二分的所有判断都建立在「有序」上，乱序数据会跳错方向、给出错误答案。用之前务必确认数据已排序。',
      '循环条件漏了等号，写成 while left < right。当范围只剩一个元素（left == right）时就不检查了，会漏掉这个元素。正确是 **while left <= right**。',
      '更新边界写成 left = mid 或 right = mid（没有 +1/-1）。中间那个 mid 已经比过、确认不是目标，应该排除掉，写 **left = mid + 1**、**right = mid - 1**；写成 mid 会反复比同一个位置，造成死循环。',
      'right 初值写成 len(nums) 而不是 len(nums) - 1。下标最大是「长度减 1」，写成 len(nums) 会越界（访问到不存在的位置）。',
      '把 mid 算成 (left + right) / 2（用了 / 而非 //）。Python 里 / 得到小数，下标必须是整数，应该用整除 **//**。',
    ],
    exercises: [
      {
        problem: '在有序列表 nums = [1, 4, 6, 9, 13, 18, 21] 中用二分查找数字 13，请逐轮写出 left、right、mid 和比较结果，最后给出返回的下标。',
        hint: '初始 left=0、right=6。每轮算 mid=(left+right)//2，比 nums[mid] 和 13 的大小，再决定动 left 还是 right。',
        answer: `**详细讲解**：

**起点**：列表有 7 个元素，下标 0~6，所以初始 left=0、right=6。目标 target=13。

**逐轮追踪**：
- **第 1 轮**：mid=(0+6)//2=3，nums[3]=9。9 < 13（中间偏小），目标在右半 → left=mid+1=4。
- **第 2 轮**：left=4、right=6，mid=(4+6)//2=5，nums[5]=18。18 > 13（中间偏大），目标在左半 → right=mid-1=4。
- **第 3 轮**：left=4、right=4，mid=(4+4)//2=4，nums[4]=13。13 = 13，**找到**！返回下标 4。

**结果**：返回下标 **4**，共比较 3 次。

**复杂度分析**：列表有 7 个元素，二分最多比较 $\\lceil\\log_2 7\\rceil = 3$ 次，这里正好 3 次。如果用线性查找，13 在第 5 个位置，要比较 5 次。元素越多差距越大——这就是 $O(\\log n)$ 对 $O(n)$ 的优势。

**小结**：每一轮都靠「比中间元素」砍掉一半范围，三步就锁定了目标。`
      },
      {
        problem: '小明的二分查找在某些情况下会**死循环**（卡住不停）。请找出 bug 并解释：\ndef bsearch(nums, target):\n    left, right = 0, len(nums) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if nums[mid] == target:\n            return mid\n        elif nums[mid] < target:\n            left = mid       # ← 这里\n        else:\n            right = mid      # ← 这里\n    return -1',
        hint: '当范围缩到只剩两个元素、而 mid 一直算出同一个值时，left 和 right 还会变吗？',
        answer: `**详细讲解**：

**bug 在哪**：两个更新边界的语句写错了——\`left = mid\` 应该是 \`left = mid + 1\`，\`right = mid\` 应该是 \`right = mid - 1\`。

**为什么会死循环**：关键在于 mid 已经被比较过、确认它**不是**目标，却没有把它排除出范围。举个具体例子：当 left=0、right=1 时，mid=(0+1)//2=0。假设 nums[0] < target，走 \`left = mid\` 这一支，于是 left 变成 0 —— **left 根本没变**！下一轮 left 还是 0、right 还是 1，mid 又算成 0……如此反复，范围永远缩不下去，程序就卡死了。

**正确做法**：mid 这个位置既然已经比过、不是答案，就必须**跳过它**：
- 中间偏小，目标在右边 → \`left = mid + 1\`（从 mid 的下一个开始）
- 中间偏大，目标在左边 → \`right = mid - 1\`（到 mid 的前一个为止）

这样每轮范围至少缩小 1，保证循环一定会结束。

**改正后代码**：
\`\`\`python
def bsearch(nums, target):
    left, right = 0, len(nums) - 1
    while left <= right:
        mid = (left + right) // 2
        if nums[mid] == target:
            return mid
        elif nums[mid] < target:
            left = mid + 1     # 排除 mid，向右收缩
        else:
            right = mid - 1    # 排除 mid，向左收缩
    return -1
\`\`\`

**复杂度分析**：改对后每次范围减半，复杂度恢复为 $O(\\log n)$。这是二分查找最经典的坑：**边界更新一定要 +1 / -1，把已比过的 mid 排除掉**。`
      },
      {
        problem: '为什么二分查找一定要求数据"有序"？请用一个**乱序**的具体例子，说明在乱序数据上用二分会出什么错。',
        hint: '二分靠「目标比中间大就往右找」。如果数据是乱的，中间元素右边的就一定都比它大吗？',
        answer: `**详细讲解**：

**核心原因**：二分查找每一步的判断——「目标比中间元素大，就只往右半边找；比中间元素小，就只往左半边找」——**完全依赖一个假设：中间元素左边的都更小、右边的都更大**。这个假设只有在数据**有序**时才成立。数据一旦乱序，这个假设就崩了，二分会把目标所在的那一半直接丢掉。

**乱序反例**：取乱序列表 nums = [5, 2, 8, 1, 9]，要找 target = 1（它真实在下标 3）。
- 第 1 轮：mid=(0+4)//2=2，nums[2]=8。比较 1 < 8（目标比中间小），二分判定「目标在左半边」，于是 right=mid-1=1，把右半边 [1, 9] **整个丢掉**。
- 可是目标 1 恰恰就在被丢掉的右半边（下标 3）！
- 接着只在左半 [5, 2] 里找：mid=(0+1)//2=0，nums[0]=5，1<5 → right=-1，范围空了。
- 最后返回 **-1**，报告「找不到」——但 1 明明在列表里。

**结论**：在乱序数据上，二分会因为「左小右大」的前提不成立而**跳错方向、误删正确的半边**，给出错误答案。

**正确做法对比**：乱序数据要查找，要么直接用**线性查找**（$O(n)$，不挑顺序）；要么**先排序再二分**（排序 $O(n\\log n)$ + 二分 $O(\\log n)$，适合反复多次查询的场景）。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 4. 冒泡排序
  // ───────────────────────────────────────────────────────────────
  'bubble-sort': {
    warmup: {
      q: '一排同学按身高站队，但站乱了。老师说："每个人只跟**旁边**的人比，谁高谁往后站一步。"这样比来比去，最后会发生什么？为什么这个方法叫"冒泡"？',
      hint: '只和相邻的比、高的往后挪。想一想：那个**最高**的同学，会不会一路被往后换、最终站到队尾？',
      answer: '只要相邻两人比、矮的在前高的在后，逆序就交换，那么走一遍下来，**最高的同学一定会一路被换到队尾**——就像水里最大的气泡咕嘟咕嘟「冒」到水面，所以叫**冒泡排序**。第一轮把最大的送到最后，第二轮把第二大的送到倒数第二……$n$ 个人重复 $n-1$ 轮就全排好了。它思路最简单，但要两两比较很多次，复杂度是 $O(n^2)$。'
    },
    intro: [
      '冒泡排序，是最容易理解的排序算法。它的做法是**反复比较相邻的两个元素，发现顺序不对（逆序）就交换**，让较大的元素一步步往后挪。每完整走一轮，当前最大的元素就会「冒」到最末尾，就像气泡上浮，名字由此而来。',
      '它怎么把整个列表排好？**一轮只能确定一个最大值的位置**：第一轮从头比到尾，把最大的送到最后；第二轮再从头比（这次末尾那个已经到位、不用动了），把第二大的送到倒数第二；……如此进行 $n-1$ 轮，每轮固定一个，全部就排好了。',
      '速度上，冒泡排序要用**双层嵌套循环**：外层控制「第几轮」（约 $n$ 轮），内层控制「每轮两两比较」（约 $n$ 次）。两层相乘，比较次数约 $n\\times n$，所以时间复杂度是 $O(n^2)$。数据一多就明显变慢，因此它主要用于教学入门，实战大数据会用快排、归并这类 $O(n\\log n)$ 的算法。',
      '冒泡排序有一个常用优化：如果**某一轮里一次交换都没发生**，说明数据已经完全有序了，可以**提前结束**，不必再跑剩下的轮次。这能让「本来就接近有序」的数据快很多。实现上用一个 swapped 标志记录「这一轮有没有交换过」。',
      '还有一个重要性质：冒泡排序是**稳定的**——值相等的两个元素，排序后它们的先后顺序不会被打乱（因为只有「严格逆序」才交换，相等不交换）。一句话记住：**相邻比较、逆序交换、大的冒到后面，这就是冒泡排序，$O(n^2)$、稳定。**'
    ],
    formulas: [
      { name: '时间复杂度', latex: 'O(n^2)',
        note: '外层约 $n$ 轮、内层每轮约 $n$ 次比较，双层嵌套相乘得 $n^2$。$n$ 翻倍，耗时约变 $4$ 倍。' },
      { name: '排序轮数', latex: 'n - 1',
        note: '$n$ 个元素，每轮确定一个最大值的最终位置，确定 $n-1$ 个后，最后一个自然到位，故跑 $n-1$ 轮。' },
      { name: '稳定性', latex: '\\text{稳定（相等元素相对顺序不变）}',
        note: '只有严格逆序（前 > 后）才交换，相等时不交换，所以相同值的先后关系被保留——这叫稳定排序。' },
    ],
    methods: [
      {
        name: '冒泡排序思路（相邻比较交换）',
        when: '要把一个列表从小到大排好，且重在「把排序原理讲清楚」时',
        steps: [
          '外层循环控制轮数：一共要进行 $n-1$ 轮',
          '内层循环负责「两两比较」：从第 0 个开始，依次比较相邻的 nums[j] 和 nums[j+1]',
          '若前一个比后一个大（逆序），就交换这两个，让大的往后走',
          '每完成一轮，最大的元素就沉到了本轮范围的末尾；下一轮内层比较范围可以少一个（末尾已到位）',
          '（优化）若某一轮一次交换都没有，说明已经有序，提前结束',
        ],
        example: {
          problem: '用冒泡排序把 [5, 3, 8, 1] 从小到大排好，写出第一轮的每次相邻比较。',
          solution: [
            '解：第一轮从头比相邻两个，逆序就交换。',
            '比 (5,3)：5>3 逆序，交换 → [3, 5, 8, 1]；比 (5,8)：5<8 不换 → [3, 5, 8, 1]。',
            '比 (8,1)：8>1 逆序，交换 → [3, 5, 1, 8]。第一轮结束，最大的 8 已冒到末尾。',
            '答：第一轮后变成 [3, 5, 1, 8]。再按同样办法跑后面几轮，最终得到 [1, 3, 5, 8]。',
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 写出冒泡排序并追踪每一轮',
        problem: '请写一个冒泡排序函数 bubble_sort(nums)，把列表从小到大排好。然后追踪对 [4, 2, 5, 1] 排序时，每一轮结束后的列表状态。',
        steps: [
          `第一步：写出代码。外层管轮数，内层管相邻比较与交换。
def bubble_sort(nums):
    n = len(nums)
    # 外层：一共 n-1 轮
    for i in range(n - 1):
        # 内层：每轮两两比较；末尾 i 个已排好，不用再比
        for j in range(n - 1 - i):
            if nums[j] > nums[j + 1]:   # 前面比后面大，逆序
                # 交换相邻两个元素
                nums[j], nums[j + 1] = nums[j + 1], nums[j]
    return nums`,
          '第二步：理解两层循环。外层 i 表示第几轮；内层 range(n-1-i) 让比较范围一轮比一轮短——因为每轮末尾都会多固定一个最大值，已到位的不必再比。',
          '第三步：追踪 [4, 2, 5, 1]，n=4，共 3 轮。',
          '第四步：第 1 轮（i=0，比较 3 次）。(4,2)→换→[2,4,5,1]；(4,5)→不换；(5,1)→换→[2,4,1,5]。本轮后最大的 5 到末尾，列表为 **[2, 4, 1, 5]**。',
          '第五步：第 2 轮（i=1，比较 2 次，末尾 5 不动）。(2,4)→不换；(4,1)→换→[2,1,4,5]。本轮后 4 到位，列表为 **[2, 1, 4, 5]**。',
          '第六步：第 3 轮（i=2，比较 1 次）。(2,1)→换→[1,2,4,5]。列表为 **[1, 2, 4, 5]**，排序完成。总共比较了 3+2+1=6 次，体现了 $O(n^2)$。',
        ],
      },
    ],
    pitfalls: [
      '内层循环越界。比较的是 nums[j] 和 nums[j+1]，j 最大只能到「长度-2」，写 range(n-1-i) 才不会让 j+1 越界访问到不存在的元素。',
      '忘了「每轮末尾已到位」的优化，内层每轮都从头比到尾。虽然结果也对，但多做了无用比较；用 range(n-1-i) 让范围逐轮缩短更高效。',
      '交换写错。Python 交换相邻两个用 nums[j], nums[j+1] = nums[j+1], nums[j]；若分两步写又用错临时变量，可能把两个值都覆盖成同一个。',
      '把「轮数」和「每轮比较次数」搞混，误以为复杂度是 $O(n)$。它是外层 $n$ 轮、内层 $n$ 次**嵌套相乘**，所以是 $O(n^2)$。',
      '以为冒泡排序不稳定。恰恰相反，它是**稳定**的——因为只在「严格大于」时才交换，相等元素不会被交换，相对顺序得以保留。',
    ],
    exercises: [
      {
        problem: '用冒泡排序把 [3, 1, 2] 从小到大排好，请逐轮写出每次相邻比较、是否交换，以及每轮结束后的列表。',
        hint: 'n=3，共 n-1=2 轮。第一轮比 2 次，第二轮比 1 次。每次只比相邻两个，前大后小才交换。',
        answer: `**详细讲解**：

**总览**：列表 [3, 1, 2] 有 3 个元素，所以跑 n-1 = 2 轮。

**第 1 轮**（i=0，从头比相邻两对）：
- 比 (3, 1)：3 > 1，逆序，**交换** → [1, 3, 2]
- 比 (3, 2)：3 > 2，逆序，**交换** → [1, 2, 3]
- 第 1 轮结束，最大的 3 冒到末尾，列表为 **[1, 2, 3]**。

**第 2 轮**（i=1，末尾 3 已到位，只比前两个）：
- 比 (1, 2)：1 < 2，不逆序，**不交换** → [1, 2, 3]
- 第 2 轮结束，列表仍为 **[1, 2, 3]**。

**最终结果**：[1, 2, 3]，排序完成。共比较了 2 + 1 = 3 次。

**复杂度分析**：本例 n=3，比较次数 = 2+1 = 3 次，约等于 $\\frac{n(n-1)}{2}$。一般情况下冒泡排序要做约 $\\frac{n^2}{2}$ 次比较，复杂度 $O(n^2)$。

**小提醒**：第 2 轮其实一次交换都没发生，说明列表已经有序了——如果加上「一轮无交换就提前结束」的优化，这里就能少跑后续轮次。`
      },
      {
        problem: '给冒泡排序加一个优化：如果**某一轮没有发生任何交换**，就提前结束。请写出带这个优化的代码，并说明它能让什么样的数据变快。',
        hint: '设一个标志 swapped，每轮开始设为 False；发生交换就置 True；一轮结束后若它还是 False，就 break。',
        answer: `**详细讲解**：

**优化思路**：如果某一轮从头到尾**一次交换都没发生**，说明所有相邻元素都已经是正确顺序，也就是**整个列表已经有序**了，那后面的轮次再跑也不会有任何改变，可以直接 break 提前结束。用一个布尔标志 swapped 来记录「这一轮有没有交换过」。

**参考代码**：
\`\`\`python
def bubble_sort(nums):
    n = len(nums)
    for i in range(n - 1):
        swapped = False                  # 本轮还没交换过
        for j in range(n - 1 - i):
            if nums[j] > nums[j + 1]:
                nums[j], nums[j + 1] = nums[j + 1], nums[j]
                swapped = True           # 发生了交换
        if not swapped:                  # 这一轮一次都没换
            break                        # 说明已经有序，提前结束
    return nums
\`\`\`

**能让什么数据变快**：对**本来就有序、或接近有序**的数据效果最好。比如对已经排好的 [1,2,3,4,5]，第一轮跑完发现一次都没交换，swapped 仍是 False，直接 break——只用了 1 轮（约 n 次比较）就结束了。

**复杂度分析**：
- **最好情况**（数据已有序）：只跑 1 轮就提前结束，复杂度降到 $O(n)$。
- **最坏情况**（完全逆序，每轮都要交换）：优化用不上，仍是 $O(n^2)$。

所以这个优化没有改变最坏复杂度，但大大改善了「数据接近有序」时的表现。`
      },
      {
        problem: '冒泡排序的复杂度是 $O(n^2)$，而快速排序平均是 $O(n\\log n)$。如果要排序 100 万个数据，为什么一般选快排而不是冒泡？请用具体的运算次数对比说明。',
        hint: '把 n = 100 万 = $10^6$ 分别代入 $n^2$ 和 $n\\log n$ 估算，看两个数字差多少。',
        answer: `**详细讲解**：

**核心原因**：当数据规模 n 很大时，$O(n^2)$ 和 $O(n\\log n)$ 的运算次数差距是天文数字，冒泡会慢到无法接受。

**具体对比**（n = 100 万 = $10^6$）：
- **冒泡排序 $O(n^2)$**：运算次数约 $n^2 = (10^6)^2 = 10^{12}$，也就是**一万亿次**。
- **快速排序 $O(n\\log n)$**：$\\log_2(10^6) \\approx 20$，所以约 $n\\log n = 10^6 \\times 20 = 2\\times10^7$，也就是**两千万次**。

**差距有多大**：$\\dfrac{10^{12}}{2\\times10^7} = 5\\times10^4$，即冒泡要做的运算次数大约是快排的 **5 万倍**！如果快排排完只要 1 秒，冒泡可能要算好几个小时。

**结论**：数据量大时必须选 $O(n\\log n)$ 的快排（或归并），冒泡的 $O(n^2)$ 完全扛不住。

**补充——什么时候冒泡也能用**：数据量很小（几十个以内），或者数据**接近有序**且用了「无交换就提前结束」的优化（此时接近 $O(n)$）。教学场景里也常用冒泡，因为它原理最直观。但凡是大规模排序，工业界一律用 $O(n\\log n)$ 的算法。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 5. 选择排序与插入排序
  // ───────────────────────────────────────────────────────────────
  'selection-insertion-sort': {
    warmup: {
      q: '整理手里的一副扑克牌（按从小到大）。第一种人：每次从乱牌里挑出**最小**的一张，放到左边排好的一摞末尾。第二种人：拿起一张新牌，就把它**插进**左手里已经排好的牌中间的合适位置。这两种整理法，分别像哪种排序？',
      hint: '一种是「每次挑最小的放过去」，一种是「把新牌插到已排好的牌里」。前者叫选择，后者叫插入。',
      answer: '第一种「每次挑出最小的放到已排好那摞的末尾」就是**选择排序**；第二种「把每张新牌插进左手已排好的牌中合适位置」就是**插入排序**。它们和冒泡都是 $O(n^2)$ 级别的简单排序，但思路不同。特别地，**插入排序**对「几乎已经排好」的牌特别快——大部分牌不用怎么挪，所以很多语言内置排序在数据量小或接近有序时会用它。'
    },
    intro: [
      '这一节讲两种和冒泡同属 $O(n^2)$ 级别、但思路各异的简单排序：**选择排序**和**插入排序**。理解它们，既能巩固「排序」的本质，也能为看懂更高级的排序打基础。',
      '**选择排序**的思路是「**每轮选出最小的，放到最前面**」：第一轮在整个列表里找出最小的元素，和第 0 位交换；第二轮在剩下的里找最小的，和第 1 位交换；……每轮固定好一个最小值的位置，$n-1$ 轮后全部有序。它的特点是**交换次数少**（每轮最多换一次），但比较次数固定是 $O(n^2)$，且不管数据原本是否有序都一样慢。',
      '**插入排序**的思路是「**把每个新元素插到前面已排好的部分里的正确位置**」：把列表看成「前面已排好 + 后面待处理」两段。每次拿出待处理的第一个元素，在前面已排好的部分里**从后往前**找到它该待的位置，把比它大的元素都向后挪一格，再把它放进空出来的位置。就像理扑克牌时插新牌。',
      '两者复杂度都是 $O(n^2)$，但有个重要区别：**插入排序对「几乎有序」的数据非常快**。如果数据本来就接近排好，每个新元素只需往前挪一点点甚至不挪，比较和移动都很少——最好情况能接近 $O(n)$。正因如此，很多语言的内置排序在数据量小或接近有序时会切换到插入排序作为优化分支。',
      '稳定性上，**插入排序是稳定的**（相等元素不会被插到对方前面），而**选择排序通常不稳定**（交换时可能把相等元素的相对顺序打乱）。一句话记住：**选择排序——每轮挑最小往前放，交换少；插入排序——把新元素插进已排好的部分，对接近有序的数据特别快、且稳定。**'
    ],
    formulas: [
      { name: '两者时间复杂度', latex: 'O(n^2)',
        note: '选择排序任何情况都是 $O(n^2)$；插入排序最坏（完全逆序）也是 $O(n^2)$，但最好情况例外（见下）。' },
      { name: '插入排序最好情况', latex: 'O(n)',
        note: '当数据**已经有序**时，每个新元素都不用往前挪，只比较一次，总共约 $n$ 次——这是插入排序的最大亮点。' },
      { name: '稳定性对比', latex: '\\text{插入：稳定；选择：通常不稳定}',
        note: '插入排序保留相等元素的先后；选择排序在交换时可能打乱相等元素的相对顺序，故通常不稳定。' },
    ],
    methods: [
      {
        name: '选择排序步骤（每轮选最小）',
        when: '想用「每轮挑出最小值放到前面」的方式排序，或希望交换次数尽量少时',
        steps: [
          '外层循环 i 从 0 到 n-2：表示「现在要确定第 i 位放谁」',
          '在未排好的部分（下标 i 到末尾）里，找出最小元素的下标 min_idx',
          '把 nums[i] 和 nums[min_idx] 交换，让这一轮的最小值就位到第 i 位',
          '重复，直到前 n-1 位都确定，最后一位自然就是最大值，排序完成',
        ],
        example: {
          problem: '用选择排序对 [4, 2, 5, 1] 排序，写出前两轮。',
          solution: [
            '解：每轮在剩余部分找最小、换到最前。',
            '第 1 轮（确定第 0 位）：整段 [4,2,5,1] 最小是 1（下标 3），和第 0 位 4 交换 → [1, 2, 5, 4]。',
            '第 2 轮（确定第 1 位）：剩余 [2,5,4] 最小是 2（已在第 1 位），与自己交换不变 → [1, 2, 5, 4]。',
            '答：继续第 3 轮在 [5,4] 选最小 4 换到前，得 [1,2,4,5]。每轮固定一个最小值。',
          ],
        },
      },
      {
        name: '插入排序步骤（插到已排好部分）',
        when: '想用「把新元素插进前面已排好部分」的方式排序，尤其数据接近有序时',
        steps: [
          '把第 0 个元素看成「已排好的部分」，从第 1 个元素开始逐个处理',
          '取出当前元素 key（待插入的牌），记住它的值',
          '在它前面的已排好部分里**从后往前**比较：凡是比 key 大的元素，都向后挪一格，给 key 腾位置',
          '挪到「前一个不再比 key 大」时停下，把 key 放进空出来的位置',
          '处理下一个元素，重复，直到所有元素都被插入',
        ],
        example: {
          problem: '用插入排序对 [3, 5, 1] 排序，写出每个元素的插入过程。',
          solution: [
            '解：把第 0 个 3 当作已排好。',
            '处理 5：和前面的 3 比，5>3 不用挪，5 留在原位 → 已排好部分 [3, 5]，列表 [3, 5, 1]。',
            '处理 1：和 5 比，5>1，5 后移；和 3 比，3>1，3 后移；前面没有了，1 插到最前 → [1, 3, 5]。',
            '答：结果 [1, 3, 5]。可见 1 因为最小，往前挪了好几步；而 5 因为本来就该在后面，一步没挪。',
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 选择排序代码与执行追踪',
        problem: '请写一个选择排序函数 selection_sort(nums)，把列表从小到大排好，并追踪对 [29, 10, 14, 37, 13] 的前三轮。',
        steps: [
          `第一步：写出代码。外层定位置，内层找最小。
def selection_sort(nums):
    n = len(nums)
    for i in range(n - 1):
        min_idx = i                    # 先假设第 i 位就是最小
        for j in range(i + 1, n):      # 在 i 后面找更小的
            if nums[j] < nums[min_idx]:
                min_idx = j            # 记下更小元素的下标
        # 把找到的最小值换到第 i 位
        nums[i], nums[min_idx] = nums[min_idx], nums[i]
    return nums`,
          '第二步：理解逻辑。每轮先假设 nums[i] 最小（min_idx=i），内层扫描它后面所有元素，遇到更小的就更新 min_idx；扫完后把真正的最小值换到第 i 位。',
          '第三步：追踪 [29, 10, 14, 37, 13]，n=5。',
          '第四步：第 1 轮（i=0）。在全段找最小：10（下标 1）最小，和 29 交换 → **[10, 29, 14, 37, 13]**。',
          '第五步：第 2 轮（i=1）。在 [29,14,37,13] 中找最小：13（下标 4）最小，和 29 交换 → **[10, 13, 14, 37, 29]**。',
          '第六步：第 3 轮（i=2）。在 [14,37,29] 中找最小：14（已在第 2 位）最小，与自己交换不变 → **[10, 13, 14, 37, 29]**。再跑第 4 轮把 29、37 排好，最终 [10,13,14,29,37]。',
        ],
      },
      {
        title: '例题 · 插入排序代码与执行追踪',
        problem: '请写一个插入排序函数 insertion_sort(nums)，并追踪对 [5, 2, 4, 1] 的插入过程。',
        steps: [
          `第一步：写出代码。从第 1 个开始，把每个 key 插进前面已排好的部分。
def insertion_sort(nums):
    n = len(nums)
    for i in range(1, n):
        key = nums[i]                  # 当前要插入的元素
        j = i - 1                      # 从它前一个开始往前看
        # 把所有比 key 大的元素向后挪一格
        while j >= 0 and nums[j] > key:
            nums[j + 1] = nums[j]      # 后移腾位置
            j = j - 1
        nums[j + 1] = key              # 把 key 放进空出的位置
    return nums`,
          '第二步：理解逻辑。key 是待插入的牌；while 循环从 key 前面往左找，凡比 key 大的都后移一格；直到遇到不比 key 大的就停，把 key 填进空位。',
          '第三步：追踪 [5, 2, 4, 1]。第 0 个 5 看作已排好。',
          '第四步：i=1，key=2。j=0，nums[0]=5>2，5 后移 → [5,5,4,1]，j=-1 停；把 2 放到下标 0 → **[2, 5, 4, 1]**。',
          '第五步：i=2，key=4。j=1，nums[1]=5>4，5 后移 → [2,5,5,1]，j=0；nums[0]=2 不大于 4，停；4 放下标 1 → **[2, 4, 5, 1]**。',
          '第六步：i=3，key=1。5>1 后移、4>1 后移、2>1 后移，一路挪到头，j=-1；把 1 放到下标 0 → **[1, 2, 4, 5]**。排序完成。注意 1 最小，挪动最多次。',
        ],
      },
    ],
    pitfalls: [
      '选择排序内层找最小时，更新的应是「下标」min_idx 而不是「值」。最后要靠下标去交换位置，记错成记值就没法交换了。',
      '插入排序的 while 条件漏了 j >= 0。一直往前挪到头时 j 会变成 -1，若不加 j>=0，会用负下标访问到列表末尾，逻辑出错。',
      '插入排序里忘了先把 key 存起来。后移操作 nums[j+1]=nums[j] 会覆盖原值，必须**先**用 key 记下当前元素，最后再放回。',
      '以为插入排序任何情况都比选择排序快。只有数据**接近有序**时插入才有明显优势（最好 $O(n)$）；完全逆序时两者都是 $O(n^2)$。',
      '混淆稳定性。**插入排序稳定**、**选择排序通常不稳定**——选择排序交换非相邻元素时，可能把相等元素的先后顺序打乱。',
    ],
    exercises: [
      {
        problem: '用**选择排序**对 [64, 25, 12, 22] 排序，逐轮写出：每轮在哪段里找最小、最小值是谁、和谁交换、以及每轮后的列表。',
        hint: 'n=4，跑 3 轮。第 i 轮在「下标 i 到末尾」里找最小，换到第 i 位。',
        answer: `**详细讲解**：

**总览**：列表 [64, 25, 12, 22] 有 4 个元素，选择排序跑 n-1 = 3 轮，每轮把当前段的最小值换到最前。

**第 1 轮**（确定下标 0）：
- 在整段 [64, 25, 12, 22] 里找最小 → **12**（下标 2）
- 把第 0 位的 64 和 12 交换 → **[12, 25, 64, 22]**

**第 2 轮**（确定下标 1）：
- 在 [25, 64, 22]（下标 1 到末尾）里找最小 → **22**（下标 3）
- 把第 1 位的 25 和 22 交换 → **[12, 22, 64, 25]**

**第 3 轮**（确定下标 2）：
- 在 [64, 25]（下标 2 到末尾）里找最小 → **25**（下标 3）
- 把第 2 位的 64 和 25 交换 → **[12, 22, 25, 64]**

**最终结果**：[12, 22, 25, 64]，排序完成。

**复杂度分析**：选择排序的比较次数固定为 (n-1)+(n-2)+...+1 = $\\frac{n(n-1)}{2}$，本例是 3+2+1=6 次，复杂度 $O(n^2)$。它的特点是**交换次数少**（每轮最多 1 次，共最多 n-1 次），但比较次数和数据是否有序无关，**任何情况都是** $O(n^2)$。`
      },
      {
        problem: '小明说"插入排序的复杂度就是 $O(n^2)$"。这句话准确吗？请分别说明插入排序在「数据已经有序」和「数据完全逆序」两种情况下的复杂度。',
        hint: '想一想：数据已经排好时，每个新元素 key 的 while 循环会执行几次？完全逆序时又会执行几次？',
        answer: `**详细讲解**：

**这句话不准确**。$O(n^2)$ 只是插入排序的**最坏情况**，它的复杂度要分情况看。关键在于「每个待插入元素 key 需要往前挪几步」——也就是内层 while 循环执行多少次。

**情况一：数据已经有序**（如 [1,2,3,4,5]）——**最好情况**
- 每取一个新元素 key，它前面一个就已经不比它大了（因为已有序），while 循环条件 nums[j] > key 第一次就不成立，**一次都不挪**。
- 每个元素只比较 1 次，n 个元素共约 n 次。
- 复杂度：**$O(n)$**。

**情况二：数据完全逆序**（如 [5,4,3,2,1]）——**最坏情况**
- 每取一个新元素 key，它比前面所有已排好的元素都小，必须一路挪到最前面。
- 第 i 个元素要挪 i 次，总移动约 1+2+...+(n-1) = $\\frac{n(n-1)}{2}$ 次。
- 复杂度：**$O(n^2)$**。

**结论**：插入排序**最好 $O(n)$、最坏 $O(n^2)$、平均 $O(n^2)$**。所以更准确的说法是「插入排序最坏是 $O(n^2)$，但对接近有序的数据可以快到接近 $O(n)$」。

**为什么这点很重要**：正因为插入排序对「几乎有序」的数据特别快，很多语言的内置排序（如对小数组）会用它作为优化分支——这是它比同为 $O(n^2)$ 的冒泡、选择排序更受青睐的原因。`
      },
      {
        problem: '冒泡、选择、插入这三种排序都是 $O(n^2)$，但「稳定性」不同。请说明什么是「稳定排序」，并指出这三种里哪些是稳定的、哪些不是。用一个有相等元素的例子帮助说明。',
        hint: '稳定 = 值相等的两个元素，排序后它们原来的先后顺序不被打乱。重点想想选择排序「交换不相邻元素」时会发生什么。',
        answer: `**详细讲解**：

**什么是稳定排序**：如果列表里有**值相等**的元素，排序后这些相等元素的**相对先后顺序保持不变**，就称这个排序是「稳定的」；如果可能被打乱，就是「不稳定的」。

**举例说明**：假设给一组成绩排序，写成 (分数, 学生) 的形式：[(90, 甲), (80, 乙), (90, 丙)]。这里甲和丙都是 90 分，且甲排在丙前面。按分数从小到大排序后：
- **稳定排序**的结果：[(80, 乙), (90, 甲), (90, 丙)] —— 甲仍在丙前面，原顺序保留。
- **不稳定排序**可能：[(80, 乙), (90, 丙), (90, 甲)] —— 甲丙顺序被颠倒了。

**三种排序的稳定性**：
- **冒泡排序：稳定** ✓ 只在「前 > 后」严格逆序时才交换，相等不交换，所以相等元素顺序不变。
- **插入排序：稳定** ✓ 插入时遇到相等元素会停在它后面（while 用的是「严格大于」），不会插到相等元素前面。
- **选择排序：通常不稳定** ✗ 它每轮把最小值和第 i 位**直接交换**，这个交换可能跨过中间的相等元素，把相等元素的先后顺序打乱。

**举个选择排序不稳定的例子**：[(90, 甲), (90, 丙), (80, 乙)]。第 1 轮找到最小的 80（乙），和第 0 位的 (90, 甲) 交换 → [(80, 乙), (90, 丙), (90, 甲)]。结果甲被换到了丙后面，原来「甲在丙前」的顺序被破坏了——这就是选择排序不稳定的典型表现。

**小结**：冒泡、插入稳定；选择通常不稳定。复杂度都是 $O(n^2)$，但当「相等元素的先后顺序有意义」时（比如多关键字排序），就要选稳定的排序。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 6. 快速排序与归并排序
  // ───────────────────────────────────────────────────────────────
  'quick-merge-sort': {
    warmup: {
      q: '要给全年级 1000 个人按身高排队，一个老师忙不过来。聪明的做法是：先按「比 1 米 5 高 / 矮」把人分成两堆，每堆再交给两个组长用同样办法继续分……或者：先让每个小组各自排好，再把排好的小组两两合并成更大的有序队伍。这两种「分而治之」的思路，正是两大高效排序的核心。你觉得它们为什么比一个一个比要快？',
      hint: '关键词是「分」——把一个大问题拆成两个规模减半的小问题。一个大问题拆成两半、再拆两半……拆几次就到底了？',
      answer: '把大问题不断**对半拆成小问题**，分别解决再合起来，这叫**分治思想**。两大高效排序都用它：**快速排序**先选一个「基准」，把比它小的甩到左边、大的甩到右边，再对左右两半递归同样操作；**归并排序**先把数组对半拆到只剩 1 个，再把有序的小段两两合并成大段。因为每次规模减半，拆 $\\log n$ 层就到底，每层处理 $n$ 个元素，所以平均都是 $O(n\\log n)$——远快于冒泡/选择/插入的 $O(n^2)$，是工业级排序的主流。'
    },
    intro: [
      '这一节讲两种**工业级**的高效排序：**快速排序**和**归并排序**。它们平均都能做到 $O(n\\log n)$，远快于前面那些 $O(n^2)$ 的简单排序，是实际编程中大规模排序的主流方案。它们共同的灵魂是**分治思想**——把一个大问题拆成规模更小的同类小问题，分别解决，再合并。',
      '**快速排序**的做法：从数组里选一个元素作**基准（pivot）**，把比基准小的都放到它左边、比基准大的都放到右边（这一步叫「划分」），这样基准就落到了它最终该在的位置；然后对左半部分和右半部分**分别递归**做同样的事。当每一段都只剩 0 或 1 个元素时，整个数组就排好了。它的「分」很费力（要划分），但「合」几乎不用做。',
      '**归并排序**的做法正好相反，「分」很简单、「合」是重头戏：先把数组从中间对半拆开，再对每一半继续对半拆，一直拆到每段只剩 1 个元素（一个元素天然有序）；然后**两两合并**——把两个已经有序的小段，像拉拉链一样比较着合成一个更大的有序段，一层层合并回去，最终得到完整的有序数组。',
      '速度上，两者**平均都是 $O(n\\log n)$**：可以理解为「拆了约 $\\log n$ 层，每一层都要处理全部 $n$ 个元素」，相乘就是 $n\\log n$。不过它们有重要差异——快排在**最坏情况下会退化到 $O(n^2)$**（当每次选的基准都恰好是最大或最小值，划分极不均衡时），而归并排序**任何情况都稳定保持 $O(n\\log n)$**，但它需要额外的辅助空间（$O(n)$）来做合并。',
      '一句话记住：**快排——选基准、小左大右、递归两半，平均 $O(n\\log n)$ 但最坏 $O(n^2)$；归并——对半拆到底、有序段两两合并，稳定 $O(n\\log n)$ 但费额外空间。** 它们都是「递归 + 分治」的经典应用，理解它们也就理解了分治的威力。'
    ],
    formulas: [
      { name: '两者平均复杂度', latex: 'O(n\\log n)',
        note: '拆约 $\\log n$ 层、每层处理 $n$ 个元素，相乘得 $n\\log n$。这是基于比较的排序能达到的最好平均水平。' },
      { name: '快排最坏复杂度', latex: 'O(n^2)',
        note: '当每次选的基准都恰是最值、划分极不均衡（一边空、一边几乎全部）时，快排退化成 $O(n^2)$。随机选基准可大幅降低这种概率。' },
      { name: '归并排序空间复杂度', latex: 'O(n)',
        note: '归并合并时需要一块和原数组差不多大的辅助空间来暂存结果，所以额外空间是 $O(n)$；快排原地划分则省空间。' },
    ],
    methods: [
      {
        name: '快速排序思路（选基准 + 划分 + 递归）',
        when: '要对大规模数据高效排序、且对额外空间敏感时（快排可原地排，省空间）',
        steps: [
          '若这段只有 0 或 1 个元素，已经有序，直接返回（这是递归的终止条件）',
          '从这段里选一个元素作为**基准 pivot**（比如选第一个或中间那个）',
          '划分：遍历其余元素，比基准小的归到「左组」、比基准大（或相等）的归到「右组」',
          '对「左组」和「右组」**分别递归**调用快排，把它们各自排好',
          '把「排好的左组 + 基准 + 排好的右组」拼起来，就是这段排序的结果',
        ],
        example: {
          problem: '用快速排序对 [5, 3, 8, 1, 4] 排序（选第一个元素 5 作基准），写出第一层划分。',
          solution: [
            '解：基准选 5。遍历其余 [3,8,1,4]，和 5 比较分组。',
            '比 5 小的：3、1、4 → 左组 [3, 1, 4]；比 5 大的：8 → 右组 [8]。',
            '此时 5 已落到正确位置：左组都比它小、右组都比它大。再对左组 [3,1,4] 递归（基准 3 → 左 [1]、右 [4]，得 [1,3,4]），右组 [8] 已有序。',
            '答：拼起来 [1,3,4] + [5] + [8] = [1, 3, 4, 5, 8]。',
          ],
        },
      },
      {
        name: '归并排序思路（对半拆 + 合并）',
        when: '要求**任何情况**都稳定 $O(n\\log n)$（不接受快排最坏退化），且能接受额外空间时',
        steps: [
          '若这段只有 0 或 1 个元素，已经有序，直接返回（递归终止条件）',
          '找到中间位置，把这段**对半拆成左、右两半**',
          '对左半、右半**分别递归**做归并排序，得到两个各自有序的小段',
          '合并：用两个指针分别指向左、右段开头，每次比较两个指针所指元素，把较小的放进结果、该指针后移',
          '某一段取完后，把另一段剩下的（已有序）直接接到结果末尾，得到合并后的有序段',
        ],
        example: {
          problem: '用归并排序对 [3, 1, 4, 2] 排序，写出拆分和合并过程。',
          solution: [
            '解：先对半拆。[3,1,4,2] → 左 [3,1]、右 [4,2]。',
            '继续拆：[3,1]→[3]、[1]；[4,2]→[4]、[2]。都只剩 1 个，天然有序。',
            '往回合并：[3] 与 [1] 合并→比较 3、1，先放 1 再放 3 → [1,3]；[4] 与 [2] 合并→ [2,4]。',
            '答：最后合并 [1,3] 与 [2,4]：比较 1<2 放 1、比较 3>2 放 2、比较 3<4 放 3、放 4 → [1, 2, 3, 4]。',
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 快速排序代码与执行追踪',
        problem: '请写一个快速排序函数 quick_sort(nums)（用「新建左右列表」的简洁写法），并追踪对 [5, 2, 7, 1, 3] 的排序过程。',
        steps: [
          `第一步：写出代码。用列表推导分出左右两组，再递归。
def quick_sort(nums):
    # 终止条件：0 或 1 个元素已有序
    if len(nums) <= 1:
        return nums
    pivot = nums[0]                              # 选第一个作基准
    rest = nums[1:]                              # 其余元素
    left = [x for x in rest if x < pivot]        # 比基准小的
    right = [x for x in rest if x >= pivot]      # 比基准大或相等的
    # 递归排好左右，再拼成「左 + 基准 + 右」
    return quick_sort(left) + [pivot] + quick_sort(right)`,
          '第二步：理解逻辑。每次拿第一个当基准，把其余元素按「小于基准」和「不小于基准」分成 left、right 两组；递归把两组各自排好，再用「左 + [基准] + 右」拼接。基准每次都落到正确位置。',
          '第三步：追踪 quick_sort([5, 2, 7, 1, 3])。基准 pivot=5，其余 [2,7,1,3]。',
          '第四步：划分。比 5 小的 left=[2,1,3]；不小于 5 的 right=[7]。返回 quick_sort([2,1,3]) + [5] + quick_sort([7])。',
          '第五步：递归左组 quick_sort([2,1,3])。基准 2，left=[1]、right=[3]；返回 quick_sort([1]) + [2] + quick_sort([3]) = [1] + [2] + [3] = [1,2,3]。右组 quick_sort([7]) 长度 1，直接返回 [7]。',
          '第六步：拼接得最终结果 [1,2,3] + [5] + [7] = **[1, 2, 3, 5, 7]**。整个过程就是「不断选基准、小左大右、递归再拼」。',
        ],
      },
      {
        title: '例题 · 归并排序「合并」步骤追踪',
        problem: '归并排序的核心是「合并两个有序段」。请写出合并函数 merge(a, b)（把两个已升序的列表合成一个升序列表），并追踪 merge([1, 4, 6], [2, 3, 5]) 的过程。',
        steps: [
          `第一步：写出合并代码。用两个指针 i、j 像拉拉链一样比较。
def merge(a, b):
    result = []
    i, j = 0, 0
    # 两段都还有元素时，比较各自当前元素，小的先进结果
    while i < len(a) and j < len(b):
        if a[i] <= b[j]:
            result.append(a[i])
            i = i + 1
        else:
            result.append(b[j])
            j = j + 1
    # 某段取完后，把另一段剩下的（已有序）直接接上
    result.extend(a[i:])
    result.extend(b[j:])
    return result`,
          '第二步：理解逻辑。i 指向 a 的当前元素、j 指向 b 的当前元素；每轮比较 a[i] 和 b[j]，把较小的放进 result 并让对应指针前移；一段走完后另一段剩余部分必然都更大且有序，直接接到末尾。',
          '第三步：追踪 merge([1,4,6], [2,3,5])。初始 result=[]，i=0、j=0。',
          '第四步：比 a[0]=1 与 b[0]=2，1≤2 → 放 1，i=1，result=[1]。比 a[1]=4 与 b[0]=2，4>2 → 放 2，j=1，result=[1,2]。比 a[1]=4 与 b[1]=3，4>3 → 放 3，j=2，result=[1,2,3]。',
          '第五步：比 a[1]=4 与 b[2]=5，4≤5 → 放 4，i=2，result=[1,2,3,4]。比 a[2]=6 与 b[2]=5，6>5 → 放 5，j=3，result=[1,2,3,4,5]。此时 j 到末尾，b 取完。',
          '第六步：b 已空，把 a 剩下的 a[2:]=[6] 接上 → **[1, 2, 3, 4, 5, 6]**。合并完成。这「拉拉链式比较」每个元素只看一次，合并 n 个元素是 $O(n)$，这也是归并每层 $O(n)$、共 $\\log n$ 层、总 $O(n\\log n)$ 的来源。',
        ],
      },
    ],
    pitfalls: [
      '**快排忘了写递归终止条件**（len <= 1 时返回）。没有终止条件，递归会无限拆下去直到爆栈（栈溢出），程序崩溃。',
      '快排划分时把「等于基准」的元素分类不一致，导致基准自己被重复放进左右组，可能元素丢失或死循环。约定好相等归哪边（如归右组 x >= pivot），且基准只单独放一次。',
      '以为快排「永远」是 $O(n\\log n)$。当基准每次都恰好选到最值（如对已经有序的数据每次选第一个），划分极不均衡，会退化到**最坏 $O(n^2)$**。随机选基准能大幅降低这种概率。',
      '归并的合并写漏了「接上剩余部分」。while 循环只在两段都还有元素时进行，循环结束后必有一段还剩元素没放，**必须把剩余部分接到结果末尾**，否则会丢数据。',
      '混淆两者的空间开销。归并排序需要 $O(n)$ 辅助空间来合并；快排可以原地划分、空间更省。空间敏感选快排，要求最坏也稳定选归并。',
    ],
    exercises: [
      {
        problem: '用**快速排序**对 [6, 2, 8, 4, 3] 排序，选第一个元素作基准。请写出第一层划分（基准是谁、左组右组各是什么），并说明基准这一步落到了哪个最终位置。',
        hint: '基准 = 第一个元素。遍历其余元素，比基准小的进左组，大的进右组。基准最终在「左组所有元素之后、右组所有元素之前」。',
        answer: `**详细讲解**：

**第一层划分**：
- **基准 pivot** = 第一个元素 = **6**
- 其余元素：[2, 8, 4, 3]，逐个和 6 比较：
  - 2 < 6 → 进**左组**
  - 8 > 6 → 进**右组**
  - 4 < 6 → 进左组
  - 3 < 6 → 进左组
- **左组（比 6 小）** = [2, 4, 3]
- **右组（比 6 大）** = [8]

**基准落到哪个位置**：划分后，左组有 3 个元素都比 6 小、右组有 1 个元素比 6 大。所以 6 的最终位置就是「左边 3 个之后、右边之前」，即排好序后的**下标 3**（第 4 个位置）。验证：最终结果是 [2, 3, 4, 6, 8]，6 确实在下标 3。这一步**基准已经一步到位、不会再移动**，这是快排的关键特点。

**接下来**：对左组 [2,4,3] 递归（基准 2 → 左 []、右 [4,3]，右组再以 4 为基准得 [3,4]，合起来 [2,3,4]），右组 [8] 已有序。最终拼成 [2,3,4] + [6] + [8] = **[2, 3, 4, 6, 8]**。

**复杂度分析**：快排平均 $O(n\\log n)$。本例每次划分比较均衡，效率高。但要注意：如果数据本来就有序、又总选第一个作基准，划分会极不均衡（一边总是空），退化成最坏 $O(n^2)$——这时应改用「随机选基准」。`
      },
      {
        problem: '归并排序的核心步骤是「合并两个有序列表」。请手工追踪把 [2, 5, 8] 和 [1, 3, 9] 合并成一个升序列表的过程，逐步写出每次比较谁、放入谁，以及最终结果。',
        hint: '用两个指针分别指向两个列表的开头，每次比较两个指针所指的数，小的放进结果、那个指针前移。某个列表空了就把另一个剩下的接上。',
        answer: `**详细讲解**：

**方法**：设指针 i 指向 a=[2,5,8] 的开头，指针 j 指向 b=[1,3,9] 的开头。每次比较 a[i] 和 b[j]，把**较小的**放进结果，对应指针后移。

**逐步追踪**（result 初始为空）：
1. 比较 a[0]=2 与 b[0]=1：**1 更小**，放 1。result=[1]，j 移到 1。
2. 比较 a[0]=2 与 b[1]=3：**2 更小**，放 2。result=[1,2]，i 移到 1。
3. 比较 a[1]=5 与 b[1]=3：**3 更小**，放 3。result=[1,2,3]，j 移到 2。
4. 比较 a[1]=5 与 b[2]=9：**5 更小**，放 5。result=[1,2,3,5]，i 移到 2。
5. 比较 a[2]=8 与 b[2]=9：**8 更小**，放 8。result=[1,2,3,5,8]，i 移到 3。
6. a 已经取完（i 到末尾），把 b 剩下的 [9] 直接接上。result=**[1, 2, 3, 5, 9... ]**

等一下，核对一下：第 5 步放了 8 之后 result=[1,2,3,5,8]，b 还剩 b[2]=9 没放，a 已空，接上 9。

**最终结果**：**[1, 2, 3, 5, 8, 9]**。

**复杂度分析**：合并时每个元素只被「看一次、放一次」，两段共 n 个元素，所以一次合并是 $O(n)$。归并排序就是「不断对半拆（约 $\\log n$ 层）+ 每层把所有元素合并一遍（$O(n)$）」，总复杂度 $O(n\\log n)$，而且**任何情况都稳定保持**这个速度，不会像快排那样退化。代价是合并需要额外的 $O(n)$ 空间来存结果。`
      },
      {
        problem: '快速排序和归并排序平均都是 $O(n\\log n)$。请从「最坏情况」和「额外空间」两个角度比较它们的区别，并说明：如果要排序的数据**可能已经基本有序**，直接用「每次选第一个元素作基准」的快排会有什么风险？',
        hint: '快排最坏会退化，归并不会；归并费空间，快排省空间。已经有序的数据，每次选第一个作基准会怎样划分？',
        answer: `**详细讲解**：

**一、最坏情况对比**
- **快速排序**：平均 $O(n\\log n)$，但**最坏会退化到 $O(n^2)$**。退化发生在每次选的基准都恰好是当前段的最大值或最小值时——划分极不均衡，一边几乎是空的、另一边几乎是全部，相当于每次只排好一个元素，跟冒泡一样慢。
- **归并排序**：**任何情况都稳定保持 $O(n\\log n)$**，没有退化风险。因为它的拆分永远是严格对半，与数据内容无关。

**二、额外空间对比**
- **快速排序**：可以**原地**划分（在原数组上交换），额外空间小（$O(\\log n)$ 量级，主要是递归栈）。
- **归并排序**：合并时需要一块和原数组差不多大的辅助数组来暂存结果，**额外空间 $O(n)$**，更费内存。

**三、对「基本有序」数据用「选第一个作基准」的风险**
- **风险很大**。如果数据已经基本有序（如 [1,2,3,4,5,...]），每次选第一个元素作基准，那个基准恰好是（接近）当前段的**最小值**——划分后左组几乎是空的、右组几乎是全部。
- 这正是快排**最坏情况**的触发条件，复杂度会从 $O(n\\log n)$ **退化到 $O(n^2)$**，本来很快的快排反而变得奇慢。
- **而且**递归会拆得很深（接近 n 层），还可能**爆栈**。

**如何避免**：① 改用**随机选基准**（每次随机挑一个元素当 pivot），让「总是选到最值」几乎不可能发生；② 或用「三数取中」选基准；③ 或干脆改用**归并排序**，它对任何数据都稳定 $O(n\\log n)$，不怕这种退化。

**结论**：要求「最坏也不退化、稳定」选归并（代价是费空间）；追求「省空间、平均最快」选快排，但**务必随机化基准**，尤其当数据可能已经有序时。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 7. 贪心算法
  // ───────────────────────────────────────────────────────────────
  'greedy': {
    warmup: {
      q: '收银员要找给顾客 36 元，手头有 20 元、10 元、5 元、1 元的纸币。怎样找，用的张数最少？大多数收银员想都不想就先拿最大的——为什么这样做又快又对？这种"每次都先拿最大"的策略，永远是对的吗？',
      hint: '先拿一张 20（剩 16），再拿一张 10（剩 6），再拿一张 5（剩 1），再拿一张 1。每一步都「能拿多大就拿多大」。但如果货币面额很奇怪，这招还灵吗？',
      answer: '收银员用的是**贪心算法**：每一步都做**当前看起来最好的选择**（找零时就是「每次拿不超过剩余金额的最大面额」），不回头、不考虑全局。36 元 = 20+10+5+1，4 张，又快又对。它的优点是**简单、快**。但**贪心不一定总能得到最优解**——比如面额若是 1、5、6、9，要找 11，贪心先拿 9 再拿两个 1（3 张），但其实 5+6（2 张）更优。所以贪心好用，但只在「局部最优能导致全局最优」的问题上才正确。'
    },
    intro: [
      '贪心算法，是一种「**每一步都选当前看起来最好的那个选项**，并且不反悔」的解题策略。它不像后面要学的动态规划那样把所有可能都考虑一遍，而是走一步看一步、每步都抓眼前最优，因此**简单、快速**。',
      '它的基本套路是：把一个大问题拆成一连串「选择」，每次都按某个「贪心标准」挑出当前最优的那个，做完这个选择就**绝不回头修改**，接着做下一个选择，直到问题解决。比如找零钱时，贪心标准是「每次拿不超过剩余金额的最大面额」；安排活动时，贪心标准常是「每次选结束最早的活动」。',
      '贪心最大的特点（也是最大的陷阱）是：**它不一定能得到全局最优解**。因为它只看眼前、不顾长远，有时「每一步都最优」拼起来反而不是「整体最优」。它只在具有**「贪心选择性质」**的问题上才正确——即「每步的局部最优选择，确实能导向全局最优」。找零（在常见货币体系下）、活动选择、最小生成树等问题满足这个性质，贪心就对；很多别的问题不满足，贪心就会得到次优解。',
      '所以用贪心前，关键是**判断这个问题适不适合贪心**。常用的检验办法是「举反例」：试着构造一个例子，看「每步贪心」得到的结果是不是真的最优；如果能找到一个贪心失手的反例，就说明这个问题不能用贪心（要改用动态规划等方法）。',
      '一句话记住：**贪心 = 每步抓眼前最优、不回头，简单又快，但不一定全局最优。** 它和动态规划是对照着学的——贪心走一条路、赌它最优；动态规划则系统地考虑所有子问题、保证最优。能用贪心（且正确）时就用，因为它更快；不确定时，先举反例验证。'
    ],
    formulas: [
      { name: '贪心核心思想', latex: '\\text{每一步选当前最优，且不反悔}',
        note: '把问题拆成一系列选择，每次按贪心标准挑当前最好的，做完不回头改。简单、快。' },
      { name: '正确的前提', latex: '\\text{局部最优} \\Rightarrow \\text{全局最优}',
        note: '只有当「每步的局部最优能导向全局最优」（贪心选择性质）时，贪心才正确，否则只得次优解。' },
      { name: '典型复杂度', latex: 'O(n\\log n)\\ \\text{或}\\ O(n)',
        note: '贪心常需先排序（如按结束时间、按价值），排序是 $O(n\\log n)$；之后扫一遍做选择是 $O(n)$。比穷举快得多。' },
    ],
    methods: [
      {
        name: '贪心解题四步法',
        when: '问题可拆成一连串选择、且怀疑「每步选当前最优」就能得到整体最优时',
        steps: [
          '确定贪心标准：每一步「当前最优」按什么来挑（最大面额？最早结束？单位价值最高？）',
          '（常需）先按这个标准给数据排序，方便每步直接取「当前最优」',
          '从头扫一遍，每步按标准做出选择并累计结果，做完不回头修改',
          '验证正确性：举几个例子（尤其极端例子）看贪心结果是否真最优；找不到反例再放心用',
        ],
        example: {
          problem: '有面额 1、5、10、20 元的纸币若干，要找零 48 元，用贪心求最少张数。',
          solution: [
            '解：贪心标准——每次拿不超过剩余金额的最大面额。',
            '剩 48：拿 20（剩 28）；剩 28：拿 20（剩 8）；剩 8：拿 5（剩 3）；剩 3：拿 1、1、1（剩 0）。',
            '统计：20×2 + 5×1 + 1×3，共 2+1+3=6 张。',
            '答：最少 6 张。在 1、5、10、20 这种「整齐」的货币体系下，贪心能得到最优解。',
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 贪心找零（代码 + 执行追踪）',
        problem: '请写一个贪心找零函数 min_coins(amount, coins)，coins 是从大到小排好的面额列表，返回找零所需的最少纸币数。然后追踪 amount=63、coins=[20,10,5,1] 的执行过程。',
        steps: [
          `第一步：写出代码。从大面额开始，每种尽量多拿。
def min_coins(amount, coins):
    count = 0                      # 已用纸币张数
    for coin in coins:            # 面额从大到小依次尝试
        # 当前面额能拿几张：剩余金额整除该面额
        num = amount // coin
        count = count + num       # 累加张数
        amount = amount - num * coin  # 扣掉这部分金额
    return count`,
          '第二步：理解逻辑。coins 已从大到小排好；对每种面额，用整除 amount // coin 算出「这种面额最多能拿几张」，累加到 count，并从 amount 里扣掉。处理完所有面额，amount 应减到 0。',
          '第三步：追踪 amount=63、coins=[20,10,5,1]。初始 count=0。',
          '第四步：coin=20。num=63//20=3，拿 3 张；count=3；amount=63-3×20=3。',
          '第五步：coin=10。num=3//10=0，拿 0 张；count=3；amount=3。coin=5。num=3//5=0，拿 0 张；count=3；amount=3。',
          '第六步：coin=1。num=3//1=3，拿 3 张；count=3+3=6；amount=3-3=0。返回 **6**。即 20×3 + 1×3 = 63，共 6 张。这就是「每步拿尽量大的面额」的贪心。',
        ],
      },
    ],
    pitfalls: [
      '**盲目相信贪心一定最优**。贪心只在「局部最优能导向全局最优」的问题上才对。用之前要么证明它对，要么举不出反例；否则可能得到次优解。',
      '没意识到货币面额会让贪心失效。在 1、5、10、20 这种「整齐」面额下找零，贪心最优；但面额奇怪时（如 1、5、6、9 找 11），贪心会失手，要改用动态规划。',
      '贪心标准选错。同一问题换个贪心标准结果可能天差地别。如活动选择应按「结束时间最早」贪心，若错按「开始最早」或「时间最短」就可能不是最优。',
      '做完选择又回头修改。贪心的精髓是「每步定了就不反悔」；如果需要回头调整、综合考虑，那其实该用动态规划，不是贪心。',
      '把贪心和动态规划搞混。贪心走一条路、赌它最优、快；动态规划系统考虑所有子问题、保证最优、相对慢。该用哪个取决于问题性质。',
    ],
    exercises: [
      {
        problem: '用贪心算法求找零 87 元的最少纸币数，面额有 50、20、10、5、1 元。请逐步写出每种面额拿几张、剩余多少，最后给出总张数。',
        hint: '从最大面额 50 开始，每种「能拿几张就拿几张」（用整除），拿完扣掉金额再处理下一种。',
        answer: `**详细讲解**：

**贪心标准**：从最大面额开始，每种面额尽量多拿（拿到不超过剩余金额为止），再处理下一种小面额。

**逐步求解**（剩余金额从 87 开始）：
- **50 元**：87 ÷ 50 = 1 张（余 37）。拿 **1 张 50**，剩 87 − 50 = 37。
- **20 元**：37 ÷ 20 = 1 张（余 17）。拿 **1 张 20**，剩 37 − 20 = 17。
- **10 元**：17 ÷ 10 = 1 张（余 7）。拿 **1 张 10**，剩 17 − 10 = 7。
- **5 元**：7 ÷ 5 = 1 张（余 2）。拿 **1 张 5**，剩 7 − 5 = 2。
- **1 元**：2 ÷ 1 = 2 张。拿 **2 张 1**，剩 2 − 2 = 0。

**总张数**：1 + 1 + 1 + 1 + 2 = **6 张**。即 50×1 + 20×1 + 10×1 + 5×1 + 1×2 = 87 元。

**复杂度分析**：设面额有 m 种，贪心只需对每种面额做一次整除和扣减，复杂度 $O(m)$（若面额未排序，需先排序 $O(m\\log m)$）。这比「穷举所有凑法」快得多。

**补充**：在 50、20、10、5、1 这种「整齐」的货币体系下，贪心保证得到最优（最少张数）解。这正是贪心适用的典型场景。`
      },
      {
        problem: '小明说"找零钱用贪心（每次拿最大面额）一定能用最少的纸币"。请举一个**反例**证明他错了——构造一组面额和一个金额，让贪心拿的张数比最优解多。',
        hint: '货币面额不一定要「整齐」。试试面额 1、5、6、要凑出 10，看贪心和最优各用几张。',
        answer: `**详细讲解**：

**小明错了**，贪心找零**不一定**最优，取决于面额是否「整齐」。下面构造反例。

**反例**：面额为 **1、5、6**，要找零 **10 元**。

**贪心的做法**（每次拿不超过剩余的最大面额）：
- 剩 10：拿一张 6（剩 4）
- 剩 4：6 太大、5 太大，拿 4 张 1（剩 0）
- 贪心结果：6×1 + 1×4 = **5 张**

**最优解**：
- 拿两张 5：5 + 5 = 10
- 最优结果：5×2 = **2 张**

**对比**：贪心用了 5 张，而最优只要 2 张——贪心多用了 3 张！所以「每次拿最大面额」在这组面额下**不是最优**。

**为什么会这样**：贪心一上来贪心地拿了最大的 6，结果剩下的 4 只能用 4 张 1 来凑，反而吃了亏。它「只顾眼前最大」，没有「全局考虑」用两张 5 更划算。

**复杂度与正确做法**：当面额「整齐」（如我国的 1、5、10、20、50、100）时贪心最优且很快（$O(m)$）；但面额任意时贪心可能失手，要改用**动态规划**求最少张数——动态规划会系统考虑所有凑法，保证最优（代价是比贪心慢）。

**结论**：这道题说明了贪心的核心局限——**简单快速，但不保证全局最优**，用前必须确认问题满足「局部最优导向全局最优」。`
      },
      {
        problem: '有 n 个活动，每个有开始时间和结束时间，同一时间只能参加一个活动（时间不能重叠），问最多能参加几个？这是经典的「活动选择问题」，可以用贪心解决。请说明应该用什么**贪心标准**，并简述理由。',
        hint: '想让能参加的活动尽量多，每次选完一个活动后，应该让它尽早结束，好给后面留出更多时间。所以按什么排序来贪心？',
        answer: `**详细讲解**：

**贪心标准**：**每次都选「在不冲突的前提下，结束时间最早」的活动**。具体做法是先把所有活动**按结束时间从早到晚排序**，然后从头扫：第一个活动一定选；之后每个活动，如果它的开始时间 ≥ 上一个已选活动的结束时间（不冲突），就选它，并更新「上一个结束时间」。

**为什么是「结束时间最早」而不是别的标准**：
- 核心思想：**一个活动越早结束，就给后面剩下越多的时间去安排更多活动**。所以每次都贪心地选「结束最早」的，能为后续保留最大空间，从而参加尽可能多的活动。
- **对比错误标准**：
  - 按「**开始时间最早**」选：可能先选了一个很早开始但拖到很晚才结束的活动，把后面一大片时间都占了，反而参加得少。
  - 按「**持续时间最短**」选：短活动也可能卡在中间，挡住它两边本可以连续安排的活动。
  - 只有「**结束时间最早**」能被严格证明得到最优解。

**参考思路代码**：
\`\`\`python
def max_activities(activities):
    # activities 是 (开始, 结束) 列表；按结束时间排序
    activities.sort(key=lambda a: a[1])
    count = 0
    last_end = 0                    # 上一个选中活动的结束时间
    for start, end in activities:
        if start >= last_end:       # 和上一个不冲突
            count = count + 1       # 选这个活动
            last_end = end          # 更新结束时间
    return count
\`\`\`

**复杂度分析**：主要开销是排序 $O(n\\log n)$，之后扫一遍做选择是 $O(n)$，总复杂度 $O(n\\log n)$。这比「枚举所有活动子集」（$O(2^n)$）快得多。

**小结**：活动选择问题满足「贪心选择性质」（局部选结束最早 → 全局活动最多），所以贪心正确且高效。选对贪心标准是关键——这里必须是「结束时间最早」。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 8. 动态规划入门
  // ───────────────────────────────────────────────────────────────
  'dp-intro': {
    warmup: {
      q: '用递归算斐波那契数列 fib(n) = fib(n-1) + fib(n-2)。算 fib(5) 时，你会发现 fib(3) 被算了好几遍、fib(2) 被算了更多遍……同一个小问题反复重算。有没有办法「算过一次就记下来，下次直接用」，省掉这些重复？',
      hint: '想象你做一道数学题，中间某一步的结果你算出来了，后面又要用——你会重新算一遍，还是把它记在草稿纸上直接抄？',
      answer: '当然是**记在草稿纸上、下次直接查**——这正是**动态规划（DP）**的核心思想！纯递归算 fib(5) 会把 fib(2)、fib(3) 这些**子问题重复计算很多遍**，导致 $O(2^n)$ 的恐怖增长。动态规划用**一张表把每个子问题的答案存起来**：算过的下次直接读表，不再重算。这样每个子问题只算一次，复杂度从 $O(2^n)$ 骤降到 $O(n)$。DP 适用于「大问题能拆成**重叠的**子问题」的情形。'
    },
    intro: [
      '动态规划（简称 DP），是一种「**把大问题拆成一个个小问题，并把每个小问题的答案存起来、避免重复计算**」的解题方法。它最适合解决这样一类问题：大问题可以由更小的同类问题的答案推出来，而且这些小问题会被**反复用到**。',
      '为什么需要它？看斐波那契：fib(n) = fib(n-1) + fib(n-2)。如果纯用递归直接算，会发现 fib(n-2) 在算 fib(n-1) 时已经算过一次，结果又被重新算——越往下重复越多，运算次数像滚雪球一样涨到 $O(2^n)$，算 fib(50) 都要等很久。**问题就出在「重复计算同一个子问题」**。',
      '动态规划怎么解决？用一张**表**（数组或字典）把每个子问题第一次算出的答案**记录下来**；以后再需要这个子问题时，**直接查表**，不再重算。这样每个子问题只算一次，复杂度就从 $O(2^n)$ 降到 $O(n)$。这种「算过就存、用时就查」的技巧叫**记忆化（memoization）**，是 DP 最直观的入门形式。',
      '一个问题能用动态规划，通常要满足两个条件：① **重叠子问题**——大问题分解出的子问题会被重复用到（不重复就没必要存表，比如归并排序的子问题各不相同，就不是 DP）；② **最优子结构**——大问题的最优解可以由子问题的最优解组合出来。斐波那契、爬楼梯、最少硬币数、背包问题等都满足，所以都能用 DP。',
      '一句话记住：**动态规划 = 把重复的子问题答案存进表里，用空间换时间。** 它和「分治」都拆问题，区别在于：分治的子问题**互不重叠**（如归并），DP 的子问题**会重叠**、所以才值得存起来复用。DP 是算法竞赛和面试的核心方法之一，入门就从「记忆化斐波那契」开始。'
    ],
    formulas: [
      { name: '纯递归复杂度', latex: 'O(2^n)',
        note: '不存表、直接递归算斐波那契时，子问题被指数级重复计算，慢得无法接受。这是要优化的对象。' },
      { name: '动态规划复杂度', latex: 'O(n)',
        note: '用表存下每个子问题的答案、各算一次，斐波那契从 $O(2^n)$ 降到 $O(n)$，是「用空间换时间」的典型。' },
      { name: '适用的两个条件', latex: '\\text{重叠子问题} + \\text{最优子结构}',
        note: '子问题会被重复用到（值得存表）、且大问题的最优解能由子问题最优解组合——同时满足才适合用 DP。' },
    ],
    methods: [
      {
        name: '记忆化（自顶向下）',
        when: '已有递归式、但发现子问题被重复计算，想直接给递归「加缓存」时',
        steps: [
          '保留原来的递归思路（如 fib(n)=fib(n-1)+fib(n-2)）',
          '准备一张表（字典或数组）当「备忘录」，存已经算过的子问题答案',
          '每次进入递归，先查表：若这个子问题算过，直接返回存的答案',
          '若没算过，照常递归计算，**算完后立刻把结果存进表**，再返回',
          '这样每个子问题只会真正计算一次，其余都是查表',
        ],
        example: {
          problem: '用「记忆化」优化斐波那契，说明它如何避免重复计算 fib(3)。',
          solution: [
            '解：用字典 memo 当备忘录。算 fib(5) 会调用 fib(4) 和 fib(3)。',
            '算 fib(4) 时内部又会算到 fib(3)，第一次算完就把 fib(3) 的结果存进 memo。',
            '等外层再要 fib(3) 时，发现 memo 里已经有了，直接取出、不再递归。',
            '答：每个 fib(k) 只真正算一次，其余都是查表，总复杂度从 $O(2^n)$ 降到 $O(n)$。',
          ],
        },
      },
      {
        name: '递推填表（自底向上）',
        when: '能想清楚「从最小的子问题一步步推到大问题」的顺序时（通常更省空间、无递归栈风险）',
        steps: [
          '定义状态：想清楚 dp[i] 到底代表什么（如 dp[i] = 第 i 个斐波那契数）',
          '确定边界：把最小的子问题答案先填好（如 dp[0]=0、dp[1]=1）',
          '写状态转移方程：大状态怎么由小状态推出（如 dp[i]=dp[i-1]+dp[i-2]）',
          '按从小到大的顺序循环填表，直到填出要求的那个 dp 值',
        ],
        example: {
          problem: '用「递推填表」自底向上计算 fib(6)。',
          solution: [
            '解：定义 dp[i]=第 i 个斐波那契数。边界 dp[0]=0、dp[1]=1。',
            '转移 dp[i]=dp[i-1]+dp[i-2]，从 i=2 往上填：',
            'dp[2]=1, dp[3]=2, dp[4]=3, dp[5]=5, dp[6]=8。',
            '答：fib(6)=8。从小到大填表，每个只算一次，复杂度 $O(n)$。',
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 斐波那契：从纯递归到动态规划',
        problem: '先写出纯递归版斐波那契，说明它为什么慢；再用「记忆化」改写，并追踪它如何避免重复计算。',
        steps: [
          `第一步：纯递归版（慢）。
def fib(n):
    if n <= 1:           # 基础情形
        return n
    return fib(n - 1) + fib(n - 2)`,
          '第二步：分析为什么慢。算 fib(5) 要算 fib(4)+fib(3)；算 fib(4) 又要 fib(3)+fib(2)……可以发现 fib(3) 被算了 2 次、fib(2) 被算了 3 次，子问题大量重复。总运算次数约 $2^n$，复杂度 $O(2^n)$，算 fib(40) 就明显卡。',
          `第三步：记忆化版（快）。用字典 memo 缓存算过的结果。
def fib(n, memo=None):
    if memo is None:
        memo = {}
    if n <= 1:               # 基础情形
        return n
    if n in memo:            # 算过了，直接查表
        return memo[n]
    # 没算过：递归计算，存表后返回
    result = fib(n - 1, memo) + fib(n - 2, memo)
    memo[n] = result
    return result`,
          '第四步：追踪记忆化如何省重复。算 fib(5)：先深入算 fib(4)、再算 fib(3)……第一次算出 fib(2)、fib(3) 时立刻存进 memo。',
          '第五步：当 fib(4) 算完回来、外层 fib(5) 还要 fib(3) 时，发现「3 in memo」成立，**直接返回存好的值，不再递归展开**。每个 fib(k) 因此只真正计算一次。',
          '第六步：作答。记忆化后每个子问题算一次、其余查表，复杂度从 $O(2^n)$ 降到 **$O(n)$**。这就是动态规划「存表避免重复」的威力——用一点存储空间换来巨大的时间提升。',
        ],
      },
    ],
    pitfalls: [
      '**没存表，假记忆化**。光写了递归却忘了把结果存进 memo、或忘了先查表，就还是纯递归的 $O(2^n)$。「算完存表 + 进来先查表」两步缺一不可。',
      '误把「分治」当「动态规划」。两者都拆问题，但**只有子问题会重叠时才用 DP**（存表才有意义）。归并排序的子问题各不相同、不重叠，那是分治不是 DP。',
      '状态定义不清。DP 第一步是想清楚「dp[i] 到底代表什么」。状态定义模糊，转移方程根本写不出来——先把 dp[i] 的含义用一句话说死。',
      '边界（基础情形）没设或设错。递归必须有终止条件（如 fib 的 n<=1），填表必须先填好最小状态（如 dp[0]、dp[1]）；漏了会无限递归或数组越界。',
      '以为 DP 一定要用递归。DP 有两种写法：自顶向下「记忆化递归」和自底向上「递推填表」。后者用循环、无递归栈风险、常更省空间，很多题用它更稳。',
    ],
    exercises: [
      {
        problem: '「爬楼梯问题」：一个楼梯有 n 级，每次可以爬 1 级或 2 级，问爬到第 n 级一共有多少种不同走法？请找出它的递推关系（状态转移方程），并算出 n=5 时的走法数。',
        hint: '想：要到达第 n 级，最后一步要么是从第 n-1 级跨 1 步上来，要么是从第 n-2 级跨 2 步上来。这两种情况的走法数加起来就是答案。',
        answer: `**详细讲解**：

**找状态转移方程**：设 dp[i] = 爬到第 i 级的不同走法数。关键是想清楚「最后一步」从哪来：
- 要到第 i 级，**最后一步**只有两种可能：从第 i-1 级跨 **1** 步上来，或从第 i-2 级跨 **2** 步上来。
- 所以到第 i 级的走法数 = 到第 i-1 级的走法数 + 到第 i-2 级的走法数。
- **状态转移方程**：$dp[i] = dp[i-1] + dp[i-2]$。

（你会发现这和斐波那契一模一样！这正是 DP 的魅力——很多不同问题背后是同一个递推结构。）

**确定边界**：
- dp[1] = 1（到第 1 级只有「爬 1 步」一种走法）
- dp[2] = 2（到第 2 级有「1+1」和「2」两种走法）

**自底向上填表算 n=5**：
- dp[1] = 1
- dp[2] = 2
- dp[3] = dp[2] + dp[1] = 2 + 1 = **3**
- dp[4] = dp[3] + dp[2] = 3 + 2 = **5**
- dp[5] = dp[4] + dp[3] = 5 + 3 = **8**

**答案**：n=5 时共有 **8 种**走法。

**参考代码**：
\`\`\`python
def climb_stairs(n):
    if n <= 2:
        return n
    dp = [0] * (n + 1)       # dp[i] = 到第 i 级的走法数
    dp[1], dp[2] = 1, 2      # 边界
    for i in range(3, n + 1):
        dp[i] = dp[i - 1] + dp[i - 2]   # 状态转移
    return dp[n]
\`\`\`

**复杂度分析**：自底向上填表，循环 n 次、每步 O(1)，时间复杂度 $O(n)$，空间 $O(n)$（其实只需记住前两个值，可优化到 $O(1)$ 空间）。如果用纯递归不存表，会重复计算大量子问题，退化成 $O(2^n)$——这正是必须用 DP 的原因。`
      },
      {
        problem: '小明用纯递归算斐波那契 fib(n)=fib(n-1)+fib(n-2)，发现算 fib(40) 要等好几秒，算 fib(50) 几乎卡死。请解释慢的根本原因，并用「记忆化」改写让它变快，分析改写前后的复杂度。',
        hint: '画一画 fib(5) 的递归展开，数一数 fib(2) 和 fib(3) 各被算了几次。慢是因为「重复」。',
        answer: `**详细讲解**：

**慢的根本原因——重复计算同一个子问题**。纯递归 fib(n) 会展开成一棵巨大的递归树。以 fib(5) 为例：
- fib(5) → fib(4) + fib(3)
- fib(4) → fib(3) + fib(2)，于是 **fib(3) 被算了 2 次**
- fib(3) → fib(2) + fib(1)，fib(2) 又被反复算……**fib(2) 被算了 3 次**

n 越大，这种重复呈**指数级**膨胀。总的递归调用次数约为 $2^n$，所以纯递归复杂度是 **$O(2^n)$**——这就是 fib(40)、fib(50) 慢到卡死的原因：要做上万亿次重复计算。

**用记忆化改写**：核心是「算过的子问题存起来，下次直接查表」。
\`\`\`python
def fib(n, memo=None):
    if memo is None:
        memo = {}
    if n <= 1:                # 基础情形
        return n
    if n in memo:             # 已经算过，直接返回（关键！）
        return memo[n]
    # 没算过：计算后存入表，再返回
    memo[n] = fib(n - 1, memo) + fib(n - 2, memo)
    return memo[n]
\`\`\`

**为什么变快**：加了 memo 后，每个 fib(k)**第一次**算出来就存进表；以后任何地方再要 fib(k)，发现「k in memo」成立，直接取出、不再展开递归树。于是**每个子问题只真正计算一次**。

**复杂度对比**：
- **改写前（纯递归）**：$O(2^n)$ —— 指数级，不可用于大 n。
- **改写后（记忆化）**：$O(n)$ —— 因为只有 n 个不同的子问题 fib(0)..fib(n)，每个算一次。额外用了 $O(n)$ 的表空间。

**结论**：从 $O(2^n)$ 到 $O(n)$ 是天壤之别——fib(50) 从「卡死」变成「瞬间出结果」。这正是动态规划「**用空间换时间、存表消除重复子问题**」的核心威力。`
      },
      {
        problem: '动态规划和「分治」（如归并排序）都是「把大问题拆成小问题」。它们到底有什么区别？为什么斐波那契适合用 DP，而归并排序不算 DP？请从「子问题是否重叠」的角度说明。',
        hint: '画出斐波那契的子问题和归并排序的子问题，看哪个会出现「同一个子问题被多次用到」。',
        answer: `**详细讲解**：

**核心区别：子问题是否「重叠」**。分治和 DP 都拆问题，但拆出来的子问题性质不同：
- **分治**：子问题**互不重叠**（各不相同，互相独立），拆开后分别解决、合并即可，**不需要存表**。
- **动态规划**：子问题**会重叠**（同一个子问题被反复用到），所以才值得把答案**存起来复用**——这正是 DP 存在的意义。

**斐波那契为什么是 DP（子问题重叠）**：
- fib(5) 要算 fib(4) 和 fib(3)；fib(4) 又要算 fib(3) 和 fib(2)……
- 可以看到 **fib(3) 被 fib(5) 和 fib(4) 都用到**，fib(2) 被用到更多次——**同一个子问题反复出现**，这就是「重叠子问题」。
- 因为重叠，存表复用能消除大量重复计算（$O(2^n)$ → $O(n)$），所以 DP 大有用武之地。

**归并排序为什么不是 DP（子问题不重叠）**：
- 归并把数组对半拆：[左半] 和 [右半]。左半和右半是**完全不同的两段数据**，没有任何重叠。
- 排序左半时用到的子问题，和排序右半时用到的子问题**互不相干**，同一个子问题不会被算第二次。
- 既然子问题不重叠、不会重复，就**没必要存表**——存了也没有可复用的，所以它是**分治**，不是 DP。

**判断口诀**：
- 拆出的子问题**会重复用到** → 用 **DP**（存表避免重算）。例：斐波那契、爬楼梯、背包。
- 拆出的子问题**各不相同、互相独立** → 用**分治**（分别解决再合并）。例：归并排序、快速排序、二分查找。

**复杂度角度**：DP 通过存表把指数级的重复消掉（如 $O(2^n)$ → $O(n)$）；分治本身子问题不重复，复杂度由「拆几层 × 每层代价」决定（如归并的 $O(n\\log n)$），不需要、也无法靠存表加速。

**结论**：两者都「拆问题」，但 **DP 的关键前提是「重叠子问题」**——有重叠才用 DP，没重叠就是分治。`
      },
    ],
  },

});
