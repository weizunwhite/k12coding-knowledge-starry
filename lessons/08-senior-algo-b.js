// 高中编程 · 图论与字符串算法 —— 详细小课（习题讲解版）
// 覆盖 3 个节点：shortest-path / mst / string-matching
// 数据结构对齐 node-detail.jsx 的渲染：warmup / intro / formulas(name,latex,note) /
//   methods(name,when,steps,example) / worked(title,problem,steps) / pitfalls / exercises(problem,hint,answer)
// 说明：
//   · 行内公式用 $...$（renderRich 会交给 KaTeX）；formulas.latex 直接写 LaTeX（无需 $）
//   · JS 字符串里反斜杠写成 \\，所以 O(E log V) 写作 $O(E\\log V)$
//   · 含代码或引号的字段值一律用反斜杠模板字符串（反引号），代码换行用真实换行
//   · node-detail.jsx 不渲染 derivation，本文件不含该字段
//   · renderRich 会把 ** 当成加粗、$ 当成公式，故代码里避免出现 ** 和裸 $（Python 幂运算改用 pow 或文字说明）

Object.assign(window.CODE_LESSON, {

  // ───────────────────────────────────────────────────────────────
  // 1. 最短路径算法（Dijkstra / Floyd）
  // ───────────────────────────────────────────────────────────────
  'shortest-path': {
    warmup: {
      q: '手机地图给你从家到学校规划路线，路口有很多、每段路的「耗时」还各不相同（有的堵、有的快）。地图怎么在成千上万种走法里，瞬间挑出「总耗时最少」的那一条？它显然不是把所有路线都试一遍——那要算到天荒地老。',
      hint: '把路口看成「节点」、把每段路的耗时看成连接两节点的「边的权重」。问题就变成：在一张带权重的图里，找从起点到终点「边权之和最小」的路径。能不能像剥洋葱一样，从起点出发，一层层确定「到每个路口的最短耗时」？',
      answer: '这是**最短路径问题**：把路口当节点、路段耗时当边权，求两点间「边权之和最小」的路径。之前学的 BFS 只能解「每段都一样长」的无权图；一旦每段路耗时不同（带权），就要用**Dijkstra**（单源、要求边权非负，配优先队列很快）或**Floyd**（一次算出任意两点间的最短路，能处理负权但慢，$O(V^3)$）。它们的共同思想是「**不断用已知的最短路去松弛（更新）还没确定的点**」，从而避免盲目枚举所有路线。'
    },
    intro: [
      '最短路径问题，是图论里最实用的问题之一：在一张**带权图**（每条边都有一个「权重」，代表距离、耗时、费用等代价）里，求某两个节点之间「**边权之和最小**」的那条路径。地图导航、网络路由、物流调度，本质上都是它。',
      '为什么不能用前面学的 BFS？因为 BFS 是「一层层向外扩」，它默认**每条边的代价都一样**（都算 1 步），所以只适合**无权图**求「最少步数」。可现实里每段路代价不同——A 到 B 走 3 分钟、B 到 C 走 10 分钟，这种**带权**图，BFS 的「层数」就不等于「总代价」了，必须换专门的最短路算法。',
      '本节讲两个经典算法。**Dijkstra（迪杰斯特拉）** 解决「**单源最短路**」——从一个固定起点出发，求到其余所有点的最短路。它的核心是**贪心 + 松弛**：每次从「还没确定的点」里挑出当前距离最小的那个，认定它的最短路已经定了，再用它去**松弛**（尝试更新）它的邻居。它有一个硬性前提：**所有边权必须非负**（不能有负权边）。',
      '另一个是 **Floyd（弗洛伊德）**，解决「**多源最短路**」——一次性算出**任意两点之间**的最短路。它的思想极其简洁：用三重循环，依次让每个节点 k 作为「中转站」，看「从 i 经过 k 再到 j」是不是比「直接从 i 到 j」更短，是就更新。它能处理负权边（只要没有负权环），但因为三重循环，复杂度高达 $O(V^3)$，只适合节点数不多的图。',
      '一句话记住：**单源、非负权、求一个起点到各点 → 用 Dijkstra（贪心选最近 + 松弛邻居，配优先队列约 $O(E\\log V)$）；要任意两点都求、点又不多 → 用 Floyd（三重循环中转，$O(V^3)$）。** 两者都靠「**松弛**」这个核心操作：若「经由某点」到达目标更近，就更新目标的最短距离。'
    ],
    formulas: [
      { name: 'Dijkstra（朴素，邻接矩阵）', latex: 'O(V^2)',
        note: '每次在所有点里线性扫描找「当前最近的未确定点」，做 $V$ 轮，故 $O(V^2)$。$V$ 是节点数。适合稠密图。' },
      { name: 'Dijkstra（堆优化，优先队列）', latex: 'O(E\\log V)',
        note: '用优先队列（小顶堆）取最近点、每条边松弛时入队，约 $O(E\\log V)$。$E$ 是边数。边稀疏时比 $O(V^2)$ 快很多。' },
      { name: 'Floyd（多源）', latex: 'O(V^3)',
        note: '三重循环：外层枚举中转点 $k$，内两层枚举起点 $i$、终点 $j$，一次算出所有点对最短路。点多就吃不消。' },
      { name: '松弛操作（核心）', latex: '\\text{dist}[v] = \\min(\\text{dist}[v],\\ \\text{dist}[u] + w(u,v))',
        note: '若「先到 $u$ 再走边 $u\\to v$」比已知的到 $v$ 的距离更短，就更新 $\\text{dist}[v]$。所有最短路算法都靠它推进。' },
    ],
    methods: [
      {
        name: 'Dijkstra 步骤（贪心选最近 + 松弛）',
        when: '求**一个起点**到图中其余各点的最短路，且图中**没有负权边**时',
        steps: [
          '初始化：起点到自己的距离 dist[起点]=0，到其余所有点的距离设为「无穷大」（表示还没找到路）',
          '准备一个集合记录「已确定最短路的点」，初始为空',
          '从「还没确定的点」里挑出 dist 最小的那个点 u——这一步就贪心地认定：u 的最短路已经定了，把它加入已确定集合',
          '松弛 u 的每个邻居 v：如果 dist[u] + 边(u,v)的权 < dist[v]，就更新 dist[v]（找到了一条经过 u 的更短的路）',
          '重复「挑最近未确定点 + 松弛邻居」，直到所有点都确定，dist 数组就是起点到各点的最短距离',
        ],
        example: {
          problem: '一张图：节点 A、B、C。边：A-B 权 1，A-C 权 4，B-C 权 2。求从 A 出发到各点的最短距离。',
          solution: [
            '解：初始 dist[A]=0，dist[B]=∞，dist[C]=∞。',
            '第 1 轮：未确定点里 A 最小（0），确定 A。松弛 A 的邻居：dist[B]=min(∞, 0+1)=1，dist[C]=min(∞, 0+4)=4。',
            '第 2 轮：未确定点 B、C 里 B 最小（1），确定 B。松弛 B 的邻居 C：dist[C]=min(4, 1+2)=3，更新成 3！',
            '答：第 3 轮确定 C（3）。最终 dist[A]=0、dist[B]=1、dist[C]=3。注意 A→C 直接走是 4，但 A→B→C 只要 3，松弛帮我们找到了这条更短的路。',
          ],
        },
      },
      {
        name: 'Floyd 步骤（三重循环中转）',
        when: '要一次求出**任意两点之间**的最短路、且节点数不大（几百以内）时',
        steps: [
          '建一个二维表 dist[i][j]，初始填「i 到 j 的直接边权」；没有直接边的填「无穷大」；dist[i][i] 填 0',
          '最外层循环枚举「中转点」k（让每个点轮流当一次中转站）',
          '中间层、内层循环枚举所有起点 i 和终点 j',
          '松弛：如果 dist[i][k] + dist[k][j] < dist[i][j]（经过 k 中转更短），就更新 dist[i][j]',
          '三层循环跑完，dist[i][j] 就是任意 i 到 j 的最短距离',
        ],
        example: {
          problem: '三个点 0、1、2。直接边：0→1 权 3，1→2 权 1，0→2 权 10。用 Floyd 看 0→2 的最短路会不会被中转点 1 更新。',
          solution: [
            '解：初始 dist[0][1]=3、dist[1][2]=1、dist[0][2]=10，对角线为 0。',
            '当中转点 k=1 时，看 i=0、j=2：dist[0][1]+dist[1][2]=3+1=4，比直接的 dist[0][2]=10 小。',
            '于是更新 dist[0][2]=4——找到了「0→1→2」这条更短的路。',
            '答：0→2 的最短距离是 4（经过中转点 1），而不是直接边的 10。这就是 Floyd「让每个点当中转、能更短就更新」的威力。',
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · Dijkstra 代码与「松弛」执行追踪',
        problem: '用邻接表 + 优先队列写一个 Dijkstra 函数 dijkstra(graph, start)，返回从 start 到各点的最短距离字典。然后追踪在下面这张图上、从 A 出发的松弛过程。图：A→B 权 1、A→C 权 4、B→C 权 2、B→D 权 5、C→D 权 1。',
        steps: [
          `第一步：写出代码。graph 用字典存邻接表：键是节点，值是 [(邻居, 边权), ...]。
import heapq   # Python 的优先队列（小顶堆）

def dijkstra(graph, start):
    # dist 记录起点到各点的当前最短距离，初始都设为无穷大
    dist = {node: float('inf') for node in graph}
    dist[start] = 0
    # 优先队列里放 (当前距离, 节点)，每次能弹出距离最小的
    pq = [(0, start)]
    while pq:
        # 取出当前距离最小的节点 u
        d, u = heapq.heappop(pq)
        # 若这个记录已过期（比已知的还大），跳过
        if d > dist[u]:
            continue
        # 松弛 u 的每个邻居 v
        for v, w in graph[u]:
            new_dist = dist[u] + w        # 经过 u 到 v 的距离
            if new_dist < dist[v]:        # 比已知的更短
                dist[v] = new_dist        # 更新（松弛）
                heapq.heappush(pq, (new_dist, v))   # 把更优的 v 入队
    return dist`,
          '第二步：理解逻辑。优先队列保证每次弹出的都是「当前距离最小的未处理节点」（贪心）；弹出后用它去松弛每个邻居——只要「经过它」能让邻居更近，就更新邻居的 dist 并把新值入队。过期的旧记录用 d > dist[u] 跳过。',
          '第三步：追踪。图：A→B 权 1、A→C 权 4、B→C 权 2、B→D 权 5、C→D 权 1。初始 dist={A:0, B:∞, C:∞, D:∞}，队列 [(0,A)]。',
          '第四步：弹出 (0,A)。松弛 A 的邻居：B：0+1=1<∞ → dist[B]=1，入队 (1,B)；C：0+4=4<∞ → dist[C]=4，入队 (4,C)。现在 dist={A:0,B:1,C:4,D:∞}。',
          '第五步：弹出队列里最小的 (1,B)。松弛 B 的邻居：C：1+2=3<4 → dist[C]=3，入队 (3,C)；D：1+5=6<∞ → dist[D]=6，入队 (6,D)。现在 dist={A:0,B:1,C:3,D:6}。',
          '第六步：弹出 (3,C)。松弛 C 的邻居 D：3+1=4<6 → dist[D]=4，入队 (4,D)。再弹出 (4,C)，但 4>dist[C]=3，过期跳过。继续弹出 (4,D) 松弛（D 无出边）、(6,D) 过期跳过。最终 dist={A:0, B:1, C:3, D:4}。注意 D 的最短路是 A→B→C→D（1+2+1=4），被一次次松弛逐步找到，而不是 A→B→D 的 6。',
        ],
      },
      {
        title: '例题 · Floyd 代码与三重循环追踪',
        problem: '用邻接矩阵写一个 Floyd 函数 floyd(dist)（dist 是初始距离矩阵，无边处为大数），原地求出任意两点最短路。然后追踪 4 个点、关注 0→3 这一对如何被中转点逐步更新。边：0→1 权 2、1→2 权 3、2→3 权 1、0→3 权 100。',
        steps: [
          `第一步：写出代码。n 是节点数，三重循环依次让每个 k 当中转站。
INF = float('inf')

def floyd(dist):
    n = len(dist)
    # 最外层：枚举中转点 k
    for k in range(n):
        # 中间层、内层：枚举所有起点 i、终点 j
        for i in range(n):
            for j in range(n):
                # 若「i 经过 k 再到 j」更短，就更新
                if dist[i][k] + dist[k][j] < dist[i][j]:
                    dist[i][j] = dist[i][k] + dist[k][j]
    return dist`,
          '第二步：理解逻辑。dist[i][j] 一开始只表示「直接边」的距离；外层每放进一个中转点 k，就给所有点对一次「能不能经过 k 走得更短」的机会。所有点都当过中转后，dist[i][j] 就是真正的最短路。',
          '第三步：追踪。4 个点，初始 dist[0][1]=2、dist[1][2]=3、dist[2][3]=1、dist[0][3]=100，其余非对角无边处为 INF，对角线为 0。重点看 0→3（初始 100）。',
          '第四步：k=0（中转点 0）。对 0→3 无帮助（dist[0][0]+dist[0][3]=0+100，没变）。此阶段也会更新别的对，这里只跟踪 0→3。',
          '第五步：k=1（中转点 1）。看 i=0、j=2：dist[0][1]+dist[1][2]=2+3=5 < dist[0][2]（INF），更新 dist[0][2]=5。这一步为后面 0→3 变短埋下伏笔。',
          '第六步：k=2（中转点 2）。看 i=0、j=3：dist[0][2]+dist[2][3]=5+1=6 < dist[0][3]=100，更新 dist[0][3]=6！最终 0→3 的最短路是 0→1→2→3（2+3+1=6），由「k=1 先把 0→2 更新成 5、k=2 再把 0→3 更新成 6」两步接力得到。这正是 Floyd「逐个中转点松弛」的过程。',
        ],
      },
    ],
    pitfalls: [
      '**Dijkstra 不能用于含负权边的图**。它的贪心前提是「已确定的点距离不会再变小」，负权边会破坏这个假设——一条后来发现的负权边可能让「已确定」的点变得更近，导致结果错误。有负权边要改用 Bellman-Ford 算法。',
      '忘了「松弛」是核心。最短路不是一次算准的，而是不断用「经过某点会不会更近」去更新。漏掉松弛、或松弛条件写反（写成 > 而非 <），结果就全错。',
      '把无权图最短路也用 Dijkstra。如果每条边权都一样（无权图），直接用 BFS 就好，$O(V+E)$ 更简单更快；Dijkstra 是为「边权不同」准备的。',
      'Floyd 三重循环把中转点 k 写在了内层。**k 必须在最外层**——要先让每个中转点完整地更新完所有点对，再换下一个中转点。把 k 放到里面会漏掉「多次中转」的路径，得到错误答案。',
      '混淆两者的适用场景。**单源**（一个起点到各点）用 Dijkstra；**多源**（任意两点都要）且点不多用 Floyd。点很多还硬用 Floyd 的 $O(V^3)$ 会非常慢。',
    ],
    exercises: [
      {
        problem: '一张有向带权图，节点 A、B、C、D。边及权重：A→B 权 2，A→C 权 5，B→C 权 1，B→D 权 6，C→D 权 2。请用 Dijkstra 从 A 出发，逐轮写出「确定了哪个点、松弛后各点的 dist」，最后给出 A 到 D 的最短距离。',
        hint: '初始 dist[A]=0，其余 ∞。每轮从未确定点里挑 dist 最小的确定它，再松弛它的邻居（dist[u]+边权 与 dist[邻居] 取小）。',
        answer: `**详细讲解**：

**初始化**：dist[A]=0，dist[B]=dist[C]=dist[D]=∞。已确定集合为空。

**第 1 轮**（确定 A）：
- 未确定点里 A 的 dist 最小（0），确定 A。
- 松弛 A 的邻居：B → 0+2=2 < ∞，dist[B]=2；C → 0+5=5 < ∞，dist[C]=5。
- 当前 dist：A=0, B=2, C=5, D=∞。

**第 2 轮**（确定 B）：
- 未确定点 B(2)、C(5)、D(∞) 里 B 最小，确定 B。
- 松弛 B 的邻居：C → 2+1=3 < 5，**更新 dist[C]=3**；D → 2+6=8 < ∞，dist[D]=8。
- 当前 dist：A=0, B=2, C=3, D=8。

**第 3 轮**（确定 C）：
- 未确定点 C(3)、D(8) 里 C 最小，确定 C。
- 松弛 C 的邻居 D → 3+2=5 < 8，**更新 dist[D]=5**。
- 当前 dist：A=0, B=2, C=3, D=5。

**第 4 轮**（确定 D）：D(5) 最小，确定 D，无出边，结束。

**结果**：A 到 D 的最短距离是 **5**，对应路径 A→B→C→D（2+1+2=5）。

**复杂度分析**：本图用朴素 Dijkstra（每轮线性找最小）是 $O(V^2)$；用优先队列堆优化是 $O(E\\log V)$。本题关键在于体会「松弛」如何一步步把 D 的距离从 8 改到 5——直接走 A→B→D 是 8，但绕经 C 反而更短，正是松弛把这条更优路径挖了出来。`
      },
      {
        problem: '小明想用 Dijkstra 算下面这张图从 A 出发的最短路，结果发现答案不对。图：A→B 权 2，A→C 权 6，B→C 权 **−4**（负权！）。请说明 Dijkstra 在这张图上会算错什么，以及为什么 Dijkstra 不能处理负权边。',
        hint: 'Dijkstra 一旦「确定」某个点就不再改它。先确定 C 之后，后面那条负权边 B→C 还能让 C 变得更近吗？它会被忽略吗？',
        answer: `**详细讲解**：

**结论**：Dijkstra **不能处理负权边**，在这张图上会把 A→C 的最短距离算成错误的 6（实际应是 −2）。

**Dijkstra 在这张图上的错误过程**：
- 初始 dist[A]=0，dist[B]=∞，dist[C]=∞。
- 第 1 轮：确定 A。松弛邻居：dist[B]=0+2=2，dist[C]=0+6=6。
- 第 2 轮：未确定点 B(2)、C(6) 里 B 最小，确定 B。松弛 B 的邻居 C：2+(−4)=−2 < 6，dist[C] 被更新成 −2。
- 第 3 轮：确定 C，dist[C]=−2。

**咦，这次好像算对了？** 这恰恰是负权边**最危险**的地方——小图上可能侥幸对，但只要图稍复杂就会错。关键问题在于 Dijkstra 的贪心假设：

**为什么 Dijkstra 不能处理负权边（根本原因）**：
- Dijkstra 每轮「确定」dist 最小的点后，就**认定这个点的最短路永远不会再变小**，以后不再更新它。
- 这个假设只在「边权非负」时成立：因为再走任何边都只会让距离**变大或不变**，绝不会变小，所以最小的那个一旦确定就是最终答案。
- 但**负权边会让距离变小**！如果一个点已经被「确定」，后面却出现一条负权边能让它更近，Dijkstra 已经不再看它了，于是漏掉这条更短的路，给出**偏大的错误结果**。

**构造一个会出错的例子**：若把上图改成 A→C 权 1、A→B 权 2、B→C 权 −4。Dijkstra 第 1 轮确定 A 后 dist[C]=1，第 2 轮会先确定 C（1 比 B 的 2 小），**C 一确定就锁死**；可后面 B→C 的 −4 本可让 C 变成 2+(−4)=−2，却因为 C 已确定而被忽略——最终错误地输出 dist[C]=1，而正确答案是 −2。

**正确做法**：含负权边（但无负权环）的最短路要用 **Bellman-Ford 算法**，它对所有边反复松弛 V−1 轮，不依赖「确定后不再变」的假设，因此能正确处理负权边。复杂度 $O(VE)$，比 Dijkstra 慢，但能扛负权。`
      },
      {
        problem: '同样求图的最短路，什么时候该用 Dijkstra、什么时候该用 Floyd？请从「单源还是多源」「复杂度」「能否负权」三个角度对比，并举一个更适合用 Floyd 的具体场景。',
        hint: 'Dijkstra 是一个起点到各点；Floyd 是任意两点都求。一个用优先队列约 $O(E\\log V)$，一个是 $O(V^3)$。再想想哪种需求是「所有点对都要」。',
        answer: `**详细讲解**：

**三个角度对比**：

**一、单源 vs 多源**
- **Dijkstra**：**单源**最短路——求**一个固定起点**到其余所有点的最短距离。
- **Floyd**：**多源**最短路——一次性求出**任意两点之间**的最短距离（所有点对）。

**二、复杂度**
- **Dijkstra**：朴素实现 $O(V^2)$，用优先队列堆优化约 $O(E\\log V)$。边稀疏时很快。
- **Floyd**：固定 $O(V^3)$（三重循环），与边数无关。点一多（比如上千）就非常慢。
- 提示：如果要求「所有点对」最短路，也可以对每个点各跑一次 Dijkstra，总复杂度约 $O(V\\cdot E\\log V)$，在**稀疏图**上往往比 Floyd 的 $O(V^3)$ 更快。

**三、能否处理负权**
- **Dijkstra**：**不能**处理负权边（贪心假设被破坏）。
- **Floyd**：**能**处理负权边（只要图中没有负权环），因为它靠「枚举中转点松弛」，不依赖「确定后不变」的假设。

**更适合用 Floyd 的具体场景**：
- 一张**小图**（比如 50 个城市），需要查询**任意两座城市之间**的最短路程，而且会被反复查询很多次。
- 这种「所有点对都要、点又不多」的需求，Floyd 一次三重循环就把整张 dist 表算好，之后每次查询直接 $O(1)$ 读表，非常方便。如果用 Dijkstra，就得为每个起点单独跑一次，代码和逻辑都更繁琐。
- 此外，如果图里**含负权边**（但无负权环），多源最短路也只能用 Floyd（或 Johnson 算法），Dijkstra 直接出局。

**选择口诀**：
- 一个起点、边非负、图较大或稀疏 → **Dijkstra（堆优化）**。
- 任意两点都要、点少、或含负权 → **Floyd**。

**结论**：没有「绝对更好」的算法，要看需求——**单源用 Dijkstra，多源小图用 Floyd**；负权边时 Dijkstra 不可用，要换 Floyd 或 Bellman-Ford。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 2. 最小生成树（Prim / Kruskal）
  // ───────────────────────────────────────────────────────────────
  'mst': {
    warmup: {
      q: '要给 6 个村庄铺设光缆，让它们彼此都能连通（任意两村之间能通过光缆互达）。任意两村之间都可以铺，但每条线路的造价不同。村庄之间不需要直接相连，只要整体连通就行。怎样选线路，才能用**最少的总造价**把所有村庄连起来？',
      hint: '6 个村庄要连通，至少需要几条线路？如果选了一条让两个「已经能互通」的村庄之间又多铺一条线，是不是浪费（形成了环、白花钱）？应该优先选造价低的、又不造成浪费的线路。',
      answer: '这是**最小生成树（MST）** 问题：在一张带权图（村庄是节点、可铺线路是带造价的边）里，选出一部分边，让所有节点**连通**且**总边权最小**。结果一定是一棵「树」——n 个节点恰好用 n−1 条边、没有环（多一条边就成环、是浪费）。两大经典解法：**Kruskal**（把所有边按造价从小到大排序，逐条尝试加入，用**并查集**判断会不会成环，成环就跳过）；**Prim**（从一个村庄出发，每次贪心地把「连接已选区域和外部、且造价最小」的那条边加进来，像滚雪球一样长大）。'
    },
    intro: [
      '最小生成树（Minimum Spanning Tree，简称 MST），解决这样一类问题：给定一张带权的**连通图**，要选出一部分边，让所有节点**都连通**，并且这些边的**权重之和最小**。铺光缆、修公路、架电网，只要目标是「用最小代价把所有点连成一片」，就是 MST。',
      '为什么结果必然是一棵「树」？因为 n 个节点要连通，**最少**就需要 n−1 条边；如果用了 n 条边，必然出现**环**——而环里任意去掉一条边，剩下的仍然连通，却更省钱。所以「连通 + 边数最少 + 无环」三者合一，恰好就是树的定义。「生成树」就是「用原图的边连通所有节点构成的树」，其中**总权重最小**的那棵，就是最小生成树。',
      '本节讲两个经典贪心算法。**Kruskal（克鲁斯卡尔）** 是「**以边为中心**」：把所有边**按权重从小到大排序**，然后从最便宜的边开始，一条条尝试加入——但加之前要判断：这条边的两个端点**是不是已经连通**了？如果已连通，加它就会成环（浪费），跳过；如果还没连通，就加入它。判断「是否已连通」正是**并查集**最擅长的事。直到选够 n−1 条边为止。',
      '**Prim（普里姆）** 是「**以点为中心**」：从任意一个起点开始，把图分成「已加入树的点」和「还在外面的点」两部分；每一步都贪心地找出「**一端在树内、另一端在树外、且权重最小**」的那条边，把它和它在外面的那个端点一起拉进树里，让树「长大」一圈；如此重复，直到所有点都进树。它像滚雪球，从一个点慢慢吸纳整张图。',
      '一句话记住：**Kruskal——边排序后从小到大加，用并查集判环（适合稀疏图）；Prim——从一点出发，每次贪心吃掉连向外部的最小边（适合稠密图）。** 两者都是贪心，结果都是同一棵（权重最小的）生成树。选哪个看图的疏密：边少（稀疏）用 Kruskal，边多（稠密）用 Prim。'
    ],
    formulas: [
      { name: 'Kruskal 复杂度', latex: 'O(E\\log E)',
        note: '主要开销是给 $E$ 条边排序（$O(E\\log E)$）；之后用并查集逐条判环，每次近乎 $O(1)$。边少的稀疏图首选。' },
      { name: 'Prim（朴素，邻接矩阵）', latex: 'O(V^2)',
        note: '每次在所有点里线性找「连向树的最小边」，做 $V$ 轮，故 $O(V^2)$。与边数无关，**稠密图**（边很多）更合适。' },
      { name: 'Prim（堆优化）', latex: 'O(E\\log V)',
        note: '用优先队列维护候选边，每条边可能入队一次。稀疏图上比 $O(V^2)$ 快，思路与堆优化 Dijkstra 类似。' },
      { name: '生成树的边数', latex: '|E_{\\text{tree}}| = V - 1',
        note: '$V$ 个节点连通且无环，恰好用 $V-1$ 条边。选够 $V-1$ 条就停——这是判断 MST 是否构建完成的标志。' },
    ],
    methods: [
      {
        name: 'Kruskal 步骤（边排序 + 并查集判环）',
        when: '求最小生成树，尤其图比较**稀疏**（边不太多）、或边本来就容易拿来排序时',
        steps: [
          '把图中所有边按权重**从小到大**排序',
          '初始化并查集：每个节点自成一个集合（一开始谁都不和谁连通）',
          '按权重从小到大依次取出每条边 (u, v, w)',
          '用并查集查 u 和 v 是否已在同一集合：若**不在**（加它不会成环），就选这条边、并把 u、v 所在集合**合并**；若已在同一集合（会成环），**跳过**这条边',
          '当已选边数达到 节点数−1 时停止，选中的边就构成最小生成树，权重之和即最小总代价',
        ],
        example: {
          problem: '4 个节点 A、B、C、D。边：A-B 权 1、C-D 权 2、A-C 权 3、B-D 权 4、A-D 权 5。用 Kruskal 求最小生成树。',
          solution: [
            '解：边按权排序：A-B(1)、C-D(2)、A-C(3)、B-D(4)、A-D(5)。需选 4−1=3 条边。',
            '取 A-B(1)：A、B 不连通，选！合并 {A,B}。取 C-D(2)：C、D 不连通，选！合并 {C,D}。',
            '取 A-C(3)：A 在 {A,B}、C 在 {C,D}，不连通，选！合并成 {A,B,C,D}。已选 3 条，停。',
            '答：选 A-B、C-D、A-C，总权 1+2+3=6。后面的 B-D(4)、A-D(5) 都因两端已连通（会成环）而不会被选。',
          ],
        },
      },
      {
        name: 'Prim 步骤（从一点长出最小边）',
        when: '求最小生成树，尤其图比较**稠密**（边很多）时；或用邻接矩阵存图时',
        steps: [
          '任选一个起点加入「树」，其余点都在树外',
          '考察所有「一端在树内、一端在树外」的边，找出其中**权重最小**的那条',
          '把这条最小边、以及它在树外的那个端点，一起加入树（树长大一个点）',
          '更新候选：新加入的点又带来一些「连向外部」的新边，纳入考察范围',
          '重复「找连向外部的最小边并加入」，直到所有点都进了树（共加入 节点数−1 条边）',
        ],
        example: {
          problem: '同上图（A、B、C、D；A-B 权 1、C-D 权 2、A-C 权 3、B-D 权 4、A-D 权 5）。从 A 出发用 Prim 求最小生成树。',
          solution: [
            '解：起点 A 进树，树={A}。连向外部的边：A-B(1)、A-C(3)、A-D(5)。',
            '第 1 步：最小是 A-B(1)，把 B 和这条边加入。树={A,B}。新候选边：B-D(4)。当前连外边有 A-C(3)、A-D(5)、B-D(4)。',
            '第 2 步：最小是 A-C(3)，把 C 加入。树={A,B,C}。C 带来新边 C-D(2)。连外边有 A-D(5)、B-D(4)、C-D(2)。',
            '答：第 3 步最小是 C-D(2)，把 D 加入。树={A,B,C,D}。选了 A-B、A-C、C-D，总权 1+3+2=6——和 Kruskal 结果一致。',
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · Kruskal + 并查集 代码与执行追踪',
        problem: '请写出带并查集的 Kruskal：先写并查集的 find（带路径压缩）和 union，再写 kruskal(n, edges) 返回最小生成树的总权重。然后追踪 n=4、edges=[(1,2,A,B),(2,3,A,C),(2,?...)] 这样一组边。具体图：节点 A,B,C,D，边 (A,B,1)、(A,C,2)、(B,C,2)、(B,D,3)、(C,D,4)。',
        steps: [
          `第一步：写并查集。用字典 parent 记录每个节点的「父亲」，同一棵树即同一集合。
def find(parent, x):
    # 查 x 所在集合的「代表元」，沿父亲一路找到根
    if parent[x] != x:
        parent[x] = find(parent, parent[x])   # 路径压缩：把沿途节点直接挂到根上
    return parent[x]

def union(parent, a, b):
    # 把 a、b 所在的两个集合合并
    root_a = find(parent, a)
    root_b = find(parent, b)
    if root_a == root_b:
        return False      # 本来就同一集合，合并失败（说明加这条边会成环）
    parent[root_a] = root_b   # 让一个根挂到另一个根下
    return True`,
          `第二步：写 Kruskal 主体。先排序，再逐条用 union 判环。
def kruskal(nodes, edges):
    # nodes：节点列表；edges：(权重, 端点u, 端点v) 列表
    parent = {node: node for node in nodes}   # 初始：每个点自成一组
    edges.sort()                              # 按权重从小到大排序（权重在元组第一位）
    total = 0                                 # 已选边的总权重
    chosen = 0                                # 已选边数
    for w, u, v in edges:
        # union 返回 True 表示 u、v 原本不连通，这条边可选
        if union(parent, u, v):
            total = total + w                 # 累加权重
            chosen = chosen + 1               # 选边数加一
            if chosen == len(nodes) - 1:      # 选够 n-1 条就完成
                break
    return total`,
          '第三步：理解逻辑。把边写成 (权重, u, v) 后排序，权重小的排前面；逐条尝试 union——能合并（原本不连通）就选它、累加权重；不能合并（已连通）就自动跳过（避免成环）。选够 n−1 条边停止。',
          '第四步：追踪。图 A,B,C,D，边排序后：(1,A,B)、(2,A,C)、(2,B,C)、(3,B,D)、(4,C,D)。初始 parent={A:A,B:B,C:C,D:D}，total=0，chosen=0。需选 3 条。',
          '第五步：取 (1,A,B)：find(A)=A、find(B)=B 不同，union 成功，parent[A]=B。total=1，chosen=1。取 (2,A,C)：find(A)→B、find(C)=C 不同，union 成功，parent[B]=C。total=3，chosen=2。',
          '第六步：取 (2,B,C)：find(B)→C、find(C)=C **相同**！union 失败（加它会成环 A-B-C），跳过。取 (3,B,D)：find(B)→C、find(D)=D 不同，union 成功。total=3+3=6，chosen=3，达到 n−1=3，停止。返回总权重 **6**。选中的边是 A-B(1)、A-C(2)、B-D(3)，注意 B-C(2) 因会成环被并查集挡掉了。',
        ],
      },
    ],
    pitfalls: [
      '**Kruskal 每加一条边必须判环**，否则会形成回路、不再是树。判环靠并查集：只有边的两个端点不在同一集合时才选，选完要把两集合合并。漏掉判环，结果会有环、边数超过 n−1。',
      '并查集忘了**路径压缩**。find 不压缩时，集合会退化成一条长链，每次查询沿链一路向上，复杂度退化。加上 path[x]=find(...) 的路径压缩，单次操作近乎 $O(1)$。',
      'Prim 和 Kruskal 用错场景导致慢。一般经验是：**稀疏图（边少）用 Kruskal**（瓶颈是边排序 $O(E\\log E)$）；**稠密图（边多）用朴素 Prim**（$O(V^2)$，与边数无关）。边非常多时用 Kruskal 排序会偏慢。',
      'Prim 找「连向外部的最小边」时，错把「树内部之间的边」也算进来。必须是**一端在树内、另一端在树外**的边才是候选；两端都在树内的边加进来会成环。',
      '没在选够 n−1 条边时停止。MST 恰好 n−1 条边；Kruskal 选够就该 break，继续遍历剩余边没有意义（也可能误选造成环的边，虽然会被判环挡住，但白白多跑）。',
    ],
    exercises: [
      {
        problem: '5 个节点 A、B、C、D、E，边及权重：A-B 1、A-C 3、B-C 2、B-D 5、C-D 4、C-E 6、D-E 2。请用 **Kruskal** 求最小生成树：写出边排序后的处理顺序、每条边是「选」还是「跳过（成环）」、以及最终选中的边和总权重。',
        hint: '先把 7 条边按权重从小到大排。逐条判断两端是否已连通（用「集合」想）：不连通就选并合并，已连通就跳过。选够 5−1=4 条停。',
        answer: `**详细讲解**：

**第一步：边按权重从小到大排序**：
A-B(1)、B-C(2)、D-E(2)、A-C(3)、C-D(4)、B-D(5)、C-E(6)。
需选 5−1 = **4 条边**。用「集合」跟踪连通情况，初始每点各自成组：{A}{B}{C}{D}{E}。

**第二步：逐条处理**：
1. **A-B(1)**：A、B 不连通 → **选**。合并：{A,B}{C}{D}{E}。已选 1 条，权和 1。
2. **B-C(2)**：B 在 {A,B}、C 在 {C}，不连通 → **选**。合并：{A,B,C}{D}{E}。已选 2 条，权和 3。
3. **D-E(2)**：D、E 不连通 → **选**。合并：{A,B,C}{D,E}。已选 3 条，权和 5。
4. **A-C(3)**：A 和 C 都在 {A,B,C} → 已连通，加它会成环 → **跳过**。
5. **C-D(4)**：C 在 {A,B,C}、D 在 {D,E}，不连通 → **选**。合并：{A,B,C,D,E}。已选 4 条，权和 9。
6. 已选够 4 条，**停止**（后面的 B-D(5)、C-E(6) 不再处理）。

**最终结果**：选中的边为 **A-B(1)、B-C(2)、D-E(2)、C-D(4)**，最小总权重 = 1+2+2+4 = **9**。

**复杂度分析**：Kruskal 的主要开销是给边排序 $O(E\\log E)$（本题 7 条边）；之后每条边用并查集判环近乎 $O(1)$。所以总复杂度 $O(E\\log E)$，稀疏图上很高效。

**小结**：Kruskal 的精髓是「**贪心地优先选最便宜的边，但绝不选会成环的边**」——A-C(3) 就是因为会让 A、B、C 成环而被并查集挡掉的典型。`
      },
      {
        problem: '在 Kruskal 里，每加一条边都要判断「会不会成环」。为什么用**并查集**来判环特别合适？请说明并查集的 find 和 union 各做什么，以及「路径压缩」为什么重要。',
        hint: '判环 = 判断一条边的两个端点「是不是已经在同一个连通块里」。并查集正是维护「谁和谁在同一组」的结构。想想不压缩路径时，集合会变成什么形状。',
        answer: `**详细讲解**：

**为什么并查集适合判环**：
- Kruskal 加一条边 (u, v) 前，要判断的是「u 和 v 是不是**已经连通**」——如果已连通，再连一条就成环了。
- 「判断两个元素是否在同一组」「把两个组合并」正是**并查集**的两项看家本领，而且都能做到近乎 $O(1)$。所以它和 Kruskal 是天作之合。

**find 做什么**：
- find(x) 返回 x 所在集合的「**代表元（根）**」。做法是顺着「父亲指针」一路往上找，直到找到根（父亲是自己的那个节点）。
- 判环就靠它：\`find(u) == find(v)\` 说明 u、v 在同一集合（已连通，加边会成环）；不相等则可以安全加入。

**union 做什么**：
- union(a, b) 把 a、b 所在的两个集合**合并成一个**。做法是先各自 find 出根，再让一个根的父亲指向另一个根。
- Kruskal 选中一条边后，就 union 它的两个端点，表示「这两块从此连通了」。

**「路径压缩」为什么重要**：
- 如果只是简单地让节点挂来挂去，集合可能退化成一条**长链**（A→B→C→D→...）。此时 find 要沿链一路爬到顶，单次就要 $O(n)$，大量查询会很慢。
- **路径压缩**：在 find 的过程中，把沿途经过的每个节点**直接挂到根上**。这样下次再查这些节点，一步就到根。
- 配合路径压缩（和按秩合并），并查集单次操作的均摊复杂度近乎 $O(1)$，让 Kruskal 的判环几乎不花时间。

**参考代码**：
\`\`\`python
def find(parent, x):
    if parent[x] != x:
        parent[x] = find(parent, parent[x])   # 路径压缩：沿途节点直接挂到根
    return parent[x]

def union(parent, a, b):
    ra, rb = find(parent, a), find(parent, b)
    if ra == rb:
        return False        # 已在同一组（加边会成环）
    parent[ra] = rb         # 合并：一个根挂到另一个根下
    return True
\`\`\`

**复杂度分析**：带路径压缩的并查集，n 次操作总复杂度近乎 $O(n)$（严格来说是 $O(n\\,\\alpha(n))$，$\\alpha$ 是增长极慢的反阿克曼函数，实际可看作常数）。这就是 Kruskal 整体能保持 $O(E\\log E)$（瓶颈在排序而非判环）的原因。`
      },
      {
        problem: 'Prim 和 Kruskal 都能求最小生成树，结果（最小总权重）也相同。请从「思路」「适合的图」两个角度比较它们，并说明：一张图有 100 个节点、却有近 5000 条边（接近稠密），更适合用哪个？为什么？',
        hint: 'Kruskal 以边为主、要排序所有边；Prim 以点为主、从一点滚雪球。边非常多的稠密图，排序所有边贵不贵？朴素 Prim 的复杂度和边数有关吗？',
        answer: `**详细讲解**：

**一、思路对比**
- **Kruskal（以边为中心）**：把**所有边**按权重从小到大排序，从最便宜的边开始逐条尝试加入，用**并查集**判断会不会成环——不成环就选、成环就跳过，直到选够 n−1 条。
- **Prim（以点为中心）**：从一个起点出发，把图分成「树内」和「树外」；每次贪心地选「一端在树内、一端在树外的**最小边**」，把对应的外部点拉进树，像**滚雪球**一样把树长大，直到所有点入树。

**二、适合的图对比**
- **Kruskal 适合稀疏图（边少）**：它的瓶颈是给 $E$ 条边排序，$O(E\\log E)$。边少时排序快；边一多，排序就拖慢它。
- **Prim（朴素版）适合稠密图（边多）**：朴素 Prim 每轮在所有点里线性找最小边，复杂度 $O(V^2)$，**只和点数有关、与边数无关**。所以哪怕边非常多，它也不会更慢。

**三、100 个节点、近 5000 条边该用哪个**
- 这张图**接近稠密**（100 个点最多约 100×99/2 ≈ 4950 条边，5000 条已几乎是满的）。
- 用 **Kruskal**：要排序约 5000 条边，$O(E\\log E)$ 开销不小。
- 用**朴素 Prim**：复杂度 $O(V^2) = 100^2 = 10000$ 量级，**与边数无关**，反而更稳更快。
- **结论：这种稠密图更适合用 Prim（朴素邻接矩阵版）**。它不受「边多」拖累，$O(V^2)$ 在点数不大时非常划算。

**反过来的情形**：如果是**稀疏图**（比如 100 个点但只有 200 条边），Kruskal 的 $O(E\\log E)$ 就很小，比 Prim 的 $O(V^2)$ 更优——这时该用 Kruskal。

**选择口诀**：
- **稀疏图（边少）→ Kruskal**（瓶颈在边排序，边少则快）。
- **稠密图（边多）→ 朴素 Prim**（$O(V^2)$ 与边数无关，不怕边多）。
- 两者结果相同（同一棵最小生成树），只是效率因图的疏密而异。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 3. 字符串匹配（朴素 / KMP）
  // ───────────────────────────────────────────────────────────────
  'string-matching': {
    warmup: {
      q: '在一篇很长的文章里用「查找」功能找一个词，比如在 "ababcababa" 里找 "ababa"。最笨的办法是：把这个词对齐到文章第 1 个字母开始逐位比，不匹配就整体右移一位、从头再比。如果文章很长、词又总是「比到最后一位才发现不对」，这样会很慢——有没有办法让「失配时不要每次都退回去重头比」？',
      hint: '朴素比法每次失配都把模式串整体右移一位、主串指针也退回去，浪费了「前面已经比对成功的那些字符」的信息。如果模式串自己内部有重复的前后缀，失配时能不能「跳」到一个聪明的位置，而不是退回原点？',
      answer: '这是**字符串匹配**问题：在长文本（主串）里找一个模式串第一次出现的位置。**朴素匹配**每次失配都让主串指针回退、模式串右移一位重来，最坏要比 $O(nm)$ 次（n、m 分别是主串和模式串长度）。**KMP 算法**的聪明之处：先对模式串预处理出一张 **next 数组（前缀函数）**，记录「模式串每个位置之前那段的最长相同前后缀长度」；匹配时一旦失配，**主串指针不回退**，只把模式串指针按 next 数组「跳」到正确位置继续比，从而做到 $O(n+m)$。理解 KMP 的关键，就是搞懂 next 数组的含义。'
    },
    intro: [
      '字符串匹配，要解决的是：在一段长**文本（主串）**里，查找一个较短的**模式串**第一次（或所有）出现的位置。文本编辑器的「查找」、搜索引擎、DNA 序列比对，底层都是它。设主串长 n、模式串长 m，目标是又快又准地定位。',
      '最直观的是**朴素匹配（暴力匹配）**：把模式串对齐到主串的第 0 位，逐字符比较；只要有一位不匹配，就把模式串整体**右移一位**、主串指针**退回**到这次起点的下一位，从头再比。它好懂、好写，但有个大问题：**每次失配都把已经比对成功的信息全部丢弃、退回去重来**。最坏情况下（比如主串 "aaaa...a" 找 "aaa...ab"，每次都比到最后一位才发现不对），要比较约 $n\\times m$ 次，复杂度 $O(nm)$。',
      '**KMP 算法**（由 Knuth、Morris、Pratt 三人提出）正是为了消除这种浪费。它的核心洞察是：失配时，模式串前面那段**已经匹配成功**的字符是已知的，如果这段字符**自身有重复的前后缀**，就可以利用它「跳过」一些注定不可能匹配的位置，**主串指针完全不用回退**。这样主串只从头到尾扫一遍，复杂度降到 $O(n+m)$。',
      'KMP 的关键，是为模式串预先算出一张 **next 数组**（也叫**前缀函数**）。next[i] 的含义是：模式串中**下标 0 到 i 这一段子串**里，「**既是它的前缀、又是它的后缀**」的**最长**那一段的长度（这里说的前缀、后缀都不能是整段本身）。直白点说，它回答：「如果比到位置 i 的下一位失配了，模式串指针应该退到哪里，才能利用已匹配的部分、不必从头再来。」',
      '一句话记住：**朴素匹配——失配就退回重比，最坏 $O(nm)$；KMP——先求 next 数组记住模式串的「最长相同前后缀」，失配时主串不回退、模式串按 next 跳，$O(n+m)$。** KMP 难就难在理解 next 数组的含义——它是模式串「自我重复结构」的浓缩，把它弄懂，KMP 就通了。'
    ],
    formulas: [
      { name: '朴素匹配复杂度', latex: 'O(nm)',
        note: '主串每个起点都可能把模式串从头比到尾，最坏比较约 $n\\times m$ 次。$n$、$m$ 分别是主串、模式串长度。' },
      { name: 'KMP 总复杂度', latex: 'O(n+m)',
        note: '预处理 next 数组花 $O(m)$，匹配阶段主串指针不回退、只扫一遍花 $O(n)$，合计 $O(n+m)$。大文本上远快于朴素。' },
      { name: 'next 数组的含义', latex: '\\text{next}[i] = \\text{子串 } P[0..i] \\text{ 的「最长相同前后缀」长度}',
        note: '即「既是该段前缀又是其后缀」的最长长度（不含整段本身）。失配时模式串指针就退到这个长度处继续比。' },
      { name: 'next 的预处理复杂度', latex: 'O(m)',
        note: '用「模式串自己和自己匹配」的方式递推求 next，每个位置均摊 $O(1)$，整张表 $O(m)$ 算完。' },
    ],
    methods: [
      {
        name: '朴素匹配步骤（逐位比，失配右移一位）',
        when: '模式串很短、文本不长，或只图实现简单时（不追求最优效率）',
        steps: [
          '让模式串对齐主串的第 i 位（i 从 0 开始），准备逐字符比较',
          '从模式串第 0 位起，逐位比较 主串[i+j] 与 模式串[j]',
          '若全部 m 位都匹配，说明在主串第 i 位找到了模式串，返回 i',
          '若中途某位失配，放弃这次对齐，让 i 增加 1（模式串整体右移一位），回到第二步从头比',
          '若 i 一直试到 主串长−模式串长 都没成功，说明没找到，返回 -1',
        ],
        example: {
          problem: '在主串 "abcab" 中用朴素匹配查找模式串 "cab"，写出每次对齐的比较。',
          solution: [
            '解：主串长 5、模式串长 3，i 从 0 试到 2。',
            'i=0：比 主串[0]="a" 与 模式[0]="c"，不匹配，右移。i=1：比 主串[1]="b" 与 模式[0]="c"，不匹配，右移。',
            'i=2：比 主串[2]="c"=模式[0]"c" √；主串[3]="a"=模式[1]"a" √；主串[4]="b"=模式[2]"b" √，全匹配！',
            '答：在主串下标 2 处找到 "cab"，返回 2。可以看到 i=0、i=1 两次都是「比第一位就失配、右移重来」。',
          ],
        },
      },
      {
        name: 'KMP 步骤（先求 next，失配按 next 跳）',
        when: '主串很长、或要在同一文本里反复查找，追求 $O(n+m)$ 高效匹配时',
        steps: [
          '预处理：对模式串求 next 数组——next[i] 是子串 模式[0..i] 的最长相同前后缀长度',
          '匹配时用两个指针：i 扫主串（只增不退）、j 指模式串当前要比的位置',
          '若 主串[i] == 模式[j]：两个指针都前进一位（i++、j++）',
          '若失配且 j>0：不动 i，把 j **跳**到 next[j-1]（利用已匹配前缀，避免回退主串），再继续比',
          '若失配且 j==0：模式串第一位就不匹配，只能 i 前进一位',
          '当 j 走到模式串末尾（j==m）：说明完整匹配，匹配起点是 i−m，返回它',
        ],
        example: {
          problem: '模式串 "ababc" 的 next 数组怎么求？说明它在失配时如何指导模式串指针跳转。',
          solution: [
            '解：逐位看「最长相同前后缀」。next[0]=0（单字符无真前后缀）。',
            '"ab"：前缀{a}、后缀{b}无公共 → next[1]=0；"aba"：前后缀都有 "a" → next[2]=1；"abab"：公共 "ab" → next[3]=2；"ababc"：c 打断，无公共 → next[4]=0。',
            '所以 next=[0,0,1,2,0]。含义举例：若匹配到模式[3]="b" 之后、比模式[4]="c" 时失配，j 从 4 跳到 next[3]=2——因为前面 "abab" 的后缀 "ab" 等于前缀 "ab"，这两位无需重比。',
            '答：next=[0,0,1,2,0]。它让失配时模式串「退到已匹配好的前缀末尾」继续，而主串指针纹丝不动。',
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 求 next 数组（前缀函数）代码与执行追踪',
        problem: 'KMP 的核心和难点是 next 数组。请写一个函数 build_next(pattern)，返回模式串的 next 数组（next[i] = 子串 pattern[0..i] 的最长相同前后缀长度）。然后追踪求 "ababab" 的 next 数组的全过程。',
        steps: [
          `第一步：写出代码。用「模式串自己和自己匹配」的思路递推：i 往后扫，j 表示当前最长相同前后缀的长度。
def build_next(pattern):
    m = len(pattern)
    next_arr = [0] * m         # next[0] 一定是 0
    j = 0                      # j：当前已匹配的最长相同前后缀长度
    # i 从 1 开始，逐位确定 next[i]
    for i in range(1, m):
        # 失配：当前字符 pattern[i] 接不上长度为 j 的前缀
        # 就把 j 回退到 next[j-1]，尝试更短的相同前后缀
        while j > 0 and pattern[i] != pattern[j]:
            j = next_arr[j - 1]
        # 若能接上，最长相同前后缀长度加一
        if pattern[i] == pattern[j]:
            j = j + 1
        next_arr[i] = j        # 记录到 next[i]
    return next_arr`,
          '第二步：理解逻辑。j 始终表示「到目前为止，最长的相同前后缀有多长」。新字符 pattern[i] 若能接在长度为 j 的前缀后面（pattern[i]==pattern[j]），j 就加一；接不上就让 j 回退到 next[j-1]（退而求其次找更短的相同前后缀），直到能接上或退到 0。每个 i 算完，j 就是 next[i]。',
          '第三步：追踪 build_next("ababab")。m=6，next=[0,0,0,0,0,0]，j=0。',
          '第四步：i=1，pattern[1]="b"。比 pattern[1]"b" 与 pattern[j=0]"a"：不等，j 已是 0 不回退。next[1]=0。i=2，pattern[2]="a"。比 "a" 与 pattern[0]"a"：相等，j=1。next[2]=1。',
          '第五步：i=3，pattern[3]="b"。比 "b" 与 pattern[j=1]"b"：相等，j=2。next[3]=2。i=4，pattern[4]="a"。比 "a" 与 pattern[j=2]"a"：相等，j=3。next[4]=3。',
          '第六步：i=5，pattern[5]="b"。比 "b" 与 pattern[j=3]"b"：相等，j=4。next[5]=4。最终 next=[0,0,1,2,3,4]。含义：如 next[5]=4 表示 "ababab" 的最长相同前后缀是 "abab"（长度 4）。这张表是 KMP 失配跳转的依据，预处理只花 $O(m)$。',
        ],
      },
      {
        title: '例题 · KMP 匹配主流程与失配跳转追踪',
        problem: '请写出完整的 KMP 匹配函数 kmp_search(text, pattern)（用上面的 build_next），返回模式串第一次出现的下标，找不到返回 -1。然后追踪在主串 "ababcababa" 中查找 "ababa" 的过程，重点展示失配时主串指针如何「不回退」。',
        steps: [
          `第一步：写出代码。i 扫主串只增不退，j 指模式串，失配时按 next 跳 j。
def kmp_search(text, pattern):
    n, m = len(text), len(pattern)
    if m == 0:
        return 0
    next_arr = build_next(pattern)   # 先求 next 数组
    j = 0                            # 模式串当前要比较的位置
    for i in range(n):               # i 扫主串，从不回退
        # 失配：把 j 按 next 回退，尝试更短的已匹配前缀
        while j > 0 and text[i] != pattern[j]:
            j = next_arr[j - 1]
        # 当前字符匹配，模式串指针前进
        if text[i] == pattern[j]:
            j = j + 1
        # 模式串走到末尾，说明完整匹配
        if j == m:
            return i - m + 1         # 匹配起点
    return -1                        # 没找到`,
          '第二步：理解逻辑。主串指针 i 在 for 里一路向前、绝不回退；j 指模式串当前位置。匹配则 j 前进；失配则 j 按 next[j-1] 跳到「已匹配前缀的合适位置」继续比。j 一旦到达 m，匹配成功，起点是 i−m+1。',
          '第三步：准备。模式串 "ababa" 的 next 数组：next=[0,0,1,2,3]（"a"→0、"ab"→0、"aba"→1、"abab"→2、"ababa"→3）。主串 "ababcababa"，n=10。j=0。',
          '第四步：i=0..3 一路匹配。text[0..3]="abab" 与 pattern[0..3]="abab" 全等，j 依次变成 1,2,3,4。到 i=4：text[4]="c"，pattern[j=4]="a"，失配！',
          '第五步：**关键的失配跳转**。j>0，把 j 跳到 next[3]=2，再比 text[4]"c" 与 pattern[2]"a"：仍不等，再跳 j=next[1]=0；此时 j==0 不再跳。比 text[4]"c" 与 pattern[0]"a"：不等，j 保持 0。注意：**整个过程主串指针 i 一直停在 4，从没回退**——这正是 KMP 比朴素法省时的地方（朴素法此时要把主串指针退回 1 重来）。',
          '第六步：i=5 起继续。text[5..9]="ababa" 与 pattern 逐位匹配，j 依次 1,2,3,4,5。到 i=9 时 j==5==m，匹配成功！返回起点 i−m+1=9−5+1=**5**。即 "ababa" 在主串下标 5 处。全程主串只扫了一遍（i 从 0 到 9），复杂度 $O(n+m)$。',
        ],
      },
    ],
    pitfalls: [
      '**朴素匹配失配时丢掉了已匹配的信息**。每次失配都把主串指针退回、从头比，最坏 $O(nm)$。KMP 的改进点就是「失配时主串指针不回退」——这是两者效率差距的根源。',
      '**误解 next 数组的含义**。next[i] 不是「位置 i 的某种编号」，而是「子串 pattern[0..i] 的**最长相同前后缀**的长度」（前后缀都不含整段本身）。理解错它，KMP 的跳转逻辑就全乱了。',
      '求 next 时「相同前后缀」把整段算进去了。前缀、后缀都必须是**真**前后缀（不能等于整段本身），否则 next[i] 会错误地等于段长，导致死循环或匹配错误。',
      'KMP 失配跳转写成 j 退到 next[j]（应是 next[j-1]）。失配发生在比较 pattern[j] 时，要利用的是「前面 j 个已匹配字符」的最长相同前后缀，即 next[j-1]，不是 next[j]。',
      '匹配成功后忘了「起点 = i − m + 1」。i 是匹配到末尾时主串的位置、m 是模式串长，起点要往前推 m−1 位。直接返回 i 会得到末尾位置而非起点。',
    ],
    exercises: [
      {
        problem: '请手工求出模式串 "aabaaab" 的 next 数组（next[i] = 子串 pattern[0..i] 的最长相同前后缀长度，前后缀都不含整段本身）。逐位写出你的判断。',
        hint: '一位一位看：对每个前缀子串，找「既是它的开头一段、又是它的结尾一段」的最长长度。比如 "aa" 的最长相同前后缀是 "a"（长度 1）。',
        answer: `**详细讲解**：

**回顾含义**：next[i] = 子串 pattern[0..i] 里「既是前缀、又是后缀」的最长那一段的长度（前缀和后缀都不能是整段本身）。下标从 0 开始，模式串 "aabaaab" 共 7 位。

**逐位判断**：
- **i=0，"a"**：单个字符，没有「不含自身」的前后缀 → next[0] = **0**。
- **i=1，"aa"**：前缀{"a"}、后缀{"a"}，公共最长 "a" → next[1] = **1**。
- **i=2，"aab"**：前缀 a/aa、后缀 b/ab，没有公共的相同前后缀 → next[2] = **0**。
- **i=3，"aaba"**：开头 "a"、结尾 "a" 相同（长度 1）；再看长度 2：开头 "aa"、结尾 "ba" 不同。最长 → next[3] = **1**。
- **i=4，"aabaa"**：长度 2：开头 "aa"、结尾 "aa" 相同！长度 3：开头 "aab"、结尾 "baa" 不同。最长 → next[4] = **2**。
- **i=5，"aabaaa"**：长度 2：开头 "aa"、结尾 "aa" 相同；长度 3：开头 "aab"、结尾 "aaa" 不同。最长 → next[5] = **2**。
- **i=6，"aabaaab"**：结尾是 "b"。长度 3：开头 "aab"、结尾 "aab" 相同！长度 4：开头 "aaba"、结尾 "aaab" 不同。最长 → next[6] = **3**。

**最终结果**：next = **[0, 1, 0, 1, 2, 2, 3]**。

**复杂度分析**：用程序递推求 next 数组是 $O(m)$（m 是模式串长度），这里 m=7。手工时是一位一位推，每位看「上一位的最长相同前后缀能否再延长一位」，本质上是同一个递推过程。

**小结**：next 数组是模式串「自我重复结构」的浓缩。比如 next[6]=3 说明 "aabaaab" 的开头 "aab" 和结尾 "aab" 一样——KMP 在匹配主串失配时，正是靠这种信息「跳」到正确位置，而不必从头重比。`
      },
      {
        problem: '用**朴素匹配**在主串 "aaaaab" 中查找模式串 "aaab"，请数一数总共进行了多少次字符比较。再说明：如果主串是 n 个 "a" 后跟一个 "b"、模式串是 (m−1) 个 "a" 后跟一个 "b"，朴素匹配为什么会达到最坏的 $O(nm)$？',
        hint: '朴素匹配每个起点都把模式串尽量往后比。"aaab" 在 "aaaa" 这种位置会比到最后一位才发现 b 对不上，几乎每次都白比 m 次。',
        answer: `**详细讲解**：

**先数 "aaaaab" 找 "aaab" 的比较次数**。主串长 n=6、模式串长 m=4，对齐起点 i 从 0 试到 n−m=2，再看后面。

- **i=0**：比 a=a √、a=a √、a=a √、第 4 位 主串[3]="a" vs 模式[3]="b" ✗。比了 **4 次**。
- **i=1**：a=a √、a=a √、a=a √、主串[4]="a" vs "b" ✗。比了 **4 次**。
- **i=2**：a=a √、a=a √、a=a √、主串[5]="b" vs "b" √，**全匹配**！比了 **4 次**，在下标 2 找到。

**总比较次数 = 4 + 4 + 4 = 12 次**。可以看到 i=0、i=1 这两次都「比到最后一位才发现 b 对不上」，前 3 次比较全白费了。

**为什么最坏是 $O(nm)$**：
- 设主串是 n−1 个 "a" 加一个 "b"（共 n 位），模式串是 m−1 个 "a" 加一个 "b"（共 m 位）。
- 对**几乎每一个**对齐起点 i，模式串前面 m−1 个 "a" 都能和主串的 "a" 匹配上，一直比到模式串**最后一位** "b" 才发现和主串当前位的 "a" 不匹配（除非正好对到主串末尾的 "b"）。
- 也就是说，几乎每个起点都要白白比较约 m 次才失配；而起点大约有 n 个。
- 总比较次数约 $n\\times m$，所以朴素匹配最坏复杂度是 **$O(nm)$**。

**KMP 如何避免**：
- 这种「大量重复字符」正是朴素匹配的死穴，却是 KMP 的强项。KMP 预处理出模式串的 next 数组后，失配时**主串指针完全不回退**，只把模式串指针按 next 跳。
- 于是主串从头到尾只扫一遍（$O(n)$），加上预处理 next 的 $O(m)$，总复杂度降到 **$O(n+m)$**——在这种最坏数据上，KMP 比朴素法快得多。

**结论**：朴素匹配在「主串模式串高度重复」时最坏退化到 $O(nm)$；KMP 用 next 数组消除主串回退，稳定做到 $O(n+m)$。`
      },
      {
        problem: 'KMP 比朴素匹配快，关键在于「失配时主串指针不回退」。请解释 next 数组到底凭什么让主串指针不用回退，并以模式串 "abab" 在某次比到第 5 位（下标 4）失配为例，说明模式串指针应该跳到哪里、为什么这样跳是正确的。',
        hint: 'next 数组记录的是模式串「最长相同前后缀」。失配时，前面已匹配的那段后缀（也就是主串对应的那段）正好等于模式串的某段前缀，所以这段不用重比，主串不用退。',
        answer: `**详细讲解**：

**next 数组凭什么让主串指针不回退**：
- 当 KMP 比到某位失配时，模式串前面已经有一段（设长度 j）和主串对应位置**完全匹配**了。
- next[j−1] 告诉我们：这段长度为 j 的已匹配前缀，它的「**最长相同前后缀**」有多长（设为 k）。这意味着：这段的**后缀**（长度 k）和它的**前缀**（长度 k）是一样的。
- 关键推理：这段已匹配前缀的「后缀」，正是**主串中刚刚比过的那一段的末尾**。既然它等于模式串的前缀（长度 k），那么把模式串指针 j 直接跳到 k，这前 k 位就**天然已经匹配**，无需重比——所以**主串指针 i 不用回退**，从失配处继续往后比就行。
- 一句话：next 数组让我们「跳过那些必然已匹配的前缀」，把模式串指针挪到正确位置，而主串指针一路向前。

**以 "abab" 比到下标 4 失配为例**：
- 先求 next：next[0]=0（"a"）、next[1]=0（"ab"）、next[2]=1（"aba"，前后缀都有 "a"）、next[3]=2（"abab"，前后缀都有 "ab"）。
- 假设主串某处与模式串 "abab" 已全部匹配了 4 位（j=4），在比「第 5 位」（模式串已没有第 5 位，或下一位）时失配。要利用的是 next[j−1] = next[3] = **2**。
- 所以模式串指针 j 从 4 **跳到 2**，主串指针不动，从下一位继续比模式串的下标 2（即第 3 个字符 "a"）。

**为什么这样跳是正确的**：
- "abab" 的最长相同前后缀是 "ab"（长度 2）：它的**前缀** "ab" 和**后缀** "ab" 相同。
- 已匹配的 4 位里，结尾那两位（主串里对应的 "ab"）等于模式串开头两位 "ab"。把 j 跳到 2 后，模式串前两位 "ab" 与主串这两位**已经对上了**，不必重新比较。
- 于是匹配可以从模式串下标 2 处无缝接着进行，主串一个字符都没回退——既不漏掉可能的匹配，又省掉了重复比较。

**复杂度分析**：正因为主串指针 i 全程只增不减（从头扫到尾），匹配阶段是 $O(n)$；预处理 next 是 $O(m)$。两者相加，KMP 总复杂度 $O(n+m)$，远优于朴素匹配最坏的 $O(nm)$。

**结论**：next 数组把模式串的「自我重复结构」预先算好，失配时据此让模式串指针「聪明地跳」、主串指针「绝不回退」——这就是 KMP 高效的全部秘密。`
      },
    ],
  },

});
