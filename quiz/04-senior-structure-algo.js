// 高中 · 数据结构与算法进阶 —— 离线选择题分片
// 覆盖：number-base / complexity-advanced / divide-conquer / graph-traversal / backtracking /
//       dp-advanced / shortest-path / mst / string-matching / hash-table / heap / bst / union-find
//       共 13 个节点，每节点 2 题（难度对应高一/高二）。
// 题型轮换：概念辨析 / 读程序写结果 / 找 bug；干扰项取材各节点小课的易错点。
// 加载顺序：必须在 knowledge-quiz-seed.js 之后加载（追加合并，不覆盖已有题目）。

function __appendQuiz(bank) {
  const Q = window.CODE_QUIZ_SEED;
  for (const [id, qs] of Object.entries(bank)) {
    (Q[id] = Q[id] || []).push(...qs);
  }
}
__appendQuiz({

  'number-base': [
    { q: '二进制数 1101 换算成十进制是（ ）',
      options: ['11', '13', '14', '1101'], correct: 1,
      explanation: '按位权展开：1×8 + 1×4 + 0×2 + 1×1 = **13**。Python 里 `int("1101", 2)` 得 13，`bin(13)` 得 "0b1101"。' },
    { q: 'Python 中 `0.1 + 0.2 == 0.3` 的结果是 False，原因是（ ）',
      options: ['Python 的加法有 bug', '0.1、0.2 在二进制里只能近似表示，相加产生极小误差', '浮点数不能参与比较运算', '应该写成 0.30 才相等'], correct: 1,
      explanation: '0.1 和 0.2 化成二进制是**无限循环小数**，计算机只能存近似值，0.1+0.2 实际是 0.30000000000000004。比较小数要用 `abs(a-b) < 1e-9` 这类容差判断。' },
  ],

  'complexity-advanced': [
    { q: '迭代版二分查找的时间复杂度和空间复杂度分别是（ ）',
      options: ['时间 O(n)、空间 O(1)', '时间 O(log n)、空间 O(1)', '时间 O(log n)、空间 O(n)', '时间 O(1)、空间 O(log n)'], correct: 1,
      explanation: '每轮范围减半 → 时间 **O(log n)**；只用 left/right/mid 几个变量 → 空间 **O(1)**。若写成递归版，递归栈让空间变 O(log n)——时间与空间要分开分析。' },
    { q: '动态数组（如 Python 列表）的 append：偶尔要扩容搬移全部元素（单次 O(n)），但连续 n 次 append 总代价仍是 O(n)。描述"单次平均下来是 O(1)"用的概念是（ ）',
      options: ['最好情形复杂度', '均摊复杂度', '空间复杂度', '对数复杂度'], correct: 1,
      explanation: '**均摊复杂度**：把偶发的昂贵操作（扩容）平摊到大量便宜操作上。append 单次最坏 O(n)，均摊 **O(1)**——这是"平均很快、偶尔很慢"的标准分析工具。' },
  ],

  'divide-conquer': [
    { q: '归并排序的递归式为 $T(n) = 2T(n/2) + O(n)$，其中 $O(n)$ 这一项对应的是（ ）',
      options: ['把数组对半拆开的开销', '把两个有序子数组合并成一个的开销', '递归调用本身的开销', '比较两个元素的开销'], correct: 1,
      explanation: '$2T(n/2)$ 是递归解两个半边（分与治），$O(n)$ 是**合并**两个有序子数组要线性扫一遍。共 $\\log n$ 层、每层 O(n)，解得 $O(n\\log n)$。' },
    { q: '下列算法中**不属于**分治思想的是（ ）',
      options: ['归并排序', '快速排序', '冒泡排序', '快速幂（把 xⁿ 拆成 x^(n/2) 的平方）'], correct: 2,
      explanation: '**冒泡排序**是逐轮两两比较，没有"拆成同类子问题递归求解再合并"的结构。归并、快排、快速幂都是典型分治。' },
  ],

  'graph-traversal': [
    { q: '在迷宫（无权图）中求从起点到终点的**最短步数**，应该选用（ ）',
      options: ['DFS，因为它能走到最深处', 'BFS，因为它按距离一层层向外扩展，第一次到达终点时步数必然最短', '随便选，两者求最短路效果一样', '必须用 Dijkstra，BFS 不行'], correct: 1,
      explanation: '**BFS 用队列按层扩展**：先访问距离 1 的所有点、再距离 2……第一次碰到终点时的层数就是最短步数。DFS 先找到的路径不保证最短；无权图不需要动用 Dijkstra。' },
    { q: '在**有环**的图上写 DFS 时忘了 visited 标记，会发生（ ）',
      options: ['结果不变，只是稍微慢一点', '沿着环反复打转，无限递归直到栈溢出', '自动跳过环', '编译器会报语法错误'], correct: 1,
      explanation: '没有 visited，走到环上就会**绕圈重复访问**，递归无法终止，最终 RecursionError/栈溢出。"访问前先标记"是图遍历和树遍历最关键的区别。' },
  ],

  'backtracking': [
    { q: '用回溯法枚举 [1, 2, 3] 的全排列，总共会得到多少个排列（ ）',
      options: ['3 个', '6 个', '9 个', '27 个'], correct: 1,
      explanation: '3 个元素的全排列有 3! = 3×2×1 = **6** 个。回溯按"选一个 → 递归 → 撤销再换下一个"系统地枚举出每一种。' },
    { q: '回溯算法中，"做选择"之后必须配对执行"撤销选择"（恢复现场），否则会（ ）',
      options: ['程序直接报语法错误', '上一条路径的状态残留下来，污染后续分支的枚举结果', '只影响运行速度，结果仍然正确', '递归无法进入下一层'], correct: 1,
      explanation: '回溯的本质是**共享一份状态走遍所有分支**：退回上一层时必须把状态还原，否则"路径列表里还留着上条路的节点"这类脏状态会让后续分支的答案全部错乱。' },
  ],

  'dp-advanced': [
    { q: '0/1 背包问题中，状态 `dp[i][w]` 通常定义为（ ）',
      options: ['第 i 件物品的价值', '前 i 件物品中、总重量不超过 w 时能取得的最大价值', '装满容量 w 需要的最少物品数', '第 i 件物品是否被选中'], correct: 1,
      explanation: 'DP 第一步是**定义状态**：`dp[i][w]` = 只考虑前 i 件、容量 w 下的最大价值。状态定义不准，转移方程根本写不出来。' },
    { q: '0/1 背包转移方程 $dp[i][w]=\\max(dp[i-1][w],\\ dp[i-1][w-w_i]+v_i)$ 中，两项分别对应的决策是（ ）',
      options: ['第一项"选第 i 件"，第二项"不选第 i 件"', '第一项"不选第 i 件"，第二项"选第 i 件"（占 wᵢ 容量、得 vᵢ 价值）', '两项都是"选第 i 件"，只是顺序不同', '第一项是最好情况，第二项是最坏情况'], correct: 1,
      explanation: '`dp[i-1][w]` = **不选**第 i 件（容量原封不动）；`dp[i-1][w-wᵢ]+vᵢ` = **选**它（先留出 wᵢ 容量，再加上其价值）。每件物品"选或不选"取较优，正是 0/1 背包的骨架。' },
  ],

  'shortest-path': [
    { q: '关于 Dijkstra 算法的适用条件，正确的是（ ）',
      options: ['可以处理任何带权图，包括负权边', '只适用于**边权非负**的图；有负权边要改用 Bellman-Ford', '只能处理无权图', '只能用于无向图'], correct: 1,
      explanation: 'Dijkstra 的贪心前提是"已确定的最短距离不会再被改进"，**负权边会破坏这个前提**导致算错。负权 → Bellman-Ford；无权图用 BFS 就够。' },
    { q: '要求出图中**任意两点之间**的最短路（多源），最直接的算法及其复杂度是（ ）',
      options: ['BFS，O(n)', 'Dijkstra 一次，O(n log n)', 'Floyd 三重循环，O(n³)', 'Kruskal，O(m log m)'], correct: 2,
      explanation: '**Floyd** 用三重循环（依次尝试每个中转点）一次算出全部点对的最短路，O(n³)，还能处理负权边（无负环）。Dijkstra 一次只解**单源**；Kruskal 是最小生成树算法。' },
  ],

  'mst': [
    { q: '一个连通图有 n 个节点，它的最小生成树有多少条边（ ）',
      options: ['n', 'n - 1', 'n(n-1)/2', '取决于边权大小'], correct: 1,
      explanation: '生成树首先是**树**：n 个节点的树恒有 **n − 1** 条边。"最小"只是在所有生成树里挑总边权最小的，边数不变。' },
    { q: 'Kruskal 算法把边按权值从小到大逐条尝试加入，每次加入前必须检查的是（ ）',
      options: ['这条边是不是图中最长的边', '加入后是否会形成环（用并查集判断两端点是否已连通）', '两个端点的度数是否超过 2', '剩余的边数是否还够'], correct: 1,
      explanation: '树中不能有环：若一条边的两端**已在同一连通块**（find 结果相同），加入必成环，必须跳过。并查集让这个判断接近 O(1)，这正是 Kruskal = 贪心 + 并查集的原因。' },
  ],

  'string-matching': [
    { q: 'KMP 算法中 next 数组（前缀函数）里每个值的含义是（ ）',
      options: ['该位置字符在字母表中的序号', '模式串该前缀中"最长的相同真前缀与真后缀"的长度', '该位置到串尾的距离', '该字符出现的总次数'], correct: 1,
      explanation: 'next[i] = 模式串前 i 个字符中**最长相同前后缀的长度**——失配时它告诉我们模式串可以直接滑到哪个位置继续比，不必从头再来。理解 next 是理解 KMP 的关键。' },
    { q: '在长度为 n 的文本中用 KMP 查找长度为 m 的模式串，时间复杂度是（ ）',
      options: ['O(nm)', 'O(n + m)', 'O(n log m)', 'O(n²)'], correct: 1,
      explanation: 'KMP = 预处理 next 数组 O(m) + 扫描主串 O(n)，共 **O(n+m)**；主串指针**从不回退**。O(nm) 是朴素匹配的最坏情况。' },
  ],

  'hash-table': [
    { q: '哈希表中的"冲突"指的是（ ）',
      options: ['两个键的值相等', '不同的键经哈希函数计算后映射到了同一个位置', '哈希表满了放不下新元素', '键的类型不一致'], correct: 1,
      explanation: '**不同键 → 同一下标**就是冲突，不可避免，只能化解：链地址法（同一格挂链表）或开放寻址（顺延找空位）。' },
    { q: '字典（哈希表）查找的平均与最坏时间复杂度分别是（ ）',
      options: ['平均 O(1)，最坏 O(n)', '平均 O(log n)，最坏 O(n)', '任何时候都是 O(1)', '平均 O(1)，最坏 O(log n)'], correct: 0,
      explanation: '哈希均匀时一步定位 → 平均 **O(1)**；哈希函数极差或表太满时所有键挤在一格退化成链表 → 最坏 **O(n)**。"字典永远 O(1)"是常见误解。' },
  ],

  'heap': [
    { q: '执行下面代码后输出是（ ）\n`import heapq`\n`h = []`\n`for v in [3, 1, 2]:`\n`    heapq.heappush(h, v)`\n`print(heapq.heappop(h))`',
      options: ['3', '1', '2', '[1, 2, 3]'], correct: 1,
      explanation: 'heapq 是**小顶堆**：根永远是最小值，heappop 取出 **1**。插入与弹出都是 O(log n)。' },
    { q: '关于堆的性质，正确的说法是（ ）',
      options: ['堆是完全有序的，从头遍历数组就得到排序结果', '堆只保证根是最大（或最小）值，兄弟之间没有大小关系', '堆中任意节点都比它的父节点大', '堆必须用链表实现'], correct: 1,
      explanation: '堆只维护"**父 ≤ 子**（小顶堆）"这一条性质，根是极值，但整体**不是有序的**——想要全部有序要连续 pop（这就是堆排序）。堆通常用数组存完全二叉树。' },
  ],

  'bst': [
    { q: '向空二叉搜索树依次插入 5、3、8、1，然后做**中序遍历**，得到的序列是（ ）',
      options: ['5, 3, 8, 1', '1, 3, 5, 8', '8, 5, 3, 1', '5, 3, 1, 8'], correct: 1,
      explanation: 'BST 的性质：左 < 根 < 右，**中序遍历（左→根→右）必然得到升序** **1, 3, 5, 8**。这也是检验一棵树是不是合法 BST 的常用方法。' },
    { q: '把**已排好序**的数据依次插入二叉搜索树，会导致（ ）',
      options: ['树更加平衡，查找更快', '树退化成一条链，查找从 O(log n) 恶化为 O(n)', '插入会报错', '没有任何影响'], correct: 1,
      explanation: '有序插入时每个新节点都比之前的大（或小），全部挂向同一侧，树**退化成链表**。AVL、红黑树等平衡树通过旋转避免这一退化。' },
  ],

  'union-find': [
    { q: '并查集支持的两种核心操作是（ ）',
      options: ['排序与查找', '查询元素所在集合的代表（find）与合并两个集合（union）', '入栈与出栈', '插入与删除任意元素'], correct: 1,
      explanation: '并查集专职维护"**谁和谁在同一组**"：`find(x)` 找到 x 所在树的根作为集合代表，`union(a, b)` 把两棵树合并。判断同组：`find(a) == find(b)`。' },
    { q: '并查集"路径压缩"优化的做法与效果是（ ）',
      options: ['把树按节点值重新排序，方便二分', 'find 时把沿途节点直接挂到根上，让后续查询几乎 O(1)', '把每个集合复制一份备用', '删除树中多余的边'], correct: 1,
      explanation: '**路径压缩**：find 沿途把经过的节点的父指针直接改指根，树被"压扁"。配合按秩合并，单次操作均摊近似 O(1)；不压缩的话长链会让查询退化成 O(n)。' },
  ],

});
