// 初中编程 · 程序设计 —— 详细小课（习题讲解版）
// 覆盖 8 个节点：function / params-return / scope / recursion / module-lib / oop-class / exception / debugging
// 数据结构对齐 node-detail.jsx 的渲染：
//   warmup{q,hint,answer} / intro[] / formulas[{name,latex,note}] /
//   methods[{name,when,steps[],example{problem,solution[]}}] / worked[{title,problem,steps[]}] /
//   pitfalls[] / exercises[{problem,hint,answer}]
// 注意：node-detail.jsx 不渲染 derivation；行内公式用 $...$，formulas.latex 直接写 LaTeX。
// 凡含代码或引号的字段值，一律用反引号模板字符串（保留 \n 表示换行）。

Object.assign(window.CODE_LESSON, {

  // ───────────────────────────────────────────────────────────────
  // 1. 函数
  // ───────────────────────────────────────────────────────────────
  'function': {
    warmup: {
      q: `你写了一个程序，要在三个地方分别打印一条漂亮的分隔线（一串 ★）。你把那行 print 复制粘贴了三遍。现在老师说"分隔线改成一串 ●"。你要改几个地方？如果有 50 处呢？`,
      hint: `想一想：同一段代码出现很多遍，会带来什么麻烦？有没有办法让它"只写一次，到处使用"？`,
      answer: `复制三遍就要改三处，50 处就要改 50 处——又累又容易漏改。更好的办法是把这段代码**打包成一个函数**：\`def line(): print("★" * 20)\`，需要的地方就写 \`line()\` 调用它。以后想换样式，只改函数里面那一行，所有调用处自动跟着变。这就是函数最大的价值——**把可重复使用的代码起个名字，写一次、用很多次**。`
    },
    intro: [
      `函数是程序里的"乐高积木"：把一段能反复使用的代码打包起来，起一个名字，以后用名字就能调用它。它不研究某个具体算法，而是教你**怎样把程序拆成一块块清晰、可复用的小零件**。`,
      `为什么需要函数？两个核心理由。第一，**避免重复**：同一段逻辑只写一次，要用就调用，改起来只改一处。第二，**让逻辑清晰**：把"算总分""判断及格""打印报告"各自封装成函数，主程序读起来就像在读一句句中文，而不是一大坨细节。`,
      `在 Python 里，用关键字 \`def\` 定义函数，调用时写"函数名 + 圆括号"。最简单的形式是：\`def 函数名():\` 下面缩进写函数体；调用就写 \`函数名()\`。注意——**只"定义"不"调用"，函数体一行都不会执行**；函数就像菜谱，写好了还得有人去"按菜谱做"才会真的做出菜。`,
      `一个好函数最好**只做一件事**，并且有一个见名知意的名字。比如 \`print_line()\` 一看就知道是打印分隔线，\`calc_average()\` 一看就知道是算平均值。函数做完一件事后，要么产生一个"效果"（比如打印到屏幕），要么交回一个"结果"（用 return，下一节细讲）。`,
      `一句话记住：**函数 = 把一段代码起名字、写一次、反复调用。** 它是后面参数、返回值、递归、面向对象等所有内容的地基。`
    ],
    formulas: [
      { name: '函数定义', latex: '\\texttt{def f(x):}',
        note: `用 \`def\` 开头，后面是函数名和圆括号，括号里是参数（可以没有），最后一个**冒号**不能漏。冒号下面缩进的部分就是函数体。` },
      { name: '函数调用', latex: '\\texttt{f(3)}',
        note: `写"函数名 + 圆括号"才会真正执行函数体。光有定义、没有调用，函数永远不会运行。括号里按需要填入实际的数据。` },
      { name: '定义 vs 调用', latex: '\\text{定义（写菜谱）} \\ne \\text{调用（照菜谱做）}',
        note: `定义只是"登记"了这个函数的做法，调用才是"真正去做"。一个函数可以定义一次、调用很多次。` },
    ],
    methods: [
      {
        name: '函数封装五步法',
        when: `当你发现一段代码被重复写了好几遍，或者一段逻辑太长想拆清楚时`,
        steps: [
          `找重复 / 找职责：圈出"反复出现的代码"或"能独立说清的一件事"（如"打印分隔线""判断成绩等级"）`,
          `起名字：用 \`def\` + 一个见名知意的动词短语命名，如 \`print_line\`、\`grade_of\``,
          `搬进函数体：把那段代码原样剪进函数里，注意整体缩进一层`,
          `留出"接口"：想清楚它需要哪些输入（参数）、要不要交回结果（return）——本节先做无参数的简单版`,
          `替换调用：把原来重复的地方统统换成"函数名()"调用，跑一遍确认效果不变`,
        ],
        example: {
          problem: `程序里有三处都写了 \`print("=" * 30)\` 来画分隔线。请把它封装成函数。`,
          solution: [
            `解：这段代码重复出现，正好封装。`,
            `定义函数：\`def print_line():\` 然后缩进写 \`print("=" * 30)\`。`,
            `把原来三处 \`print("=" * 30)\` 都改成调用 \`print_line()\`。`,
            `答：以后想把分隔线改成 40 个 "-"，只需改函数体里那一行 \`print("-" * 40)\`，三处调用自动生效。`,
          ],
        },
      },
      {
        name: '"先定义后调用"检查法',
        when: `当程序报 \`NameError: name 'xxx' is not defined\`、或者函数好像"没运行"时`,
        steps: [
          `检查一：函数是不是只"定义"了却忘了"调用"？只有写了 \`函数名()\` 才会执行`,
          `检查二：调用是不是写在了定义的**上面**？Python 从上往下读，必须先 \`def\` 定义、后面才能调用`,
          `检查三：调用时函数名有没有写错、圆括号有没有漏？漏了括号只是"提到"函数，不会运行它`,
          `检查四：函数体有没有正确缩进？缩进错了，Python 会以为函数体是空的或越界`,
        ],
        example: {
          problem: `下面代码为什么没有任何输出？\n\`def hello():\n    print("你好")\``,
          solution: [
            `解：代码只**定义**了 \`hello\` 函数，从头到尾没有**调用**它。`,
            `函数像菜谱：写好了还得有人照着做。`,
            `答：在定义后面补一行调用 \`hello()\`，就会打印"你好"了。`,
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 把重复代码封装成函数',
        problem: `小明要分别给小红、小华、小刚发同样格式的问候："你好，XXX！欢迎来到编程课。" 他原本复制了三遍 print。请用函数改写，让"问候格式"只写一次。`,
        steps: [
          `第一步：审题。三句问候只有名字不同，格式完全一样——这是典型的"该封装成函数"的信号。`,
          `第二步：定义函数。给它一个名字 \`greet\`，并让名字作为输入（参数 name）：\n\`def greet(name):\n    print("你好，" + name + "！欢迎来到编程课。")\``,
          `第三步：调用三次，每次传不同的名字：\n\`greet("小红")\n\`greet("小华")\n\`greet("小刚")\``,
          `第四步：体会好处。如果以后格式要改成"Hi，XXX！"，只改函数体那一行，三处调用全部自动更新。这就是"写一次、用很多次"。`,
        ],
      },
    ],
    pitfalls: [
      `**只定义、忘调用**：写了 \`def f(): ...\` 却没写 \`f()\`，函数体一行都不会执行，程序"没反应"。`,
      `**漏掉冒号或缩进**：\`def f()\` 后面必须有冒号，函数体必须缩进。漏冒号是语法错，缩进错会让函数体跑空或报错。`,
      `**调用写在定义上面**：Python 从上往下执行，必须先 \`def\` 定义、之后才能调用，否则 \`NameError\`。`,
      `**调用时漏写圆括号**：只写函数名 \`f\` 是在"提到"这个函数（拿到函数本身），写 \`f()\` 才是"运行"它。`,
      `**一个函数塞太多事**：函数最好只做一件事。又算分、又打印、又存文件全堆在一个函数里，会很难复用和调试。`,
    ],
    exercises: [
      {
        problem: `下面这段代码运行后会输出什么？为什么？\n\`def say():\n    print("hi")\n\nprint("start")\nsay()\nsay()\``,
        hint: `把程序一行一行"假装自己是电脑"执行：遇到 \`def\` 时做什么？遇到 \`say()\` 时又做什么？`,
        answer: `**详细讲解**：输出是三行——\`start\`、\`hi\`、\`hi\`。我们逐行模拟。第一段 \`def say(): print("hi")\` 只是**定义**函数，**此时并不会打印 hi**，电脑只是把"say 的做法"记下来。接着执行 \`print("start")\`，屏幕出现 \`start\`。然后 \`say()\` 是第一次**调用**，函数体运行，打印 \`hi\`。再 \`say()\` 又调用一次，再打印一个 \`hi\`。所以顺序是 \`start\` → \`hi\` → \`hi\`。这道题的关键在于分清**定义不执行、调用才执行**：同一个函数定义一次，可以调用任意多次。`
      },
      {
        problem: `小华写了个函数想算两个数的和，可是运行时报错 \`NameError: name 'add' is not defined\`。他的代码是：\n\`add(3, 5)\n\ndef add(a, b):\n    print(a + b)\`\n请指出错在哪、怎么改。`,
        hint: `Python 是从上往下一行行读的。报错说"add 没有被定义"，可是下面明明定义了 add……是不是顺序有问题？`,
        answer: `**详细讲解**：错在**调用写在了定义的前面**。Python 从上往下执行，读到第一行 \`add(3, 5)\` 时，下面的 \`def add\` 还没被执行过，电脑还不知道 \`add\` 是什么，于是报 \`NameError: name 'add' is not defined\`。改法是把"定义"放到"调用"的**上面**：\n\`def add(a, b):\n    print(a + b)\n\nadd(3, 5)\`\n这样先登记好 \`add\` 的做法，再去调用就能找到了，输出 8。记住规则：**先 def 定义，后调用**。`
      },
      {
        problem: `请写一个函数 \`print_star_line\`，作用是打印一行 20 个星号（★）。然后调用它两次。`,
        hint: `定义用 \`def 函数名():\`，函数体里用 \`print\`；想打印 20 个星号可以用字符串乘法 \`"★" * 20\`。调用就是写"函数名()"。`,
        answer: `**详细讲解**：思路分两步——先定义、再调用。参考代码：\n\`def print_star_line():\n    print("★" * 20)   # 字符串乘 20，得到 20 个星号\n\nprint_star_line()   # 第一次调用\nprint_star_line()   # 第二次调用\`\n为什么这样写：\`"★" * 20\` 利用了字符串"乘一个整数 = 重复多次"的特性，比手敲 20 个星号省事又不易错；把它放进 \`def print_star_line():\` 里就完成了封装。最后写两行 \`print_star_line()\` 就会打印两行星号。好处是——以后想改成 30 个、或换成别的符号，只改函数体那一行即可，所有调用处都跟着变。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 2. 参数与返回值
  // ───────────────────────────────────────────────────────────────
  'params-return': {
    warmup: {
      q: `有两个函数都是"算两个数的和"。\nA：\`def add_a(a, b): print(a + b)\`\nB：\`def add_b(a, b): return a + b\`\n如果我想把和再乘以 2，哪个函数能直接做到 \`add(3, 5) * 2\`？为什么？`,
      hint: `\`print\` 是"把结果显示出来"，\`return\` 是"把结果交回给调用者"。被显示出来的数字，程序还能再拿去算吗？`,
      answer: `只有 B 能做到 \`add_b(3, 5) * 2 = 16\`。区别在于：A 用 \`print\` 只是把和**显示在屏幕上**，函数本身没有"交回"任何值（默认返回 None），所以 \`add_a(3,5) * 2\` 会报错（None 不能乘）。B 用 \`return\` 把和**交回给调用处**，于是 \`add_b(3,5)\` 这个表达式就等于 8，可以继续参与运算。**print 是给人看，return 是给程序用**——这是初学者最容易混的一点。`
    },
    intro: [
      `这一节讲函数的"输入"和"输出"。**参数**是函数的输入口——让同一个函数能处理不同的数据；**返回值**（return）是函数的输出口——把算好的结果交回给调用它的地方。`,
      `先说参数。定义时括号里写的叫**形参**（形式参数，是个占位的名字），调用时括号里填的叫**实参**（实际参数，是真正的数据）。调用时实参会按顺序"对号入座"传给形参。比如 \`def power(base, n)\` 里 base、n 是形参，调用 \`power(2, 3)\` 时 2 给 base、3 给 n。`,
      `再说返回值。\`return\` 做两件事：把一个值交回给调用处，并**立即结束函数**（return 后面的代码不再执行）。拿到返回值后，调用处可以把它存进变量、参与运算、再传给别的函数。**没有写 return 的函数，默认返回 None**。`,
      `Python 的参数还有几种好用的写法。**默认参数**：定义时给形参一个默认值，调用时不传就用默认值，如 \`def greet(name, greeting="你好")\`。**关键字参数**：调用时写 \`参数名=值\`，可以不按顺序、还更清楚，如 \`greet(greeting="Hi", name="Tom")\`。这些让函数更灵活，但要节制使用，保持接口简单。`,
      `一句话抓住核心：**参数让函数"能处理不同输入"，return 让调用者"拿得到结果"。** 想清楚一个函数"需要什么输入、要交回什么输出"，函数就设计对了一大半。`
    ],
    formulas: [
      { name: '带参数与返回值', latex: '\\texttt{def f(x): return x + 1}',
        note: `\`x\` 是参数（输入），\`return x + 1\` 把结果交回（输出）。调用 \`f(10)\` 这个表达式就等于 11，可以继续参与运算。` },
      { name: '默认参数', latex: "\\texttt{def f(x, step=1): ...}",
        note: `给形参一个默认值，调用时不传就用默认值。如 \`f(10)\` 用 step=1，\`f(10, 5)\` 用 step=5。默认参数必须写在普通参数后面。` },
      { name: 'return 的两个作用', latex: '\\text{交回一个值} + \\text{立即结束函数}',
        note: `\`return\` 之后的代码不会执行；没写 \`return\` 的函数默认返回 \`None\`。\`print\` 只是显示，不等于 \`return\`。` },
    ],
    methods: [
      {
        name: '设计函数接口：想清"进什么、出什么"',
        when: `当你要写一个新函数、纠结括号里该放什么、要不要 return 时`,
        steps: [
          `问"它需要哪些信息才能干活"——这些就是**参数**（如算长方形面积需要长和宽）`,
          `问"它干完后，调用者要不要拿到一个结果继续用"——要就用 \`return\` 交回，不要（只是打印/存档）就不用`,
          `给参数起见名知意的名字，定义形参；在函数体最后用 \`return\` 交回结果`,
          `调用时按顺序填实参，并用一个变量接住返回值，如 \`area = rect_area(3, 4)\``,
        ],
        example: {
          problem: `写一个函数算长方形面积，并能把结果继续用（比如再求两个长方形面积之和）。`,
          solution: [
            `解：算面积需要"长"和"宽"两个输入，所以设两个参数；结果要能继续算，所以用 return 交回。`,
            `定义：\`def rect_area(length, width):\` 然后 \`return length * width\`。`,
            `调用并接住结果：\`a = rect_area(3, 4)\`（a=12），\`b = rect_area(2, 5)\`（b=10）。`,
            `答：\`total = a + b\` 得 22。因为用了 return，面积才能被再次拿去相加。`,
          ],
        },
      },
      {
        name: '默认参数 / 关键字参数用法',
        when: `当一个函数大多数时候用同一个值、偶尔才需要换，或参数太多怕传错顺序时`,
        steps: [
          `把"常用值"设成默认参数：\`def greet(name, greeting="你好")\`，平时调 \`greet("小明")\` 即可`,
          `需要换时再传：\`greet("Tom", "Hi")\`，或用关键字更清楚 \`greet("Tom", greeting="Hi")\``,
          `参数多、易混顺序时，调用全部写成"参数名=值"，如 \`draw(x=10, y=20, color="red")\``,
          `牢记：**默认参数必须放在普通参数后面**，否则语法错`,
        ],
        example: {
          problem: `写一个问候函数，默认说"你好"，但允许调用者换成别的招呼语。`,
          solution: [
            `解：把招呼语设成默认参数。`,
            `定义：\`def greet(name, greeting="你好"):\` 然后 \`return greeting + "，" + name\`。`,
            `调用 \`greet("小明")\` → "你好，小明"（用了默认值）。`,
            `答：调用 \`greet("Tom", "Hi")\` → "Hi，Tom"（默认值被覆盖）。`,
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · return 与 print 的区别',
        problem: `下面两个函数，分别调用 \`r1 = f1(4)\` 和 \`r2 = f2(4)\` 之后，再执行 \`print(r1)\`、\`print(r2)\`，各会打印什么？\n\`def f1(x):\n    print(x * x)\n\ndef f2(x):\n    return x * x\``,
        steps: [
          `第一步：分析 f1。\`f1(4)\` 内部执行 \`print(16)\`，屏幕先出现 \`16\`；但 f1 没有 return，所以函数**返回 None**，于是 \`r1 = None\`。`,
          `第二步：分析 f2。\`f2(4)\` 内部 \`return 16\`，**不打印**任何东西，但把 16 交回，于是 \`r2 = 16\`。`,
          `第三步：执行后面的两个 print。\`print(r1)\` 打印 \`None\`；\`print(r2)\` 打印 \`16\`。`,
          `第四步：汇总屏幕输出顺序——先是 f1 内部打的 \`16\`，然后 \`None\`（来自 print(r1)），最后 \`16\`（来自 print(r2)）。结论：**print 把结果"显示出来"，return 把结果"交回去存进变量"**，两者完全不同。`,
        ],
      },
      {
        title: '例题 · 用默认参数算幂',
        problem: `写一个函数 \`power\`，求 base 的 n 次方；当不指定 n 时默认求平方。要求：\`power(5)\` 得 25，\`power(2, 3)\` 得 8。`,
        steps: [
          `第一步：审题。需要两个输入：底数 base、指数 n；其中 n 要有默认值 2（不传时求平方）。结果要交回，所以用 return。`,
          `第二步：定义函数（默认参数写在后面）：\n\`def power(base, n=2):\n    return base ** n\`（\`**\` 是 Python 的幂运算符）。`,
          `第三步：验证调用。\`power(5)\` 没传 n，用默认 n=2，得 \`5 ** 2 = 25\`；\`power(2, 3)\` 传了 n=3，得 \`2 ** 3 = 8\`。`,
          `第四步：作答。函数同时满足两种调用方式，靠的就是默认参数——常用情形省着写，特殊情形再补上。`,
        ],
      },
    ],
    pitfalls: [
      `**混淆 print 和 return**：\`print\` 只是把结果显示给人看，函数本身仍返回 None；要让结果能被继续运算，必须用 \`return\` 交回。`,
      `**忘记接住返回值**：写了 \`add(3, 5)\` 却没赋给变量，结果就"飞走了"。要写 \`s = add(3, 5)\` 才留得住。`,
      `**实参顺序传错**：\`def sub(a, b): return a - b\`，调用 \`sub(3, 10)\` 是 3-10=-7，不是 10-3。位置参数严格按顺序对号入座。`,
      `**默认参数放错位置**：\`def f(a=1, b)\` 是语法错。默认参数必须排在没有默认值的参数后面：\`def f(b, a=1)\`。`,
      `**可变对象当默认参数**：\`def f(x=[])\` 多次调用会共用同一个列表，是经典陷阱。需要默认空列表时应写 \`def f(x=None): if x is None: x = []\`。`,
    ],
    exercises: [
      {
        problem: `判断下面代码的输出，并解释原因：\n\`def double(x):\n    print(x * 2)\n\nresult = double(5)\nprint(result)\``,
        hint: `\`double\` 里用的是 print 还是 return？一个没有 return 的函数，它"交回"给 result 的是什么？`,
        answer: `**详细讲解**：输出两行——\`10\` 和 \`None\`。逐步看：调用 \`double(5)\` 时，函数体执行 \`print(5 * 2)\`，屏幕先出现 \`10\`。但 \`double\` 函数里**只有 print、没有 return**，所以它默认**返回 None**，于是 \`result\` 被赋成 \`None\`。接着 \`print(result)\` 就打印出 \`None\`。这道题专门考"print ≠ return"：很多同学以为函数打印了 10，result 就应该是 10，其实 print 只是顺手显示，函数若不写 return 就什么都不交回。要让 result 等于 10，得把函数改成 \`return x * 2\`。`
      },
      {
        problem: `写一个函数 \`grade\`，输入一个分数 score，**返回**（不是打印）等级字符串：90 及以上返回 "A"，60 到 89 返回 "B"，60 以下返回 "C"。然后用它打印 \`grade(95)\` 的结果。`,
        hint: `用 if / elif / else 分支判断；题目强调"返回"，所以每个分支用 \`return\` 交回等级，而不是 print。`,
        answer: `**详细讲解**：题目要求"返回"等级，所以函数内用 return、把打印留到外面。参考代码：\n\`def grade(score):\n    if score >= 90:\n        return "A"\n    elif score >= 60:\n        return "B"\n    else:\n        return "C"\n\nprint(grade(95))   # 输出 A\`\n为什么用 return 而不是 print：返回值能让调用者"拿到结果再决定怎么用"——可以打印、可以存进列表、可以再比较。这里 \`grade(95)\` 这个表达式等于 "A"，外面再用 print 显示。另外，return 还会**立即结束函数**，所以一旦 \`score >= 90\` 成立返回 "A"，后面的 elif/else 就不会再执行，不会出现重复判断。`
      },
      {
        problem: `下面想用默认参数写一个"重复打印一句话"的函数，但有一处写错了，运行报 SyntaxError。请找出错误并改正：\n\`def repeat(times=3, msg):\n    for i in range(times):\n        print(msg)\``,
        hint: `Python 规定带默认值的参数和不带默认值的参数，谁在前谁在后是有要求的。看看 times 和 msg 的位置。`,
        answer: `**详细讲解**：错误在于**带默认值的参数 \`times=3\` 排在了没有默认值的参数 \`msg\` 前面**，这是 Python 不允许的，会报 \`SyntaxError: non-default argument follows default argument\`。规则是：**默认参数必须放在普通参数的后面**。改法是把没默认值的 \`msg\` 放前面、有默认值的 \`times\` 放后面：\n\`def repeat(msg, times=3):\n    for i in range(times):\n        print(msg)\`\n这样调用 \`repeat("hi")\` 会打印 3 次 "hi"（times 用默认值 3），\`repeat("yo", 2)\` 打印 2 次。道理是：如果允许默认参数在前，调用 \`repeat("hi")\` 时电脑就分不清这个 "hi" 到底是给 times 还是给 msg，所以语言干脆规定默认参数靠后。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 3. 作用域
  // ───────────────────────────────────────────────────────────────
  'scope': {
    warmup: {
      q: `运行下面代码会输出什么？\n\`x = 10\n\ndef change():\n    x = 99\n\nchange()\nprint(x)\`\n你觉得是 99 还是 10？`,
      hint: `函数里那个 \`x = 99\` 到底改的是外面的 x，还是函数自己新建了一个"同名但不同"的 x？函数结束后，它内部的变量去哪了？`,
      answer: `输出是 **10**，不是 99。函数 \`change\` 里的 \`x = 99\` 并没有改外面的全局 x，而是在函数内部**新建了一个局部的 x**（恰好同名）。这个局部 x 只在函数里有效，函数一结束就消失了，外面的全局 x 始终是 10。这就是**作用域**：变量"在哪里可见、活多久"是有范围的。要想在函数里真的改全局变量，得用 \`global\` 声明（后面细讲）。`
    },
    intro: [
      `作用域，研究的是一个变量"在哪里能被看见、能活多久"。它不教你算什么，而是解释一个常让初学者困惑的现象——"为什么我在函数里改了变量，外面却没变？"`,
      `先理解两类变量。**局部变量**：在函数内部定义（包括参数），只在这个函数里可见，**函数一结束就被销毁**，外面访问会报错。**全局变量**：在所有函数外面（模块顶层）定义，整个文件都能看到，程序运行期间一直存在。`,
      `关键规则一：**函数可以"读"外面的全局变量**。如果函数里用到一个变量、而函数内部没给它赋过值，Python 就去外面找全局的。比如全局有 \`pi = 3.14\`，函数里直接 \`print(pi)\` 是可以的。`,
      `关键规则二：**函数里一旦给某个变量赋值，它就被当成局部变量**。这正是热身题的陷阱——\`x = 99\` 让 x 变成了函数内部的新变量，跟外面的全局 x 没关系。如果你**真的想在函数里修改全局变量**，必须先用 \`global x\` 声明"我说的就是外面那个 x"。`,
      `一句话记住：**函数里能看外面，外面看不进函数里；函数里给变量赋值会"自立门户"建局部变量，想动全局得先 global 声明。** 理解作用域，就能想清楚"我的变量为什么没生效"。`
    ],
    formulas: [
      { name: '局部变量', latex: '\\text{函数内定义 → 只在函数内可见，函数结束即消失}',
        note: `函数的参数、函数体里赋值的变量都是局部的。在函数外面访问局部变量会 \`NameError\`。` },
      { name: '全局变量', latex: '\\text{函数外定义 → 整个文件可见，程序运行期间存在}',
        note: `函数内可以直接"读"全局变量；但函数内对它"赋值"会新建同名局部变量，而不是改全局。` },
      { name: 'global 声明', latex: '\\texttt{global x}',
        note: `想在函数内部"修改"全局变量 x，必须在函数开头写 \`global x\`，否则赋值只会创建局部变量。` },
    ],
    methods: [
      {
        name: '判断"这个变量是局部还是全局"',
        when: `当你搞不清某行代码里的变量指的是哪一个、或函数里改了变量外面没变时`,
        steps: [
          `看它在**哪里被赋值**：在函数体内被赋值（含参数）→ 通常是局部变量`,
          `看它在函数里是"读"还是"写"：只读不写、且函数内没赋过值 → 用的是外面的全局变量`,
          `若函数内对它赋了值，则它整段在函数里都算局部（哪怕和全局同名）`,
          `若确实要在函数内改全局，检查有没有写 \`global 变量名\` 声明`,
        ],
        example: {
          problem: `分析下面两个 x 分别是什么：\n\`x = 10\ndef f():\n    print(x)   # 这里的 x\n    y = x + 1  # 这里读 x\nf()\``,
          solution: [
            `解：函数 \`f\` 内部**只读 x、从没给 x 赋值**。`,
            `按规则，函数内没赋值的变量会去外面找全局的。`,
            `所以两处 x 用的都是**全局 x（10）**；\`print(x)\` 打印 10，\`y\` 变成 11（y 是局部的）。`,
            `答：x 是全局变量，y 是局部变量。`,
          ],
        },
      },
      {
        name: '在函数里安全修改全局变量',
        when: `当你确实需要让函数改变一个全局状态（如全局计数器）时`,
        steps: [
          `首选：尽量**不改全局**，改成"用参数传进来、用 return 交回去"，代码更清晰、更好测试`,
          `若确实要改全局：在函数**开头**写 \`global 变量名\``,
          `然后再对它赋值，这时改的就是外面那个全局变量`,
          `提醒自己：滥用 global 会让程序变量到处被偷偷改、难以追踪，能不用就不用`,
        ],
        example: {
          problem: `有一个全局计数器 \`count = 0\`，想用函数 \`add_one()\` 让它加 1。`,
          solution: [
            `解：要在函数里"修改"全局变量，必须 global 声明。`,
            `\`count = 0\n\ndef add_one():\n    global count   # 声明：我说的是外面那个 count\n    count = count + 1\``,
            `调用两次 \`add_one()\` 后，\`print(count)\` 输出 2。`,
            `答：没有 \`global count\` 这一行，\`count = count + 1\` 会因为"先读一个还没赋值的局部 count"而直接报错。`,
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 局部变量出了函数就消失',
        problem: `下面代码会怎样？请说明运行结果。\n\`def f():\n    msg = "我在函数里"\n    print(msg)\n\nf()\nprint(msg)\``,
        steps: [
          `第一步：分析函数内。\`msg\` 是在函数 \`f\` 内部赋值的，所以它是**局部变量**，只在 f 内可见。`,
          `第二步：执行 \`f()\`。函数体里 \`print(msg)\` 正常打印 "我在函数里"。`,
          `第三步：函数结束。f 一返回，它的局部变量 msg 立刻被销毁，外面再也看不到。`,
          `第四步：执行 \`print(msg)\`。此时外面（全局）根本没有 msg 这个变量，于是报错 \`NameError: name 'msg' is not defined\`。结论：**局部变量活不过函数本身**，想在外面用就得 return 出来。`,
        ],
      },
      {
        title: '例题 · global 让函数改动全局',
        problem: `对比下面两段代码，\`print(score)\` 分别输出什么？\n甲：\`score = 60\ndef up():\n    score = 100\nup()\nprint(score)\`\n乙：\`score = 60\ndef up():\n    global score\n    score = 100\nup()\nprint(score)\``,
        steps: [
          `第一步：看甲。函数 \`up\` 里 \`score = 100\` 是赋值，于是 score 被当成**局部变量**，外面的全局 score 没被动。`,
          `第二步：甲的结果。\`up()\` 内部新建并改了局部 score=100，函数结束就丢；外面 \`print(score)\` 仍是 **60**。`,
          `第三步：看乙。函数开头写了 \`global score\`，声明"我要操作的是外面那个 score"，于是 \`score = 100\` 改的是全局。`,
          `第四步：乙的结果。\`print(score)\` 输出 **100**。对比可见：决定"改不改得到全局"的，就是那一行 \`global\` 声明。`,
        ],
      },
    ],
    pitfalls: [
      `**以为函数里赋值能改全局**：函数里 \`x = 新值\` 默认是新建局部变量，外面的全局不变（这是最经典的作用域误解）。`,
      `**在函数外访问局部变量**：局部变量函数一结束就消失，外面用它会 \`NameError\`。要把结果带出来请用 \`return\`。`,
      `**忘写 global 就改全局**：函数里写 \`count = count + 1\` 但没 \`global count\`，会因为"先读了一个尚未赋值的局部 count"而报 \`UnboundLocalError\`。`,
      `**滥用 global**：到处用 global 改全局，会让变量在程序各处被偷偷修改、极难调试。优先用"参数进、return 出"。`,
      `**局部变量和全局同名造成误读**：同名不代表同一个。判断时先看"在函数里有没有被赋值"，被赋值就是局部的那个。`,
    ],
    exercises: [
      {
        problem: `运行下面代码会输出什么？解释为什么。\n\`n = 5\n\ndef show():\n    n = 20\n    print("函数内:", n)\n\nshow()\nprint("函数外:", n)\``,
        hint: `函数里 \`n = 20\` 是"赋值"，这会让 n 变成什么变量？它影响得到外面的全局 n 吗？`,
        answer: `**详细讲解**：输出两行——\`函数内: 20\` 和 \`函数外: 5\`。原因在作用域规则：函数 \`show\` 里写了 \`n = 20\`，这是一次**赋值**，于是 Python 把这个 n 当成 \`show\` 内部的**局部变量**，它和外面的全局 \`n\`（值为 5）是两个不同的变量，只是名字相同。所以函数内的 \`print\` 看到的是局部 n=20；函数运行完，局部 n 被销毁；外面的 \`print\` 看到的还是全局 n=5，从头到尾没被改过。这正是"函数里赋值不会改全局"的典型例子。如果想让函数真的把外面的 n 改成 20，需要在函数开头加一行 \`global n\`。`
      },
      {
        problem: `下面代码本想让函数把全局 \`total\` 累加 10，运行却报 \`UnboundLocalError: local variable 'total' referenced before assignment\`。请说明原因并改正：\n\`total = 0\n\ndef add_ten():\n    total = total + 10\n\nadd_ten()\nprint(total)\``,
        hint: `函数里既"读"又"写"了 total。一旦函数里对某变量赋值，Python 就把它当局部的——那么 \`total + 10\` 里读的那个 total 是谁？它有值吗？`,
        answer: `**详细讲解**：原因是作用域规则的连锁反应。函数里有 \`total = total + 10\`，因为左边对 total 赋了值，Python 就把这一整个函数里的 total **当成局部变量**。可是等号右边的 \`total + 10\` 又要先"读"这个局部 total——而此刻局部 total 还没被赋过值，于是报 \`UnboundLocalError\`（局部变量在赋值前就被使用）。改正办法是在函数开头声明 \`global total\`，告诉 Python"我操作的是外面那个 total"：\n\`total = 0\n\ndef add_ten():\n    global total\n    total = total + 10\n\nadd_ten()\nprint(total)   # 输出 10\`\n加了 global 后，读和写的都是全局 total，它本来就有值 0，所以不再报错。`
      },
      {
        problem: `不使用 global，改写下面这个"用全局变量传结果"的写法，让它通过"参数 + 返回值"来完成。原代码：\n\`result = 0\ndef square(x):\n    global result\n    result = x * x\nsquare(6)\nprint(result)\``,
        hint: `想想这一节强调的更好做法："参数进、return 出"。函数需要的输入用参数给，算出的结果用 return 交回，外面用变量接住。`,
        answer: `**详细讲解**：用 global 改全局虽然能跑，但不是好习惯——变量被函数偷偷改、难追踪。更清晰的写法是让函数"输入靠参数、输出靠 return"：\n\`def square(x):\n    return x * x      # 把结果交回，不碰任何全局变量\n\nresult = square(6)   # 外面用变量接住返回值\nprint(result)        # 输出 36\`\n为什么这样更好：第一，函数变成一个"纯粹的计算"——给它 x 就交回 x²，不依赖也不修改外部状态，谁调用都放心；第二，不需要 global，避免了作用域陷阱；第三，结果由调用者用变量接住，想叫 result 还是别的名字都行，函数本身不关心。这正是本节倡导的方向：**能用"参数 + return"就别用 global**。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 4. 递归
  // ───────────────────────────────────────────────────────────────
  'recursion': {
    warmup: {
      q: `俄罗斯套娃：打开一个娃娃，里面还是一个娃娃，再打开还是……直到最里面一个实心的小娃娃为止。如果让你用"函数调用自己"来描述"打开套娃"，你觉得最关键、绝对不能漏的一步是什么？`,
      hint: `如果套娃永远打不到底（没有最里面那个实心的），会怎样？程序里"函数一直调用自己、永远停不下来"会发生什么？`,
      answer: `最关键、绝对不能漏的，是**"最里面那个实心小娃娃"——也就是停下来的条件**。递归就是"函数在自己内部再调用自己"，每次处理一个更小的问题（打开一层），但**必须有一个"不再往下、直接给答案"的情形**（基线条件），否则就会无限调用自己、直到把内存的"调用栈"撑爆（栈溢出报错）。所以递归三要素是：**基线条件（何时停）+ 递归式（怎样调用更小的自己）+ 每次都向基线收敛（问题确实在变小）**。`
    },
    intro: [
      `递归，是一种"函数在自己内部再调用自己"的解题思路。它用"把大问题交给一个更小的同类问题"的方式，层层缩小，直到小到可以直接回答。很多看似复杂的问题（阶乘、斐波那契、树和图的遍历、分治排序）用递归写出来都格外简洁。`,
      `理解递归，抓住三要素就够了。**① 基线条件（base case）**：问题小到可以直接给出答案、不必再调用自己的情形——这是递归的"刹车"。**② 递归式**：把原问题表示成"一个更小的同类问题 + 一点额外处理"，并在函数里调用自己去解决那个更小的问题。**③ 向基线收敛**：每次递归，问题规模都必须**确实变小**，这样才能一步步逼近基线条件、最终停下来。`,
      `以阶乘为例：$n! = n \\times (n-1)!$，而 $1! = 1$。这里"$1! = 1$"就是基线条件，"$n \\times (n-1)!$"就是递归式（求 n! 要先求更小的 (n-1)!），而 n 每次减 1，是在向基线 1 收敛。写成代码就是：先判断 \`if n <= 1: return 1\`（刹车），否则 \`return n * factorial(n-1)\`（调用更小的自己）。`,
      `递归是怎么"算出来"的？分两个阶段。**递去**：f(4) 要等 f(3)，f(3) 要等 f(2)，f(2) 要等 f(1)，一层层往下调用、压进"调用栈"；到 f(1) 命中基线、直接返回 1。**归来**：从最里层往外回填——f(1)=1，f(2)=2×1=2，f(3)=3×2=6，f(4)=4×6=24。每一层都在等它"更小的自己"算完后再算自己。`,
      `一句话记住：**递归 = 自己调用自己解决更小的问题，但必须有基线条件让它停下来。** 没有基线条件，递归就会无限进行直到爆栈——这是递归第一大坑。复杂度上要小心：朴素递归算斐波那契会重复计算大量子问题，达到指数级 $O(2^n)$，慢得惊人（后面学动态规划会解决它）。`
    ],
    formulas: [
      { name: '递归三要素', latex: '\\text{基线条件} + \\text{递归式} + \\text{向基线收敛}',
        note: `缺基线 → 无限递归爆栈；缺"变小"→ 永远到不了基线。三者缺一不可。` },
      { name: '阶乘的递归式', latex: 'n! = n \\times (n-1)!,\\quad 1! = 1',
        note: `\`if n <= 1: return 1\`（基线），否则 \`return n * factorial(n-1)\`（递归式，n 每次减 1 向基线收敛）。` },
      { name: '斐波那契的递归式', latex: 'F(n) = F(n-1) + F(n-2),\\quad F(0)=0,\\ F(1)=1',
        note: `两个基线 F(0)=0、F(1)=1。注意它每次分裂成两个子调用，朴素递归会大量重复计算。` },
      { name: '朴素斐波那契复杂度', latex: '$O(2^n)$',
        note: `因为 F(n) 反复重算相同的子问题，调用次数随 n 指数级膨胀，非常慢；记忆化 / 动态规划可降到 $O(n)$。` },
    ],
    methods: [
      {
        name: '写递归三步法（先定基线，再写递归式）',
        when: `当一个问题可以"用它自己更小的版本"来描述时（如阶乘、求和、遍历嵌套结构）`,
        steps: [
          `第一步 · 找基线：问"什么情况下答案可以直接给出、不用再往下递归？"——写成 \`if 基线条件: return 直接答案\``,
          `第二步 · 写递归式：把原问题表示成"更小的同类问题 + 一点处理"，并 \`return 处理(自己(更小的输入))\``,
          `第三步 · 验收敛：确认每次调用时输入都在变小、且终会落到基线，否则就是无限递归`,
          `第四步 · 手动展开验证：拿一个小例子（如 n=3）在纸上一层层展开、再回填，确认结果正确`,
        ],
        example: {
          problem: `用递归求 1 + 2 + 3 + ... + n 的和。`,
          solution: [
            `解：基线——当 n 等于 1 时，和就是 1，直接返回。`,
            `递归式——前 n 个的和 = n + (前 n-1 个的和)，即 \`return n + sum_to(n - 1)\`。`,
            `收敛——每次 n 减 1，必然落到 n==1 的基线。`,
            `答：代码：\`def sum_to(n):\n    if n == 1:\n        return 1\n    return n + sum_to(n - 1)\`。\`sum_to(4)\` = 4+3+2+1 = 10。`,
          ],
        },
      },
      {
        name: '递归调用过程"展开—回填"追踪法',
        when: `当你想看清一个递归到底是怎么一步步算出结果、或怀疑它写错时`,
        steps: [
          `从最外层调用写起，把每一层"它在等谁"列出来（递去阶段，逐层变小）`,
          `一直写到命中基线条件、能直接返回的那一层`,
          `从基线那层开始往回填值（归来阶段），把每层"等到的结果"代入算出自己`,
          `回到最外层得到最终答案；过程中能直观看到"为什么必须有基线"`,
        ],
        example: {
          problem: `追踪 \`factorial(4)\` 的调用过程。`,
          solution: [
            `递去：factorial(4)=4×factorial(3)；factorial(3)=3×factorial(2)；factorial(2)=2×factorial(1)；factorial(1) 命中基线 = 1。`,
            `归来：factorial(1)=1 → factorial(2)=2×1=2 → factorial(3)=3×2=6 → factorial(4)=4×6=24。`,
            `答：factorial(4)=24。可以看到，正是 factorial(1) 的基线让"递去"停住、"归来"才能开始。`,
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 阶乘（含代码与调用过程）',
        problem: `用递归实现求 $n!$（$n! = n \\times (n-1) \\times \\cdots \\times 2 \\times 1$，规定 $0! = 1$），并写出 \`factorial(5)\` 的完整计算过程。`,
        steps: [
          `第一步：定基线。当 \`n\` 为 0 或 1 时，阶乘就是 1，可以直接返回，不必再递归。`,
          `第二步：写递归式。\`n!\` 等于 \`n\` 乘以"更小的 \`(n-1)!\`"，于是调用自己求 \`factorial(n-1)\`。参考代码：\n\`def factorial(n):\n    if n <= 1:            # 基线条件：0! 和 1! 都等于 1\n        return 1\n    return n * factorial(n - 1)   # 递归式：n 乘以更小的阶乘\``,
          `第三步：递去（一层层往下调用）。\nfactorial(5) = 5 × factorial(4)\nfactorial(4) = 4 × factorial(3)\nfactorial(3) = 3 × factorial(2)\nfactorial(2) = 2 × factorial(1)\nfactorial(1) → 命中基线，返回 1。`,
          `第四步：归来（从基线往回算）。\nfactorial(1)=1 → factorial(2)=2×1=2 → factorial(3)=3×2=6 → factorial(4)=4×6=24 → factorial(5)=5×24=**120**。可见基线条件 \`n <= 1\` 是整段递归能停下来的关键——漏了它，n 会一直减成负数、无限递归直到爆栈。`,
        ],
      },
      {
        title: '例题 · 斐波那契（含代码与为什么慢）',
        problem: `斐波那契数列是 0, 1, 1, 2, 3, 5, 8, 13...，规律是"每个数等于前两个数之和"，即 $F(n)=F(n-1)+F(n-2)$，且 $F(0)=0$、$F(1)=1$。用递归实现，并说明为什么 n 大了会很慢。`,
        steps: [
          `第一步：定基线（这次有两个）。\`F(0)=0\`、\`F(1)=1\` 可直接返回。`,
          `第二步：写递归式。\`F(n) = F(n-1) + F(n-2)\`。参考代码：\n\`def fib(n):\n    if n == 0:\n        return 0\n    if n == 1:        # 两个基线条件\n        return 1\n    return fib(n - 1) + fib(n - 2)   # 一个 n 分裂成两个子问题\``,
          `第三步：看调用过程（以 fib(5) 为例）。fib(5)=fib(4)+fib(3)，fib(4)=fib(3)+fib(2)……你会发现 \`fib(3)\` 被算了不止一次、\`fib(2)\` 被算了更多次——**同一个子问题被重复计算**。`,
          `第四步：为什么慢。因为每个 fib(n) 都分裂成两个子调用，调用总次数随 n 指数级膨胀，复杂度高达 $O(2^n)$。比如 fib(40) 就要算上亿次。解决办法是"把算过的结果记下来下次直接查"（记忆化 / 动态规划），能把复杂度降到 $O(n)$——这是后面要学的内容。结论：递归写起来优雅，但要警惕重复子问题带来的指数级开销。`,
        ],
      },
    ],
    pitfalls: [
      `**忘了基线条件 → 栈溢出**：没有"何时停"的判断，函数会无限调用自己，直到 \`RecursionError: maximum recursion depth exceeded\`（爆栈）。这是递归第一大坑。`,
      `**递归没有向基线收敛**：每次调用问题没真正变小（如 \`factorial(n)\` 里写成 \`factorial(n)\` 而不是 \`factorial(n-1)\`），永远到不了基线，照样无限递归。`,
      `**基线条件写漏了一种情况**：如斐波那契只写了 \`n==1\` 却漏了 \`n==0\`，遇到 fib(0) 时会越过基线继续递归到负数。`,
      `**朴素递归的重复计算**：斐波那契这类"一分为二"的递归会重复算大量子问题，复杂度 $O(2^n)$，n 稍大就极慢——要用记忆化优化。`,
      `**递归深度过大**：Python 默认递归深度约 1000 层，数据很深时（如很长的链、很深的树）朴素递归会爆栈，需改用循环或显式栈。`,
    ],
    exercises: [
      {
        problem: `下面这个递归函数有一个严重问题，运行 \`countdown(3)\` 会报 \`RecursionError\`。请指出问题并改正：\n\`def countdown(n):\n    print(n)\n    countdown(n - 1)\``,
        hint: `递归三要素里，这个函数缺了最关键的哪一个？它会一直数下去（3、2、1、0、-1、-2……）停得下来吗？`,
        answer: `**详细讲解**：问题是**缺少基线条件**——没有任何一句话告诉函数"什么时候该停"。\`countdown(3)\` 会打印 3，然后调用 \`countdown(2)\`，再 \`countdown(1)\`、\`countdown(0)\`、\`countdown(-1)\`……n 一直减下去永远不停，每调用一层就往"调用栈"压一层，最终把栈撑爆，报 \`RecursionError: maximum recursion depth exceeded\`。改正办法是加一个基线条件让它停下来，比如 n 减到小于 1 就返回：\n\`def countdown(n):\n    if n < 1:        # 基线条件：数到头了就停\n        return\n    print(n)\n    countdown(n - 1) # 每次 n 减 1，向基线收敛\`\n这样 \`countdown(3)\` 会依次打印 3、2、1，到 \`countdown(0)\` 命中基线 \`return\`，递归停止。记住递归三要素：**基线条件 + 递归式 + 向基线收敛**，这道题就是漏了第一个。`
      },
      {
        problem: `用递归写一个函数 \`power(base, n)\`，计算 base 的 n 次方（n 是非负整数），不能用 \`**\`。提示：$base^n = base \\times base^{n-1}$，且 $base^0 = 1$。然后说明 \`power(2, 3)\` 的计算过程。`,
        hint: `先想基线：任何数的 0 次方等于几？再想递归式：$base^n$ 怎么用更小的 $base^{n-1}$ 表示？每次让指数 n 减 1。`,
        answer: `**详细讲解**：按递归三要素来设计。**基线**：任何数的 0 次方都是 1，所以 \`if n == 0: return 1\`。**递归式**：$base^n = base \\times base^{n-1}$，即 \`return base * power(base, n - 1)\`。**收敛**：每次 n 减 1，必然落到 n==0。参考代码：\n\`def power(base, n):\n    if n == 0:        # 基线：base 的 0 次方 = 1\n        return 1\n    return base * power(base, n - 1)   # 递归式：指数每次减 1\`\n\`power(2, 3)\` 的过程——递去：power(2,3)=2×power(2,2)；power(2,2)=2×power(2,1)；power(2,1)=2×power(2,0)；power(2,0) 命中基线返回 1。归来：power(2,1)=2×1=2 → power(2,2)=2×2=4 → power(2,3)=2×4=**8**。结果正确（2³=8）。这道题帮你练习"把幂运算拆成更小的幂"，正是递归"大问题化小问题"的典型。`
      },
      {
        problem: `阅读下面用递归求列表元素之和的代码，判断 \`list_sum([1, 2, 3, 4])\` 的输出，并简述它的基线条件和递归式分别是什么：\n\`def list_sum(nums):\n    if len(nums) == 0:\n        return 0\n    return nums[0] + list_sum(nums[1:])\``,
        hint: `\`nums[0]\` 是第一个元素，\`nums[1:]\` 是"去掉第一个之后剩下的列表"。每递归一次，列表就短一截，最终会变成什么？`,
        answer: `**详细讲解**：输出是 **10**（1+2+3+4）。先看两要素：**基线条件**是 \`if len(nums) == 0: return 0\`——当列表为空时，和是 0，直接返回，不再递归；**递归式**是 \`nums[0] + list_sum(nums[1:])\`——整个列表的和 = 第一个元素 + 剩下那截列表的和，其中 \`nums[1:]\` 是"去掉第 0 个后剩下的子列表"，每次都比原来短一个，所以在向空列表（基线）收敛。计算过程：list_sum([1,2,3,4]) = 1 + list_sum([2,3,4]) = 1 + (2 + list_sum([3,4])) = 1 + (2 + (3 + list_sum([4]))) = 1 + (2 + (3 + (4 + list_sum([])))) ；最里层 list_sum([]) 命中基线返回 0，然后往回填：4+0=4 → 3+4=7 → 2+7=9 → 1+9=**10**。这道题展示了递归处理列表的常见套路：**"取出第一个 + 递归处理剩下的"，空列表作基线**。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 5. 模块与库
  // ───────────────────────────────────────────────────────────────
  'module-lib': {
    warmup: {
      q: `你要算一个数的平方根。你打算自己写一段很长的逼近算法，还是希望有一行现成的代码直接给你答案？Python 里到底有没有"现成的轮子"？`,
      hint: `想想生活里——要喝水你会自己挖井、还是拧开水龙头？编程里别人已经写好、测试好的代码，能不能直接拿来用？`,
      answer: `当然用现成的！Python 标准库里的 \`math\` 模块就有平方根：\`import math\` 之后写 \`math.sqrt(16)\` 直接得到 \`4.0\`，不用自己写逼近算法。这就是**模块与库**：把别人写好、测试过的代码，用 \`import\` 拿来直接用。会"用现成的轮子"是工程能力的重要一面——不必所有功能都从零造起。`
    },
    intro: [
      `模块与库，讲的是"如何站在别人的肩膀上写程序"。一个**模块**就是一个装满现成函数、变量的 Python 文件；很多模块打包在一起就成了**库**。用 \`import\` 把它们引进来，就能直接调用里面写好的功能，不必自己从零实现。`,
      `为什么要用？因为很多功能（算平方根、生成随机数、获取日期、处理网络请求）前人早已写好并反复优化、测试过。**重复造轮子既慢又容易出错**；学会找现成的库、读它的用法，是从"会写代码"走向"会做项目"的关键一步。`,
      `Python 的库分两类。**标准库**：随 Python 自带、不用安装，直接 import 即可，如 \`math\`（数学）、\`random\`（随机）、\`datetime\`（日期时间）。**第三方库**：别人发布的、需要先用 \`pip install\` 安装，再 import，如 \`numpy\`（数值计算）、\`pandas\`（数据分析）、\`requests\`（网络请求）。`,
      `导入有几种常见写法。① \`import math\`，用时写 \`math.sqrt(16)\`（带模块名前缀，最清楚）；② \`from math import sqrt\`，用时直接写 \`sqrt(16)\`（只取需要的，简洁）；③ \`import numpy as np\`，给模块起个简短别名，用 \`np.array(...)\`（长名字常用别名）。`,
      `一句话记住：**模块与库就是"别人写好的代码仓库"，用 import 拿来即用。** 遇到一个需求，先想想"有没有现成的库能做"，往往能省下大量时间，还更可靠。`
    ],
    formulas: [
      { name: '导入整个模块', latex: '\\texttt{import math}',
        note: `引入后用"模块名.功能"调用，如 \`math.sqrt(16)\`、\`math.pi\`。带前缀最清楚，能避免重名冲突。` },
      { name: '只导入需要的', latex: '\\texttt{from math import sqrt}',
        note: `只取 sqrt 这一个，之后直接写 \`sqrt(16)\`，不必加 \`math.\` 前缀。简洁，但要小心和自己的变量重名。` },
      { name: '导入并起别名', latex: '\\texttt{import numpy as np}',
        note: `给模块起短别名，之后用 \`np.\` 调用。常用于名字较长的库（numpy→np、pandas→pd）。` },
    ],
    methods: [
      {
        name: '"先找轮子再动手"工作法',
        when: `当你遇到一个常见需求（数学计算、随机、日期、读写文件、网络请求……）时`,
        steps: [
          `先问"这个需求是不是很常见、别人多半写过？"——常见功能优先找现成库`,
          `想用哪个库：数学找 \`math\`，随机找 \`random\`，日期找 \`datetime\`，数据分析找 \`pandas\`……`,
          `第三方库先安装：在终端运行 \`pip install 库名\`（标准库不用装）`,
          `用 \`import\` 引入，照着文档 / 例子调用它的函数，先用小例子验证一下`,
        ],
        example: {
          problem: `想模拟掷一个六面骰子，得到 1 到 6 的随机整数。`,
          solution: [
            `解：随机相关找 \`random\` 模块（标准库，自带）。`,
            `引入：\`import random\`。`,
            `调用：\`random.randint(1, 6)\` 会返回 1 到 6 之间（含两端）的随机整数。`,
            `答：每次运行 \`print(random.randint(1, 6))\` 都可能得到不同的点数，不用自己实现随机算法。`,
          ],
        },
      },
      {
        name: '选对导入写法',
        when: `当你决定怎么把模块引进来、纠结写 \`import X\` 还是 \`from X import y\` 时`,
        steps: [
          `默认用 \`import 模块名\`：调用时带前缀（\`math.sqrt\`），最清楚、不怕重名`,
          `只用到一两个功能、想写得简洁，用 \`from 模块 import 功能\`，但注意别和自己的变量重名`,
          `库名很长或社区有惯例别名，用 \`import 模块 as 别名\`（如 \`import pandas as pd\`）`,
          `尽量避免 \`from 模块 import *\`：会一次把所有名字倒进来，容易和你的变量撞名、看不清谁是谁`,
        ],
        example: {
          problem: `程序里要多次用到圆周率和平方根，怎么导入比较清楚？`,
          solution: [
            `解：用 \`import math\` 最稳妥，调用时带前缀，一看就知道来自 math。`,
            `\`import math\``,
            `\`area = math.pi * r * r\`（用 math.pi），\`d = math.sqrt(x*x + y*y)\`（用 math.sqrt）。`,
            `答：带 \`math.\` 前缀虽然多打几个字，但谁都看得出这些功能来自 math 模块，不会和自己定义的变量冲突。`,
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 用 math 模块算斜边长度',
        problem: `已知直角三角形两条直角边 a=3、b=4，求斜边长度（斜边 = $\\sqrt{a^2 + b^2}$）。请用 \`math\` 模块实现。`,
        steps: [
          `第一步：审题。要用到平方根，这是数学功能，找标准库 \`math\`。`,
          `第二步：引入模块。\`import math\`（math 是标准库，不用安装）。`,
          `第三步：套公式计算。斜边 = √(a²+b²)，代码：\n\`import math\na = 3\nb = 4\nc = math.sqrt(a * a + b * b)   # 先算 9+16=25，再开方\nprint(c)\``,
          `第四步：得结果。\`math.sqrt(25)\` 返回 \`5.0\`，所以斜边长 5。可见：开平方这种功能直接调 \`math.sqrt\` 即可，不用自己写求根算法——这就是用库的好处。`,
        ],
      },
      {
        title: '例题 · 用 random 做一个猜数小游戏的核心',
        problem: `要做一个"猜数字"游戏：程序先随机想一个 1 到 100 的整数，玩家来猜。请写出"程序随机选一个数、并判断玩家某次猜测是大了、小了还是猜对了"的核心代码。`,
        steps: [
          `第一步：审题。需要"随机选一个整数"——用 \`random\` 模块的 \`randint\`。`,
          `第二步：引入并随机选数。\n\`import random\nanswer = random.randint(1, 100)   # 程序想的那个数\``,
          `第三步：读入玩家猜测并判断（用 if/elif/else）。\n\`guess = int(input("猜一个 1-100 的数："))\nif guess == answer:\n    print("猜对了！")\nelif guess < answer:\n    print("小了")\nelse:\n    print("大了")\``,
          `第四步：体会用库的价值。\`random.randint(1, 100)\` 一行就解决了"产生随机数"这件并不简单的事；我们只要专注写游戏逻辑。把它放进 while 循环里反复猜，就是完整的猜数游戏。`,
        ],
      },
    ],
    pitfalls: [
      `**没 import 就用**：直接写 \`sqrt(16)\` 会报 \`NameError\`；用任何模块前都要先 \`import\`。`,
      `**第三方库没安装就 import**：\`import numpy\` 若没装会报 \`ModuleNotFoundError\`，要先在终端 \`pip install numpy\`（标准库则不用装）。`,
      `**调用时漏写模块前缀**：用了 \`import math\` 却写 \`sqrt(16)\` 会报错，必须写 \`math.sqrt(16)\`；除非当初是 \`from math import sqrt\`。`,
      `**给自己的文件起名和库重名**：把文件命名为 \`random.py\`、\`math.py\` 会让 import 引到自己这个空文件、引发奇怪错误。别用库名当文件名。`,
      `**滥用 \`from 模块 import *\`**：一次倒进所有名字，容易和自己的变量撞名、也看不出某个函数到底来自哪。尽量明确导入。`,
    ],
    exercises: [
      {
        problem: `下面代码报错 \`NameError: name 'pi' is not defined\`，但作者觉得"我明明 import 了 math 啊"。请指出问题并给出两种改法：\n\`import math\nprint(pi)\``,
        hint: `\`import math\` 之后，math 里的东西要怎么称呼才找得到？是直接写 \`pi\`，还是要带个前缀？`,
        answer: `**详细讲解**：问题在于**导入方式和调用方式不匹配**。用 \`import math\` 时，math 里的 pi 必须**带模块名前缀**来访问，写成 \`math.pi\`；直接写 \`pi\` 时当前作用域里并没有这个名字，所以报 \`NameError\`。两种改法：\n**改法一**（保留 import math，调用加前缀）：\n\`import math\nprint(math.pi)   # 输出 3.141592...\`\n**改法二**（改成 from 导入，调用就不用前缀）：\n\`from math import pi\nprint(pi)        # 这样直接写 pi 才合法\`\n核心规则：\`import math\` → 用 \`math.xxx\`；\`from math import xxx\` → 直接用 \`xxx\`。两种各有好处：带前缀更清楚来源、不怕重名；from 导入更简洁。`
      },
      {
        problem: `请用标准库写一段代码：随机生成一个 1 到 6 的整数模拟掷骰子，把它打印出来；并说明这里为什么不需要 \`pip install\`。`,
        hint: `掷骰子=随机整数，用哪个模块？这个模块是 Python 自带的标准库还是需要额外安装的第三方库？`,
        answer: `**详细讲解**：掷骰子就是要一个范围内的随机整数，用标准库 \`random\` 的 \`randint\`。参考代码：\n\`import random\npoint = random.randint(1, 6)   # 返回 1 到 6（含 1 和 6）的随机整数\nprint("掷出了", point, "点")\`\n为什么不用 \`pip install\`：\`random\` 是 Python 的**标准库**，安装 Python 时就自带了，所以直接 \`import random\` 即可，无需额外安装。只有 \`numpy\`、\`pandas\`、\`requests\` 这类**第三方库**才需要先在终端运行 \`pip install 库名\` 装好、再 import。补充：\`random.randint(a, b)\` 是**含两端**的，所以写 \`(1, 6)\` 正好覆盖骰子的 1 到 6；如果用 \`random.randrange(1, 6)\` 则不含 6，要小心区别。`
      },
      {
        problem: `小明把自己写的练习文件命名为 \`random.py\`，里面第一行就是 \`import random\`，想用 \`random.randint\`，结果运行报错、行为很奇怪。你能猜到原因并给出建议吗？`,
        hint: `当你 import random 时，Python 会去找名叫 random 的模块。如果你自己的文件就叫 random.py，它会先找到谁？`,
        answer: `**详细讲解**：原因是**文件名和标准库重名了**。当小明的文件叫 \`random.py\`、里面又写 \`import random\` 时，Python 在查找名为 \`random\` 的模块时，会**优先找到当前目录下小明自己的这个 random.py**（而不是标准库的 random），于是 \`random.randint\` 根本不存在（自己的文件里没有这个函数），就报错或出现莫名其妙的行为。建议：**不要用标准库 / 第三方库的名字给自己的文件命名**，把文件改名为 \`dice_game.py\`、\`my_random_test.py\` 之类即可，再运行就会正确引入真正的标准库 random。同理也要避免把文件叫 \`math.py\`、\`json.py\`、\`requests.py\` 等。这是初学者很常踩的隐蔽坑——报错信息往往看不出是命名冲突造成的。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 6. 面向对象（类与对象）
  // ───────────────────────────────────────────────────────────────
  'oop-class': {
    warmup: {
      q: `要在程序里管理一个班 50 名学生，每个学生都有"姓名、年龄、成绩"，还要能"打印自我介绍"。如果用 50 组各自独立的变量（name1, age1, score1, name2...）来存，会有多乱？有没有更整齐的办法？`,
      hint: `这 50 个学生，是不是都"长得一个样"——都有姓名、年龄、成绩，都会自我介绍？能不能先做一个"学生模板"，再照着造 50 个？`,
      answer: `用一堆散变量当然会乱成一团、根本管不过来。更整齐的办法是**面向对象**：先定义一个"学生"**类**（模板），规定每个学生都有 \`name\`、\`age\`、\`score\` 三个属性、以及一个 \`introduce()\` 自我介绍的方法；然后照着这个模板**造出 50 个对象**（具体的学生）。类把"数据（属性）"和"操作数据的函数（方法）"打包在一起，造对象就像照图纸盖房子——又整齐又好管理。`
    },
    intro: [
      `面向对象，是一种"把数据和操作数据的函数捆在一起"的编程思想。它的两个核心概念是**类**和**对象**：类是"模板 / 图纸"，描述某一类东西**有哪些属性、能做哪些事**；对象是"照模板造出来的具体实例"。`,
      `打个比方：**类是"狗"这个概念**（规定狗都有名字、都会叫），**对象是某一条具体的狗**（比如"我家的小白"）。同一个类可以造出很多对象，它们结构相同、但各自的数据不同（小白和小黑都是狗，但名字不同）。`,
      `在 Python 里用 \`class\` 定义类。类里有两种东西：**属性**（对象的数据，如学生的 name、score）和**方法**（对象能做的事，本质是定义在类里的函数，如 introduce()）。一个特殊方法 \`__init__\` 叫"构造方法"，在**创建对象时自动被调用**，用来给对象设置初始属性。`,
      `这里有一个绕不开的关键字 \`self\`。类里每个方法的第一个参数都要写 \`self\`，它代表"当前这个对象自己"。在方法里用 \`self.name\` 就是访问"这个对象的 name 属性"。创建对象时写 \`s = Student("小明", 14)\`，**self 会自动指向新造出来的 s，不用你手动传**。`,
      `一句话记住：**类是模板、对象是实例；属性是它的数据、方法是它的本领；__init__ 在造对象时初始化、self 代表对象自己。** 面向对象还带来封装、继承、多态等组织大程序的强力工具（继承多态是后续内容）。`
    ],
    formulas: [
      { name: '定义类', latex: '\\texttt{class Student:}',
        note: `用 \`class\` + 类名（习惯首字母大写）定义。类名后面加冒号，下面缩进写它的属性和方法。` },
      { name: '构造方法', latex: '\\texttt{def __init__(self, name):}',
        note: `创建对象时**自动调用**，用来初始化属性。第一个参数固定是 \`self\`；\`self.name = name\` 把传进来的值存到对象上。` },
      { name: '创建对象（实例化）', latex: '\\texttt{s = Student("小明")}',
        note: `用"类名 + 圆括号"造对象，括号里的实参会传给 \`__init__\`（self 自动对应新对象，不用手传）。s 就是一个对象。` },
      { name: '访问属性 / 调用方法', latex: '\\texttt{s.name}\\quad \\texttt{s.introduce()}',
        note: `用"对象.属性"读取数据、用"对象.方法()"让它做事。点号前面是哪个对象，操作的就是哪个对象的数据。` },
    ],
    methods: [
      {
        name: '定义一个类的四步法',
        when: `当你要管理一批"结构相同、数据不同"的东西（学生、商品、怪物、汽车……）时`,
        steps: [
          `第一步 · 起类名：用 \`class 类名:\`，类名首字母大写，如 \`class Dog:\``,
          `第二步 · 写构造方法 \`__init__\`：列出每个对象都该有的属性，用 \`self.属性 = 参数\` 存起来`,
          `第三步 · 写方法：把"这类东西能做的事"写成方法（第一个参数 self），方法里用 \`self.属性\` 访问自己的数据`,
          `第四步 · 造对象并使用：\`d = Dog("小白")\` 创建对象，\`d.bark()\` 调用方法、\`d.name\` 读属性`,
        ],
        example: {
          problem: `定义一个"狗"类，每条狗有名字，并能"汪汪叫"（叫的时候带上自己的名字）。`,
          solution: [
            `解：用 class 定义模板，name 作属性，bark 作方法。`,
            `\`class Dog:\n    def __init__(self, name):\n        self.name = name        # 每条狗的名字\n    def bark(self):\n        return self.name + " 汪汪！"\``,
            `造对象并使用：\`d = Dog("小白")\`，\`print(d.bark())\` 输出 "小白 汪汪！"。`,
            `答：再造一条 \`Dog("小黑")\` 调 bark 就会输出 "小黑 汪汪！"——同一个模板，数据各不相同。`,
          ],
        },
      },
      {
        name: '理解 self：方法里怎么用"自己的"数据',
        when: `当你写方法时不知道第一个参数为什么是 self、或不知道怎么访问对象属性时`,
        steps: [
          `规则：类里每个方法的**第一个参数都写 self**，它代表"调用这个方法的那个对象自己"`,
          `在方法内部，用 \`self.属性名\` 读写这个对象的属性（如 \`self.score\`）`,
          `调用时**不用手动传 self**：写 \`s.introduce()\`，Python 自动把 s 作为 self 传进去`,
          `因此同一个方法对不同对象调用，\`self\` 就指向不同对象，自然取到各自的数据`,
        ],
        example: {
          problem: `给学生类加一个 \`introduce\` 方法，打印"我叫XX，今年X岁"。`,
          solution: [
            `解：方法第一个参数写 self，内部用 self.name、self.age 取这个对象的数据。`,
            `\`class Student:\n    def __init__(self, name, age):\n        self.name = name\n        self.age = age\n    def introduce(self):\n        print("我叫" + self.name + "，今年" + str(self.age) + "岁")\``,
            `\`a = Student("小明", 14)\`，\`a.introduce()\` → 我叫小明，今年14岁。`,
            `答：再 \`Student("小红", 13).introduce()\` → 我叫小红，今年13岁。self 自动指向"点号前面那个对象"，所以各取各的数据。`,
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 定义学生类并造对象',
        problem: `定义一个"学生"类 Student：每个学生有姓名 name 和成绩 score 两个属性；有一个方法 \`is_pass()\`，成绩 ≥ 60 返回 True、否则 False。然后创建两个学生并分别判断是否及格。`,
        steps: [
          `第一步：审题。要管理多个"有姓名、有成绩、能判断及格"的学生——典型的用类来建模。属性：name、score；方法：is_pass。`,
          `第二步：写类（构造方法 + 方法）。\n\`class Student:\n    def __init__(self, name, score):   # 创建对象时自动调用\n        self.name = name              # 存这个学生的姓名\n        self.score = score            # 存这个学生的成绩\n    def is_pass(self):\n        return self.score >= 60       # 用 self.score 取自己的成绩\``,
          `第三步：造对象。\`a = Student("小明", 85)\`、\`b = Student("小华", 50)\`。创建时实参 "小明"、85 会传给 __init__ 的 name、score（self 自动指向新对象）。`,
          `第四步：调用方法验证。\`print(a.name, a.is_pass())\` → 小明 True；\`print(b.name, b.is_pass())\` → 小华 False。可见同一个 is_pass 方法，对 a 调用时 self 指向 a（取 85），对 b 调用时 self 指向 b（取 50），各判各的——这正是"一个模板、多个对象、数据独立"的威力。`,
        ],
      },
    ],
    pitfalls: [
      `**方法忘写 self 参数**：类里方法第一个参数必须是 \`self\`，漏了它，调用时会报参数个数不对的错。`,
      `**方法内访问属性忘加 self.**：要写 \`self.name\` 才是"这个对象的 name"；只写 \`name\` 会被当成局部变量、找不到而报错。`,
      `**__init__ 拼写 / 下划线写错**：必须是前后各两个下划线 \`__init__\`，写成 \`_init_\`（单下划线）就不会在创建对象时自动调用。`,
      `**混淆类和对象**：\`Student\` 是模板（类），\`Student("小明",14)\` 造出来的才是对象。属性和方法是作用在对象上的（如 \`s.score\`），不是直接用类名。`,
      `**创建对象时手动传 self**：写成 \`Student(self, "小明", 14)\` 是错的——self 由 Python 自动传入，你只需传 \`__init__\` 里 self 之后的那些参数。`,
    ],
    exercises: [
      {
        problem: `下面代码运行 \`p.show()\` 时报错 \`NameError: name 'name' is not defined\`。请找出原因并改正：\n\`class Person:\n    def __init__(self, name):\n        self.name = name\n    def show(self):\n        print("我是", name)\n\np = Person("小李")\np.show()\``,
        hint: `\`show\` 方法里那个 \`name\`，是想表示"这个对象的名字"吧？要访问对象自己的属性，前面该不该加点什么？`,
        answer: `**详细讲解**：原因是 \`show\` 方法里**访问属性时漏写了 \`self.\`**。\`__init__\` 里确实把名字存成了对象的属性 \`self.name\`，但在 \`show\` 里写的是 \`print("我是", name)\`——这里的 \`name\` 没有前缀，Python 会把它当成一个**局部变量**去找，而 show 里根本没有这个局部变量，于是报 \`NameError\`。改正：访问对象自己的属性必须用 \`self.name\`：\n\`class Person:\n    def __init__(self, name):\n        self.name = name\n    def show(self):\n        print("我是", self.name)   # 加上 self.\n\np = Person("小李")\np.show()        # 输出：我是 小李\`\n记住规则：在方法内部，凡是要用"这个对象的属性"，前面都要加 \`self.\`；\`self\` 就代表当前这个对象自己。`
      },
      {
        problem: `定义一个"长方形"类 Rectangle：有 width 和 height 两个属性；有一个方法 \`area()\` 返回面积。然后创建一个宽 3、高 4 的长方形并打印它的面积。`,
        hint: `属性在 \`__init__(self, width, height)\` 里用 \`self.width = width\` 存；\`area\` 方法第一个参数是 self，里面用 \`self.width * self.height\`。`,
        answer: `**详细讲解**：按"定义类四步法"来做。参考代码：\n\`class Rectangle:\n    def __init__(self, width, height):   # 创建对象时自动调用\n        self.width = width               # 存宽\n        self.height = height             # 存高\n    def area(self):\n        return self.width * self.height  # 用 self 取自己的宽和高\n\nr = Rectangle(3, 4)     # 造对象，3 给 width、4 给 height\nprint(r.area())          # 输出 12\`\n讲解要点：第一，\`__init__\` 的参数除了 self，还有 width、height，创建对象 \`Rectangle(3, 4)\` 时这两个实参对号入座（self 自动指向新对象，不用传）；第二，\`area\` 方法第一个参数必须是 self，方法内用 \`self.width\`、\`self.height\` 才能拿到"这个长方形自己"的尺寸；第三，\`area\` 用 return 把面积交回，外面用 \`print(r.area())\` 显示。好处是——要再算一个长方形，只需 \`Rectangle(5, 2).area()\`，模板复用、各算各的。`
      },
      {
        problem: `阅读代码，判断输出，并解释"为什么 a 和 b 调用同一个 greet 方法，结果却不一样"：\n\`class Cat:\n    def __init__(self, name):\n        self.name = name\n    def greet(self):\n        print(self.name, "喵~")\n\na = Cat("咪咪")\nb = Cat("花花")\na.greet()\nb.greet()\``,
        hint: `\`greet\` 里的 \`self\` 代表谁？当你写 \`a.greet()\` 时 self 指向 a，写 \`b.greet()\` 时 self 又指向谁？`,
        answer: `**详细讲解**：输出两行——\`咪咪 喵~\` 和 \`花花 喵~\`。为什么同一个 \`greet\` 方法结果不同？关键在 \`self\`：\`self\` 代表"调用这个方法的那个对象自己"。\`a = Cat("咪咪")\` 和 \`b = Cat("花花")\` 是用同一个类 Cat 造出的**两个不同对象**，它们各自的 \`self.name\` 不同（a 的是 "咪咪"，b 的是 "花花"）。当执行 \`a.greet()\` 时，Python 自动把 a 作为 self 传进去，于是 \`self.name\` 取到 "咪咪"；执行 \`b.greet()\` 时 self 指向 b，\`self.name\` 取到 "花花"。所以**方法只写一次（在类里），但对不同对象调用时 self 指向不同对象，自然访问到各自的数据**——这正是面向对象"一个模板、多个对象、数据独立"的精髓。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 7. 异常处理
  // ───────────────────────────────────────────────────────────────
  'exception': {
    warmup: {
      q: `你写了个程序让用户输入年龄：\`age = int(input("年龄："))\`。如果用户手滑输入了"十八"（中文）或者直接按回车，程序会怎样？整个程序会不会因为这一行就崩掉？`,
      hint: `\`int("十八")\` 能转成数字吗？转不了会发生什么？有没有办法"接住"这个错误，让程序别直接死掉、而是友好地提示用户重输？`,
      answer: `会**崩溃**！\`int("十八")\` 转不了，Python 抛出 \`ValueError\`，如果不管它，程序就直接报错中断、后面的代码全不执行。解决办法是用 \`try / except\` 把可能出错的代码"包起来"：\`try\` 里放可能出错的 \`int(input(...))\`，\`except ValueError\` 里写"出错了怎么办"（比如提示"请输入数字"）。这样即使用户乱输，程序也不会崩，而是优雅地处理。这就是**异常处理**——接住运行时错误，让程序更健壮。`
    },
    intro: [
      `异常处理，讲的是"当程序运行中出了意外，怎样优雅地接住它、而不是直接崩溃"。有些错误是防不胜防的：用户乱输入、文件不存在、网络断了、除数恰好是 0……异常处理就是给程序加一张"安全网"。`,
      `先分清两类问题。**语法错误**是程序根本跑不起来（括号没配对、缩进错），得改代码才行，不归异常处理管。**异常（运行时错误）**是程序能启动、但跑到某一行时出了意外而崩溃，如 \`ValueError\`（值不对，int("abc")）、\`ZeroDivisionError\`（除以 0）、\`KeyError\`（字典里没这个键）、\`FileNotFoundError\`（文件找不到）——这些正是异常处理能"接住"的。`,
      `核心工具是 \`try / except\`。把**可能出错的代码**放进 \`try:\` 块；一旦它出错，程序不会崩溃，而是跳到对应的 \`except 错误类型:\` 块去执行"补救代码"。如果 try 里平安无事，except 就被跳过。结构是：\`try: 冒险的代码\` + \`except 某种错误: 出错时怎么办\`。`,
      `还有两个可选搭档。\`else\`：try 里**没出错时**才执行的代码。\`finally\`：**无论出不出错都一定会执行**的代码，常用来做"收尾"（如关闭文件）。一个常见完整结构是 \`try / except / finally\`。`,
      `一句话记住：**异常处理 = 用 try 包住可能出错的代码、用 except 接住对应的错误，让程序遇到意外也能稳住。** 但有条铁律：**只接住你知道怎么应对的异常，写明具体类型**（如 \`except ValueError\`），千万别用一个光秃秃的 \`except:\` 把所有错误（包括你自己的笔误）都吞掉——那会让 bug 极难定位。`
    ],
    formulas: [
      { name: 'try / except 基本结构', latex: '\\texttt{try: ... except ValueError: ...}',
        note: `\`try\` 里放可能出错的代码；出 \`ValueError\` 就跳到 \`except\` 里补救。try 平安无事时 except 被跳过。` },
      { name: '捕获具体异常类型', latex: '\\texttt{except ZeroDivisionError:}',
        note: `写明要接哪种错误（如除零、值错误、键错误）。**不要写裸 \`except:\`**——那会连你的笔误都一并吞掉，难以排查。` },
      { name: 'finally 一定执行', latex: '\\texttt{try: ... finally: ...}',
        note: `\`finally\` 块无论 try 是否出错都会执行，常用于"收尾"工作，如关闭文件、释放资源。` },
    ],
    methods: [
      {
        name: '加异常处理三步法',
        when: `当某行代码可能因"外部因素"（用户输入、文件、网络、除数为 0）而出错，你不想让程序崩溃时`,
        steps: [
          `第一步 · 找冒险点：定位"哪一行可能出错"，**只把这一行（或这一小段）放进 try**，范围别太大`,
          `第二步 · 想清楚会出什么错：是 \`ValueError\`？\`ZeroDivisionError\`？\`FileNotFoundError\`？写明具体类型`,
          `第三步 · 写补救：在 \`except 具体类型:\` 里写"出错了怎么办"——友好提示、给默认值、或让用户重试`,
          `（可选）需要"无论如何都收尾"的，加 \`finally\`；需要"没出错才做"的，加 \`else\``,
        ],
        example: {
          problem: `让用户输入一个整数，输入非法时不崩溃，而是提示"请输入数字"。`,
          solution: [
            `解：可能出错的是 \`int(input(...))\`（用户输入字母会 ValueError），用 try 包住它。`,
            `\`try:\n    n = int(input("输入一个整数："))\n    print("你输入的是", n)\nexcept ValueError:\n    print("请输入数字！")\``,
            `用户输入 5 → 正常打印；输入 abc → 不崩溃，打印"请输入数字！"。`,
            `答：注意只接 \`ValueError\` 这一种"已知会发生且知道怎么应对"的错误，没有用裸 except 吞掉一切。`,
          ],
        },
      },
      {
        name: '只接该接的：避免裸 except',
        when: `当你想加异常处理、但纠结"是写 except: 还是写具体类型"时`,
        steps: [
          `默认写**具体类型**：\`except ValueError:\`、\`except ZeroDivisionError:\`——清楚知道在防哪种错`,
          `需要分别处理多种错时，写多个 except：\`except ValueError: ...\` 后再跟 \`except ZeroDivisionError: ...\``,
          `坚决避免裸 \`except:\`（什么都接）：它会把你自己的拼写错误、逻辑错误也一并"吞掉"，程序看似没崩、实则带病运行，bug 极难找`,
          `原则一句话：**只接住你知道怎么应对的异常**，其他的让它正常报出来、好让你修`,
        ],
        example: {
          problem: `算两个数相除，要同时防"输入不是数字"和"除数为 0"。`,
          solution: [
            `解：两种错分别接，写两个 except。`,
            `\`try:\n    a = int(input("被除数："))\n    b = int(input("除数："))\n    print(a / b)\nexcept ValueError:\n    print("请输入整数！")\nexcept ZeroDivisionError:\n    print("除数不能为 0！")\``,
            `输入 abc → 提示"请输入整数"；输入 6 和 0 → 提示"除数不能为 0"。`,
            `答：分类型接住，既稳住了程序，又能给用户对症的提示，比一个裸 except 笼统报错好得多。`,
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 用 try/except 让除法程序不崩溃',
        problem: `写一个程序，读入两个整数并打印它们的商。要求：当用户输入的不是整数、或者除数是 0 时，程序都不崩溃，而是给出对应的友好提示。`,
        steps: [
          `第一步：审题，找冒险点。可能出错的有两处：\`int(input(...))\` 遇到非数字会抛 \`ValueError\`；\`a / b\` 当 b 为 0 时会抛 \`ZeroDivisionError\`。把这些放进 try。`,
          `第二步：写 try 块（把可能出错的代码包起来）。\n\`try:\n    a = int(input("被除数："))\n    b = int(input("除数："))\n    print("商是：", a / b)\``,
          `第三步：分类型写 except（对症补救）。\n\`except ValueError:\n    print("输入有误，请输入整数！")\nexcept ZeroDivisionError:\n    print("除数不能为 0！")\``,
          `第四步：验证三种情形。输入 6、2 → 打印"商是：3.0"（try 顺利，except 跳过）；输入 abc → 被 ValueError 接住，提示"请输入整数"；输入 6、0 → 被 ZeroDivisionError 接住，提示"除数不能为 0"。三种情况程序都**没有崩溃**。注意：我们写明了**具体的异常类型**、分别处理，而不是用一个裸 \`except:\` 含糊吞掉——这样用户得到的提示更准确，我们也不会把别的潜在 bug 一起藏起来。`,
        ],
      },
    ],
    pitfalls: [
      `**用裸 \`except:\` 吞掉所有错误**：它会连你自己的拼写错、逻辑错也一起吞掉，程序看似不崩、实则带病运行，bug 极难定位。要写明具体类型。`,
      `**try 包的范围太大**：把一大段代码都塞进 try，出错时你分不清到底是哪一行错的。应只把"真正可能出错的那一行（或一小段）"包起来。`,
      `**用异常处理掩盖本该修的 bug**：异常处理是应对"防不胜防的外部意外"，不是用来盖住自己写错的逻辑。该改的代码要改，不能靠 except 蒙混。`,
      `**except 里什么都不做（\`except: pass\`）**：错误被悄悄吃掉、连提示都没有，出了问题完全无感知。至少要打印 / 记录一下。`,
      `**异常类型写错或写反**：比如本该接 \`ValueError\` 却写成 \`TypeError\`，那真正发生的 ValueError 还是会让程序崩溃。要对准实际会抛出的类型。`,
    ],
    exercises: [
      {
        problem: `下面代码想在用户输入非数字时友好提示，但写法有问题——它会把所有错误（包括程序员自己的笔误）都吞掉。请指出问题并改成更好的写法：\n\`try:\n    n = int(input("年龄："))\n    print("明年", n + 1, "岁")\nexcept:\n    print("出错了")\``,
        hint: `\`except\` 后面没写任何错误类型，意味着它会接住"一切"错误。如果 try 里其实是你把变量名打错了，这个 except 会怎样？`,
        answer: `**详细讲解**：问题是用了**裸 \`except:\`（不写具体类型）**，它会接住**所有**异常——不只是用户输入非数字的 \`ValueError\`，连你自己可能犯的笔误（比如把 \`print\` 打成 \`prnit\`、或写错变量名导致的 \`NameError\`）也会被一并吞掉，统统只显示一句"出错了"。这样真正的 bug 被掩盖，调试时根本不知道错在哪。更好的写法是**写明你确实想处理的具体异常类型**：\n\`try:\n    n = int(input("年龄："))\n    print("明年", n + 1, "岁")\nexcept ValueError:\n    print("请输入数字！")\`\n这样只有"输入的内容转不成整数"这种 \`ValueError\` 才会被接住、给出对症提示；而如果代码里有其他类型的错误（如笔误），它会正常报出来，方便你发现并修复。核心原则：**只接住你知道怎么应对的异常**，别用裸 except 把一切都藏起来。`
      },
      {
        problem: `请用 try/except 写一段代码：从一个字典 \`info = {"name": "小明"}\` 中读取键 "age" 的值并打印；如果这个键不存在，就打印"没有年龄信息"，而不要让程序崩溃。（提示：访问字典里不存在的键会抛 \`KeyError\`。）`,
        hint: `把 \`info["age"]\` 这行放进 try，因为它可能抛 KeyError；在 \`except KeyError:\` 里写找不到时的提示。`,
        answer: `**详细讲解**：访问字典中不存在的键会抛出 \`KeyError\`，所以把读取那行包进 try、用 \`except KeyError\` 接住。参考代码：\n\`info = {"name": "小明"}\ntry:\n    print("年龄是：", info["age"])   # "age" 不存在，会抛 KeyError\nexcept KeyError:\n    print("没有年龄信息")\`\n运行结果：因为字典里只有 "name" 没有 "age"，\`info["age"]\` 抛 KeyError，程序不崩溃，转而执行 except，打印"没有年龄信息"。讲解要点：第一，try 里只放"可能出错的那一行"，范围精准；第二，写明了具体类型 \`KeyError\`，不是裸 except。补充一个更简洁的非异常写法供对照——字典本身提供了安全取值的方法 \`info.get("age", "没有年龄信息")\`，找不到键时返回默认值而不报错。两种思路都能避免崩溃：try/except 适合"出错是意外情况"，\`get\` 适合"键可能没有是很正常的事"。`
      },
      {
        problem: `判断下面代码在两种输入下分别打印什么：(1) 用户输入 10；(2) 用户输入 0。\n\`try:\n    x = int(input("输入除数："))\n    print(100 / x)\nexcept ZeroDivisionError:\n    print("不能除以 0")\nfinally:\n    print("程序结束")\``,
        hint: `分两种情况走一遍。注意 \`finally\` 块的特点——它在什么情况下会执行？出错时执行吗？不出错时也执行吗？`,
        answer: `**详细讲解**：分两种输入追踪，重点理解 \`finally\` 的"无论如何都执行"。\n**(1) 输入 10**：\`x = 10\`，\`100 / 10 = 10.0\`，try 块顺利执行，打印 \`10.0\`；没有发生异常，所以 \`except\` 被跳过；最后 \`finally\` **一定执行**，打印 \`程序结束\`。输出两行：\`10.0\` 和 \`程序结束\`。\n**(2) 输入 0**：\`x = 0\`，执行 \`100 / 0\` 时抛出 \`ZeroDivisionError\`，try 中断，跳到 \`except ZeroDivisionError\`，打印 \`不能除以 0\`；然后 \`finally\` **仍然执行**，打印 \`程序结束\`。输出两行：\`不能除以 0\` 和 \`程序结束\`。\n核心结论：\`finally\` 块的代码**不管 try 里出不出错都会执行**，所以两种情况下"程序结束"都会被打印。这正是 finally 的用途——放那些"无论成功失败都必须做的收尾工作"，比如关闭文件、释放资源。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 8. 调试方法
  // ───────────────────────────────────────────────────────────────
  'debugging': {
    warmup: {
      q: `你写了个函数想算 1 到 5 的和，期望得到 15，可它输出了 10。代码看起来"没毛病"、也不报错。你会怎么找出问题出在哪？是把代码从头到尾死盯着看，还是有更聪明的办法？`,
      hint: `程序不报错、但结果不对——这叫"逻辑错误"，最难抓。有没有办法让程序"边算边告诉你"它每一步算到了多少，从而看出哪一步开始不对？`,
      answer: `死盯着看效率很低。聪明的办法是**打印调试**：在循环里加一句 \`print\`，让程序把每一步的中间结果"说出来"。比如在累加时打印 \`print("加到", i, "时 total =", total)\`，你就能看到 total 是从哪一步开始不符合预期的——很可能是 \`range\` 写成了 \`range(1, 5)\`（只到 4，漏了 5）。**调试的核心就是"让看不见的中间状态变得看得见"**，从而定位 bug。`
    },
    intro: [
      `调试，是"找出并修正程序里的 bug"的能力。写程序时出 bug 是再正常不过的事——真正区分高手和新手的，往往不是"少出 bug"，而是"出了 bug 能不能快速找到并修好"。所以调试是程序员的核心基本功。`,
      `先认识三类 bug，对症才能下药。**语法错误**：程序根本跑不起来，Python 会指出大概在哪一行（如少冒号、括号没配对），照着改即可。**运行时错误**：跑到一半崩溃并抛出异常（如 \`NameError\`、\`ZeroDivisionError\`），报错信息的**最后一行**会告诉你错误类型和原因。**逻辑错误**最难：程序能跑、不报错，但**结果是错的**——它不会主动告诉你哪里错，全靠你去查。`,
      `最基础、最常用的武器是**打印调试（print 大法）**：在你怀疑的关键位置插入 \`print\`，把变量的值、走到了哪一步打印出来，看是否符合你的预期。哪一步的输出开始不对，bug 多半就在那附近。它不需要任何额外工具，立竿见影。`,
      `更进阶的是用 IDE 的**断点调试**：在某一行设"断点"，程序运行到那里会暂停，你可以逐行（单步）往下走，随时查看每个变量当前的值、观察调用过程。比起反复加删 print，断点更系统、更适合复杂的 bug。`,
      `一句话记住：**调试 = 让程序的中间状态"看得见"，再对照预期找出哪一步不对。** 有一个特别管用的思维习惯——每修一个 bug，都问自己一句：**"我刚才假设了什么、而这个假设其实没成立？"** 因为绝大多数 bug，本质都是"你以为会这样、实际却不是这样"。`
    ],
    formulas: [
      { name: '三类 bug', latex: '\\text{语法错误} \\mid \\text{运行时错误} \\mid \\text{逻辑错误}',
        note: `语法错跑不起来；运行时错跑一半崩溃（看报错最后一行）；逻辑错能跑但结果不对（最难、靠打印 / 断点查）。` },
      { name: '打印调试', latex: '\\texttt{print("变量 x =", x)}',
        note: `在关键位置打印变量值或"走到这儿了"，看是否符合预期。哪一步输出开始不对，bug 多半在那附近。最简单有效。` },
      { name: '看报错的最后一行', latex: '\\text{Traceback ... → 最后一行才是错误类型与原因}',
        note: `Python 的报错（Traceback）从上往下是调用链，**最后一行**写明了错误类型（如 ValueError）和具体原因——先看它。` },
    ],
    methods: [
      {
        name: '打印调试法（print 大法）',
        when: `当程序结果不对（逻辑错误）、或想搞清楚某段代码到底执行了什么时`,
        steps: [
          `先明确"预期"：你认为这段代码每一步应该得到什么值？心里（或纸上）有个预期`,
          `在关键位置插 \`print\`：把循环变量、累加器、函数返回值等"中间状态"打印出来，并带上说明文字`,
          `运行，对照预期逐行看输出：找到"第一处和预期不符"的地方——bug 通常就在它附近`,
          `修正后再跑一遍确认；确认无误后**记得删掉调试用的 print**，别留在最终代码里`,
        ],
        example: {
          problem: `下面想求 1 到 5 的和（期望 15），却输出 10。用打印调试找出问题。\n\`total = 0\nfor i in range(1, 5):\n    total = total + i\nprint(total)\``,
          solution: [
            `解：在循环里加打印，看每一步加了谁。\n\`total = 0\nfor i in range(1, 5):\n    total = total + i\n    print("加了", i, "之后 total =", total)\``,
            `运行看到输出：加了 1…加了 2…加了 3…加了 **4** 之后 total = 10——发现循环只数到 4，根本没加 5！`,
            `定位原因：\`range(1, 5)\` 是左闭右开，只给 1、2、3、4。`,
            `答：把 \`range(1, 5)\` 改成 \`range(1, 6)\`，结果就是 15。这就是 print 大法——让"漏掉 5"这个看不见的问题暴露出来。`,
          ],
        },
      },
      {
        name: '读报错信息：先看最后一行',
        when: `当程序运行中崩溃、抛出一大段红色 Traceback 时`,
        steps: [
          `别被一大段吓到：Traceback 从上到下是"函数调用链"，**最关键的是最后一行**`,
          `看最后一行的**错误类型 + 说明**：如 \`ValueError: invalid literal for int()\`、\`NameError: name 'x' is not defined\``,
          `看倒数第二行附近的**文件名和行号**：它指出错误发生在哪个文件第几行`,
          `回到那一行，结合错误类型想"为什么会这样"——常见就是变量没定义、类型不对、下标越界、除以 0`,
        ],
        example: {
          problem: `运行报错，最后一行是 \`ZeroDivisionError: division by zero\`，怎么办？`,
          solution: [
            `解：最后一行直接点明了错误类型——除以了 0。`,
            `根据上面给的行号找到出错那行，例如 \`avg = total / count\`。`,
            `推断 \`count\` 此刻是 0（可能列表为空）。`,
            `答：在做除法前加判断 \`if count == 0: ...\`，或用 try/except 接住 ZeroDivisionError。读懂报错最后一行，就抓住了问题的七成。`,
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 用打印调试抓出逻辑错误',
        problem: `下面函数想找出列表里的最大值，但 \`find_max([3, 9, 5, 2])\` 返回了 9 是对的，可 \`find_max([-3, -9, -5, -2])\` 却返回了 0（应该是 -2）。程序不报错。请用调试方法找出 bug 并修正。`,
        steps: [
          `第一步：看代码，找可疑处。\n\`def find_max(nums):\n    biggest = 0          # 一开始把"最大值"设成 0\n    for x in nums:\n        if x > biggest:\n            biggest = x\n    return biggest\``,
          `第二步：用打印让中间状态现形。在循环里加 print：\n\`def find_max(nums):\n    biggest = 0\n    for x in nums:\n        print("当前 x =", x, "，目前 biggest =", biggest)\n        if x > biggest:\n            biggest = x\n    return biggest\`\n对 \`[-3, -9, -5, -2]\` 运行会看到：每个 x 都是负数，**没有一个比初始的 0 大**，所以 biggest 始终停在 0、从没被更新。`,
          `第三步：定位根因。bug 在 \`biggest = 0\` 这个初始值——它假设"最大值至少是 0"，但全是负数时这个假设不成立，导致 biggest 永远不会被换成真正的最大值。这正应了那句话："我假设了什么没成立？"——这里假设了"数都 ≥ 0"。`,
          `第四步：修正。把初始值改成"列表的第一个元素"，这样无论正负都能正确比较：\n\`def find_max(nums):\n    biggest = nums[0]    # 用第一个元素作起点，而不是 0\n    for x in nums:\n        if x > biggest:\n            biggest = x\n    return biggest\`\n再跑 \`find_max([-3, -9, -5, -2])\` 返回 -2，正确。最后别忘了把调试用的 print 删掉。`,
        ],
      },
    ],
    pitfalls: [
      `**只盯着代码干看、不动手验证**：逻辑错误光看很难发现，应主动加 \`print\` 让中间状态"现形"，用证据定位，而不是凭空猜。`,
      `**被报错吓住、不读最后一行**：Python 的 Traceback 最后一行才是错误类型和原因，先读它；前面是调用链。读懂报错能省一半时间。`,
      `**一次改好几处再运行**：改了三个地方一起跑，出了问题分不清是哪处引起的。应**一次只改一处、改完就跑**，缩小范围。`,
      `**调试用的 print 忘了删**：调试完留下一堆打印，会污染正式输出、显得很乱。确认修好后要清理掉。`,
      `**修了 bug 却不弄懂为什么**：靠瞎改"碰巧好了"，下次还会犯。每修一个 bug 都要想清"我刚才哪个假设没成立"，才真正学到东西。`,
    ],
    exercises: [
      {
        problem: `小明运行程序时崩溃，屏幕最后一行是：\`NameError: name 'totl' is not defined\`。他的相关代码是：\n\`total = 0\nfor i in range(5):\n    total = total + i\nprint(totl)\`\n请根据报错找出 bug 并改正。`,
        hint: `报错类型是 \`NameError\`（某个名字没定义）。它说没定义的是 \`totl\`——你在代码里定义过这个名字吗？是不是和某个变量长得很像？`,
        answer: `**详细讲解**：先读报错的最后一行——\`NameError: name 'totl' is not defined\`，它明确告诉我们：程序用到了一个**从没定义过的名字 \`totl\`**。回到代码看，前面定义和累加的变量都叫 \`total\`（带 a），而最后一行 \`print(totl)\` 把名字**拼错**成了 \`totl\`（漏了 a）。Python 找不到 \`totl\` 这个变量，于是抛 NameError。改正很简单，把拼写改对：\n\`total = 0\nfor i in range(5):\n    total = total + i\nprint(total)   # 改成 total\`\n这样就会正确打印 0+1+2+3+4=10。这道题教两件事：第一，**\`NameError\` 几乎都是"变量名拼错"或"还没赋值就使用"**；第二，**读报错先看最后一行**——它直接点出了是哪个名字出了问题，按图索骥即可，不用从头瞎找。`
      },
      {
        problem: `下面函数想数出列表里有多少个正数，但 \`count_positive([1, -2, 3, -4, 5])\` 返回了 1（应该是 3）。程序不报错。请用打印调试的思路说明你会怎么找 bug，并指出最可能的错误。\n\`def count_positive(nums):\n    count = 0\n    for x in nums:\n        if x > 0:\n            count = count + 1\n            return count\``,
        hint: `结果偏小、不报错，是逻辑错误。试着在循环里打印 count 和当前 x，看循环到底跑了几次。特别注意 \`return\` 的位置和缩进——它是不是太早把函数结束了？`,
        answer: `**详细讲解**：这是逻辑错误（能跑、结果不对），用打印调试来定位。先在循环里加 \`print("看到", x, "count 现在是", count)\`，运行会发现——**循环只打印了一次（x=1）就结束了**，根本没遍历完整个列表。顺着这个线索看代码，问题出在 \`return count\` 的**缩进位置**：它被放在了 \`if\` 内部、和 \`count = count + 1\` 同一层，意思变成"只要遇到第一个正数、加完 1 就立刻 return、结束整个函数"。所以遇到 1 之后 count=1 就直接返回了，后面的 3、5 根本没数到。修正办法是把 \`return\` 移到**循环外面**（与 for 同级），让函数先数完所有元素再返回：\n\`def count_positive(nums):\n    count = 0\n    for x in nums:\n        if x > 0:\n            count = count + 1\n    return count        # 移到循环外，数完再返回\`\n这样 \`count_positive([1, -2, 3, -4, 5])\` 会正确返回 3。这道题的教训：**return 的缩进/位置很关键**——放错地方会让循环提前结束；而打印调试能立刻暴露"循环只跑了一次"这个反常现象，帮你锁定问题。`
      },
      {
        problem: `请描述：当你的程序"能运行但结果不对"（逻辑错误）时，用打印调试法定位 bug 的完整步骤是怎样的？并说明为什么"读报错最后一行"对这种 bug 帮不上忙。`,
        hint: `回顾本节"打印调试法"的几个步骤：先有什么、再插什么、然后对照什么、最后做什么。再想想：逻辑错误会不会产生报错信息？`,
        answer: `**详细讲解**：先回答步骤，再解释为什么报错帮不上忙。\n**打印调试法的完整步骤**：① **明确预期**——先想清楚这段代码每一步"应该"得到什么值，心里有个标准；② **插入 print**——在你怀疑的关键位置（循环变量、累加器、函数返回值等）加 \`print\`，把这些"中间状态"连同说明文字打印出来；③ **运行并对照**——逐行查看输出，找出"第一处和预期不符"的地方，bug 通常就在它附近；④ **修正并复跑**——改正后再运行确认结果对了；⑤ **清理**——把调试用的 print 删掉，保持代码整洁。\n**为什么"读报错最后一行"帮不上这种 bug**：因为**逻辑错误并不会让程序崩溃，也就根本不产生报错信息（Traceback）**——程序顺顺当当跑完了，只是算出的结果是错的。既然没有报错，自然也就没有"最后一行"可读。语法错误、运行时错误会有报错可看，而逻辑错误必须靠你**主动**让中间状态"看得见"（打印或断点），再用自己心里的预期去对照，才能发现问题。这也是逻辑错误最难、最考验调试功力的原因。`
      },
    ],
  },

});
