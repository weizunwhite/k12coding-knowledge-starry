// 高中编程 · 高级数据结构 —— 详细小课（习题讲解版）
// 覆盖 5 个节点：number-base（进制与数据编码）/ hash-table（哈希表）/
//   heap（堆与优先队列）/ bst（二叉搜索树）/ union-find（并查集）
// 数据结构对齐 node-detail.jsx 的渲染：warmup / intro / formulas(name,latex,note) /
//   methods(name,when,steps,example) / worked(title,problem,steps) / pitfalls / exercises(problem,hint,answer)
// 说明：
//   · 行内公式用 $...$（renderRich 会交给 KaTeX）；formulas.latex 直接写 LaTeX（无需 $）
//   · JS 字符串里反斜杠写成 \\，所以 O(log n) 写作 $O(\\log n)$
//   · 含代码或引号的字段值一律用反斜杠模板字符串（反引号），代码换行用真实换行
//   · node-detail.jsx 不渲染 derivation，本文件不含该字段
//   · renderRich 会把 ** 当成加粗、$ 当成公式，故代码里避免出现 ** 和裸 $（幂运算改用 pow 或文字说明）

Object.assign(window.CODE_LESSON, {

  // ───────────────────────────────────────────────────────────────
  // 1. 进制与数据编码
  // ───────────────────────────────────────────────────────────────
  'number-base': {
    warmup: {
      q: '我们平时数数用 0~9 十个数字，逢十进一。可计算机里只有「通电 / 断电」两种状态。如果你只能用 0 和 1 两个数字来记数，该怎么表示 13 这个数？为什么程序员说「计算机的世界只有 0 和 1」？',
      hint: '十进制逢十进一，那「只有两个数字」的进制就该逢二进一。想一想：8、4、2、1 这几个数，挑哪几个加起来正好是 13？',
      answer: '计算机底层每个存储单元只有「通电=1 / 断电=0」两种状态，所以一切数据都要用 0 和 1 来表示——这叫**二进制**。二进制逢二进一，各位的「位权」是 8、4、2、1（即 $2^3,2^2,2^1,2^0$）。13 = 8 + 4 + 1，对应的位是 1101，所以十进制 13 写成二进制就是 $1101$。理解进制转换，你才能看懂内存、颜色值（如 #FF0000 是十六进制）、位运算，以及「为什么 0.1 + 0.2 不等于 0.3」这类小数陷阱。'
    },
    intro: [
      '进制，是「用几个数字、逢几进一」来记数的规则。我们日常用**十进制**（10 个数字 0~9，逢十进一），而计算机底层只有「通电 / 断电」两种状态，所以它天生用**二进制**（2 个数字 0、1，逢二进一）。理解进制，是看懂计算机如何存储一切数据的第一步。',
      '任何进制的核心都是**位权**：同一个数字写在不同位置，代表的大小不同。十进制里 235 = $2\\times10^2 + 3\\times10^1 + 5\\times10^0$；二进制里 1101 = $1\\times2^3 + 1\\times2^2 + 0\\times2^1 + 1\\times2^0$ = 8+4+0+1 = 13。把每一位的数字乘以它的位权再相加，就能把任意进制还原成十进制。',
      '除了二进制，程序员还常用**十六进制**（16 个符号：0~9 再加 A~F 代表 10~15，逢十六进一）。为什么？因为二进制写起来又长又容易看花眼，而**每 4 位二进制正好对应 1 位十六进制**，于是十六进制成了二进制的「速记」。比如颜色 #FF0000 里，FF 是十六进制的 255（即二进制 11111111），表示红色分量拉满。',
      '字符也要变成数字才能存进计算机，这靠**编码表**。最基础的是 ASCII 表：字母 "A" 对应 65、"a" 对应 97、"0" 这个字符对应 48。中文等更多字符则用 Unicode（常以 UTF-8 方式存储）。所以「文本」在底层其实是「一串数字」，再由这些数字编码成二进制存进内存。',
      '一句话记住：**计算机底层只有 0 和 1（二进制），十六进制是它的速记，字符靠编码表变成数字。** Python 里 bin(13) 得到 "0b1101"、hex(255) 得到 "0xff"、ord("A") 得到 65、chr(65) 得到 "A"。要特别小心：浮点小数在二进制里只能**近似**表示，所以判断两个小数是否相等，绝不能直接用 == 。'
    ],
    formulas: [
      { name: '位权展开（任意进制转十进制）', latex: '\\sum_i d_i \\times \\text{base}^{\\,i}',
        note: '把每一位数字 $d_i$ 乘以「进制的 $i$ 次方」再相加。如二进制 $1101 = 1\\cdot2^3+1\\cdot2^2+0\\cdot2^1+1\\cdot2^0 = 13$。' },
      { name: '十进制转 R 进制', latex: '\\text{除 R 取余，余数倒序排列}',
        note: '反复用十进制数除以进制 R、记下余数，直到商为 0，再把余数**从下往上**读，就是 R 进制结果。' },
      { name: '二进制与十六进制对应', latex: '\\texttt{4 位二进制} \\leftrightarrow \\texttt{1 位十六进制}',
        note: '每 4 位二进制对应 1 位十六进制（$2^4=16$）。如 1111→F、1010→A，所以十六进制是二进制的紧凑写法。' },
      { name: '字符编码', latex: '\\texttt{ord("A")}=65,\\ \\texttt{chr(65)}="A"',
        note: 'ASCII 用整数表示字符：大写 A~Z 是 65~90，小写 a~z 是 97~122，字符 "0"~"9" 是 48~57。' },
      { name: '浮点近似陷阱', latex: '0.1+0.2 \\ne 0.3',
        note: '小数用二进制只能近似存储，相加会有微小误差。比较小数别用 ==，应判断「差的绝对值是否小于一个极小量」。' },
    ],
    methods: [
      {
        name: '二进制 ↔ 十进制互转',
        when: '需要手工把一个二进制数还原成十进制，或把十进制数拆成二进制时',
        steps: [
          '二进制转十进制：从右到左给每一位标上位权 $2^0,2^1,2^2,\\dots$，把「数字为 1 的那些位」的位权加起来',
          '十进制转二进制：用「除 2 取余法」——不断用这个数除以 2、记下余数（0 或 1），直到商为 0',
          '把记下的余数**从下往上（从最后一个到第一个）**连起来，就是二进制结果',
          '可用 8、4、2、1、… 这些位权「凑数」来快速验证：哪几个位权相加等于目标数，对应位就是 1',
        ],
        example: {
          problem: '（1）把二进制 10110 转成十进制。（2）把十进制 13 转成二进制。',
          solution: [
            '解（1）：给 10110 从右到左标位权——1·16 + 0·8 + 1·4 + 1·2 + 0·1 = 16+4+2 = 22。所以二进制 10110 = 十进制 22。',
            '解（2）：除 2 取余。13÷2=6 余 1；6÷2=3 余 0；3÷2=1 余 1；1÷2=0 余 1。余数从下往上读是 1101。',
            '验证：1101 = 8+4+0+1 = 13，正确。也可用凑数法：13 = 8+4+1，对应 8、4、1 位为 1，得 1101。',
            '答：（1）22；（2）1101。',
          ],
        },
      },
      {
        name: '用 Python 内置函数做进制与编码转换',
        when: '在程序里需要查看一个数的二/十六进制写法，或字符与编码的对应时',
        steps: [
          '十进制转二进制字符串：bin(n)，结果带前缀 "0b"，如 bin(13) → "0b1101"',
          '十进制转十六进制字符串：hex(n)，结果带前缀 "0x"，如 hex(255) → "0xff"',
          '其它进制字符串转十进制：int(s, base)，如 int("1101", 2) → 13、int("ff", 16) → 255',
          '字符与编码互转：ord(字符) 得到编码整数、chr(整数) 得到字符，如 ord("A") → 65、chr(97) → "a"',
        ],
        example: {
          problem: '用 Python 求：（1）255 的十六进制写法；（2）二进制字符串 "1010" 代表的十进制数；（3）字符 "Z" 的 ASCII 编码。',
          solution: [
            '解（1）：hex(255) 返回 "0xff"，去掉前缀就是 ff，即十进制 255 = 十六进制 FF。',
            '解（2）：int("1010", 2) 把 "1010" 按 2 进制解析，返回 10。',
            '解（3）：ord("Z") 返回 90（大写字母 A~Z 是 65~90，Z 是第 26 个，65+25=90）。',
            '答：（1）0xff；（2）10；（3）90。',
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 手写「十进制转二进制」函数并追踪执行',
        problem: '请写一个函数 to_binary(n)，把一个正整数 n 转成二进制字符串（不带 "0b" 前缀），用「除 2 取余」的思路实现。然后追踪 to_binary(13) 的执行过程。',
        steps: [
          `第一步：写出代码。不断对 2 取余、把余数收集起来，最后倒序拼成字符串。
def to_binary(n):
    # 特判：0 直接返回 "0"
    if n == 0:
        return "0"
    bits = []                  # 收集每一步的余数（二进制位）
    while n > 0:
        remainder = n % 2      # 取余，得到当前最低位（0 或 1）
        bits.append(str(remainder))
        n = n // 2             # 整除 2，去掉已处理的最低位
    # 余数是「从低位到高位」收集的，要倒过来才是正确顺序
    bits.reverse()
    return "".join(bits)`,
          '第二步：理解逻辑。每次 n % 2 得到「当前最低位」，n // 2 把这一位「去掉」；先得到的是低位、后得到的是高位，所以最后要 reverse 倒序，再用 join 拼成字符串。',
          '第三步：追踪 to_binary(13)。n=13 不为 0，进入 while 循环，bits 初始为空。',
          '第四步：第 1 轮 n=13：13%2=1，bits=["1"]，n=13//2=6。第 2 轮 n=6：6%2=0，bits=["1","0"]，n=6//2=3。',
          '第五步：第 3 轮 n=3：3%2=1，bits=["1","0","1"]，n=3//2=1。第 4 轮 n=1：1%2=1，bits=["1","0","1","1"]，n=1//2=0，循环结束。',
          '第六步：bits 此时是 ["1","0","1","1"]（低位在前）。reverse 后变成 ["1","1","0","1"]，join 得到 "1101"。返回 **"1101"**。验证 1101 = 8+4+1 = 13，正确。',
        ],
      },
      {
        title: '例题 · 凯撒密码：字符编码的小应用',
        problem: '凯撒密码把每个大写字母「向后移 k 位」加密（如 k=3 时 A→D、B→E，到 Z 之后绕回 A）。请用 ord / chr 写一个加密函数 caesar(text, k)，并追踪 caesar("ABZ", 3) 的过程。',
        steps: [
          `第一步：写出代码。把字母转成编码，相对 "A" 偏移后对 26 取模实现「绕回」。
def caesar(text, k):
    result = ""
    for ch in text:                       # 逐个处理字符
        # 当前字母相对 A 的位置（A=0, B=1, ..., Z=25）
        offset = ord(ch) - ord("A")
        # 向后移 k 位，对 26 取模实现「超过 Z 绕回 A」
        new_offset = (offset + k) % 26
        # 转回字符：A 的编码加上新偏移
        new_char = chr(ord("A") + new_offset)
        result = result + new_char
    return result`,
          '第二步：理解逻辑。ord(ch) - ord("A") 把字母映射成 0~25 的编号；加 k 后用 % 26 让超过 25 的值绕回开头；再用 chr(ord("A") + 新编号) 转回字母。% 26 是实现「Z 之后回到 A」的关键。',
          '第三步：追踪 caesar("ABZ", 3)。result 初始为空字符串。',
          '第四步：处理 "A"：offset = 65-65 = 0，new_offset = (0+3)%26 = 3，new_char = chr(65+3) = chr(68) = "D"。result = "D"。',
          '第五步：处理 "B"：offset = 66-65 = 1，new_offset = (1+3)%26 = 4，new_char = chr(69) = "E"。result = "DE"。',
          '第六步：处理 "Z"：offset = 90-65 = 25，new_offset = (25+3)%26 = 28%26 = 2，new_char = chr(65+2) = chr(67) = "C"。result = "DEC"。返回 **"DEC"**。注意 Z 因为绕回，移 3 位后变成了 C，这正是 % 26 的作用。',
        ],
      },
    ],
    pitfalls: [
      '**用 == 比较两个浮点小数**。小数在二进制里是近似存储的，0.1+0.2 实际等于 0.30000000000000004，不等于 0.3。判断小数相等应改成「abs(a - b) < 1e-9」这类「差足够小」的写法。',
      '搞错进制转换的「读数方向」。十进制转二进制用「除 2 取余」，余数必须**从下往上（最后一个余数到第一个）**读；顺着读会得到完全错误的结果。',
      '混淆「字符 0」和「数字 0」。字符 "0" 的 ASCII 编码是 48，不是 0；要把字符数字 "7" 变成整数 7，得用 int("7") 或 ord("7")-ord("0")，不能直接当数字用。',
      '把十六进制的 A~F 当字母而不是数值。十六进制里 A=10、B=11、…、F=15，它们是「数字」。如 0x1F = 1×16 + 15 = 31，别把 F 漏算。',
      '忘了 bin() / hex() 带前缀。bin(13) 返回的是 "0b1101"，hex(255) 返回 "0xff"，前面的 "0b"、"0x" 是进制标记；做字符串处理时若不需要，要记得去掉。',
    ],
    exercises: [
      {
        problem: '不用 Python 内置函数，手工完成下面两个转换，并写出过程：（1）把二进制 11010 转成十进制；（2）把十进制 45 转成二进制。',
        hint: '（1）给每位标位权 16、8、4、2、1，把数字为 1 的位权加起来。（2）用「除 2 取余」，余数从下往上读。',
        answer: `**详细讲解**：

**（1）二进制 11010 转十进制——位权展开法**

从右到左给每一位标上位权（$2^0$ 到 $2^4$）：

| 二进制位 | 1 | 1 | 0 | 1 | 0 |
|---|---|---|---|---|---|
| 位权 | 16 | 8 | 4 | 2 | 1 |

把「数字为 1」的位权加起来：16 + 8 + 0 + 2 + 0 = **26**。

所以二进制 11010 = 十进制 **26**。

**（2）十进制 45 转二进制——除 2 取余法**

反复除以 2、记下余数：
- 45 ÷ 2 = 22 余 **1**
- 22 ÷ 2 = 11 余 **0**
- 11 ÷ 2 = 5 余 **1**
- 5 ÷ 2 = 2 余 **1**
- 2 ÷ 2 = 1 余 **0**
- 1 ÷ 2 = 0 余 **1**

余数**从下往上**读：101101。

所以十进制 45 = 二进制 **101101**。

**验证**：101101 = 32+0+8+4+0+1 = 45，正确。

**小结**：二进制转十进制靠「位权相加」，十进制转二进制靠「除 2 取余、倒序读」。两个方向互为逆运算，记住「逢二进一、位权是 2 的幂」就不会乱。`
      },
      {
        problem: '颜色在网页里常用十六进制表示，如 #FF0000 是纯红、#00FF00 是纯绿。每个颜色由「红、绿、蓝」三段各两位十六进制组成（每段范围 00~FF）。请问：（1）#FF0000 里红色分量的十进制值是多少？（2）十六进制 FF 为什么是该分量的最大值？',
        hint: '两位十六进制 FF = F×16 + F×1，其中 F 代表 15。想一想两位十六进制最大能表示到多少。',
        answer: `**详细讲解**：

**（1）#FF0000 里红色分量的十进制值**

#RRGGBB 格式中，前两位 RR 是红色分量。这里 RR = FF。

十六进制 FF 转十进制（用位权展开，十六进制位权是 16 的幂）：
- F 在十六位（$16^1$）：F = 15，贡献 15 × 16 = 240
- F 在个位（$16^0$）：F = 15，贡献 15 × 1 = 15
- 合计：240 + 15 = **255**

所以 #FF0000 的红色分量 = **255**（红色拉到最满），绿、蓝都是 00（即 0），所以显示为纯红色。

**（2）为什么 FF 是该分量的最大值**

每个颜色分量用**两位十六进制**表示。两位十六进制能表示的范围是 00 到 FF：
- 最小：00 = 0
- 最大：FF = 15×16 + 15 = 255

也就是说，两位十六进制正好覆盖 0~255 共 256 个值（$256 = 16^2 = 2^8$，正好是 8 个二进制位、即 1 个字节能表示的范围）。所以每个颜色分量最大就是 255（十六进制 FF），#FFFFFF 就是「红绿蓝全满」的白色，#000000 是全灭的黑色。

**复杂度 / 知识小结**：这道题没有算法复杂度，但揭示了一个重要事实——**1 个字节（8 位二进制）= 两位十六进制 = 0~255**。这就是为什么颜色、内存地址都爱用十六进制：紧凑且和字节对齐。`
      },
      {
        problem: '小明写了一段判断浮点结果的代码，发现明明算对了却总输出「不相等」：\nprice = 0.1 + 0.2\nif price == 0.3:\n    print("相等")\nelse:\n    print("不相等")\n请解释为什么会这样，并给出正确的比较写法。',
        hint: '小数在计算机里用二进制存储，0.1、0.2、0.3 都无法被二进制精确表示。打印 0.1+0.2 看看实际是多少。',
        answer: `**详细讲解**：

**为什么输出「不相等」——浮点数的二进制近似问题**

计算机用二进制存储小数。但很多十进制小数（如 0.1、0.2、0.3）在二进制里是**无限循环小数**，无法被精确表示，只能存一个**最接近的近似值**。

于是 0.1 + 0.2 的实际结果不是恰好的 0.3，而是 0.30000000000000004（末尾有微小误差）。你可以打印出来看：
\`\`\`python
print(0.1 + 0.2)   # 输出 0.30000000000000004
\`\`\`
这个值和 0.3 的存储近似值**不完全相等**，所以 \`price == 0.3\` 判定为 False，走了 else 分支。

**正确写法——判断「差足够小」**

既然小数有微小误差，就不该要求「严格相等」，而应判断「两数之差的绝对值是否小于一个极小量（容差）」：
\`\`\`python
price = 0.1 + 0.2
if abs(price - 0.3) < 1e-9:    # 差小于 0.000000001 就认为相等
    print("相等")
else:
    print("不相等")
\`\`\`
这里 1e-9 是「容差」（一个极小的数）。两个浮点数只要差距比它还小，就认为它们「实质相等」。

**也可以用标准库**：
\`\`\`python
import math
if math.isclose(price, 0.3):   # Python 内置的浮点近似比较
    print("相等")
\`\`\`

**知识小结**：这是浮点运算的经典陷阱——**永远不要用 == 直接比较两个浮点小数**。根本原因是二进制无法精确表示大多数十进制小数。涉及金额这类需要精确小数的场景，还可以改用整数（以「分」为单位）或 Python 的 decimal 模块来避免误差。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 2. 哈希表
  // ───────────────────────────────────────────────────────────────
  'hash-table': {
    warmup: {
      q: '图书馆有 10 万本书。如果不给书编号、随便堆放，找一本书要一本本翻（最坏 10 万次）。但现实中你报出书名，管理员几秒就能定位它在哪个架、哪一层。为什么这么快？计算机里的「字典」查一个键也几乎是瞬间完成，它背后用了什么魔法？',
      hint: '管理员不是「一本本找」，而是「按某种规则一步算出位置」。想一想：能不能设计一个函数，输入「键」，直接算出它该放在数组的哪个格子？',
      answer: '关键在于「**不靠逐个比较，而是用一个函数直接算出位置**」。计算机里的字典底层是**哈希表**：它有一个叫**哈希函数**的「计算规则」，把任意键（书名、学号…）算成一个数组下标，于是存的时候算一次下标直接放进去，查的时候再算一次下标直接取出来——平均只要 $O(1)$（常数时间），跟数据有多少几乎无关。这就是「字典查找几乎是瞬间」的原因。当然，不同键可能算到同一个格子（叫「冲突」），需要专门的办法处理。'
    },
    intro: [
      '哈希表（Hash Table），是一种能做到**平均 $O(1)$ 时间查找、插入、删除**的数据结构。Python 里的字典 dict、集合 set 底层都是哈希表。它快的秘诀是：**不靠逐个比较找数据，而是用一个函数直接「算出」数据该在的位置**。',
      '核心部件叫**哈希函数**：它接收一个「键」（key，可以是数字、字符串等），通过一系列计算，输出一个数组下标。最简单的形式是「先把键转成一个整数（哈希值），再对数组长度取模」，得到 0 到「长度-1」之间的一个位置。存数据时算出下标、放进对应格子；查数据时同样算一次下标、直接到那个格子取——所以无论数据多少，都只是「算一次、取一次」，平均 $O(1)$。',
      '但有个绕不开的问题：**不同的键，可能被哈希函数算到同一个下标**，这叫**哈希冲突**。就像两本不同的书被分到了同一个格子。冲突无法完全避免（键的种类远多于格子数），只能想办法**解决**。最常见的两种办法：① **链地址法**——每个格子挂一个「小链表/小列表」，冲突的元素都串在同一个格子的链表里；② **开放寻址法**——发现格子被占了，就按规则往后找下一个空格子放。',
      '哈希表会不会退化？会。如果哈希函数设计得不好、键的分布不均，大量键挤进少数几个格子，链表越来越长，查找就要在长链表里逐个比较，**最坏情况退化到 $O(n)$**。另外，当表「太满」（已用格子占比过高，这个比例叫**装填因子**）时冲突会激增，所以真实的哈希表会在变满时自动「扩容」（换更大的数组并重新分布所有元素），以维持高效。',
      '一句话记住：**哈希表用哈希函数把键直接算成数组下标，平均 $O(1)$；冲突用链地址法或开放寻址解决；表太满或哈希不均会退化到 $O(n)$。** 它是字典、集合、去重、缓存的底层引擎，理解它就明白了「为什么 in 判断和 dict 取值几乎不花时间」。'
    ],
    formulas: [
      { name: '平均时间复杂度', latex: 'O(1)',
        note: '哈希函数直接算出位置，存取都是「算一次、取一次」，与元素总数无关。这是字典查找飞快的根本原因。' },
      { name: '最坏时间复杂度', latex: 'O(n)',
        note: '哈希函数差或表太满导致大量冲突时，元素挤在同一格子的长链表里，查找退化成逐个比较，变成 $O(n)$。' },
      { name: '哈希函数（取模法）', latex: '\\text{index} = \\text{hash}(key) \\bmod m',
        note: '把键算成哈希值后，对数组长度 $m$ 取模，得到 $0$ 到 $m-1$ 的下标。$m$ 常取质数以让分布更均匀。' },
      { name: '装填因子', latex: '\\alpha = \\dfrac{n}{m}',
        note: '已存元素数 $n$ 除以数组容量 $m$。$\\alpha$ 越接近 1 冲突越多；超过阈值（如 0.75）通常触发扩容以保持高效。' },
    ],
    methods: [
      {
        name: '哈希表的「存」与「查」流程',
        when: '想理解字典 / 集合内部如何用哈希函数实现快速存取时',
        steps: [
          '准备一个长度为 m 的数组（桶 buckets），每个格子可挂一个小列表（链地址法）',
          '存入键值对 (key, value)：先用哈希函数算出 index = hash(key) % m',
          '到第 index 个格子，把 (key, value) 放进去；若该格子已有别的键（冲突），就追加到这个格子的小列表里',
          '查找某个 key：同样算出 index = hash(key) % m，到第 index 个格子；若格子里有多个元素，再在这个小列表里逐个比对 key 找到目标',
          '因为每个格子里的元素通常很少，「算下标 + 在短列表里比对」整体仍是平均 $O(1)$',
        ],
        example: {
          problem: '设数组长 m=5，哈希函数为 hash(key)=key（键就是它自己），用链地址法依次存入键 12、7、22、5。它们分别落在哪个格子？哪里发生了冲突？',
          solution: [
            '解：对每个键算 index = key % 5。',
            '12 % 5 = 2 → 放进格子 2；7 % 5 = 2 → 也算到格子 2，和 12 冲突，追加到格子 2 的列表，格子 2 = [12, 7]。',
            '22 % 5 = 2 → 又是格子 2！追加，格子 2 = [12, 7, 22]；5 % 5 = 0 → 放进格子 0。',
            '答：格子 0 = [5]，格子 2 = [12, 7, 22]。格子 2 发生了三次碰撞，说明这组键 % 5 后分布很不均，查找格子 2 里的键时要在长度 3 的列表里比对——这正是冲突拖慢查找的来源。',
          ],
        },
      },
      {
        name: '用字典实现「快速统计 / 查重」',
        when: '需要「统计每个元素出现次数」或「快速判断某元素是否出现过」时（最常见的哈希表应用）',
        steps: [
          '建一个空字典 counts（底层就是哈希表，键到值的映射）',
          '遍历数据，对每个元素 x：用 x 当键去字典里查/改它的计数',
          '若 x 已在字典里，counts[x] 加 1；否则 counts[x] = 1（第一次出现）',
          '查「某元素出现过吗」直接用 x in counts（哈希查找，平均 $O(1)$），不必遍历整个数据',
        ],
        example: {
          problem: '统计列表 ["a", "b", "a", "c", "b", "a"] 中每个字母出现的次数。',
          solution: [
            '解：建空字典 counts={}，逐个处理。',
            '"a"：不在 → counts={"a":1}；"b"：不在 → counts={"a":1,"b":1}；"a"：已在 → counts={"a":2,"b":1}。',
            '"c"：不在 → 加入；"b"：已在 → +1；"a"：已在 → +1。最终 counts={"a":3,"b":2,"c":1}。',
            '答：a 出现 3 次、b 出现 2 次、c 出现 1 次。每次「查 + 改」字典都是平均 $O(1)$，整体只需遍历一遍，总复杂度 $O(n)$。',
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 手写一个简易哈希表（链地址法）并追踪执行',
        problem: '请用「链地址法」实现一个极简哈希表类 SimpleHashSet，支持 add(key) 添加和 contains(key) 查询。数组长度固定 m=7，哈希函数用 key % 7。然后追踪依次 add(10)、add(17)、add(3) 后，contains(17) 的查询过程。',
        steps: [
          `第一步：写出代码。用「列表套列表」表示桶数组，每个桶是一个小列表。
class SimpleHashSet:
    def __init__(self, m=7):
        self.m = m
        # 建 m 个空桶，每个桶是一个列表（链地址法）
        self.buckets = [[] for _ in range(m)]

    def _index(self, key):
        # 哈希函数：键对桶数取模，得到桶下标
        return key % self.m

    def add(self, key):
        idx = self._index(key)         # 算出该放哪个桶
        if key not in self.buckets[idx]:   # 避免重复添加
            self.buckets[idx].append(key)  # 放进对应桶的列表

    def contains(self, key):
        idx = self._index(key)         # 算出它该在哪个桶
        # 只需在这一个桶的（通常很短的）列表里找
        return key in self.buckets[idx]`,
          '第二步：理解逻辑。buckets 是 m 个小列表。add 先用 key % m 算出桶号，再把 key 放进那个桶；contains 同样算出桶号，只在那一个桶里查。关键是「算下标」把查找范围从「整个集合」缩到「一个桶」。',
          '第三步：追踪三次 add。m=7，buckets 初始是 7 个空列表 [[],[],[],[],[],[],[]]。',
          '第四步：add(10)：10%7=3，桶 3 没有 10，放入 → 桶 3 = [10]。add(17)：17%7=3，和 10 冲突，桶 3 追加 17 → 桶 3 = [10, 17]。',
          '第五步：add(3)：3%7=3，又算到桶 3！桶 3 追加 3 → 桶 3 = [10, 17, 3]。此时只有桶 3 有元素，其余桶都空——这组键碰巧都落在桶 3，发生了连续冲突。',
          '第六步：追踪 contains(17)。先算 17%7=3，定位到桶 3；在桶 3 的列表 [10, 17, 3] 里查 17：比 10（不是）、比 17（是！）→ 返回 **True**。注意：虽然这次桶 3 里有 3 个元素要比对，但若哈希分布均匀、每个桶只有 1~2 个元素，查找就接近 $O(1)$。',
        ],
      },
      {
        title: '例题 · 用字典解「两数之和」（哈希表的经典应用）',
        problem: '给一个整数列表 nums 和目标值 target，找出两个数使它们的和等于 target，返回这两个数的下标。请用哈希表把暴力 $O(n^2)$ 优化到 $O(n)$，并追踪 nums=[2, 7, 11, 4]、target=9 的过程。',
        steps: [
          `第一步：写出代码。一边遍历一边用字典记「已见过的数 → 它的下标」。
def two_sum(nums, target):
    seen = {}                       # 键：已遍历过的数；值：它的下标
    for i, x in enumerate(nums):
        need = target - x           # 想凑成 target，还需要的另一个数
        if need in seen:            # 这个「需要的数」之前见过吗（哈希查找 O(1)）
            return [seen[need], i]  # 见过：返回那个数的下标和当前下标
        seen[x] = i                 # 没凑成：把当前数记进字典，供后面的数查
    return None                     # 没有符合的两个数`,
          '第二步：理解逻辑。对每个数 x，它需要的「搭档」是 target - x。我们用字典 seen 记下「之前出现过的数」，于是判断「搭档是否出现过」是一次哈希查找（$O(1)$），不用再套一层循环去找——这就把 $O(n^2)$ 降到了 $O(n)$。',
          '第三步：追踪 nums=[2,7,11,4]、target=9。seen 初始为空字典。',
          '第四步：i=0, x=2：need = 9-2 = 7，seen 里没有 7，把 2 记入 → seen={2:0}。',
          '第五步：i=1, x=7：need = 9-7 = 2，查 seen 发现 2 在里面（下标 0）！返回 [seen[2], 1] = **[0, 1]**，函数结束。',
          '第六步：作答。下标 0 和 1 对应的数是 2 和 7，2+7=9，正确。整个过程只遍历了一遍、每步做一次哈希查找，总复杂度 $O(n)$；相比「双层循环两两相加」的 $O(n^2)$ 快得多。这就是哈希表把「查找」变快后带来的算法优化。',
        ],
      },
    ],
    pitfalls: [
      '**以为哈希表永远是 $O(1)$**。那是「平均」情况。哈希函数设计差、键分布不均、或表太满时，大量元素挤在同一桶里，查找退化成在长链表里逐个比较，**最坏 $O(n)$**。',
      '冲突没处理 / 处理错。不同键算到同一下标是必然会发生的，必须用链地址法或开放寻址处理；直接覆盖会丢数据，不比较 key 直接返回会拿到错误的值。',
      '把「无序」当成 bug。哈希表（dict、set）里元素的存放位置由哈希值决定，**不保证按插入或大小顺序**。需要有序结果时要另行排序，别指望遍历哈希表就有序。',
      '用「不可哈希」的对象当键。字典的键必须是不可变的（数字、字符串、元组），**列表、字典不能当键**（它们可变、哈希值会变），否则报 unhashable 错误。',
      '装填因子过高还不扩容。表越满冲突越多。真实哈希表会在装填因子超过阈值时自动扩容重排；自己实现时若桶数固定又塞太多元素，性能会显著下降。',
    ],
    exercises: [
      {
        problem: '设哈希表数组长度 m=6，哈希函数 hash(key)=key % 6，用链地址法。请依次插入键 8、14、6、20、3，画出每个桶最终的内容，并指出哪些桶发生了冲突。',
        hint: '对每个键算 key % 6 得到桶号。算到已有元素的桶就是冲突，把新键追加到那个桶的列表末尾。',
        answer: `**详细讲解**：

**方法**：对每个键算 index = key % 6，放进对应桶；若桶已有元素则追加（冲突）。

**逐个插入**：
- 8 % 6 = **2** → 桶 2 = [8]
- 14 % 6 = **2** → 桶 2 已有 8，**冲突**，追加 → 桶 2 = [8, 14]
- 6 % 6 = **0** → 桶 0 = [6]
- 20 % 6 = **2** → 桶 2 又被算到，**冲突**，追加 → 桶 2 = [8, 14, 20]
- 3 % 6 = **3** → 桶 3 = [3]

**最终各桶内容**：

| 桶号 | 内容 |
|---|---|
| 0 | [6] |
| 1 | [] |
| 2 | [8, 14, 20] |
| 3 | [3] |
| 4 | [] |
| 5 | [] |

**发生冲突的桶**：桶 2（8、14、20 三个键都落在这里，连续冲突两次）。

**复杂度分析**：理想情况下哈希表查找是 $O(1)$，但本例里 8、14、20 全挤在桶 2，查找桶 2 中的键要在长度 3 的列表里逐个比较，这部分退化成 $O(\\text{桶长})$。

**小结**：这组键碰巧 % 6 后大量相同，导致分布不均、桶 2 过长。现实中要靠「好的哈希函数 + 合适的桶数（常取质数）+ 装填因子超标就扩容」来让元素尽量均匀分散，维持平均 $O(1)$。`
      },
      {
        problem: '请用字典写一个函数 first_unique(s)，返回字符串 s 中**第一个只出现一次**的字符；如果没有，返回 None。例如 first_unique("aabbcd") 应返回 "c"。说明它的时间复杂度。',
        hint: '分两步：先用一个字典统计每个字符出现几次；再按原字符串顺序遍历一遍，返回第一个「计数为 1」的字符。',
        answer: `**详细讲解**：

**思路**：要找「第一个只出现一次」的字符，需要两件事——① 知道每个字符出现几次；② 按原顺序找第一个次数为 1 的。用哈希表（字典）统计次数最合适，因为「查/改某字符的计数」是平均 $O(1)$。

**两步法**：
1. 第一遍遍历：用字典 counts 统计每个字符出现的次数。
2. 第二遍遍历（按原字符串顺序）：返回第一个 counts 值为 1 的字符。

**参考代码**：
\`\`\`python
def first_unique(s):
    counts = {}                    # 字符 -> 出现次数
    # 第一遍：统计每个字符出现几次
    for ch in s:
        if ch in counts:
            counts[ch] = counts[ch] + 1
        else:
            counts[ch] = 1
    # 第二遍：按原顺序找第一个「只出现一次」的字符
    for ch in s:
        if counts[ch] == 1:
            return ch
    return None                    # 没有只出现一次的字符
\`\`\`

**执行示例**：s = "aabbcd"
- 第一遍统计：counts = {"a":2, "b":2, "c":1, "d":1}
- 第二遍按顺序看：a(2 跳过)、a(跳过)、b(2 跳过)、b(跳过)、c(**1，返回**) → 返回 "c"

**复杂度分析**：
- 第一遍遍历 n 个字符，每次字典操作平均 $O(1)$，合计 $O(n)$。
- 第二遍同样 $O(n)$。
- 总时间复杂度 **$O(n)$**，额外空间 $O(k)$（k 为不同字符的种类数）。

**为什么用哈希表**：如果不用字典，要统计每个字符次数就得对每个字符再扫一遍整串，变成 $O(n^2)$。哈希表把「统计次数」和「判断是否出现过」都降到平均 $O(1)$，是这类「计数 / 查重」问题的标准利器。`
      },
      {
        problem: '同桌说："哈希表查找是 $O(1)$，所以无论什么情况都比二分查找（$O(\\log n)$）快，应该永远用哈希表。"这个说法对吗？请从「最坏情况」「是否需要有序」两个角度反驳。',
        hint: '$O(1)$ 是哈希表的「平均」情况，最坏会怎样？另外，哈希表能像有序结构那样做「范围查询」「找最接近的数」吗？',
        answer: `**详细讲解**：

**这个说法不全对**。哈希表确实平均很快，但「永远用哈希表」是错的，理由有二。

**反驳一：$O(1)$ 是「平均」，最坏会退化到 $O(n)$**
- 哈希表的 $O(1)$ 是**平均**时间复杂度，前提是哈希函数好、键分布均匀、表不太满。
- 一旦哈希函数设计差或键恶意构造，大量键挤进同一个桶，桶里的列表很长，查找要逐个比较，**最坏退化到 $O(n)$**。
- 而二分查找在有序数据上**任何情况都稳定 $O(\\log n)$**，没有退化风险。所以「无论什么情况都比二分快」不成立——最坏情况下哈希表反而更慢。

**反驳二：哈希表不保证有序，做不了「范围 / 顺序」查询**
- 哈希表为了 $O(1)$ 的速度，把元素按哈希值打散存放，**完全没有顺序**。
- 因此它只能回答「这个键在不在 / 它的值是多少」这类**精确匹配**问题。
- 但很多需求需要「有序」：比如「找出所有 80~90 分之间的成绩」（范围查询）、「找比 x 大的最小的数」、「按顺序输出」——这些哈希表都做不到，而建立在有序数据上的二分查找（或有序结构）天然支持。

**结论 / 怎么选**：
- **只做精确查找、不要求顺序** → 用哈希表（平均 $O(1)$，最快）。例：判断某学号是否存在、按 key 取 value。
- **需要范围查询、找最接近值、按序遍历，或要求最坏也稳定** → 用有序结构 + 二分查找（$O(\\log n)$），或二叉搜索树。

**一句话**：哈希表「平均最快」但「无序且最坏会退化」；二分「稍慢一点」但「稳定且支持有序操作」。没有「永远最好」的结构，要看需求选。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 3. 堆与优先队列
  // ───────────────────────────────────────────────────────────────
  'heap': {
    warmup: {
      q: '医院急诊室不是「先到先看」，而是「**病情最重的先看**」。新来一个病人就插进队伍，每次叫号都要叫出当前最危重的那个。如果用普通排队（列表），每次找最重的要扫一遍所有人（$O(n)$）。有没有一种结构，让「插入新病人」和「取出最危重的」都很快？',
      hint: '不需要让所有病人「完全排好序」，只要每次能**快速拿到最危重的那一个**就够了。想象一棵树，把最重的永远放在树根。',
      answer: '这正是**优先队列**要解决的问题：不按「先来后到」，而按「优先级」出队，每次取出优先级最高（如病情最重）的元素。它的底层用一种叫**堆**的结构实现——堆是一棵特殊的完全二叉树，**树根永远是最大值（或最小值）**。靠这个性质，「取出极值」直接看树根、「插入新元素」只需局部调整，两者都只要 $O(\\log n)$，远快于在普通列表里每次扫一遍的 $O(n)$。Python 的 heapq 模块就提供了现成的堆。'
    },
    intro: [
      '堆（Heap），是一种特殊的**完全二叉树**，它的核心性质是：**每个父节点都不小于（或不大于）它的子节点**。父节点都≥子节点的叫**大顶堆**（树根是整棵树的最大值）；父节点都≤子节点的叫**小顶堆**（树根是最小值）。堆最大的用途，是**实现优先队列**——能反复快速取出「最大」或「最小」的那个元素。',
      '为什么用堆？因为它在「取极值」和「插入」之间取得了很好的平衡。普通做法里，用有序列表取极值快（$O(1)$）但插入慢（$O(n)$，要挪位置）；用无序列表插入快（$O(1)$）但取极值慢（$O(n)$，要扫一遍）。而堆让**插入和取极值都做到 $O(\\log n)$**，特别适合「不断有新元素进来、又不断要取走当前极值」的场景。',
      '堆通常**用一个数组（列表）来存储**，而不需要真的建树。诀窍是：把完全二叉树「按层从上到下、从左到右」编号放进数组。这样下标 i 的节点，它的左孩子在 2i+1、右孩子在 2i+2、父节点在 (i-1)//2。靠这套下标公式，就能在数组上模拟出树的父子关系，省去指针。',
      '堆怎么维持性质？靠两个调整动作：① **上浮（sift up）**——插入新元素时放到数组末尾，再不断和父节点比较，若比父节点「更该在上面」就交换、一路上浮到合适位置；② **下沉（sift down）**——取走堆顶后，把末尾元素移到堆顶，再不断和较优的孩子比较、交换、一路下沉到位。因为完全二叉树的高度约 $\\log n$，所以一次上浮/下沉最多走 $\\log n$ 步，这就是插入和取极值都是 $O(\\log n)$ 的原因。',
      '一句话记住：**堆是「根为极值」的完全二叉树，用数组存，靠上浮/下沉维持，插入和取极值都是 $O(\\log n)$。** 它是堆排序、Dijkstra 最短路、Top-K（求第 K 大/小）问题的引擎。Python 直接用 heapq（heappush 入堆、heappop 取最小，它实现的是小顶堆）。切记：**堆只保证「根是极值」，整体并不是有序的**，别指望遍历堆数组就能得到排好序的结果。'
    ],
    formulas: [
      { name: '插入 / 取极值复杂度', latex: 'O(\\log n)',
        note: '完全二叉树高度约 $\\log n$；插入靠上浮、取极值后靠下沉，最多走树高那么多步，所以都是 $O(\\log n)$。' },
      { name: '查看堆顶（极值）', latex: 'O(1)',
        note: '大顶堆的最大值、小顶堆的最小值永远在树根（数组第 0 个），直接读取，无需查找。' },
      { name: '数组下标关系', latex: '\\text{左}=2i{+}1,\\ \\text{右}=2i{+}2,\\ \\text{父}=(i{-}1)//2',
        note: '把完全二叉树按层放进数组后，下标 $i$ 节点的左右孩子和父节点位置由这组公式确定，省去指针。' },
      { name: '堆排序复杂度', latex: 'O(n\\log n)',
        note: '把 $n$ 个元素逐个入堆再逐个取出，每次操作 $O(\\log n)$，共 $n$ 次，合计 $O(n\\log n)$。' },
    ],
    methods: [
      {
        name: '插入元素（上浮）',
        when: '向堆中加入一个新元素，同时要保持堆的性质时',
        steps: [
          '把新元素放到数组**末尾**（相当于挂在完全二叉树最后一个位置）',
          '把它和**父节点**比较（小顶堆里看「是否比父节点小」）',
          '若它比父节点「更该在上面」，就和父节点交换，它上移一层',
          '重复比较 / 交换，直到它不再比父节点更优，或已到达根——这叫「上浮」',
        ],
        example: {
          problem: '一个小顶堆的数组是 [1, 3, 5, 7, 4]（已满足堆性质）。现在插入元素 2，写出上浮过程。',
          solution: [
            '解：把 2 放到末尾 → [1, 3, 5, 7, 4, 2]，2 的下标是 5。',
            '2 的父节点下标 = (5-1)//2 = 2，即元素 5。比较 2 < 5（小顶堆，2 更该在上面），交换 → [1, 3, 2, 7, 4, 5]，2 移到下标 2。',
            '2 的新父节点下标 = (2-1)//2 = 0，即元素 1。比较 2 < 1？不成立，停止上浮。',
            '答：插入后堆数组变为 [1, 3, 2, 7, 4, 5]，根仍是最小值 1。上浮只走了 1 步，体现 $O(\\log n)$。',
          ],
        },
      },
      {
        name: '取出堆顶极值（下沉）',
        when: '要取走当前最小（小顶堆）或最大（大顶堆）的元素，并保持堆性质时',
        steps: [
          '记下根（数组第 0 个）就是要返回的极值',
          '把数组**最后一个元素移到根的位置**，并删掉末尾（保持完全二叉树形状）',
          '把这个新根和它**较优的那个孩子**比较（小顶堆里找较小的孩子）',
          '若孩子比它更优，就交换、它下移一层；重复直到它比两个孩子都更优、或已无孩子——这叫「下沉」',
        ],
        example: {
          problem: '小顶堆数组 [1, 3, 2, 7, 4]，取出最小值后，写出下沉调整的过程。',
          solution: [
            '解：根是最小值 1，记下它作为返回值。把末尾的 4 移到根 → [4, 3, 2, 7]（删掉了原末尾）。',
            '新根 4（下标 0）的孩子：左 3（下标 1）、右 2（下标 2）。较小的孩子是 2。比较 4 > 2，交换 → [2, 3, 4, 7]，4 下移到下标 2。',
            '4（下标 2）的孩子下标是 5、6，已超出数组范围（没有孩子），停止下沉。',
            '答：取出最小值 1，调整后堆为 [2, 3, 4, 7]，新的根 2 是剩余元素的最小值。下沉走了 1 步，$O(\\log n)$。',
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 用 heapq 求「数据流中第 K 大」并追踪执行',
        problem: '请用 Python 的 heapq 写一个函数 kth_largest(nums, k)，返回列表 nums 中第 k 大的元素。要求用「大小为 k 的小顶堆」来做（这是经典 Top-K 解法）。然后追踪 nums=[3, 1, 5, 2, 4]、k=2 的过程。',
        steps: [
          `第一步：写出代码。维护一个只装 k 个元素的小顶堆，堆顶就是「第 k 大」。
import heapq

def kth_largest(nums, k):
    heap = []                       # 小顶堆，最多装 k 个元素
    for x in nums:
        heapq.heappush(heap, x)     # 先把当前元素入堆
        if len(heap) > k:           # 堆里超过 k 个
            heapq.heappop(heap)     # 弹出最小的，只保留最大的 k 个
    # 堆里留下的是最大的 k 个，堆顶（最小的那个）正是第 k 大
    return heap[0]`,
          '第二步：理解思路。我们始终只保留「目前见过的最大的 k 个数」在堆里。因为是小顶堆，堆顶是这 k 个里最小的——也就是「第 k 大」。每来一个数就入堆，超过 k 个就弹掉最小的，保证堆里永远是「最大的 k 个」。',
          '第三步：追踪 nums=[3,1,5,2,4]、k=2。heap 初始为空。',
          '第四步：x=3 入堆 → heap=[3]，长度 1≤2 不弹。x=1 入堆 → heap=[1,3]（小顶堆，顶是 1），长度 2≤2 不弹。',
          '第五步：x=5 入堆 → heap=[1,3,5]，长度 3>2，弹出最小的 1 → heap=[3,5]。x=2 入堆 → heap=[2,5,3]（顶是 2），长度 3>2，弹出 2 → heap=[3,5]。',
          '第六步：x=4 入堆 → heap=[3,5,4]（顶 3），长度 3>2，弹出 3 → heap=[4,5]。遍历结束，堆里是最大的两个数 {4,5}，堆顶 heap[0]=**4**，即第 2 大。验证：nums 从大到小是 5,4,3,2,1，第 2 大确实是 4。每次堆操作 $O(\\log k)$，总复杂度 $O(n\\log k)$，比「整体排序」的 $O(n\\log n)$ 在 k 很小时更省。',
        ],
      },
      {
        title: '例题 · 堆排序：用堆把列表排好序并追踪',
        problem: '请用 heapq 写一个堆排序函数 heap_sort(nums)，返回升序排列的新列表。然后追踪对 [4, 1, 3, 2] 的排序过程（说明「全部入堆」后「逐个弹出」的顺序）。',
        steps: [
          `第一步：写出代码。先把所有元素入小顶堆，再不断弹出最小的。
import heapq

def heap_sort(nums):
    heap = []
    # 第一步：把所有元素逐个压入小顶堆
    for x in nums:
        heapq.heappush(heap, x)
    # 第二步：不断弹出堆顶（当前最小），依次放进结果
    result = []
    while heap:
        result.append(heapq.heappop(heap))
    return result`,
          '第二步：理解思路。小顶堆每次 heappop 弹出的都是当前最小值；把元素全压进堆后，反复弹出，弹出的顺序自然就是「从小到大」。这就是堆排序的本质——「全入堆，再逐个取极值」。',
          '第三步：追踪入堆阶段，nums=[4,1,3,2]。压 4 → [4]；压 1 → [1,4]（1 上浮到顶）；压 3 → [1,4,3]；压 2 → [1,2,3,4]（2 上浮）。入堆完成，堆顶是最小值 1。',
          '第四步：弹出阶段第 1 次。heappop 取走堆顶 1 → result=[1]，堆下沉调整后堆顶变成 2。',
          '第五步：继续弹出。取 2 → result=[1,2]；取 3 → result=[1,2,3]；取 4 → result=[1,2,3,4]，堆空，循环结束。',
          '第六步：返回 **[1, 2, 3, 4]**。整个过程：n 次入堆（每次 $O(\\log n)$）+ n 次弹出（每次 $O(\\log n)$），总复杂度 $O(n\\log n)$。注意：入堆完成时，堆数组本身并不是有序的（它只保证根最小），必须**靠「逐个弹出」才得到有序序列**——这印证了「堆不是有序数组」。',
        ],
      },
    ],
    pitfalls: [
      '**以为堆数组本身就是有序的**。堆只保证「根是极值、父优于子」，兄弟节点之间、不同分支之间没有顺序。直接遍历堆数组得到的不是排序结果，必须靠「逐个取极值」才能得到有序序列。',
      '混淆大顶堆和小顶堆。Python 的 heapq 实现的是**小顶堆**（heappop 弹最小）。要当大顶堆用，常用技巧是「存入相反数」（push -x，pop 后再取负），别直接以为 heappop 给的是最大值。',
      '把堆和「完全有序的树（如 BST）」搞混。堆是「父子之间有序、左右无序」，只为快速取极值；BST 是「左小右大、可有序遍历」，为快速查找。两者结构和用途都不同。',
      '取极值后忘了调整。heappop 不只是「删根」，还要把末尾元素移到根并**下沉**重建堆性质；自己手写堆若漏了下沉，堆就坏了，后续取到的不再是极值。',
      '插入后忘了上浮。新元素直接 append 到末尾还不够，必须**上浮**到合适位置才能维持堆性质；漏了上浮，根可能不再是极值。',
    ],
    exercises: [
      {
        problem: '把元素 7、3、9、1、5 依次插入一个**小顶堆**（初始为空），用数组表示。请逐个写出每次插入后的堆数组，并说明发生的上浮交换。',
        hint: '每次把新元素放到数组末尾，再和父节点（下标 (i-1)//2）比较，比父节点小就交换上浮，直到不小于父节点或到根。',
        answer: `**详细讲解**：

**规则**：小顶堆里父节点必须≤孩子。每次新元素放末尾，再不断和父节点比，比父节点小就交换上浮。

**逐个插入**：

**插入 7**：堆空，直接放 → **[7]**

**插入 3**：放末尾 → [7, 3]，3 在下标 1，父节点下标 (1-1)//2=0 是 7。3 < 7，交换上浮 → **[3, 7]**

**插入 9**：放末尾 → [3, 7, 9]，9 在下标 2，父节点下标 (2-1)//2=0 是 3。9 < 3 不成立，不动 → **[3, 7, 9]**

**插入 1**：放末尾 → [3, 7, 9, 1]，1 在下标 3，父节点下标 (3-1)//2=1 是 7。1 < 7，交换 → [3, 1, 9, 7]，1 移到下标 1；新父节点下标 (1-1)//2=0 是 3，1 < 3，再交换 → **[1, 3, 9, 7]**

**插入 5**：放末尾 → [1, 3, 9, 7, 5]，5 在下标 4，父节点下标 (4-1)//2=1 是 3。5 < 3 不成立，不动 → **[1, 3, 9, 7, 5]**

**最终堆数组**：[1, 3, 9, 7, 5]，根是最小值 1。

**复杂度分析**：每次插入的上浮最多走「树高」约 $\\log n$ 步，所以单次插入是 $O(\\log n)$；插入 n 个元素建堆是 $O(n\\log n)$。

**小提醒**：注意最终数组 [1, 3, 9, 7, 5] **并不是有序的**（9 在 7 前面）——它只满足「父≤子」，这再次说明堆不等于有序数组。`
      },
      {
        problem: 'Python 的 heapq 只提供**小顶堆**（heappop 弹出最小值）。如果想要一个「每次弹出最大值」的大顶堆，该怎么用 heapq 实现？请写出入堆和取最大值的代码，并解释原理。',
        hint: '小顶堆弹最小，要它弹最大，可以把元素「取相反数」再入堆——最小的相反数对应原来最大的数。',
        answer: `**详细讲解**：

**问题**：heapq 是小顶堆，heappop 永远弹最小值。但有时我们想要「每次取最大值」（大顶堆）。

**核心技巧——存相反数**：把每个数取负号再入堆。因为「原来最大的数」取负后变成「最小的负数」，会被小顶堆排到堆顶；弹出时再取一次负号，就还原成原来的最大值。

**参考代码**：
\`\`\`python
import heapq

class MaxHeap:
    def __init__(self):
        self.heap = []

    def push(self, x):
        # 存相反数：原来最大的 x，-x 最小，会浮到小顶堆顶
        heapq.heappush(self.heap, -x)

    def pop_max(self):
        # 弹出的是最小的负数，取负还原成原来的最大值
        return -heapq.heappop(self.heap)

    def peek_max(self):
        # 堆顶是最小负数，取负就是当前最大值
        return -self.heap[0]
\`\`\`

**执行示例**：依次 push(3)、push(8)、push(5)
- 堆里实际存的是 [-8, -3, -5]（小顶堆，顶是 -8）
- pop_max()：heappop 弹出 -8，取负返回 **8**（原来的最大值），正确！
- 再 pop_max()：弹出 -5，返回 **5**

**原理小结**：小顶堆按「最小」排序，对「负数」而言「最小的负数」正对应「最大的正数」。所以「全部取负 → 用小顶堆 → 弹出后再取负」就等价于一个大顶堆。

**复杂度分析**：和普通堆完全一样——push 和 pop_max 都是 $O(\\log n)$，peek_max 是 $O(1)$。取负只是 $O(1)$ 的额外操作，不影响复杂度。这是用 heapq 实现大顶堆最常用的写法。`
      },
      {
        problem: '要在 100 万个数据里找出「最大的 10 个」。方法一：把全部数据排序（$O(n\\log n)$）后取前 10 个。方法二：用一个大小为 10 的小顶堆扫一遍（$O(n\\log k)$）。请比较两种方法，说明在「只要前 10 个」时为什么堆的方法更好。',
        hint: '把 n=100 万、k=10 代入两个复杂度。$\\log n$ 约 20，$\\log k$ 约 3.3。另外想想：排序做了多少「用不到」的工作？',
        answer: `**详细讲解**：

**两种方法对比**（n = 100 万，k = 10）：

**方法一：整体排序后取前 k 个**
- 复杂度 $O(n\\log n)$，代入约 $10^6 \\times \\log_2(10^6) \\approx 10^6 \\times 20 = 2\\times10^7$ 次操作。
- 问题：为了拿前 10 个，它把**全部 100 万个数都完全排好序**，而后面 999990 个的顺序我们根本不需要——做了大量无用功。

**方法二：大小为 k 的小顶堆扫一遍**
- 维护一个只装 k=10 个元素的小顶堆：遍历每个数，入堆；堆超过 k 个就弹掉最小的，始终只保留「最大的 10 个」。
- 复杂度 $O(n\\log k)$，代入约 $10^6 \\times \\log_2(10) \\approx 10^6 \\times 3.3 = 3.3\\times10^6$ 次操作。

**为什么堆更好**：
1. **复杂度更低**：$\\log k$（约 3.3）远小于 $\\log n$（约 20），所以方法二的运算次数大约只有方法一的 $\\frac{3.3}{20} \\approx \\frac{1}{6}$。
2. **不做无用功**：堆只关心「最大的 k 个」，从不去排序其余的数据；而整体排序把所有元素都排好，浪费在我们不需要的部分。
3. **省内存**：堆只占 k=10 个元素的空间（若数据是流式来的、无法一次全装进内存，这点尤其关键）；整体排序通常要把全部数据放进内存。

**参考代码**：
\`\`\`python
import heapq

def top_k(nums, k):
    heap = []
    for x in nums:
        heapq.heappush(heap, x)     # 入堆
        if len(heap) > k:
            heapq.heappop(heap)     # 弹掉最小，只留最大的 k 个
    return heap                     # 堆里就是最大的 k 个
\`\`\`

**结论**：当「只要前 k 个、k 远小于 n」时，用大小为 k 的堆（$O(n\\log k)$）比整体排序（$O(n\\log n)$）更快、更省内存。这正是堆在 **Top-K 问题**上的经典优势。如果 k 接近 n（要几乎全部），那整体排序反而更简单划算。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 4. 二叉搜索树
  // ───────────────────────────────────────────────────────────────
  'bst': {
    warmup: {
      q: '二分查找很快（$O(\\log n)$），但它要求数据放在**有序数组**里——这样「往中间砍」才有意义。可数组有个麻烦：往中间插一个新数，后面所有元素都要往后挪一格（$O(n)$）。有没有一种结构，既能像二分一样快速查找，又能快速插入新元素？',
      hint: '二分靠「比中间大就往右、比中间小就往左」。能不能把这个规则做成一棵树——每个节点都「左边都更小、右边都更大」？',
      answer: '这正是**二叉搜索树（BST）**的思路：把二分查找的规则「搬到树上」。BST 是一棵二叉树，规定**每个节点的左子树都比它小、右子树都比它大**。于是查找时从根开始「比当前节点小就往左走、大就往右走」，每一步都砍掉一半——和二分一样平均 $O(\\log n)$。而且它插入新元素时只要顺着规则找到空位挂上去，**不用像数组那样挪动一大片元素**。额外福利：对 BST 做「中序遍历」（左→根→右）会**自动按从小到大输出**。'
    },
    intro: [
      '二叉搜索树（Binary Search Tree，简称 BST），是一种把「二分查找」思想搬到树结构上的数据结构。它是一棵二叉树（每个节点最多两个孩子），并满足一条铁律：**对任意节点，它左子树里所有值都比它小，右子树里所有值都比它大**。正是这条「左小右大」的规则，让查找像二分一样快。',
      'BST 怎么查找？从**根节点**开始，把目标和当前节点比较：相等就找到了；目标更小就往**左子树**走，目标更大就往**右子树**走；一路下行，直到找到或走到空（没找到）。因为每比较一次就排除了另一边的整棵子树（相当于砍掉一半），所以平均只需 $O(\\log n)$ 次比较——和二分查找如出一辙。',
      'BST 怎么插入？和查找走一样的路：从根开始按「小往左、大往右」往下找，直到遇到一个**空位**（某个孩子为空），就把新节点挂在那里。关键好处是：**插入只是在末端挂一个新节点，不需要像有序数组那样把后面一大片元素整体后移**。删除稍复杂（要处理「被删节点有两个孩子」的情况），但思路同样基于这棵树的有序性。',
      'BST 还有一个漂亮性质：对它做**中序遍历**（先访问左子树、再访问根、最后访问右子树，记作「左→根→右」）会**恰好按从小到大的顺序输出所有值**。这是「左小右大」规则的直接结果，让 BST 天然支持「有序输出」「范围查询」「找前驱后继」等哈希表做不到的操作。',
      '一句话记住：**BST 是「左小右大」的二叉树，查找 / 插入 / 删除平均都是 $O(\\log n)$，中序遍历得到升序。** 但它有个致命陷阱：**如果按已经排好序的数据依次插入（如 1,2,3,4,5），每个新节点都挂在右边，树会退化成一条「斜链」，高度变成 n，所有操作退化到 $O(n)$**——这正是 AVL 树、红黑树等「平衡二叉搜索树」要解决的问题：它们在插入时自动调整，保证树始终「矮胖均衡」，把最坏也压在 $O(\\log n)$。'
    ],
    formulas: [
      { name: '平均时间复杂度', latex: 'O(\\log n)',
        note: '树平衡时高度约 $\\log n$，查找 / 插入 / 删除都是「从根走到叶」，最多走树高那么多步，故平均 $O(\\log n)$。' },
      { name: '最坏时间复杂度', latex: 'O(n)',
        note: '按有序数据依次插入会让树退化成一条链（高度 $n$），此时操作退化到 $O(n)$。平衡树（AVL / 红黑树）可避免。' },
      { name: 'BST 性质', latex: '\\text{左子树} < \\text{节点} < \\text{右子树}',
        note: '对每个节点都成立的递归规则。它是 BST 一切高效操作的根基，也是中序遍历能升序输出的原因。' },
      { name: '中序遍历结果', latex: '\\text{左} \\to \\text{根} \\to \\text{右} = \\text{升序}',
        note: '按「左子树→根→右子树」访问 BST，得到的序列恰好从小到大排列。这是 BST 独有、哈希表没有的能力。' },
    ],
    methods: [
      {
        name: 'BST 查找',
        when: '在一棵 BST 里判断某个值是否存在、或定位它的节点时',
        steps: [
          '从根节点开始，作为「当前节点」',
          '把目标值和当前节点的值比较：相等 → 找到，返回',
          '目标值 < 当前节点 → 往**左孩子**走（去左子树继续找）',
          '目标值 > 当前节点 → 往**右孩子**走（去右子树继续找）',
          '若走到空节点（孩子为 None）仍没找到，说明该值不在树中，返回「未找到」',
        ],
        example: {
          problem: '一棵 BST 的结构是：根 8，左孩子 3（其左 1、右 6），右孩子 10（其右 14）。在它里面查找 6，写出查找路径。',
          solution: [
            '解：从根 8 开始。目标 6 < 8 → 往左走，到节点 3。',
            '目标 6 > 3 → 往右走，到节点 6。目标 6 == 6 → 找到！',
            '查找路径：8 → 3 → 6，只比较了 3 次。',
            '答：6 在树中，路径为 8→3→6。若查 7：8→3（7>3 往右）→6（7>6 往右）→ 6 的右孩子为空 → 未找到，路径 8→3→6→空。',
          ],
        },
      },
      {
        name: 'BST 插入',
        when: '向 BST 中加入一个新值，同时保持「左小右大」性质时',
        steps: [
          '从根开始，按「小往左、大往右」的规则往下走，找新值该去的方向',
          '一路下行，直到走到某个**空位**（要走的那个孩子是 None）',
          '把新值做成一个新节点，挂在这个空位上',
          '（不需要移动任何已有节点——这是 BST 插入比有序数组快的关键）',
        ],
        example: {
          problem: '空树中依次插入 5、3、8、1、4，画出最终 BST 的结构。',
          solution: [
            '解：插 5 → 5 成为根。插 3 → 3<5 往左，左空，挂在 5 的左 → 5 的左是 3。',
            '插 8 → 8>5 往右，右空，挂在 5 的右 → 5 的右是 8。插 1 → 1<5 往左到 3，1<3 往左，空，挂在 3 的左。',
            '插 4 → 4<5 往左到 3，4>3 往右，空，挂在 3 的右。',
            '答：根 5；5 的左是 3（3 的左 1、右 4）、右是 8。这棵树平衡，查找任意元素最多 3 步。',
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 实现 BST 的插入与查找并追踪执行',
        problem: '请定义一个 BST 节点类，并写出插入函数 insert(root, val) 和查找函数 search(root, val)。然后追踪：在空树中依次插入 5、3、8、1，再查找 1 的过程。',
        steps: [
          `第一步：写出代码。节点存「值 + 左孩子 + 右孩子」，插入和查找都用「小往左、大往右」。
class TreeNode:
    def __init__(self, val):
        self.val = val
        self.left = None       # 左孩子
        self.right = None      # 右孩子

def insert(root, val):
    # 空位：新建节点挂上（递归的终止与挂载点）
    if root is None:
        return TreeNode(val)
    if val < root.val:
        root.left = insert(root.left, val)    # 小，往左子树插
    elif val > root.val:
        root.right = insert(root.right, val)  # 大，往右子树插
    # 等于则不重复插入
    return root

def search(root, val):
    if root is None:           # 走到空，没找到
        return False
    if val == root.val:        # 找到
        return True
    if val < root.val:
        return search(root.left, val)    # 小，去左子树找
    else:
        return search(root.right, val)   # 大，去右子树找`,
          '第二步：理解逻辑。insert 递归往下找空位：比当前小就进左子树、大就进右子树，遇到 None 就新建节点挂上。search 走同样的路：相等返回 True、到空返回 False。两者都靠「左小右大」每步排除半边。',
          '第三步：追踪插入。空树插 5 → 根 = 5。插 3：3<5 进左子树（空），新建挂在 5.left → 5 的左是 3。',
          '第四步：插 8：8>5 进右子树（空），新建挂在 5.right → 5 的右是 8。插 1：1<5 进左到 3，1<3 进左（空），新建挂在 3.left → 3 的左是 1。',
          '第五步：此时树的结构：根 5，左孩子 3（3 的左孩子 1），右孩子 8。',
          '第六步：追踪 search(root, 1)。从根 5：1<5 → 去左子树到 3；1<3 → 去左子树到 1；1==1 → 返回 **True**。查找路径 5→3→1，共比较 3 次。若查找 7：5→8（7<8 往左）→ 8 的左为空 → 返回 False。',
        ],
      },
      {
        title: '例题 · 中序遍历让 BST 自动升序输出',
        problem: '请写一个中序遍历函数 inorder(root)，返回 BST 中所有值的**升序列表**。然后对「根 5，左 3（左 1、右 4），右 8」这棵树追踪遍历顺序，验证结果有序。',
        steps: [
          `第一步：写出代码。中序 = 「先左子树、再根、最后右子树」。
def inorder(root):
    result = []
    if root is None:
        return result
    # 1) 先递归遍历左子树（更小的部分）
    result.extend(inorder(root.left))
    # 2) 再访问根自己
    result.append(root.val)
    # 3) 最后递归遍历右子树（更大的部分）
    result.extend(inorder(root.right))
    return result`,
          '第二步：理解为什么有序。BST 里「左子树都比根小、右子树都比根大」。中序按「左→根→右」访问，就是「先输出所有更小的、再输出自己、最后输出所有更大的」，对每个节点都成立，所以整体必然从小到大。',
          '第三步：追踪这棵树（根 5；3 是左孩子，3 的左 1、右 4；8 是右孩子）。inorder(5) 先要 inorder(3)。',
          '第四步：inorder(3) 先要 inorder(1)。inorder(1)：1 没有孩子 → 左空返回 []，访问 1 → [1]，右空 → 结果 [1]。回到 inorder(3)：左子树给了 [1]，访问根 3 → [1,3]，再 inorder(4)=[4] → [1,3,4]。',
          '第五步：回到 inorder(5)：左子树（以 3 为根）返回 [1,3,4]，访问根 5 → [1,3,4,5]，再 inorder(8)。',
          '第六步：inorder(8)：8 没有孩子 → [8]。拼到结果 → **[1, 3, 4, 5, 8]**。访问顺序正是 1→3→4→5→8，完美升序！这验证了「BST 中序遍历 = 升序」。遍历每个节点恰好一次，复杂度 $O(n)$。',
        ],
      },
    ],
    pitfalls: [
      '**按有序数据依次插入导致退化成链**。若插入顺序是 1,2,3,4,5（已排好），每个新节点都挂到右边，树变成一条「斜线」，高度 = n，查找 / 插入退化到 $O(n)$，和链表一样慢。这是 BST 最大的坑，平衡树（AVL / 红黑树）就是为解决它而生。',
      '把 BST 和「堆」搞混。BST 是「左小右大、中序有序」、为快速查找而生；堆是「父优于子、左右无序」、为快速取极值而生。两者结构和用途完全不同。',
      '插入 / 查找时方向走反。规则是「目标比当前小往左、比当前大往右」。把大小判断写反，会一路走错方向，查不到本来存在的值。',
      '忽略「相等值」的处理。标准 BST 通常不存重复值（插入时遇到相等就忽略，或约定放固定一侧）。不约定清楚，可能重复插入或破坏「严格左小右大」。',
      '删除节点处理不当。删一个有两个孩子的节点，不能简单摘掉——要用「右子树的最小值（或左子树的最大值）」来顶替它，否则会破坏 BST 性质或丢失子树。',
    ],
    exercises: [
      {
        problem: '在空树中依次插入 6、2、8、1、4、7、9，请画出（用文字描述）最终的 BST 结构（指出每个节点的左右孩子），并写出对它做中序遍历得到的序列。',
        hint: '每个新值从根开始「小往左、大往右」找空位挂上。中序遍历（左→根→右）的结果应当是有序的。',
        answer: `**详细讲解**：

**逐个插入**（规则：小往左、大往右，到空位挂上）：
- 插 **6**：成为根。
- 插 **2**：2<6 往左，空 → 挂在 6 的左。
- 插 **8**：8>6 往右，空 → 挂在 6 的右。
- 插 **1**：1<6 往左到 2，1<2 往左，空 → 挂在 2 的左。
- 插 **4**：4<6 往左到 2，4>2 往右，空 → 挂在 2 的右。
- 插 **7**：7>6 往右到 8，7<8 往左，空 → 挂在 8 的左。
- 插 **9**：9>6 往右到 8，9>8 往右，空 → 挂在 8 的右。

**最终 BST 结构**：
\`\`\`
            6
          /   \\
         2     8
        / \\   / \\
       1   4 7   9
\`\`\`
- 根：6（左孩子 2，右孩子 8）
- 2：左孩子 1，右孩子 4
- 8：左孩子 7，右孩子 9
- 1、4、7、9 都是叶子（没有孩子）

**中序遍历（左→根→右）**：
- 遍历 6 的左子树（以 2 为根）：左 1 → 根 2 → 右 4，得 [1, 2, 4]
- 访问根 6，得 [1, 2, 4, 6]
- 遍历 6 的右子树（以 8 为根）：左 7 → 根 8 → 右 9，得 [7, 8, 9]
- 拼接 → **[1, 2, 4, 6, 7, 8, 9]**

**复杂度分析**：这棵树很「均衡」（高度只有 3），所以查找任意元素最多 3 步，是 $O(\\log n)$ 的理想状态。中序遍历访问每个节点一次，是 $O(n)$。结果 [1,2,4,6,7,8,9] 完美升序，验证了「BST 中序遍历 = 排序」。`
      },
      {
        problem: '小明说"BST 查找、插入都是 $O(\\log n)$，所以一定比线性查找快"。请举一个具体的插入顺序，说明这种情况下 BST 会退化、查找变成 $O(n)$，并说明该如何避免。',
        hint: '如果插入的数据本来就是从小到大排好的，每个新节点会挂在哪边？画一画 1,2,3,4,5 依次插入后的树形状。',
        answer: `**详细讲解**：

**小明的说法不全对**。$O(\\log n)$ 是 BST 在「树平衡」时的复杂度；如果树退化，会变成 $O(n)$。

**退化反例——按有序数据插入**：依次插入 **1、2、3、4、5**。
- 插 1：根 1。
- 插 2：2>1 往右，空 → 挂在 1 的右。
- 插 3：3>1 往右到 2，3>2 往右，空 → 挂在 2 的右。
- 插 4：4>1→4>2→4>3 一路往右 → 挂在 3 的右。
- 插 5：同理一路往右 → 挂在 4 的右。

**退化后的「树」**：
\`\`\`
1
 \\
  2
   \\
    3
     \\
      4
       \\
        5
\`\`\`
每个节点都只有右孩子——整棵树退化成了一条**斜链**，和链表一模一样！

**为什么变成 $O(n)$**：这棵树的高度是 n（5 个节点高度就是 5），不再是 $\\log n$。查找最大值 5 要从根 1 一路走到底，比较 5 次；查找一个不存在的更大值也要走完整条链。所以查找 / 插入都退化成 **$O(n)$**，和线性查找一样慢，完全失去了 BST 的优势。

**根本原因**：BST 的高效依赖「树是矮胖均衡的」（高度约 $\\log n$）。但插入顺序若是有序的，新节点总往同一边挂，树就「长高变瘦」退化成链。

**如何避免**：
1. **用平衡二叉搜索树**：如 **AVL 树**、**红黑树**。它们在每次插入 / 删除后自动做「旋转」调整，**始终保证树的高度维持在 $O(\\log n)$**，从而把最坏情况也压在 $O(\\log n)$。
2. **插入前打乱顺序**：如果数据可以一次性拿到，先随机打乱再插入，能大概率避免退化（但不如平衡树可靠）。

**结论**：普通 BST **平均** $O(\\log n)$、**最坏** $O(n)$（有序插入时退化成链）。要保证任何情况都 $O(\\log n)$，必须用自平衡的 AVL / 红黑树——这正是它们存在的意义。`
      },
      {
        problem: 'BST 和哈希表都能做「查找」。BST 平均 $O(\\log n)$、哈希表平均 $O(1)$，看起来哈希表更快。那么什么场景下反而应该选 BST？请至少说出两个 BST 能做、哈希表做不到的操作。',
        hint: 'BST 有「有序」性质（中序遍历升序），哈希表是「无序」的。想想「找 80~90 分之间的所有成绩」「按顺序输出」这类需求哈希表能不能做。',
        answer: `**详细讲解**：

哈希表平均 $O(1)$ 确实比 BST 的 $O(\\log n)$ 在「单纯查一个键」时更快。但 BST 有一个哈希表完全不具备的能力——**有序性**。当需求涉及「顺序」时，就该选 BST。

**BST 能做、哈希表做不到的操作（有序相关）**：

1. **有序输出 / 排序**
   - BST 做一次中序遍历（左→根→右）就能得到**从小到大排好序**的全部元素，$O(n)$。
   - 哈希表元素无序存放，想有序输出必须额外排序 $O(n\\log n)$。

2. **范围查询**（找某区间内的所有元素）
   - 例如「找出所有 80~90 分之间的成绩」。BST 可以利用「左小右大」高效地只遍历落在区间里的部分。
   - 哈希表只能精确匹配「某个键在不在」，无法回答「80 到 90 之间有哪些」，只能把所有元素扫一遍逐个判断（$O(n)$ 且毫无结构优势）。

3. **找前驱 / 后继、找最接近的值**
   - 例如「比 50 大的最小的数是多少」「树里的最小值 / 最大值」。BST 顺着结构就能 $O(\\log n)$ 找到。
   - 哈希表做不到——它不知道谁「比谁大一点」，因为根本没有顺序信息。

4. **找最大 / 最小值**
   - BST 一路往左到底是最小值、一路往右到底是最大值，$O(\\log n)$。
   - 哈希表要找最值得扫全部元素 $O(n)$。

**怎么选——一句话总结**：
- **只做「精确查找 / 插入 / 删除」、不关心顺序** → 选**哈希表**（平均 $O(1)$，最快）。
- **需要有序输出、范围查询、找前驱后继 / 最值、按序遍历** → 选 **BST**（或平衡 BST），$O(\\log n)$ 但支持这些有序操作。

**补充**：实际工程里，C++ 的 std::map、Java 的 TreeMap 底层就是平衡 BST（红黑树），专门用于「需要有序的键值存储」；而 unordered_map / HashMap 用哈希表，用于「只要快速精确查找」。两者各有舞台，不是谁绝对更好。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 5. 并查集
  // ───────────────────────────────────────────────────────────────
  'union-find': {
    warmup: {
      q: '班里同学不断「交朋友」：A 和 B 成了朋友、B 和 C 成了朋友……朋友的朋友也算一个圈子。现在不断有人问「X 和 Y 是不是在同一个朋友圈？」如果每次都从 X 出发把整张关系网搜一遍来回答，会很慢。有没有一种结构，能飞快地回答「这两人是否同圈」并「把两个圈合并」？',
      hint: '给每个圈选一个「圈主（代表）」。判断两人是否同圈，只要看他们的「圈主」是不是同一个人。交朋友合并两圈时，让一个圈主认另一个当老大就行。',
      answer: '这正是**并查集（Union-Find）**要解决的问题。它给每个集合（朋友圈）选一个**代表（根）**，用一个数组记录「每个人的上级是谁」，顺着上级往上找就能找到代表。判断「X、Y 是否同圈」只需比较他们的代表是否相同（find 操作）；「交朋友」就把一个圈的代表挂到另一个圈的代表下（union 操作）。配合「路径压缩」「按秩合并」两个优化，每次操作几乎是 $O(1)$。它是求连通分量、判断有没有环、最小生成树（Kruskal）的利器。'
    },
    intro: [
      '并查集（Union-Find，又叫不相交集合），是一种专门维护「**谁和谁在同一组**」的数据结构。它高效支持两个操作：**find（查）**——查出某个元素属于哪个集合（返回该集合的「代表」）；**union（并）**——把两个元素所在的集合合并成一个。名字里的「并」「查」正来自这两个操作。',
      '它的核心实现非常巧妙：用一个数组 parent，**记录每个元素的「父亲」（上级）**。同一个集合里的元素，顺着「父亲」一路往上找，最终会汇聚到同一个**根（代表）**——根的父亲就是它自己。于是：判断两个元素是否同组，就看它们「往上找到的根」是否相同；合并两个集合，就让一个集合的根「认另一个集合的根当父亲」。',
      'find 操作：从某元素出发，沿 parent 不断往上走，直到找到「父亲是自己」的那个根，返回它。union 操作：先分别 find 出两个元素的根，若根不同，就把其中一个根的父亲指向另一个根（两棵树合成一棵），它们就属于同一集合了；若根本来就相同，说明已在同组，无需合并。',
      '不优化的话并查集也会退化：如果合并时总是「随意挂」，可能形成一条很长的链，find 要沿链一路往上爬，单次退化到 $O(n)$。两个经典优化解决它：① **路径压缩**——find 的过程中，把沿途经过的节点「直接挂到根上」，让下次查找一步到位；② **按秩合并（或按大小合并）**——合并时总是把「矮的树」挂到「高的树」下，避免树越长越高。两者结合后，单次操作的均摊复杂度几乎是常数（记作近似 $O(1)$）。',
      '一句话记住：**并查集用「父亲数组」维护分组，find 找代表、union 合并集合，加上「路径压缩 + 按秩合并」后单次几乎 $O(1)$。** 它是判断「连通性」「有没有环」的神器：求图里有几个连通块、判断加一条边会不会成环（最小生成树 Kruskal 算法的关键步骤）、判断朋友圈，都靠它。'
    ],
    formulas: [
      { name: '单次操作均摊复杂度', latex: 'O(\\alpha(n)) \\approx O(1)',
        note: '路径压缩 + 按秩合并后，单次 find / union 的均摊复杂度是反阿克曼函数 $\\alpha(n)$，在一切现实规模下都小于 5，可当作常数 $O(1)$。' },
      { name: '未优化时最坏复杂度', latex: 'O(n)',
        note: '若合并时随意挂、又不做路径压缩，可能形成一条长链，find 要沿链爬到顶，单次退化到 $O(n)$。' },
      { name: 'find（查代表）', latex: '\\text{沿 parent 上溯到「父亲=自己」的根}',
        note: '根是集合的唯一代表。两元素的根相同 $\\iff$ 它们在同一集合。这是判断连通性的依据。' },
      { name: 'union（合并）', latex: '\\text{parent}[\\,\\text{find}(a)\\,] = \\text{find}(b)',
        note: '先各自找根，再把 a 的根挂到 b 的根下，两集合合一。若两根相同则已同组、无需合并。' },
    ],
    methods: [
      {
        name: 'find：查找元素的代表（根）',
        when: '要知道某元素属于哪个集合、或判断两元素是否同组时',
        steps: [
          '从该元素 x 出发，看 parent[x]（它的父亲）',
          '若 parent[x] 就是 x 自己，说明 x 是根（代表），返回 x',
          '否则把当前位置移到 parent[x]，继续往上找',
          '（路径压缩优化）找到根后，把沿途经过的节点的父亲都**直接改成根**，下次查找一步到位',
        ],
        example: {
          problem: '设 parent = [0, 0, 1, 2]（即 0 的父亲是 0/它是根，1 的父亲是 0，2 的父亲是 1，3 的父亲是 2）。求 find(3)，并说明路径压缩做了什么。',
          solution: [
            '解：从 3 出发。parent[3]=2 → 到 2；parent[2]=1 → 到 1；parent[1]=0 → 到 0；parent[0]=0（自己），0 是根，返回 0。',
            '不压缩的话，这条链 3→2→1→0 共爬了 3 步。',
            '路径压缩：找到根 0 后，把沿途的 3、2、1 的父亲都直接改成 0，parent 变为 [0,0,0,0]。',
            '答：find(3)=0。压缩后下次 find(3)、find(2)、find(1) 都只需 1 步直达根，大幅加速后续查询。',
          ],
        },
      },
      {
        name: 'union：合并两个元素所在的集合',
        when: '要把两个元素（及它们各自所在的整个集合）合并成同一组时',
        steps: [
          '先用 find 找出元素 a 的根 root_a、元素 b 的根 root_b',
          '若 root_a == root_b，说明两者本来就在同一集合，无需操作',
          '若不同，把其中一个根的父亲指向另一个根（如 parent[root_a] = root_b），两集合即合并',
          '（按秩 / 按大小合并优化）合并时让「较小 / 较矮的树」挂到「较大 / 较高的树」下，避免树长高',
        ],
        example: {
          problem: '初始每个元素自成一组：parent = [0, 1, 2, 3]（4 个元素 0~3）。依次执行 union(0,1)、union(2,3)、union(0,2)，写出每步后 parent 的变化和最终分组。',
          solution: [
            '解：union(0,1)：find(0)=0、find(1)=1，根不同，令 parent[0]=1 → parent=[1,1,2,3]，{0,1} 一组（根 1）。',
            'union(2,3)：find(2)=2、find(3)=3，令 parent[2]=3 → parent=[1,1,3,3]，{2,3} 一组（根 3）。',
            'union(0,2)：find(0)：0→1（根 1）；find(2)：2→3（根 3）；根 1≠3，令 parent[1]=3 → parent=[1,3,3,3]。',
            '答：最终 parent=[1,3,3,3]，所有元素往上找都到根 3，{0,1,2,3} 合并成了同一组。',
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 实现带路径压缩的并查集并追踪执行',
        problem: '请实现一个并查集类 UnionFind，包含 find（带路径压缩）和 union 两个方法。然后追踪：初始化 5 个元素（0~4），依次执行 union(0,1)、union(1,2)、find(2) 的过程。',
        steps: [
          `第一步：写出代码。parent[i] 记录 i 的父亲，初始每个元素的父亲是自己。
class UnionFind:
    def __init__(self, n):
        # 初始：每个元素自成一组，父亲是自己
        self.parent = list(range(n))   # parent = [0, 1, 2, ..., n-1]

    def find(self, x):
        # 沿父亲上溯找根，并做路径压缩
        if self.parent[x] != x:        # x 不是根
            # 递归找到根，并把 x 直接挂到根上（路径压缩）
            self.parent[x] = self.find(self.parent[x])
        return self.parent[x]          # 返回根（代表）

    def union(self, a, b):
        root_a = self.find(a)          # a 的根
        root_b = self.find(b)          # b 的根
        if root_a != root_b:           # 不在同一组才合并
            self.parent[root_a] = root_b   # 把 a 的根挂到 b 的根下`,
          '第二步：理解逻辑。find 顺着 parent 往上找根；路径压缩体现在「self.parent[x] = self.find(...)」——递归回来时把 x 的父亲直接改成根。union 先找两个根，不同就把一个挂到另一个下。',
          '第三步：追踪。UnionFind(5)：parent = [0, 1, 2, 3, 4]（每个元素自成一组）。',
          '第四步：union(0,1)：find(0)=0、find(1)=1，根不同，parent[0]=1 → parent=[1,1,2,3,4]。现在 {0,1} 同组（根 1）。',
          '第五步：union(1,2)：find(1)=1、find(2)=2，根不同，parent[1]=2 → parent=[1,2,2,3,4]。现在 0→1→2，{0,1,2} 同组（根 2）。',
          '第六步：追踪 find(2)。parent[2]=2，2 就是根，直接返回 **2**。若改为 find(0)：parent[0]=1≠0 → 递归 find(1)：parent[1]=2≠1 → 递归 find(2)=2；回溯时路径压缩，把 parent[1]、parent[0] 都改成 2，parent 变为 [2,2,2,3,4]。下次 find(0) 就一步到根。这就是路径压缩让操作趋近 $O(1)$ 的原理。',
        ],
      },
      {
        title: '例题 · 用并查集数「连通块个数」并追踪',
        problem: '有 n 个城市（编号 0~n-1），给一些「直接相连」的道路对，求最终有几个互相连通的「城市群」（连通块）。请用并查集实现 count_groups(n, roads)，并追踪 n=5、roads=[(0,1),(1,2),(3,4)] 的过程。',
        steps: [
          `第一步：写出代码。每条路 union 两个城市；最后数有几个不同的根。
def count_groups(n, roads):
    parent = list(range(n))        # 初始每城自成一群

    def find(x):
        while parent[x] != x:      # 沿父亲找根
            parent[x] = parent[parent[x]]   # 路径压缩（指向爷爷，压扁链）
            x = parent[x]
        return x

    def union(a, b):
        ra, rb = find(a), find(b)
        if ra != rb:
            parent[ra] = rb        # 合并两群

    # 每条道路把两个城市并到一组
    for a, b in roads:
        union(a, b)

    # 数有多少个「根」（父亲是自己的元素）= 连通块个数
    groups = 0
    for i in range(n):
        if find(i) == i:           # i 是某个群的代表
            groups = groups + 1
    return groups`,
          '第二步：理解思路。一开始 n 个城市是 n 个独立群；每条道路就 union 两个城市所在的群（群数可能减少）。最后统计「根的个数」——每个连通块恰有一个根，所以根的数量就是连通块数量。',
          '第三步：追踪 n=5、roads=[(0,1),(1,2),(3,4)]。初始 parent=[0,1,2,3,4]，相当于 5 个独立群。',
          '第四步：处理 (0,1)：find(0)=0、find(1)=1，parent[0]=1 → parent=[1,1,2,3,4]。{0,1} 合并。处理 (1,2)：find(1)=1、find(2)=2，parent[1]=2 → parent=[1,2,2,3,4]。现在 {0,1,2} 一群。',
          '第五步：处理 (3,4)：find(3)=3、find(4)=4，parent[3]=4 → parent=[1,2,2,4,4]。{3,4} 一群。所有道路处理完。',
          '第六步：数根。find(0)：0→1→2，根 2（≠0）；find(1)：→2，根 2（≠1）；find(2)=2（是根，+1）；find(3)：3→4，根 4（≠3）；find(4)=4（是根，+1）。共 **2** 个根 → 返回 2。即 {0,1,2} 和 {3,4} 两个城市群。整个过程每次操作近似 $O(1)$，总复杂度近似 $O(n + 道路数)$。',
        ],
      },
    ],
    pitfalls: [
      '**忘了路径压缩，查询退化变慢**。不做路径压缩时，合并可能形成一条长链，find 要沿链一路爬到顶，单次退化到 $O(n)$。路径压缩把沿途节点直接挂到根上，是并查集高效的关键，别省。',
      'union 时直接对元素操作、没先 find 到根。合并必须先 find 出两个元素的**根**，再把一个根挂到另一个根下；若直接 parent[a]=b（用元素而非根），会破坏集合结构、导致 find 错乱。',
      '把「父亲」当成「直接朋友关系」来读。parent 数组只是为了「往上找到代表」而设的内部指针，parent[x] 不代表「x 和它在现实中直接相连」；判断两人是否同组要看**根**是否相同，不是看 parent 是否相等。',
      '初始化漏了「每个元素父亲是自己」。并查集开始时每个元素自成一组，parent[i] 必须初始化为 i；漏了初始化，find 会找不到正确的根。',
      '用它做「拆分集合」。并查集擅长「合并」和「查询同组」，但**不支持高效地把一个集合再拆开**（撤销 union 很难）。需要频繁拆分的场景，并查集不适用。',
    ],
    exercises: [
      {
        problem: '初始有 6 个元素（0~5），每个自成一组。依次执行 union(0,1)、union(2,3)、union(1,3)、union(4,5)，请逐步写出每次合并后的分组情况，最后说明一共有几个集合。',
        hint: '每次 union 先想清楚两个元素当前各属于哪个组，再把两个组合并。union(1,3) 时 1 在 {0,1}、3 在 {2,3}，合并后变成一个组。',
        answer: `**详细讲解**：

**初始状态**：每个元素自成一组，共 6 组：{0} {1} {2} {3} {4} {5}。

**逐步合并**：

**union(0,1)**：0 在 {0}、1 在 {1}，合并 → **{0,1}**。
当前分组：{0,1} {2} {3} {4} {5}（5 组）

**union(2,3)**：2 在 {2}、3 在 {3}，合并 → **{2,3}**。
当前分组：{0,1} {2,3} {4} {5}（4 组）

**union(1,3)**：1 属于 {0,1}、3 属于 {2,3}，把这两个组合并 → **{0,1,2,3}**。
当前分组：{0,1,2,3} {4} {5}（3 组）

**union(4,5)**：4 在 {4}、5 在 {5}，合并 → **{4,5}**。
当前分组：**{0,1,2,3}** 和 **{4,5}**（2 组）

**最终结果**：一共 **2 个集合**：{0, 1, 2, 3} 和 {4, 5}。

**复杂度分析**：每次 union 包含两次 find 和一次挂接，配合路径压缩 + 按秩合并后均摊近似 $O(1)$。这里做了 4 次 union，总开销近似 $O(4)$。

**小结**：并查集的妙处在于——合并 {0,1} 和 {2,3} 时，不需要逐个去改 0、1、2、3 的归属，只要把一个组的**根**挂到另一个组的根下，整组瞬间合并。这就是它判断连通性又快又省的原因。`
      },
      {
        problem: 'find 操作的「路径压缩」优化是并查集高效的关键。请解释什么是路径压缩、它解决了什么问题，并用一个具体例子说明压缩前后查找步数的区别。',
        hint: '不压缩时，元素可能连成一条长链，find 要一路爬到顶。压缩就是「找到根后，把沿途节点都直接挂到根上」。',
        answer: `**详细讲解**：

**什么是路径压缩**：在 find 查找根的过程中，**把沿途经过的每个节点的父亲都直接改成根**。这样这些节点下次再查找时，一步就能到达根，不必重新爬整条链。

**它解决什么问题**：如果合并时不加控制，元素可能连成一条很长的「链」。比如 parent 形成 4→3→2→1→0（0 是根），那么 find(4) 要走 4→3→2→1→0 共 4 步；如果链有 n 个节点，单次 find 就退化到 $O(n)$，并查集就和链表一样慢了。路径压缩把这条链「压扁」，避免反复爬长链。

**具体例子**：设 parent = [0, 0, 1, 2, 3]，即形成链 4→3→2→1→0（0 是根）。

**压缩前**：
- find(4)：4→3→2→1→0，爬 **4 步**才到根。
- 如果接着 find(3)、find(2)，又要各自爬 3 步、2 步——每次都重走长链。

**执行带路径压缩的 find(4) 后**：
- 找到根 0，把沿途的 4、3、2、1 的父亲**全部直接改成 0**。
- parent 变成 [0, 0, 0, 0, 0]——这棵树被「压扁」成「所有节点直接挂在根 0 下」。

**压缩后**：
- 再 find(4)：parent[4]=0，**1 步**直达根！
- find(3)、find(2)、find(1) 也都只需 1 步。

**参考代码**（递归式路径压缩）：
\`\`\`python
def find(self, x):
    if self.parent[x] != x:
        # 递归找根，并把 x 直接挂到根上
        self.parent[x] = self.find(self.parent[x])
    return self.parent[x]
\`\`\`

**复杂度分析**：路径压缩（配合按秩合并）后，单次操作的均摊复杂度是反阿克曼函数 $O(\\alpha(n))$，在任何现实数据规模下都小于 5，可当作**常数 $O(1)$**。而不做压缩、最坏链化时单次是 $O(n)$。

**结论**：路径压缩用「查找时顺手把树压扁」的小代价，换来后续所有查找的飞快，是并查集从「可能 $O(n)$」变成「几乎 $O(1)$」的核心优化，绝不能省。`
      },
      {
        problem: '最小生成树的 Kruskal 算法要「把边从小到大尝试加入，但如果加某条边会形成环就跳过它」。请说明为什么并查集特别适合用来「判断加一条边会不会成环」，并简述判断方法。',
        hint: '一条边连接两个顶点。如果这两个顶点本来就已经连通（在同一集合），再连它们就会形成环。怎么判断两点是否已连通？',
        answer: `**详细讲解**：

**Kruskal 算法回顾**：要用最小总边权把所有顶点连成一棵树（最小生成树）。做法是把所有边**按权值从小到大排序**，然后逐条尝试加入；但**如果加入某条边会形成环，就跳过它**（因为树里不能有环）。难点就在于「如何快速判断加一条边会不会成环」——这正是并查集的拿手好戏。

**为什么并查集适合判断成环**：

关键观察：一条边连接顶点 a 和 b。**如果 a 和 b 在加这条边之前就已经连通**（即通过别的已选边能互相到达），那么再加 a—b 这条边，就会形成一个环（多了一条「冗余」的连接路径）。反之，如果 a、b 原本不连通，加上这条边就是把两个原本分开的部分连起来，不会成环。

而「判断 a、b 是否已连通」正是并查集的 find 操作能瞬间回答的：
- **a、b 的根相同** → 它们已在同一连通块 → 加这条边会**成环** → **跳过**。
- **a、b 的根不同** → 它们还不连通 → 加这条边**安全**（且把两个连通块合并）→ **选这条边**，并 union(a, b)。

**判断 + 处理方法（Kruskal 中的核心步骤）**：
\`\`\`python
def kruskal(n, edges):
    # edges: (权值, a, b) 列表
    edges.sort()                   # 按权值从小到大排序
    parent = list(range(n))

    def find(x):
        while parent[x] != x:
            parent[x] = parent[parent[x]]   # 路径压缩
            x = parent[x]
        return x

    total = 0
    for w, a, b in edges:          # 从小到大尝试每条边
        if find(a) != find(b):     # 两端不连通：加这条边不成环
            parent[find(a)] = find(b)   # union：合并两端
            total = total + w      # 累加边权
        # 否则（根相同）：会成环，跳过这条边
    return total                   # 最小生成树的总权值
\`\`\`

**为什么用并查集而不是别的方法**：
- 如果不用并查集，判断「a、b 是否已连通」就得每次在已选的边里做一遍图搜索（DFS/BFS），代价 $O(\\text{顶点数 + 边数})$，整体会很慢。
- 用并查集，每次「判断连通 + 合并」都近似 $O(1)$（均摊），让 Kruskal 的瓶颈只剩「边排序」的 $O(E\\log E)$，非常高效。

**复杂度分析**：Kruskal 总复杂度 $O(E\\log E)$（E 是边数），主要花在排序上；并查集的判环与合并部分近似 $O(E\\cdot\\alpha(n)) \\approx O(E)$，几乎不增加负担。

**结论**：并查集天生擅长「维护连通性 + 判断两点是否同组」，而 Kruskal 判环的本质就是「这条边的两端是否已连通」——所以并查集是 Kruskal 算法的黄金搭档。这也是并查集最经典的应用场景之一。`
      },
    ],
  },

});
