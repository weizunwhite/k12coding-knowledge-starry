// 初中编程 · 编程基础 —— 详细小课（习题讲解版）
// 覆盖 7 个节点：variable-assign / data-types / io-basic / operators / comments-style / string / error-types
// 数据结构对齐 node-detail.jsx 的渲染：
//   warmup{q,hint,answer} / intro[...] / formulas[{name,latex,note}] /
//   methods[{name,when,steps,example{problem,solution}}] / worked[{title,problem,steps}] /
//   pitfalls[...] / exercises[{problem,hint,answer}]
// 约定：
//   1) 语言以 Python 为主，全部中文讲解；
//   2) 含代码或引号的字段值一律用「反引号模板字符串」，避免单双引号转义冲突；
//      代码里的换行用真实换行（在反引号里直接换行即可）；
//   3) formulas[].latex 会过 KaTeX(MathBlock) 渲染，所以「语法/要点卡」里的代码统一用
//      \\texttt{...} 包裹，特殊字符按 KaTeX 规则转义（# 写成 \\#，下划线 _ 写成 \\_，
//      百分号 % 写成 \\%，左右花括号写成 \\{ \\}）；行内偶尔用 $...$。

Object.assign(window.CODE_LESSON, {

  // ───────────────────────────────────────────────────────────────
  // 1. 变量与赋值  variable-assign
  // ───────────────────────────────────────────────────────────────
  'variable-assign': {
    warmup: {
      q: `游戏里你的血量一开始是 100，被怪物打了一下掉了 30。如果让你用程序记住"现在还剩多少血"，并且以后还能继续加血、扣血，你会怎么把这个"血量"存起来？`,
      hint: `想象一个贴了标签的盒子：标签上写"血量"，盒子里放数字。被打了，就把盒子里的数换成新的。标签名不变，里面的值会变。`,
      answer: `用一个**变量**来存。先 \`hp = 100\` 把 100 装进名叫 hp 的盒子；被打了就 \`hp = hp - 30\`，先算右边 100−30＝70，再把 70 重新装回 hp，旧的 100 被覆盖。变量就是"带名字、能反复改"的存储位，名字要见名知意（用 hp 不用 a），这样程序才记得住、看得懂。`
    },
    intro: [
      `变量，是程序里"带名字的盒子"，专门用来保存数据，方便以后反复使用和修改。你给数据起一个名字（变量名），以后想用这个数据，直接写名字就行，不用再记它具体是多少。`,
      `赋值是给盒子装东西的动作，用一个等号 \`=\` 完成：**左边写变量名，右边写值或表达式**。Python 会先把右边算出来，再把结果"贴"到左边的名字上。所以 \`x = 3 + 4\` 的意思是"先算出 7，再让 x 指向 7"，而不是数学里"x 等于一个式子"。`,
      `变量最关键的特点是**可以被重新赋值**：再写一次 \`x = ...\`，旧值就被新值覆盖，名字还是那个名字。正因如此，才有了编程里非常常见、但第一次看会懵的写法 \`x = x + 1\`——它不是方程，而是"把 x 现在的值加 1，再存回 x"。`,
      `还有一个进阶但重要的理解：在 Python 里，变量更像"贴在数据上的标签"，而不是装着数据的容器本身。也就是说变量保存的是"指向某个数据的引用"。现在先记住"名字 → 值"这个箭头关系就够用，它是后面学函数、列表、对象的基础。`,
      `一句话记住核心：**赋值是"先算右边、再存到左边"，名字不变、值可换。** 取名要让人一看就懂——用 \`score\`、\`student_count\` 这样的名字，别用 \`a\`、\`b\` 这种看不出含义的。`
    ],
    formulas: [
      { name: '赋值语法', latex: `\\texttt{变量名 = 值}`,
        note: `等号 \`=\` 是"赋值"，把右边的值存进左边的名字。例如 \`age = 14\`。注意它不是数学里的"相等"。` },
      { name: '自我更新', latex: `\\texttt{x = x + 1}`,
        note: `先算右边 \`x + 1\`（用 x 的旧值），再把结果存回 x。常用于计数、累加，例如每过一关就 \`level = level + 1\`。` },
      { name: '多重赋值', latex: `\\texttt{a, b = 1, 2}`,
        note: `Python 可以一行同时给多个变量赋值：a 得到 1，b 得到 2。交换两个变量也很方便：\`a, b = b, a\`。` },
      { name: '取名规则', latex: `\\texttt{见名知意 + 小写下划线}`,
        note: `变量名用字母/数字/下划线，不能以数字开头、不能用空格。推荐 \`student_count\` 这种 snake\\_case 风格，一眼看懂含义。` },
    ],
    methods: [
      {
        name: '"先算右边再存左边"读法',
        when: `每次看到一行 \`xxx = ...\`，尤其是右边出现了左边那个变量自己时（如 x = x + 1）`,
        steps: [
          `先盖住左边的变量名，只看等号右边`,
          `用变量"当前的值"把右边的表达式算成一个具体结果`,
          `把这个结果"装回"左边的变量名，旧值作废`,
          `如果右边用到了左边变量，记住用的是它"被覆盖之前"的旧值`,
        ],
        example: {
          problem: `已知 \`count = 5\`，执行 \`count = count * 2 + 1\` 之后，count 是多少？`,
          solution: [
            `解：先只看右边 \`count * 2 + 1\`，此时 count 的旧值是 5。`,
            `算右边：5 * 2 + 1 = 11。`,
            `再把 11 存回 count，旧值 5 被覆盖。`,
            `答：执行后 count 的值是 11。`,
          ],
        },
      },
      {
        name: '用第三个变量交换两值（经典）',
        when: `想把两个变量 a、b 里的值互换时`,
        steps: [
          `不能直接 \`a = b\` 再 \`b = a\`——第一步就把 a 的旧值冲掉了，第二步只会让 b 拿回它自己`,
          `办法：先用一个临时变量 temp 把 a 存起来`,
          `再把 b 的值放进 a`,
          `最后把 temp（也就是 a 的旧值）放进 b，交换完成`,
        ],
        example: {
          problem: `\`a = 1\`，\`b = 2\`，怎样让它们交换成 a 是 2、b 是 1？`,
          solution: [
            `解：借助临时变量 temp。`,
            `代码：\ntemp = a    # temp 先记住 a 的旧值 1\na = b       # a 变成 2\nb = temp    # b 拿到 temp 里的 1`,
            `答：交换后 a 是 2、b 是 1。（Python 里也可以一行搞定：\`a, b = b, a\`，但理解临时变量的过程更重要。）`,
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 计数器的累加',
        problem: `小明用程序统计他做了几道题。一开始 \`done = 0\`。他先做完 1 道写 \`done = done + 1\`，又做完 1 道再写一次 \`done = done + 1\`。问最后 done 是多少？请逐行说明每次赋值后 done 的值。`,
        steps: [
          `第一步：初始化。\`done = 0\`，此时 done 的值是 0。`,
          `第二步：第一次累加 \`done = done + 1\`。先算右边：用旧值 0，0 + 1 = 1；再存回 done，done 变成 1。`,
          `第三步：第二次累加 \`done = done + 1\`。先算右边：这次用的是上一步的新值 1，1 + 1 = 2；再存回 done，done 变成 2。`,
          `第四步：作答。最后 done 是 2。关键在于——\`done = done + 1\` 每执行一次，都是拿"当前最新的值"加 1 再存回去，所以两次累加就从 0 变成了 2。`,
        ],
      },
    ],
    pitfalls: [
      `把赋值号 \`=\` 当成数学的"相等"。\`x = x + 1\` 在数学里无解，但在编程里完全正常——它是"用旧 x 算出新值再存回 x"。`,
      `以为赋值是"双向绑定"。\`b = a\` 只是把 a 当时的值复制给 b；之后再改 a，b 不会跟着变。`,
      `用变量前忘了先赋值。直接写 \`print(x)\` 而 x 从没出现过，会报 \`NameError: name 'x' is not defined\`。变量要先"装过东西"才能用。`,
      `变量名取得看不懂或写错大小写。\`Score\` 和 \`score\` 是两个不同的变量；名字用 \`a\`、\`b\` 以后自己都看不懂，应见名知意。`,
      `想交换两个变量却直接 \`a = b\` 再 \`b = a\`。第一步就把 a 的旧值冲没了，结果两个都变成 b 的值，要用临时变量或 \`a, b = b, a\`。`,
    ],
    exercises: [
      {
        problem: `阅读下面的代码，说出最后 \`print(x)\` 会输出什么：\nx = 10\nx = x + 5\nx = x * 2\nprint(x)`,
        hint: `一行一行执行，每行都"先算右边、再存回 x"，把每一步 x 的新值写在旁边。`,
        answer: `**详细讲解**：逐行追踪 x 的值。\n第 1 行 \`x = 10\`：x 装进 10，现在 x 是 **10**。\n第 2 行 \`x = x + 5\`：先算右边，用旧值 10，10 + 5 = 15，存回 x，现在 x 是 **15**。\n第 3 行 \`x = x * 2\`：先算右边，用上一步的 15，15 * 2 = 30，存回 x，现在 x 是 **30**。\n第 4 行 \`print(x)\` 把 x 的当前值打印出来，所以输出 **30**。\n**为什么**：变量是会被反复覆盖的，每次 \`x = ...\` 都用"此刻最新的 x"去算，再把结果存回去。读这类题的诀窍就是在每行后面标一个小注，记下 x 变成了多少，顺着往下推绝不会错。`
      },
      {
        problem: `小红想交换两个变量的值。她写了下面的代码，结果发现 a 和 b 最后都是 8。请指出错在哪里，并写出正确的交换代码。\na = 3\nb = 8\na = b\nb = a`,
        hint: `执行到第 3 行 \`a = b\` 之后，a 的旧值 3 还在不在？第 4 行的 a 已经是几了？`,
        answer: `**详细讲解**：先看她为什么错。第 3 行 \`a = b\` 把 b 的值 8 存进了 a，于是 a 变成 8——**a 原来的 3 在这一步就被冲掉、永远丢失了**。第 4 行 \`b = a\`，此时 a 已经是 8，所以只是把 8 又存回 b，b 还是 8。结果两个都成了 8。\n问题的根源是：直接互相赋值会让先被覆盖的那个变量丢掉旧值。正确做法是用一个**临时变量**先把要丢的值存起来：\ntemp = a    # 先记住 a 的旧值 3\na = b       # a 变成 8\nb = temp    # b 拿回 temp 里的 3\n这样执行后 a 是 8、b 是 3，交换成功。**为什么这样对**：temp 在 a 被覆盖之前抢先保存了它的旧值，所以后面还能把这个旧值交给 b。Python 里也可以直接写 \`a, b = b, a\` 一步到位，它在底层正是先把右边 (b, a) 的值都取好，再一起赋给左边。`
      },
      {
        problem: `写一段程序：用变量记录一个长方形的长和宽（长 6、宽 4），算出它的面积并打印出来。要求变量名见名知意。`,
        hint: `面积 = 长 × 宽。先用两个变量分别存长和宽，再用第三个变量存面积，最后 print。`,
        answer: `**详细讲解**：这道题练"用变量保存数据、再用变量参与计算"。思路分三步：先把长、宽各存进一个变量；再把"长×宽"算出来存进面积变量；最后打印。参考代码：\nlength = 6        # 长方形的长\nwidth = 4         # 长方形的宽\narea = length * width   # 面积 = 长 × 宽\nprint("面积是", area)\n运行结果是 \`面积是 24\`。\n**为什么这样写**：① 用 \`length\`、\`width\`、\`area\` 这种见名知意的名字，别人（和半年后的自己）一眼就懂每个变量代表什么，这正是变量"带名字"的价值；② \`area = length * width\` 体现了赋值"先算右边再存左边"——先用 length、width 的值算出 24，再存进 area；③ 如果以后长方形尺寸变了，只要改 \`length\`、\`width\` 两个变量的值，面积会重新算，这就是变量"可复用、可修改"的好处。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 2. 数据类型  data-types
  // ───────────────────────────────────────────────────────────────
  'data-types': {
    warmup: {
      q: `在 Python 里，\`5 + 3\` 等于 8。那 \`"5" + "3"\` 等于多少？很多人会脱口而出"8"，但其实不是。你猜结果是什么？为什么会不一样？`,
      hint: `注意有没有引号。带了引号的 "5" 还是数字吗？两个"5"和"3"如果都不是数字，那个 + 还表示"相加"吗？`,
      answer: `\`"5" + "3"\` 的结果是字符串 \`"53"\`，不是 8！因为带引号的 \`"5"\` 是**字符串**（一串字符），不是数字。对字符串来说，\`+\` 不是"相加"而是"拼接"，把两段文字接起来就成了 "53"。这说明**数据是有类型的**：同样一个 \`+\`，作用在数字上是加法、作用在字符串上是拼接。心里清楚每个值是什么类型，才不会被这种结果坑到。`
    },
    intro: [
      `数据类型，指的是数据的"种类"。程序里的数据不是一锅乱炖，而是分门别类的：有整数、有小数、有文字、有"是/否"。不同类型能做的事不一样，这是初学编程必须建立的第一个意识。`,
      `初中阶段要认识四种最常用的类型：**整数 int**（如 10、-3，没有小数点的数）；**浮点数 float**（如 3.14、0.5，带小数点的数）；**字符串 str**（用引号包起来的文字，如 "hello"、"小明"）；**布尔 bool**（只有两个值 True 和 False，表示"对/错""是/否"）。`,
      `为什么类型这么重要？因为"能做什么"由类型决定。数能加减乘除；字符串能拼接、能取其中一部分；布尔只用来判断条件成立与否。最典型的坑就是热身题里的——数字的 \`+\` 是相加，字符串的 \`+\` 是拼接，混着用就会得到意外结果，甚至直接报错。`,
      `Python 是"动态类型"语言：你不用提前声明变量是什么类型，给它赋什么值，它就是什么类型，而且后面还能换成别的类型。方便归方便，但你**心里必须清楚**每个变量此刻装的是什么类型，否则容易出 bug。想查一个值的类型，用 \`type(x)\`，比如 \`type(10)\` 会告诉你是 \`<class 'int'>\`。`,
      `类型之间可以**转换**：\`int("5")\` 把字符串 "5" 变成数字 5，\`str(5)\` 把数字 5 变成字符串 "5"，\`float("3.14")\` 把字符串变成小数。这一招在下一节"输入输出"里特别关键——因为 input 读进来的永远是字符串，要做数学就得先转成数字。`
    ],
    formulas: [
      { name: '四种基本类型', latex: `\\texttt{int / float / str / bool}`,
        note: `整数 \`int\`（10）、浮点数 \`float\`（3.14）、字符串 \`str\`（"hi"）、布尔 \`bool\`（True/False）。字符串一定带引号。` },
      { name: '查看类型', latex: `\\texttt{type(x)}`,
        note: `返回 x 的类型。如 \`type(10)\` 是 int，\`type("10")\` 是 str——带不带引号，类型完全不同。` },
      { name: '转成数字', latex: `\\texttt{int(s)} \\;/\\; \\texttt{float(s)}`,
        note: `把字符串或浮点转成整数/小数。\`int("5")\` → 5，\`float("3.14")\` → 3.14。注意 \`int("abc")\` 会报错。` },
      { name: '转成字符串', latex: `\\texttt{str(x)}`,
        note: `把数字等转成字符串。\`str(5)\` → "5"。想把数字接到文字后面拼接时常要用它。` },
    ],
    methods: [
      {
        name: '判断该不该转换类型',
        when: `当你要把两个值用 \`+\` 连起来，或者要对输入的数据做数学运算时`,
        steps: [
          `先问：这两个值各是什么类型？带引号的是字符串，不带引号的数字才是 int/float`,
          `如果想做"数学加法"，两边都必须是数字，字符串要先用 int() 或 float() 转`,
          `如果想做"文字拼接"，两边都要是字符串，数字要先用 str() 转`,
          `拿不准就用 type() 打印出来看一眼，别靠猜`,
        ],
        example: {
          problem: `想把分数 \`score = 95\`（整数）和文字拼成一句话 "我的分数是95"，直接 \`"我的分数是" + score\` 行不行？`,
          solution: [
            `解：不行。\`"我的分数是"\` 是字符串，\`score\` 是整数，类型不同不能直接用 + 拼。`,
            `要先把数字转成字符串：\`str(score)\` 得到 "95"。`,
            `代码：\nmessage = "我的分数是" + str(score)\nprint(message)   # 输出：我的分数是95`,
            `答：先 \`str(score)\` 再拼接即可；否则会报 TypeError。`,
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 看类型、做转换',
        problem: `有两行输入分别存进了字符串变量：\`a = "12"\`、\`b = "8"\`（注意都带引号）。现在要算它们的和（应该是 20）。直接 \`a + b\` 会得到什么？怎么改才能算出 20？`,
        steps: [
          `第一步：判断类型。\`a = "12"\`、\`b = "8"\` 都带引号，所以它们是**字符串**，不是数字。`,
          `第二步：看 \`a + b\` 的结果。字符串的 + 是拼接，"12" 接上 "8" 得到字符串 \`"128"\`，而不是数字 20。`,
          `第三步：改正。要做数学加法，先把两个字符串各自转成整数：\`int(a)\` 得到 12，\`int(b)\` 得到 8。`,
          `第四步：写出正确代码并作答。\ntotal = int(a) + int(b)\nprint(total)   # 输出 20\n所以直接相加得到 "128"（拼接），转成 int 后相加才得到 20。`,
        ],
      },
    ],
    pitfalls: [
      `把带引号的 "数字" 当成数字。\`"5"\` 是字符串，\`5\` 才是整数；\`"5" + "5"\` 得 "55"（拼接），\`5 + 5\` 得 10（加法）。`,
      `数字和字符串直接用 + 连接。\`"年龄" + 14\` 会报 \`TypeError\`，必须写成 \`"年龄" + str(14)\`。`,
      `以为整数和小数完全一样。\`10\` 是 int、\`10.0\` 是 float；除法 \`/\` 的结果总是 float（如 \`6 / 2\` 是 3.0 不是 3）。`,
      `对不能转的字符串硬转。\`int("abc")\`、\`int("3.5")\` 都会报错；\`int()\` 只认整数样子的字符串。`,
      `布尔值的写法记错。Python 里是 \`True\`/\`False\`，**首字母大写**，写成 \`true\`/\`false\` 会被当成未定义的变量而报错。`,
    ],
    exercises: [
      {
        problem: `不运行代码，先判断下面每个表达式的结果和它的类型：\n（1）\`7 + 2\`\n（2）\`"7" + "2"\`\n（3）\`"7" * 3\`\n（4）\`6 / 2\``,
        hint: `带引号的是字符串，不带的是数字。字符串的 + 是拼接；字符串 * 整数是"重复"；除法 / 的结果是小数。`,
        answer: `**详细讲解**：关键是先看清每个值是数字还是字符串。\n（1）\`7 + 2\`：两个都是整数，+ 是加法，结果 **9**，类型 **int**。\n（2）\`"7" + "2"\`：两个都是字符串，+ 是拼接，把 "7" 和 "2" 接起来得 **"72"**，类型 **str**——不是 9！\n（3）\`"7" * 3\`：字符串乘以整数表示"把这个字符串重复几遍"，所以 "7" 重复 3 次得 **"777"**，类型 **str**。\n（4）\`6 / 2\`：在 Python 里单斜杠除法的结果一律是小数，所以是 **3.0**，类型 **float**（不是 3）。\n**为什么**：同一个符号作用在不同类型上含义不同（+ 对数字是加、对字符串是拼接，* 对"字符串和整数"是重复）。做这类题一定先标出每个值的类型，再决定运算符干什么。`
      },
      {
        problem: `下面代码想输出 "明年你 15 岁"，但运行报错了。请说明报错原因，并改正。\nage = 14\nprint("明年你 " + age + 1 + " 岁")`,
        hint: `\`age\` 是什么类型？字符串能直接和整数用 + 连吗？另外 \`age + 1\` 想表达什么，要不要先算？`,
        answer: `**详细讲解**：先找错。\`age = 14\` 是整数，而 \`"明年你 "\` 是字符串。表达式里出现了"字符串 + 整数"（字符串和 age、和 1 相加），类型不同不能直接用 + 连接，Python 会抛出 \`TypeError: can only concatenate str ... to str\`。\n还有一个意图问题：作者其实想先算出"明年的年龄" \`age + 1\`（也就是 15），再把这个数字拼进句子里。\n正确改法是：先把要拼接的数字用 \`str()\` 转成字符串。\nage = 14\nprint("明年你 " + str(age + 1) + " 岁")\n这样 \`age + 1\` 先算出整数 15，\`str(15)\` 变成 "15"，再和前后两段文字拼成 "明年你 15 岁"。\n**为什么**：拼接要求参与的都是字符串，所以数字必须先转换；而且要注意运算顺序——先把 \`age + 1\` 用括号或自然优先级算成 15，再转字符串，否则容易拼出 "明年你 141 岁" 这种错的结果。（顺带一提：也可以用更方便的 f-string 写成 \`print(f"明年你 {age + 1} 岁")\`，它会自动处理类型转换。）`
      },
      {
        problem: `写一段代码：用 \`type()\` 分别查看 \`100\`、\`3.14\`、\`"hello"\`、\`True\` 这四个值的类型，并把结果打印出来。然后用一句话说明：为什么 \`"100"\` 和 \`100\` 的类型不一样？`,
        hint: `直接 \`print(type(...))\` 即可。最后一问抓住"有没有引号"这个区别。`,
        answer: `**详细讲解**：用 \`type()\` 查类型非常直接，把每个值丢进去打印即可。参考代码：\nprint(type(100))      # <class 'int'>\nprint(type(3.14))     # <class 'float'>\nprint(type("hello"))  # <class 'str'>\nprint(type(True))     # <class 'bool'>\n运行后能看到四种类型：整数、浮点数、字符串、布尔，正好对应本节四种基本类型。\n关于最后一问：\`"100"\`（带引号）是**字符串**，它表示"由 1、0、0 三个字符组成的一串文字"，不能直接做加减乘除（\`"100" + "1"\` 会得到 "1001"）；而 \`100\`（不带引号）是**整数**，可以正常参与数学运算。**为什么不同**：引号是 Python 区分"文字"和"数字"的标志——加了引号就告诉 Python"把里面的内容当文字看"，所以哪怕里面写的是数字样子，类型也是 str。这正是初学者最容易混的地方，记住"看引号定类型"就不会错。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 3. 输入与输出  io-basic
  // ───────────────────────────────────────────────────────────────
  'io-basic': {
    warmup: {
      q: `你写了个小程序，想让用户输入年龄，然后告诉他"明年你就 X 岁了"。你写了 \`age = input("年龄：")\`，再写 \`print("明年", age + 1, "岁")\`，结果一运行就报错。问题可能出在哪？`,
      hint: `想一想：用户从键盘敲进来的东西，程序拿到手里时是"数字"还是"一串字符"？\`age + 1\` 里的 age 真的是数字吗？`,
      answer: `问题在于：**input 读到的永远是字符串**，哪怕用户敲的是 "14"，age 拿到的也是字符串 "14"，不是数字 14。字符串和整数 1 不能直接相加，所以 \`age + 1\` 报错。解决办法是先转换类型：\`age = int(input("年龄："))\`，把输入转成整数，之后 \`age + 1\` 才是真正的数学加法。记住这条铁律：**input 进来先转换，再做运算。**`
    },
    intro: [
      `输入与输出（简称 I/O），是程序和人"对话"的基本方式。**输入**是程序从外界（通常是键盘）获取数据，**输出**是程序把结果展示给人看（通常是打印到屏幕）。没有输入输出，程序就只能自言自语，没法和用户互动。`,
      `输出用 \`print()\`。把想显示的内容放进括号里，运行时就打印到屏幕上。\`print()\` 很灵活：可以打印文字 \`print("你好")\`，可以打印变量 \`print(score)\`，还可以用逗号把多项一起打印 \`print("你好", name)\`——逗号分隔的各项之间会自动加一个空格。`,
      `输入用 \`input()\`。括号里可以写一句提示语，运行到这里程序会**暂停**，等用户敲完字、按下回车，再把用户输入的内容交给程序。通常我们用一个变量去接住它：\`name = input("你叫什么？")\`，这样 name 里就存着用户输入的内容。`,
      `这里有一条最容易踩的坑、也是本节的重点：**input 读回来的结果永远是字符串（str）**，不管用户敲的看起来多么像数字。所以如果你要对输入做数学运算，必须先转换类型——用 \`int()\` 转成整数、用 \`float()\` 转成小数。例如 \`age = int(input("年龄："))\`。`,
      `把这一节连起来记：**用 input 拿数据 → 需要算数就先 int()/float() 转换 → 用 print 把结果显示出来。** 这三步是几乎所有"输入数据、处理、输出结果"小程序的骨架。`
    ],
    formulas: [
      { name: '输出语法', latex: `\\texttt{print(内容)}`,
        note: `把括号里的内容显示到屏幕。多项用逗号隔开会自动加空格：\`print("你好", name)\`。` },
      { name: '输入语法', latex: `\\texttt{变量 = input("提示语")}`,
        note: `显示提示语并等用户输入，回车后把输入的内容（字符串）存进变量。提示语可省略但建议写上。` },
      { name: '输入转整数', latex: `\\texttt{int(input("..."))}`,
        note: `input 返回字符串，套一层 \`int()\` 把它转成整数，才能做数学运算。小数则用 \`float(input(...))\`。` },
      { name: '格式化输出', latex: `\\texttt{print(f"分数 \\{score\\}")}`,
        note: `f-string：在字符串前加 f，用花括号 {} 直接嵌入变量，输出 "分数 95"。比用逗号或 + 拼接更清爽。` },
    ],
    methods: [
      {
        name: '"读—转—算—印"四步法',
        when: `写一个"让用户输入数据，算一算，再把结果显示出来"的小程序时`,
        steps: [
          `读：用 input 读入数据，存进变量（此时是字符串）`,
          `转：如果接下来要做数学运算，立刻用 int() 或 float() 把它转成数字`,
          `算：用运算符算出你要的结果`,
          `印：用 print 把结果显示给用户，必要时配上说明文字`,
        ],
        example: {
          problem: `让用户输入两个整数，输出它们的和。`,
          solution: [
            `解：按"读—转—算—印"来。`,
            `代码：\na = int(input("第一个数："))   # 读 + 转\nb = int(input("第二个数："))   # 读 + 转\ntotal = a + b                  # 算\nprint("它们的和是", total)     # 印`,
            `答：因为两次输入都用 int() 转成了整数，\`a + b\` 才是真正的加法；如果不转，"3"+"5" 会拼成 "35"。`,
          ],
        },
      },
      {
        name: '三种把变量"印"进文字的写法',
        when: `想把变量的值和说明文字一起输出时`,
        steps: [
          `逗号法：\`print("分数", score)\`——最简单，各项间自动加空格`,
          `拼接法：\`print("分数" + str(score))\`——要先把数字 str() 转字符串，否则报错`,
          `f-string 法（推荐）：\`print(f"分数{score}")\`——字符串前加 f，花括号里直接写变量，自动转换`,
          `初学优先用逗号法和 f-string，能少踩"类型不匹配"的坑`,
        ],
        example: {
          problem: `已知 \`name = "小明"\`、\`score = 95\`，用 f-string 输出 "小明的分数是95"。`,
          solution: [
            `解：用 f-string，在字符串前加 f，把变量放进花括号。`,
            `代码：\nprint(f"{name}的分数是{score}")`,
            `答：输出"小明的分数是95"。f-string 会自动把 score（整数）变成文字嵌进去，不用手动 str() 转换，很省心。`,
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 算平均分',
        problem: `写一个程序：让用户依次输入语文、数学两科的成绩（可能是小数），算出平均分并打印，保留原样即可。请说明每一步为什么这么写。`,
        steps: [
          `第一步：读入并转换。成绩可能带小数，所以用 \`float()\` 转（不用 int，否则小数会被截断）。\nchinese = float(input("语文成绩："))\nmath = float(input("数学成绩："))`,
          `第二步：为什么必须转换？因为 input 返回的是字符串，"85" 和 "90" 直接相加会拼成 "8590"，根本不是平均分。转成 float 后才能做数学。`,
          `第三步：计算平均分。两科平均就是和除以 2：\naverage = (chinese + math) / 2\n注意括号——要先把两科加起来，再除以 2，否则按优先级会变成"只把 math 除以 2 再加 chinese"。`,
          `第四步：输出。用 f-string 把结果印出来：\nprint(f"平均分是 {average}")\n这样用户就看到了最终结果。整段体现了"读—转—算—印"的完整流程。`,
        ],
      },
    ],
    pitfalls: [
      `忘了 input 返回字符串就直接做数学。\`age = input(); age + 1\` 会报错，必须 \`age = int(input())\` 先转换。`,
      `该用 float 却用了 int。输入可能是小数（如身高 1.75）时用 \`int()\` 会报错或截断，应该用 \`float()\`。`,
      `转换时括号没套对。要写 \`int(input("..."))\`——input 在里、int 在外；写成 \`input(int("..."))\` 完全不对。`,
      `print 里把变量加了引号当成文字。\`print("score")\` 打印的是文字 score 这五个字母，\`print(score)\` 才打印变量的值。`,
      `想拼接却忘了数字要转。\`print("得分：" + score)\` 报错（str+int），用逗号 \`print("得分：", score)\` 或 f-string 更稳。`,
    ],
    exercises: [
      {
        problem: `下面代码想让用户输入一个数字，输出它的 2 倍。运行时用户输入了 5，结果却打印出 \`55\`。请解释为什么，并改正代码。\nn = input("输入一个数：")\nprint(n * 2)`,
        hint: `\`input\` 读进来的 n 是什么类型？字符串乘以 2 表示什么？想得到 10 应该先做什么？`,
        answer: `**详细讲解**：先弄清为什么是 55 而不是 10。\`n = input(...)\` 读回来的是**字符串**，所以用户输入 5 后，n 是字符串 \`"5"\`，不是数字 5。而字符串乘以整数表示"把这个字符串重复几遍"，所以 \`"5" * 2\` 得到 \`"55"\`（两个 5 拼起来），于是打印出 55。\n要得到数学上的 2 倍（10），必须先把输入转成数字。改正：\nn = int(input("输入一个数："))   # 先转成整数\nprint(n * 2)                     # 5 * 2 = 10\n这样 n 是整数 5，\`n * 2\` 就是真正的乘法，输出 10。\n**为什么**：这正是本节最核心的坑——input 永远给你字符串。一旦后面要做加减乘除，第一件事就是用 \`int()\` 或 \`float()\` 转换。乘法在"字符串×整数"和"数字×数字"下含义完全不同，这道题就是它俩的鲜明对比。`
      },
      {
        problem: `用 input 和 print 写一个小程序：问用户的名字和年龄，然后输出一句问候，例如用户输入"小明"和"14"，就打印"小明你好，你今年14岁，明年就15岁啦！"。`,
        hint: `名字不用转换（本来就是文字），但年龄要参与"+1"运算，必须转成整数。输出建议用 f-string。`,
        answer: `**详细讲解**：分两类数据处理。名字是纯文字，用 input 读进来直接用即可；年龄要做"明年 = 今年 + 1"的运算，所以必须用 \`int()\` 转成整数。输出用 f-string 最清爽，能把变量直接嵌进句子。参考代码：\nname = input("你叫什么名字？")\nage = int(input("你今年几岁？"))   # 年龄要算 +1，必须转成整数\nprint(f"{name}你好，你今年{age}岁，明年就{age + 1}岁啦！")\n用户输入"小明""14"后，输出"小明你好，你今年14岁，明年就15岁啦！"。\n**为什么这样写**：① name 不参与计算，转不转都行，保持字符串最自然；② age 参与了 \`age + 1\`，如果不转成整数，"14" + 1 会报错（字符串不能加整数），所以这里 int() 不能省；③ 用 f-string 把 \`{name}\`、\`{age}\`、\`{age + 1}\` 直接写进句子，Python 会自动算出 \`age + 1\`（15）并转成文字，比用逗号或 + 拼接更不容易出错。`
      },
      {
        problem: `阅读代码，说出屏幕上会显示什么（注意有没有引号）：\nx = 8\nprint("x")\nprint(x)\nprint("x =", x)`,
        hint: `带引号的是"原样的文字"，不带引号的变量会被替换成它的值。一行一行看。`,
        answer: `**详细讲解**：关键在区分"带引号的文字"和"不带引号的变量"。\n第 1 行 \`x = 8\`：把 8 存进变量 x（这行不打印东西）。\n第 2 行 \`print("x")\`：括号里 \`"x"\` 带引号，是**文字**，所以原样打印字母 **x**。\n第 3 行 \`print(x)\`：括号里 \`x\` 不带引号，是**变量**，会被替换成它的值，所以打印 **8**。\n第 4 行 \`print("x =", x)\`：用逗号分隔了两项，前一项 \`"x ="\` 是文字原样打印，后一项 \`x\` 是变量打印成 8，逗号之间自动加一个空格，所以这行显示 **x = 8**。\n汇总，屏幕从上到下显示三行：\nx\n8\nx = 8\n**为什么**：print 的规则是——加引号的内容当文字原样输出，不加引号的名字当变量、输出它的值。初学者常把 \`print("score")\` 和 \`print(score)\` 搞混，这道题就是专门练这个区别的。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 4. 运算符与表达式  operators
  // ───────────────────────────────────────────────────────────────
  'operators': {
    warmup: {
      q: `你能很快说出 \`7 / 2\`、\`7 // 2\`、\`7 % 2\` 这三个结果分别是多少吗？它们都和"7 除以 2"有关，但答案完全不同。试试看，再想想它们各自表示什么。`,
      hint: `一个斜杠是普通除法（要小数），两个斜杠是"只要整数部分"，百分号是"取余数"。把"7÷2＝3 余 1"这句口算拆开来看。`,
      answer: `\`7 / 2\` = **3.5**（普通除法，结果是小数）；\`7 // 2\` = **3**（整除，只取商的整数部分，扔掉小数）；\`7 % 2\` = **1**（取模，即余数，7÷2 商 3 余 1）。它们对应小学口算"7÷2＝3 余 1"的三个角度：完整商、整数商、余数。其中 \`%\` 取余特别有用——\`n % 2\` 等于 0 就说明 n 是偶数，是判断奇偶、整除的常用招。`
    },
    intro: [
      `运算符，是把数据"组合"起来算出新值的符号；由数据和运算符搭起来的式子叫**表达式**，它最终会算出一个结果。比如 \`3 + 4\` 是表达式，结果是 7。学运算符，就是学"程序里怎么算"。`,
      `初中要掌握三类运算符。**算术运算符**做数学计算：加 \`+\`、减 \`-\`、乘 \`*\`、除 \`/\`，还有两个 Python 特色的——整除 \`//\`（只取整数商）和取模 \`%\`（取余数），以及幂 \`**\`（\`2 ** 3\` 是 2 的 3 次方＝8）。`,
      `**比较运算符**用来"比大小、判相等"：大于 \`>\`、小于 \`<\`、大于等于 \`>=\`、小于等于 \`<=\`、等于 \`==\`、不等于 \`!=\`。它们的结果不是数字，而是布尔值 True 或 False。特别注意：判断"相等"要用**两个等号** \`==\`，一个等号 \`=\` 是赋值，这是初学者头号易错点。`,
      `**逻辑运算符**把多个条件连起来：\`and\`（并且，两个都成立才为真）、\`or\`（或者，有一个成立就为真）、\`not\`（取反，真变假、假变真）。比如判断"是青少年"可以写 \`age >= 13 and age <= 19\`。`,
      `运算是有**优先级**的，和数学一致：先乘除后加减，比较和逻辑更靠后。拿不准时**多用括号**——括号里的先算，既保证顺序对，也让别人看得更清楚。一句话总结：表达式就是"用运算符把数据算成新值"，记牢 \`/ // %\` 的区别和 \`= 与 ==\` 的区别，就跨过了大半个坎。`
    ],
    formulas: [
      { name: '算术运算符', latex: `\\texttt{+ - * / // \\% **}`,
        note: `加减乘、除 \`/\`（得小数）、整除 \`//\`（取整数商）、取模 \`%\`（取余数）、幂 \`**\`。如 \`2 ** 10\` 是 1024。` },
      { name: '整除与取模', latex: `\\texttt{7 // 2 = 3,\\; 7 \\% 2 = 1}`,
        note: `\`//\` 取商的整数部分，\`%\` 取余数。判断偶数常用 \`n \\% 2 == 0\`，判断整除用 \`a \\% b == 0\`。` },
      { name: '比较运算符', latex: `\\texttt{== != > < >= <=}`,
        note: `结果是布尔值 True/False。判断"相等"用两个等号 \`==\`；一个等号 \`=\` 是赋值，千万别混。` },
      { name: '逻辑运算符', latex: `\\texttt{and \\; or \\; not}`,
        note: `\`and\` 两边都真才真；\`or\` 有一边真就真；\`not\` 取反。如 \`5 > 3 and 2 < 4\` → True。` },
    ],
    methods: [
      {
        name: '按优先级算表达式',
        when: `遇到一个混合了加减乘除、比较、逻辑的长表达式，要算出结果时`,
        steps: [
          `先算括号里的（括号优先级最高）`,
          `再算幂 \`**\``,
          `然后算乘、除、整除、取模 \`* / // %\`（同级从左到右）`,
          `接着算加、减 \`+ -\``,
          `再算比较 \`> < == !=\` 等（结果是布尔值）`,
          `最后算逻辑 \`not\` → \`and\` → \`or\``,
        ],
        example: {
          problem: `算出 \`2 + 3 * 4 > 10 and 1 < 2\` 的结果。`,
          solution: [
            `解：按优先级一步步来。`,
            `先算乘法：3 * 4 = 12，式子变成 \`2 + 12 > 10 and 1 < 2\`。`,
            `再算加法：2 + 12 = 14，变成 \`14 > 10 and 1 < 2\`。`,
            `算比较：14 > 10 是 True，1 < 2 是 True，变成 \`True and True\`。`,
            `答：\`True and True\` 是 **True**。可见比较、逻辑都排在算术后面。`,
          ],
        },
      },
      {
        name: '用 % 判断奇偶 / 整除',
        when: `需要判断一个数是奇数还是偶数、或能不能被某个数整除时`,
        steps: [
          `取模 \`%\` 得到的是"除完之后的余数"`,
          `判断偶数：\`n % 2 == 0\` 为真说明能被 2 整除，是偶数`,
          `判断奇数：\`n % 2 == 1\`（或 \`n % 2 != 0\`）说明是奇数`,
          `判断能否被 k 整除：\`n % k == 0\``,
        ],
        example: {
          problem: `怎么判断整数 \`n\` 是不是偶数？`,
          solution: [
            `解：偶数的特点是除以 2 没有余数，所以看 \`n % 2\` 是不是 0。`,
            `代码：\nif n % 2 == 0:\n    print("偶数")\nelse:\n    print("奇数")`,
            `答：\`n % 2 == 0\` 成立就是偶数。例如 \`8 % 2\` 是 0（偶），\`7 % 2\` 是 1（奇）。`,
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 拆分时间',
        problem: `有一个总秒数 \`total = 125\`，要把它换算成"几分几秒"的形式（应该是 2 分 5 秒）。请用整除和取模算出"分"和"秒"，并打印出来。`,
        steps: [
          `第一步：审题。1 分 = 60 秒。"分"就是 125 里包含多少个完整的 60 秒，"秒"就是分完整分钟后剩下的零头。这正好对应整除和取模。`,
          `第二步：算分钟。用整除 \`//\` 取"125 里有几个完整的 60"：\`minutes = total // 60\`，125 // 60 = 2，所以是 2 分钟。`,
          `第三步：算剩余秒数。用取模 \`%\` 取"除完 60 后剩下的余数"：\`seconds = total % 60\`，125 % 60 = 5，所以剩 5 秒。`,
          `第四步：输出并作答。\nminutes = total // 60   # 2\nseconds = total % 60    # 5\nprint(f"{minutes} 分 {seconds} 秒")   # 2 分 5 秒\n这道题展示了 \`//\` 和 \`%\` 是"黄金搭档"：一个取完整份数，一个取剩下的零头。`,
        ],
      },
    ],
    pitfalls: [
      `把赋值 \`=\` 和比较 \`==\` 搞混。判断相等必须用 \`==\`，写成 \`if x = 5\` 会直接报语法错误。`,
      `混淆 \`/\`、\`//\`、\`%\`。\`/\` 得小数（7/2=3.5），\`//\` 取整数商（7//2=3），\`%\` 取余数（7%2=1），用错结果完全不同。`,
      `忘记运算优先级。\`2 + 3 * 4\` 是 14（先乘后加）不是 20；不确定就加括号 \`(2 + 3) * 4\`。`,
      `逻辑运算符写成了符号。Python 用单词 \`and\`/\`or\`/\`not\`，不是 \`&&\`/\`||\`/\`!\`（那是别的语言的写法）。`,
      `比较运算符顺序写反。是 \`>=\`、\`<=\`、\`!=\`，不能写成 \`=>\`、\`=<\`、\`=!\`，写反会报语法错误。`,
    ],
    exercises: [
      {
        problem: `不运行，分别算出下面表达式的值：\n（1）\`10 // 3\`\n（2）\`10 % 3\`\n（3）\`2 ** 4\`\n（4）\`(5 > 3) and (2 == 2)\``,
        hint: `\`//\` 取整数商，\`%\` 取余数，\`**\` 是幂；比较运算结果是 True/False，再用 and 连起来。`,
        answer: `**详细讲解**：逐个分析。\n（1）\`10 // 3\`：整除取商的整数部分。10÷3＝3 余 1，整数商是 **3**。\n（2）\`10 % 3\`：取模取余数。10÷3 余 **1**。（注意 (1)(2) 正好是同一个除法的"商"和"余"。）\n（3）\`2 ** 4\`：\`**\` 是幂运算，2 的 4 次方 = 2×2×2×2 = **16**。\n（4）\`(5 > 3) and (2 == 2)\`：先算两个括号里的比较——\`5 > 3\` 是 True，\`2 == 2\` 是 True（两个等号判断相等）；再算 \`True and True\`，and 要求两边都真才真，所以结果是 **True**。\n**为什么**：\`//\` 和 \`%\` 是配对的（商与余），\`**\` 别和乘法 \`*\` 搞混，比较运算的结果是布尔值、可以继续用逻辑运算符连接。把每一步的中间结果写下来，长表达式也不会乱。`
      },
      {
        problem: `下面代码想判断"x 等于 10 就打印 yes"，但运行报错了。指出错误并改正。\nx = 10\nif x = 10:\n    print("yes")`,
        hint: `判断"相等"和"赋值"用的是同一个符号吗？一个等号是干什么的，两个等号又是干什么的？`,
        answer: `**详细讲解**：错误在 \`if x = 10:\` 这一行。一个等号 \`=\` 是**赋值**（把 10 装进 x），不能用来做判断；而 if 后面需要的是一个"判断条件"。Python 看到 if 后面跟了个赋值，会报 \`SyntaxError\`（语法错误）。\n判断"x 是不是等于 10"必须用**两个等号** \`==\`。改正：\nx = 10\nif x == 10:        # 两个等号才是"判断相等"\n    print("yes")\n这样 \`x == 10\` 算出布尔值 True，条件成立就打印 yes。\n**为什么**：这是初学者最经典的错误之一。记住口诀——"一个等号是赋值（装东西），两个等号是判断（比一比）"。赋值是"动作"，比较是"问问题"，用途完全不同，所以符号也不同。`
      },
      {
        problem: `写一段代码：让用户输入一个整数，判断它是奇数还是偶数，并打印结果（提示：用取模 % 运算）。`,
        hint: `偶数的特点是"除以 2 余 0"。先用 int(input()) 读入数字，再用 \`n % 2 == 0\` 判断。`,
        answer: `**详细讲解**：判断奇偶的核心是取模运算 \`%\`——一个数除以 2 的余数只可能是 0 或 1，余 0 是偶数、余 1 是奇数。步骤：先读入并转成整数（input 返回字符串，必须 int()），再用 \`n % 2 == 0\` 判断，配合 if-else 输出。参考代码：\nn = int(input("输入一个整数："))\nif n % 2 == 0:        # 除以 2 余数为 0\n    print(f"{n} 是偶数")\nelse:\n    print(f"{n} 是奇数")\n比如输入 8，\`8 % 2\` 是 0，打印"8 是偶数"；输入 7，\`7 % 2\` 是 1，走 else，打印"7 是奇数"。\n**为什么这样写**：① 必须 \`int()\` 转换，否则字符串没法做 % 运算；② \`n % 2\` 取的是余数，用它和 0 比较（注意是两个等号 \`==\`）就能判断整除；③ 用 if-else 是因为结果只有"偶/奇"两种情况，二选一正好用一对 if-else。取模判奇偶是非常常见的小技巧，以后判断"能不能被某个数整除"都用 \`n % k == 0\`。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 5. 注释与代码风格  comments-style
  // ───────────────────────────────────────────────────────────────
  'comments-style': {
    warmup: {
      q: `半年前你写了一段代码 \`a = b * 0.6 + c * 0.4\`，今天再打开，怎么都想不起来这一行在算什么、为什么是 0.6 和 0.4。如果当初做点什么，今天就能一眼看懂——你觉得该做什么？`,
      hint: `代码不只是给电脑跑的，也是给"未来的你"和队友看的。能不能在旁边留句说明？变量名能不能取得更明白？`,
      answer: `当初应该写**注释**并用**好的变量名**。比如改成 \`final = midterm * 0.6 + final_exam * 0.4  # 期中占60%、期末占40%\`——变量名说清了 a、b、c 是什么，注释说清了 0.6/0.4 是权重。代码是写给人看的，机器只是顺带执行。好注释解释"**为什么**"，好命名让代码本身就能读懂，这就是"代码风格"要解决的事。`
    },
    intro: [
      `这一节讲的不是"让程序能跑"，而是"让程序好懂"。一句话先立住观念：**代码是写给人看的，机器只是顺带执行。** 同一个功能有很多种写法，专业和业余的差距，很大程度上就在代码读不读得懂。`,
      `第一件工具是**注释**。在 Python 里用井号 \`#\` 开头写注释，\`#\` 后面到行尾的内容程序会完全忽略，只给人看。注释是用来解释"**为什么这么做**"的，而不是把代码翻译一遍。比如 \`tax = price * 0.13  # 13% 是当地的增值税率\` 这个注释有价值；而 \`x = x + 1  # 把 x 加 1\` 这种"复读机注释"没意义，因为代码本身已经说清楚了。`,
      `第二件是**好的命名**。变量、函数的名字要"见名知意"：用 \`student_count\` 而不是 \`n\`，用 \`total_price\` 而不是 \`tp\`。好名字能让代码"自己解释自己"，常常比注释还管用。Python 习惯变量用小写加下划线的 snake_case 风格。`,
      `第三件是 Python 特别强调的**缩进**。和很多语言用花括号 {} 不同，Python 用"缩进"（行首的空格）来表示哪些代码属于同一个代码块（比如 if、for、函数体内部）。**同一层的代码缩进必须完全一致**——通常用 4 个空格。缩进错了不是风格问题，而是直接报错 \`IndentationError\`。`,
      `把这节连起来：注释解释为什么、命名让代码自解释、缩进保证结构清晰且不报错。再加上"别写太挤"（适当空行、运算符两边留空格）。养成好风格不是为了好看，而是为了**让代码好读、好改、少出错**——这是从"会写"到"写好"的关键一步。`
    ],
    formulas: [
      { name: '单行注释', latex: `\\texttt{\\# 这是注释}`,
        note: `\`#\` 到行尾都被忽略，只给人看。注释要解释"为什么"，别复述代码做了什么。` },
      { name: '好命名', latex: `\\texttt{student\\_count}`,
        note: `见名知意 + 小写下划线（snake\\_case）。用 \`student\\_count\` 而不是 \`n\`，代码能自我解释。` },
      { name: '缩进表示代码块', latex: `\\texttt{4 个空格 = 一层}`,
        note: `Python 靠缩进区分代码块，同层缩进必须一致（推荐 4 空格）。缩进错会报 \`IndentationError\`。` },
      { name: '留白更易读', latex: `\\texttt{a = b + c}`,
        note: `运算符两边、逗号后加一个空格，逻辑段落间空一行。\`a=b+c\` 不报错但挤，\`a = b + c\` 更清楚。` },
    ],
    methods: [
      {
        name: '写"好注释"的判断法',
        when: `每次想加注释时，先想一下"这条注释到底有没有用"`,
        steps: [
          `问自己：这条注释是在解释"为什么"还是在复读"做了什么"`,
          `如果只是把代码翻译成中文（如"x 加 1"），删掉——代码本身已经说清了`,
          `如果解释了背景、原因、特殊取值（如"0.6 是期中权重"），保留`,
          `复杂逻辑、容易看错的地方，加注释；一目了然的简单行，不用注释`,
        ],
        example: {
          problem: `下面两条注释，哪条好、哪条该删？\n\`count = count + 1  # count 加一\`\n\`count = count + 1  # 每答对一题就计一次数\``,
          solution: [
            `解：看注释是"复读代码"还是"解释意图"。`,
            `第一条 "count 加一" 只是把代码翻译了一遍，没提供任何新信息，应**删掉**。`,
            `第二条 "每答对一题就计一次数" 解释了这行在业务上的**意义**（为什么要加一），帮助读者理解，应**保留**。`,
            `答：好注释解释"为什么/什么含义"，而不是"做了什么"。`,
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 给"差代码"做风格改造',
        problem: `下面这段代码能跑，但很难读。请把它改写得更易懂（改好命名、加有用的注释、规范留白），并说明你改了哪些地方、为什么。\nx=85\ny=92\nz=(x+y)/2\nprint(z)`,
        steps: [
          `第一步：诊断问题。① 变量名 x、y、z 看不出含义；② 没有任何注释，不知道在算什么；③ 等号、运算符两边没空格，挤在一起。`,
          `第二步：改命名。从 print 的意图能看出这是在算两科平均分，于是把 x、y 改成 \`chinese\`、\`math\`，z 改成 \`average\`，名字一出来含义就清楚了。`,
          `第三步：加有用的注释 + 规范留白。在关键行解释意图，运算符两边加空格。`,
          `第四步：给出改造后的代码并总结。\n# 计算语文、数学两科的平均分\nchinese = 85          # 语文成绩\nmath = 92             # 数学成绩\naverage = (chinese + math) / 2   # 两科平均\nprint(average)\n改造后，不看 print 也能从命名和注释读懂整段在干什么——这就是好风格的价值：**功能没变，可读性大大提高**。`,
        ],
      },
    ],
    pitfalls: [
      `缩进不一致。Python 靠缩进分代码块，同一层有的缩 2 格有的缩 4 格，会报 \`IndentationError\`；建议统一用 4 个空格，别混用空格和 Tab。`,
      `写"复读机"注释。\`x = x + 1  # x 加一\` 这种没用，注释应解释"为什么"而不是复述"做了什么"。`,
      `变量名图省事。用 \`a\`、\`b\`、\`tp\` 这种名字，过几天自己都看不懂，应见名知意如 \`total_price\`。`,
      `以为风格只是"好看"。缩进错会直接报错让程序跑不了；命名乱、没注释会让 debug 和改代码事倍功半，风格是实打实影响开发的。`,
      `中文注释里用了中文标点导致整行被注释还以为没事，或在该写代码处误加了 #。\`#\` 之后的内容一律不执行，别把要运行的代码也跟在 # 后面。`,
    ],
    exercises: [
      {
        problem: `下面哪条是"好注释"，哪条是该删的"无用注释"？请分别说明理由。\n（甲）\`price = price * 0.8  # 会员打八折\`\n（乙）\`price = price * 0.8  # price 乘以 0.8\``,
        hint: `好注释解释"为什么/这个数字代表什么"，无用注释只是把代码又用中文说了一遍。`,
        answer: `**详细讲解**：判断标准是——注释有没有提供"代码本身看不出来的信息"。\n（甲）\`# 会员打八折\` 是**好注释**：它解释了 0.8 这个数字的**业务含义**（为什么要乘 0.8——因为会员打八折）。光看代码 \`price * 0.8\` 你知道乘了 0.8，但不知道为什么是 0.8，这条注释补上了这个"为什么"。\n（乙）\`# price 乘以 0.8\` 是**该删的无用注释**：它只是把 \`price = price * 0.8\` 这行代码用中文又念了一遍，没有任何新信息。任何会看代码的人都知道这是"price 乘以 0.8"，注释等于白写。\n**为什么**：注释的价值在于解释代码"无法自我表达"的东西——意图、原因、特殊数字的来历、容易踩的坑。重复代码字面意思的注释不仅没用，还会增加阅读负担、改代码时容易忘记同步更新。记住一句话：**注释写"为什么"，不写"做了什么"。**`
      },
      {
        problem: `下面代码运行时报了 \`IndentationError\`（缩进错误）。请找出问题并改正。\nscore = 70\nif score >= 60:\nprint("及格")`,
        hint: `if 后面冒号下面的那一行，是属于 if 的"代码块"。Python 靠什么来表示"它属于 if"？`,
        answer: `**详细讲解**：错误在最后一行 \`print("及格")\`。在 Python 里，if 条件成立时要执行的代码，必须**缩进**（往右空几格）来表示"我属于这个 if 代码块"。这里 \`print("及格")\` 顶格写、没有缩进，Python 就认为 if 下面"空着"没有内容，于是报 \`IndentationError\`。\n改正：把 print 这一行往右缩进 4 个空格。\nscore = 70\nif score >= 60:\n    print("及格")      # 缩进 4 格，表示它属于 if\n这样运行就正常了：score 是 70，满足 \`>= 60\`，打印"及格"。\n**为什么**：很多语言用花括号 {} 把代码块括起来，而 Python 独特地用"缩进"来表示代码块的从属关系。冒号 \`:\` 后面那一块（if/else/for/函数体里的内容）都要统一往右缩进，而且同一层缩进量必须一致（推荐 4 个空格）。缩进在 Python 里不是"排版好看"，而是**语法的一部分**，错了就跑不起来。`
      },
      {
        problem: `把下面这段"挤在一起、命名差"的代码，改写成命名清楚、加了有用注释、留白规范的版本。\nr=5\nc=2*3.14*r\nprint(c)`,
        hint: `从 \`2*3.14*r\` 这个公式猜猜在算什么（圆的周长 = 2πr）。把 r、c 改成看得懂的名字，给关键行加注释，运算符两边加空格。`,
        answer: `**详细讲解**：先读懂代码——\`2 * 3.14 * r\` 是圆周长公式 $C = 2\\pi r$，所以 r 是半径、c 是周长、3.14 是圆周率 π 的近似值。改造分三方面：① 命名见名知意，r 改 \`radius\`、c 改 \`circumference\`；② 给关键行加解释"为什么"的注释（说明这是周长公式、3.14 是 π）；③ 运算符两边加空格、加一行总说明。参考改造：\n# 计算圆的周长：周长 = 2 × π × 半径\nradius = 5                       # 圆的半径\nPI = 3.14                        # 圆周率 π 的近似值\ncircumference = 2 * PI * radius  # 套用周长公式\nprint(circumference)\n运行结果不变（输出 31.4），但任何人一看就懂这段在算圆周长。\n**为什么这样改**：① \`radius\`、\`circumference\` 这种名字让代码自己解释自己，不用再猜 r、c 是什么；② 把魔数 3.14 提取成有名字的 \`PI\` 并注释，既说清了它是圆周率，以后想更精确（如改成 3.14159）也只改一处；③ 顶部的总注释说明整段意图，运算符留白让式子不拥挤。功能完全没变，但从"能跑"变成了"好读、好改"，这正是代码风格的意义。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 6. 字符串  string
  // ───────────────────────────────────────────────────────────────
  'string': {
    warmup: {
      q: `有个字符串 \`s = "Python"\`。你想取出它的第一个字符 "P"，写了 \`s[1]\`，结果取出来的却是 "y"。这是怎么回事？要取 "P" 应该写什么？`,
      hint: `字符串里每个字符都有一个编号（位置），但这个编号不是从 1 开始数的。第一个字符的编号是几？`,
      answer: `因为字符串的位置编号（下标）是**从 0 开始**的，不是从 1！所以 \`s[0]\` 才是第一个字符 "P"，\`s[1]\` 是第二个 "y"。这是编程里几乎所有"序列"的通用规则。要取 "P" 就写 \`s[0]\`。记住："第 n 个"对应的下标是 n−1，最后一个字符还可以用 \`s[-1]\` 倒着取。`
    },
    intro: [
      `字符串，就是用引号（单引号 '' 或双引号 ""）包起来的一串字符，比如 \`"hello"\`、\`"小明"\`、\`"2024年"\`。它是程序里最常用的非数值数据——名字、地址、一句话、一段文本，全是字符串。掌握字符串，就拿到了处理文字的入口。`,
      `字符串里的每个字符都有一个位置编号，叫**下标（索引）**，而且**从 0 开始数**：\`"Hello"\` 里 'H' 是 0 号、'e' 是 1 号……用 \`s[下标]\` 取出对应字符，如 \`s[0]\` 是 'H'。还可以用负数从后往前数，\`s[-1]\` 是最后一个字符。这条"从 0 开始"的规则是初学者最容易栽的坑。`,
      `常用操作有这么几样：求长度 \`len(s)\`（"Hello" 长度是 5）；**切片** \`s[起:止]\` 取出一段（注意"顾头不顾尾"，取到"止"的前一位）；**拼接**用 \`+\` 把两段接起来；**判断包含**用 \`in\`（\`"lo" in "Hello"\` 是 True）；查找位置、大小写转换、替换等也很常用。`,
      `字符串还有一个重要性质：它是**不可变的**。意思是你不能直接改字符串里的某个字符（\`s[0] = "h"\` 会报错），任何"修改"其实都是**生成一个新字符串**。比如想替换、想拼接，得到的都是新的字符串，原来的 s 不变（除非你重新赋值给它）。`,
      `最后特别推荐 **f-string** 这个把变量"插进"字符串的利器：在字符串前加字母 f，把变量写进花括号里，如 \`f"成绩：{score}"\` 会自动把 score 的值嵌进去。它比用 + 拼接清爽得多，还省去手动转类型的麻烦，是日常拼字符串的首选。`
    ],
    formulas: [
      { name: '下标取字符', latex: `\\texttt{s[0]}`,
        note: `下标从 0 开始。\`s[0]\` 是第一个字符，\`s[-1]\` 是最后一个。取第 n 个字符用下标 n−1。` },
      { name: '求长度', latex: `\\texttt{len(s)}`,
        note: `返回字符串里字符的个数。\`len("Hello")\` 是 5。常配合下标用——最后一个字符是 \`s[len(s)-1]\`。` },
      { name: '切片', latex: `\\texttt{s[a:b]}`,
        note: `取下标从 a 到 b−1 的一段（顾头不顾尾）。\`"Hello"[1:3]\` 是 "el"。省略 a 从头取，省略 b 取到尾。` },
      { name: '拼接与重复', latex: `\\texttt{"Hi " + name,\\; "ab" * 3}`,
        note: `\`+\` 把两个字符串接起来；\`字符串 * 整数\` 把它重复几遍，\`"ab" * 3\` 得 "ababab"。` },
      { name: '判断包含', latex: `\\texttt{"lo" in s}`,
        note: `\`in\` 判断一个子串在不在字符串里，结果是 True/False。\`"lo" in "Hello"\` → True。` },
      { name: '格式化嵌入', latex: `\\texttt{f"分数\\{score\\}"}`,
        note: `f-string：字符串前加 f，花括号里写变量，自动把值嵌进文本。\`f"分数{95}"\` → "分数95"。` },
    ],
    methods: [
      {
        name: '切片"顾头不顾尾"读法',
        when: `用 \`s[a:b]\` 取字符串的一段，搞不清取到哪里时`,
        steps: [
          `记住下标从 0 开始`,
          `\`s[a:b]\` 取的是下标 a、a+1、……一直到 b−1，**不包括 b**（顾头不顾尾）`,
          `算长度小技巧：取出的段长度正好是 b − a`,
          `省略写法：\`s[:b]\` 从头取到 b−1，\`s[a:]\` 从 a 取到末尾，\`s[:]\` 取整段`,
        ],
        example: {
          problem: `\`s = "abcdef"\`，\`s[1:4]\` 取出的是什么？`,
          solution: [
            `解：下标从 0 开始，\`a\`=0，\`b\`=1，……\`f\`=5。`,
            `\`s[1:4]\` 取下标 1、2、3（到 4 前一位，不含 4），即 'b'、'c'、'd'。`,
            `答：结果是 \`"bcd"\`，长度是 4 − 1 = 3，正好对上。`,
          ],
        },
      },
      {
        name: '用 f-string 拼出一句话',
        when: `想把若干变量和固定文字组合成一句完整的话时`,
        steps: [
          `在字符串前面加字母 f`,
          `固定的文字照常写`,
          `要嵌入的变量用花括号 {} 包起来写在句中`,
          `运行时花括号会被替换成变量的值，数字也会自动转成文字，不用手动 str()`,
        ],
        example: {
          problem: `已知 \`name = "小明"\`、\`age = 14\`，拼出 "小明今年14岁"。`,
          solution: [
            `解：用 f-string，把 name、age 放进花括号。`,
            `代码：\nresult = f"{name}今年{age}岁"\nprint(result)   # 小明今年14岁`,
            `答："小明今年14岁"。比起 \`name + "今年" + str(age) + "岁"\`，f-string 不用手动转换、读起来也更顺。`,
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 取首尾字符、判断长度',
        problem: `用户输入了一个用户名（存进字符串 \`name\`）。请写代码输出：这个用户名有多少个字符、它的第一个字符是什么、最后一个字符是什么。以输入 "Python" 为例说明结果。`,
        steps: [
          `第一步：求长度。用 \`len(name)\`，"Python" 有 6 个字符，所以 len 是 6。`,
          `第二步：取第一个字符。下标从 0 开始，第一个就是 \`name[0]\`，对 "Python" 来说是 'P'。`,
          `第三步：取最后一个字符。最稳的写法是 \`name[-1]\`（倒数第一个），对 "Python" 是 'n'；也可以写 \`name[len(name)-1]\`，即 name[5]，同样是 'n'。`,
          `第四步：组织输出并作答。\nprint(f"长度：{len(name)}")        # 长度：6\nprint(f"第一个字符：{name[0]}")    # 第一个字符：P\nprint(f"最后一个字符：{name[-1]}") # 最后一个字符：n\n关键点：下标从 0 开始，所以第一个是 [0]；用 [-1] 取最后一个最方便，不用先算长度。`,
        ],
      },
    ],
    pitfalls: [
      `以为下标从 1 开始。字符串下标**从 0 开始**，第一个字符是 \`s[0]\`，\`s[1]\` 已经是第二个了。`,
      `切片忘了"顾头不顾尾"。\`s[1:3]\` 取的是下标 1 和 2（不含 3），共 2 个字符，别误以为含 3。`,
      `想直接改字符串里的字符。字符串**不可变**，\`s[0] = "x"\` 会报错；要"改"只能生成新字符串（如切片+拼接或 replace）。`,
      `下标越界。字符串长度是 5（下标 0~4），写 \`s[5]\` 会报 \`IndexError\`；最后一个用 \`s[-1]\` 或 \`s[len(s)-1]\`。`,
      `把数字直接和字符串用 + 拼。\`"得分" + 95\` 报错，要 \`"得分" + str(95)\` 或更省事地用 f-string。`,
    ],
    exercises: [
      {
        problem: `已知 \`s = "Hello"\`，不运行，分别说出下面每个表达式的结果：\n（1）\`len(s)\`\n（2）\`s[0]\`\n（3）\`s[-1]\`\n（4）\`s[1:4]\`\n（5）\`"e" in s\``,
        hint: `下标从 0 开始数：H=0, e=1, l=2, l=3, o=4。切片"顾头不顾尾"；\`in\` 判断包含，结果是 True/False。`,
        answer: `**详细讲解**：先把每个字符的下标标出来——H(0) e(1) l(2) l(3) o(4)，长度 5。\n（1）\`len(s)\`：字符个数，"Hello" 有 5 个，结果 **5**。\n（2）\`s[0]\`：下标 0 是第一个字符，结果 **"H"**（不是 "e"！下标从 0 起）。\n（3）\`s[-1]\`：负下标从后往前数，−1 是最后一个，结果 **"o"**。\n（4）\`s[1:4]\`：切片取下标 1、2、3（到 4 前一位，不含 4），即 e、l、l，结果 **"ell"**，长度正好 4−1=3。\n（5）\`"e" in s\`：判断 "e" 在不在 "Hello" 里，在，结果 **True**。\n**为什么**：① 下标从 0 开始是核心规则，\`s[0]\` 才是第一个；② 切片"顾头不顾尾"，所以 [1:4] 不含第 4 位；③ \`in\` 用来判断包含关系、返回布尔值。把下标编号写在每个字符下面，这类题就一目了然了。`
      },
      {
        problem: `下面代码想把字符串 \`name = "tom"\` 的第一个字母改成大写 T，得到 "Tom"。运行报错了，请说明原因，并写出能正确得到 "Tom" 的代码。\nname = "tom"\nname[0] = "T"\nprint(name)`,
        hint: `字符串能不能直接修改它的某一个字符？字符串有一个重要性质叫"不可变"。要"改"，是不是只能拼一个新的出来？`,
        answer: `**详细讲解**：报错的原因是字符串是**不可变（immutable）**的——一旦创建，就不能直接修改其中某个位置的字符。所以 \`name[0] = "T"\` 这种"就地改字符"的写法会报 \`TypeError: 'str' object does not support item assignment\`。\n字符串的任何"修改"，本质都是**生成一个新字符串**。要得到 "Tom"，可以把大写的 "T" 和原字符串从第 2 个字符起的部分（\`name[1:]\`，即 "om"）拼起来，再赋回 name：\nname = "tom"\nname = "T" + name[1:]   # "T" 拼上 "om" → "Tom"\nprint(name)             # Tom\n这里 \`name[1:]\` 是从下标 1 取到末尾，得到 "om"；和 "T" 拼接生成新字符串 "Tom"，再用 \`=\` 让 name 指向这个新串。\n（其实 Python 还有更简单的内置方法 \`name.capitalize()\` 或 \`name.upper()\`，但理解"不可变 → 只能造新串"这个原理更重要。）\n**为什么**：因为不可变，所以"改字符串"永远是"造一个新的并替换引用"，而不是在原地动它。这也解释了为什么 \`+\`、切片、replace 这些操作都是返回新字符串、不影响原来的——记住这一点能避免很多困惑。`
      },
      {
        problem: `写一段代码：让用户输入自己的名字和最喜欢的科目，然后用 f-string 输出一句话，例如输入"小红"和"数学"，就输出"小红最喜欢的科目是数学，加油！"。`,
        hint: `名字和科目都是字符串，用 input 读进来直接用即可（不用转类型）。输出用 f-string 把两个变量嵌进句子最方便。`,
        answer: `**详细讲解**：这道题练 f-string 的实际运用。名字和科目本来就是文字，用 input 读进来直接是字符串，不需要 int()/float() 转换。把两个变量用花括号嵌进一句固定文字里即可。参考代码：\nname = input("你叫什么名字？")\nsubject = input("你最喜欢哪一科？")\nprint(f"{name}最喜欢的科目是{subject}，加油！")\n输入"小红""数学"后，输出"小红最喜欢的科目是数学，加油！"。\n**为什么用 f-string**：① 它把变量直接写在句子该出现的位置（\`{name}\`、\`{subject}\`），读代码时一眼就能看出最终句子长什么样，比 \`name + "最喜欢的科目是" + subject + "，加油！"\` 这种一堆加号清爽多了；② 如果句子里要嵌的是数字，f-string 还会自动转换、不用手动 str()，更省心。这里虽然嵌的是字符串，但养成用 f-string 拼句子的习惯，以后嵌数字时就不会踩"字符串 + 数字报错"的坑。`
      },
    ],
  },

  // ───────────────────────────────────────────────────────────────
  // 7. 常见错误类型  error-types
  // ───────────────────────────────────────────────────────────────
  'error-types': {
    warmup: {
      q: `三个同学的程序都"出问题"了：A 的程序一运行就提示 \`SyntaxError\` 根本跑不起来；B 的程序跑到一半突然崩溃报 \`NameError\`；C 的程序顺利跑完了，但算出的平均分明显不对。这三种"问题"是一回事吗？你觉得哪种最难发现？`,
      hint: `从"程序能不能跑起来、能不能跑完、跑完的结果对不对"三个角度看，它们其实是三类完全不同的错。`,
      answer: `不是一回事，正好是三大类错误。A 是**语法错误**——代码写得不符合规则，程序根本启动不了；B 是**运行时错误**——能启动，但跑到某一步出了状况（如用了没定义的变量）而崩溃；C 是**逻辑错误**——程序顺利跑完、不报任何错，但结果是错的。其中 **C（逻辑错误）最难发现**，因为电脑不会提示你，得靠自己检查。看懂报错信息、再揪出不报错的逻辑错，是初学者必过的一关。`
    },
    intro: [
      `写程序出错是再正常不过的事——专业程序员也天天和错误打交道。关键不是"不出错"，而是"看懂错、会找错"。编程里的错误分三大类，搞清楚它们的区别，是高效改 bug 的前提。`,
      `第一类，**语法错误（SyntaxError）**：代码写得不符合语言规则，程序**根本跑不起来**。比如括号没配对、字符串引号没闭合、if 后面忘了冒号、缩进乱了。这类错最"友好"——Python 会在运行前就指出大概在第几行，照着改通常很快。`,
      `第二类，**运行时错误（运行中报错并崩溃）**：代码语法没问题、程序能启动，但**跑到某一步出了状况**而中断。常见的有：除以 0（\`ZeroDivisionError\`）、用了没定义的变量（\`NameError\`）、类型不匹配（\`TypeError\`，如字符串加数字）、下标越界（\`IndexError\`）。它要等执行到那一行才暴露。`,
      `第三类，**逻辑错误**：这是最隐蔽、最难缠的。程序能正常跑完、**不报任何错**，但结果是错的——比如本该算平均分却忘了除以个数、把 \`+\` 写成了 \`-\`、循环条件写错少算了一次。电脑只会忠实执行你写的，不会判断你"想干什么"，所以这类错全靠自己发现。`,
      `怎么应对？面对前两类，**学会读报错信息**是第一关：报错通常会告诉你错误类型和大概位置（哪一行），顺着去看往往能定位。面对逻辑错误，靠的是"打印中间值"等调试手段，每修一个 bug 都问自己一句"我以为成立、但其实没成立的假设是什么"。能读懂报错、能揪出逻辑错，你就真正会 debug 了。`
    ],
    formulas: [
      { name: '三大类错误', latex: `\\texttt{语法 / 运行时 / 逻辑}`,
        note: `语法错：跑不起来；运行时错：跑一半崩溃；逻辑错：能跑完但结果不对。逻辑错最难发现。` },
      { name: '语法错误', latex: `\\texttt{SyntaxError}`,
        note: `代码不符合规则，运行前就报。如缺冒号、括号/引号不配对、缩进错。Python 会指出大概行号。` },
      { name: '常见运行时错误', latex: `\\texttt{NameError / TypeError}`,
        note: `\`NameError\` 用了没定义的变量；\`TypeError\` 类型不匹配（如 str+int）；还有除零、下标越界等。` },
      { name: '逻辑错误', latex: `\\texttt{不报错但结果错}`,
        note: `程序顺利跑完却算错了（公式写错、条件写反等）。电脑不会提示，只能靠自己检查、打印调试。` },
    ],
    methods: [
      {
        name: '看报错信息定位错误',
        when: `程序报错崩溃了，想快速找到错在哪、是什么错时`,
        steps: [
          `先看报错信息的**最后一行**——它写明了错误类型（如 NameError、TypeError）`,
          `再看报错指出的**行号**——错误通常就在那一行或附近`,
          `根据错误类型联想原因：NameError 想"是不是变量名写错/没定义"，TypeError 想"是不是类型不匹配"，ZeroDivisionError 想"是不是除数为 0"`,
          `回到那一行检查、改正，再重新运行`,
        ],
        example: {
          problem: `运行后报 \`NameError: name 'scoer' is not defined\`，可能是什么问题？`,
          solution: [
            `解：错误类型是 NameError（用了没定义的名字），引号里是 'scoer'。`,
            `"scoer" 看起来像是 "score" 拼错了——你定义的是 score，用的时候手滑写成了 scoer，Python 找不到这个名字。`,
            `答：把 \`scoer\` 改回正确的 \`score\` 即可。NameError 十有八九是变量名拼错或忘了先赋值。`,
          ],
        },
      },
      {
        name: '用打印调试揪逻辑错误',
        when: `程序不报错，但结果不对，怀疑某处逻辑算错了时`,
        steps: [
          `先确定"正确结果应该是多少"，和实际输出对比`,
          `在可疑的地方插入 \`print()\`，把中间变量的值打印出来`,
          `一步步看：从哪一步开始，中间值和你预期的对不上，问题就在那附近`,
          `找到后改正，再把临时的 print 删掉`,
        ],
        example: {
          problem: `算 1 到 3 的和，期望是 6，但结果是 5，怀疑循环少算了。怎么查？`,
          solution: [
            `解：在累加的地方打印中间值看看。`,
            `代码：\ntotal = 0\nfor i in range(1, 3):    # 怀疑这里范围不对\n    total = total + i\n    print("加了", i, "现在 total =", total)`,
            `答：打印会发现只加了 1 和 2（range(1,3) 不含 3），所以是 3 而非 6——其实应是 \`range(1, 4)\`。打印中间值能直接看出"少加了 3"这个逻辑错。`,
          ],
        },
      },
    ],
    worked: [
      {
        title: '例题 · 给三段错误代码"分类"并改正',
        problem: `判断下面三段代码各属于哪一类错误（语法/运行时/逻辑），并分别改正。\n（甲）\nif x > 0\n    print("正数")\n（乙）\nprint(10 / 0)\n（丙）\n# 想算平均分\ntotal = 80 + 90\naverage = total\nprint(average)`,
        steps: [
          `第一步：看（甲）。\`if x > 0\` 后面**漏了冒号**，不符合 Python 语法，程序根本跑不起来——属于**语法错误（SyntaxError）**。改正：在条件后加冒号 \`if x > 0:\`。`,
          `第二步：看（乙）。语法没问题，能启动，但执行到 \`10 / 0\` 时除数为 0，运行中崩溃报 \`ZeroDivisionError\`——属于**运行时错误**。改正：除数不能为 0，要保证分母非 0（如先判断 \`if b != 0:\` 再做除法）。`,
          `第三步：看（丙）。这段能顺利跑完、不报任何错，但结果是错的：注释说要算平均分，却写成了 \`average = total\`（直接等于和 170），忘了除以科目数 2——属于**逻辑错误**。`,
          `第四步：改正（丙）并总结。改成 \`average = total / 2\`，结果才是正确的 85。三段对照可见：语法错跑不起来、运行时错跑一半崩、逻辑错跑完但答案错；前两类有报错提示，第三类最隐蔽，要靠自己核对结果。`,
        ],
      },
    ],
    pitfalls: [
      `分不清三类错误。语法错=跑不起来、运行时错=跑一半崩溃、逻辑错=跑完但结果错；对症才好下药。`,
      `不读报错信息就瞎改。报错最后一行写了错误类型、还给了行号，先读它能省一大半时间。`,
      `以为"不报错"就等于"对了"。逻辑错误最坑——程序乖乖跑完却算错了，必须自己核对结果是否符合预期。`,
      `把 NameError 当成大问题。它多半只是变量名拼错或忘了先赋值，看清引号里的名字、对照定义处即可。`,
      `遇到 TypeError 不往"类型"上想。它几乎都是类型不匹配（如 str+int、对数字用了字符串方法），检查相关变量是什么类型并按需转换。`,
    ],
    exercises: [
      {
        problem: `下面三种情况，分别属于哪一类错误（语法错误 / 运行时错误 / 逻辑错误）？\n（甲）忘了给 if 语句的条件后面加冒号，程序一运行就报 SyntaxError。\n（乙）程序里写了 \`print(age)\`，但 age 这个变量从来没赋过值，运行到这行崩溃。\n（丙）想求两个数的平均值，却写成了 \`(a + b) * 2\`，程序能跑完，但结果是错的。`,
        hint: `抓住三个判断点：能不能"跑起来"、能不能"跑完"、跑完"结果对不对"。`,
        answer: `**详细讲解**：用"能不能跑起来 / 能不能跑完 / 结果对不对"三把尺子来分。\n（甲）忘加冒号报 SyntaxError——代码不符合语法规则，程序**根本启动不了**，所以是**语法错误**。\n（乙）用了从没赋值的 age，程序能启动、但**跑到这一行才崩溃**，报的是 NameError，属于**运行时错误**（运行过程中才暴露的错）。\n（丙）程序**顺利跑完、不报任何错**，但把"平均值"算成了 \`(a+b)*2\`（应该是 \`(a+b)/2\`），结果是错的，所以是**逻辑错误**。\n**为什么这样分**：① 语法错误在"运行前"就被 Python 拦下，连跑都跑不了；② 运行时错误能跑起来，但执行到出问题的那一步才中断崩溃；③ 逻辑错误最隐蔽——电脑只会忠实执行你写的公式，不会判断你"本意"是什么，所以哪怕公式写错它也照算不误、不报错。这也是为什么逻辑错误最难发现、最需要自己核对结果。`
      },
      {
        problem: `小明的程序运行后报错：\n\`\`\`\nTypeError: can only concatenate str (not "int") to str\n\`\`\`\n出错的那一行是 \`print("你的年龄是" + age)\`，其中 \`age = 14\`。请说明这是哪一类错误、为什么会报这个错，并改正。`,
        hint: `报错类型是 TypeError，提示是"只能把 str 和 str 拼接"。\`age\` 是什么类型？字符串能直接和整数用 + 连吗？`,
        answer: `**详细讲解**：先定性——这是程序运行起来、执行到这一行才崩溃的，属于**运行时错误**，具体类型是 \`TypeError\`（类型错误）。\n再看为什么报错：\`"你的年龄是"\` 是字符串（str），而 \`age = 14\` 是整数（int）。字符串的 \`+\` 表示"拼接"，但**拼接只能在字符串之间进行**，不能把字符串和整数直接用 + 连起来。报错信息说得很清楚：\`can only concatenate str (not "int") to str\`——只能把 str 拼到 str 上，不能拼 int。\n改正办法是把整数 \`age\` 先转成字符串：\nage = 14\nprint("你的年龄是" + str(age))    # str(14) → "14"，再拼接\n这样 \`str(age)\` 得到 "14"，和前面的文字拼成 "你的年龄是14"，正常输出。\n（也可以用 f-string：\`print(f"你的年龄是{age}")\`，它会自动转换类型，更省事。）\n**为什么**：\`TypeError\` 几乎都意味着"类型不匹配"——你让两个不能放一起运算的类型凑到了一起。看到 TypeError，第一反应就是检查涉及的变量各是什么类型，然后用 \`str()\`、\`int()\` 等做必要的转换。`
      },
      {
        problem: `下面代码想数出列表里有几个正数，但运行后结果总是 0（列表里明明有正数）。程序不报错。请判断这是哪类错误，找出问题并改正。\nnums = [3, -1, 5, -2, 8]\ncount = 0\nfor n in nums:\n    if n < 0:\n        count = count + 1\nprint("正数有", count, "个")`,
        hint: `程序能跑完、不报错，但结果不对——先想这是哪类错误。再仔细看 if 的条件：想数"正数"，条件却写的是什么？`,
        answer: `**详细讲解**：先定性。程序能顺利跑完、不报任何错，只是**结果不对**（数正数却数出 0），所以这是**逻辑错误**——最隐蔽的一类，电脑不会提示，全靠自己核对。\n再找问题。代码想数"正数"的个数，但 if 的条件写成了 \`if n < 0\`（小于 0，这是**负数**的条件）。于是它实际在数负数，而 count 只在遇到负数时才加……等等，更准确地说：作者把条件写反了，本意是"正数就计数"，却写成了"负数才计数"。不过题目说结果是 0，说明真正的意图判断写错了方向——总之**条件和意图不符**，这是典型的逻辑错误。\n改正：把条件改成判断正数 \`n > 0\`。\nnums = [3, -1, 5, -2, 8]\ncount = 0\nfor n in nums:\n    if n > 0:            # 想数正数，条件就该是 > 0\n        count = count + 1\nprint("正数有", count, "个")   # 正数有 3 个（3、5、8）\n这样遇到 3、5、8 时 count 各加一次，最终输出 3，正确。\n**为什么这类错最难找**：\`if n < 0\` 在语法上完全合法、运行也不报错，Python 只会忠实地"遇到负数就计数"，它不知道你心里想的是"正数"。要发现这种错，办法是用打印调试——比如在 if 里加 \`print(n, "被计数了")\`，就会发现被计数的全是负数，立刻意识到条件写反了。记住：不报错 ≠ 正确，结果一定要自己验。`
      },
    ],
  },

});
